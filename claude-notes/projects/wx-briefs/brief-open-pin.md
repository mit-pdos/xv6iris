You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is OPEN-PIN, an APPLICATION-side lane: (a) the pinning construction is FACTORED so that exec and open are two instantiations of one general lemma rather than two copies; (b) an era-0 pin for the console device node; (c) a pinned OPEN bundle for init's `open("console")` and a user-tier open leaf that KEEPS its receipt, so init's descriptor ledger records fd 0 as the console device; (d) init's two dups go through the tracked dup leaf. Design of record: claude-notes/projects/app-echo.md "PINNING -- THE OWNER'S RULING" (binding: pinned lemmas are part of the APPLICATION proof and touch the kernel only by satisfying a spec's bundle precondition; no kernel contract names a pin; factor before a second instance), "SH-LINE PHASE 1 LANDED + TWO MORE KERNEL SEAMS RULED" item (2), "THE PINNED EXEC BUNDLE — design for ARM-c (1) and L6's init/sh", "ARM-c (1a) LANDED", "E1 ECHO-PRED LANDED" (`FsEchoPin` on `FsShPin`'s mold; `echo_pred = echo_taint ∨ pins`). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle" -- "A hoist proposed because two near-duplicates cannot see each other is usually a generalization in disguise", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; design/user-wp-slot.md; design/user-fd.md; iris/PinnedExec.v (whole: `pin_resolves` :123, `pex_P` :151, `pex_Pmiss` :156, `pex_recv` :162, `pex_Fo` :~168, `pex_slot` :~347, `pinned_exec_bundle_at` :419, `pinned_exec_bundle` :~459 -- the ingredients to factor); iris/UInitSh.v (the one instance: `init_sh_pin_resolves` :~110, `init_sh_slot` :~362, `init_exec_sup_of_sh_slot` :~425); iris/FsShPin.v + iris/FsEchoPin.v (the pin-file mold: `SH_INO`, `sh_path`, `era0_sh_pins`, the boot transport, the resource forms); iris/FsInitPinBoot.v (`era0_pins`, `era0_recovery`); iris/FsImgCheck.v (`fname_echo/init/sh/sync` :402-408, `fsimg_*_path` :434-446 -- there is NO console entry yet; the image's `console` is mkfs's device node: find its inum and `di_major`/`di_minor` in the tracked image (`FsAbsDefs.fn_major` :66, `abs_of` :249 gives `ADev major minor`; `ConsoleInv.CONSOLE = 1` :999)); iris/FsAbsDefs.v (`anode` :72 `ADev`, :100); iris/SpecSysOpen.v (`open_in` :863 -- `open_au_pre_plain Γ γfs cw P Pmiss Fo Ft` for a non-create open; `open_receipt` :1005 → `open_receipt_plain` :908: what it says about `fdv'`'s new row and the observed node; the families `P Pmiss Farm Fun Fok Fex Fo Ft`); iris/UexecExecInst.v (`xv6_sbundle` row 15 :~486-489, `xv6_spost` row 15 :~569-572, the `xfam` fields `of_P … of_Ft`); iris/UkRunSys.v (`wp_uk_ecall_open` :~718-790: the generic open leaf, descriptor type existential at :~727, `spost_at` bound as `_` at :~785; `wp_uk_ecall_dup` :~851 (tracked), `wp_uk_ecall_dup_untracked` :~1000); iris/UkInit.v (`wp_kinit_open` :135 -- calls the generic leaf at 0x3b4 and DISCARDS the handle at :~189; `wp_kinit_dup` :285 -- the untracked leaf at :325); iris/UkInitMain.v (open at 0x16, dups at 0x20/0x26, the fork at 0x38; the ledger `ustd_any` threaded); iris/UserFd.v (`ufd`, `ustd`, `ustd_any`, `ualloc`, `fd_lowest_closed`); iris/UConsLine.v (`ush_std_cons γfd l := ustd γfd l ∗ ⌜∃ wr, l !! 0 = Some (FdOpen true wr (FdDevice CONSOLE))⌝` -- what this lane must be able to establish for sh's entry); iris/AppEcho.v (`echo_fs_pure`, `echo_pred`, `echo_taint`; the pins the claim carries -- the console pin JOINS `echo_fs_pure`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `openpin1`, `openpin2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. New files into `iris/_CoqProject` in dependency order. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim -- this lane must not move the generic theorem); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. KERNEL FILES ARE OFF LIMITS: `Spec*.v`, `Proof*.v`, `ConsoleInv`, `UexecExecInst`'s bundle/post rows and the `uexecSG` class must not change (the bundle rows are what you SATISFY); if a kernel-side change seems needed, that is design friction -- STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact.

=== THE DESIGN ===
O1 FACTOR THE PIN.  New `iris/PinnedObs.v` (below `PinnedExec`): the general "pinned observation family" over the pieces `PinnedExec` already defines -- `pin_resolves`, `pex_P`/`pex_Pmiss` (the per-hop cursor: `⌜d = hops !!! k⌝ ∨ T`; a miss is `T`), `pex_recv`/`pex_Fo` (the observed row at the pinned view: `⌜arow_at v i a⌝ ∗ (⌜Pin v⌝ ∨ T)`), and the claim-law hypothesis `□ (∀ v, app_pred app_run v -∗ app_pred app_run v ∗ (⌜Pin v⌝ ∨ T))` -- stated ONCE, syscall-agnostically: a lemma (or a small record of lemmas) that, from the claim law and `app_inv`, discharges the AU/family preconditions a walk-shaped bundle has (the cursor family at each hop, the miss arm, the observation piece), for ANY syscall whose bundle is "walk a path, observe the node". `PinnedExec.pinned_exec_bundle` becomes an INSTANCE of it at exec's families (`exec_au_pre`), proved by applying the general lemma plus exec's slot piece; its statement and `UInitSh`'s use do not change (or change only in proof). Report the general lemma's statement verbatim.
O2 THE CONSOLE PIN.  `iris/FsConsPin.v` on `FsShPin`/`FsEchoPin`'s mold: `CONS_INO` (read off the image: mkfs creates `console` as the first inode after the root -- VERIFY against the tracked image; `FsImgCheck` gains `fname_console`/`fsimg_console_path`/`fsimg_console_type` on the existing four's pattern), `cons_path := [fname_console]`, `era0_cons_pins av := apath_at av ROOTINO cons_path = Some CONS_INO /\ av !! CONS_INO = Some (MkAnode (ADev CONSOLE 0) 1%nat) /\ arun av ROOTINO cons_path [ROOTINO; CONS_INO]` (the minor: read it off the image), `era0_recovery_cons_pins` and the boot transport/resource forms the mold has. `AppEcho.echo_fs_pure` gains `era0_cons_pins av` (a fourth conjunct); `echo_Happ_init`/`echo_init_img` extend by the recovery pin; the `xv6_app` record is unchanged in shape. NOTE the owner's rule (2): the pin's obligation is that the application's invariant keeps the node unchanged; echo's claim is `taint ∨ pins` of every view, so this is immediate here -- write the note at `era0_cons_pins` saying an application that writes the fs must prove its writes miss the pinned inodes.
O3 THE PINNED OPEN.  `iris/PinnedOpen.v` (application side): `pinned_open_bundle` at open's families -- for a NON-create open (`om_create vom = false`; init's `open("console", O_RDWR)`): `open_au_pre_plain Γ γfs cw P Pmiss Fo Ft` answered from O1's general lemma at `Pin := era0_cons_pins`, `hops := [ROOTINO; CONS_INO]`, plus open's own pieces (`Ft`? -- read `open_au_pre_plain` to see what `Ft` is (the file-type/truncate piece?) and answer it at the pinned node: the observed node is `ADev CONSOLE 0`, so the receipt's new row is `FdOpen true true (FdDevice CONSOLE)` at the lowest free descriptor); the receipt: `open_receipt_plain … sts r fdv'` at the pinned observation gives `r = the fd` and `fdv' = <[fd := FdOpen rd wr (FdDevice CONSOLE)]> sts` (or whatever the receipt states -- quote it) OR the taint. `init_cons_open_slot T := app_inv fsc_fs ∗ □ (claim law at era0_cons_pins) ∗ …` on `UInitSh.init_sh_slot`'s mold, persistent (init opens once per boot but the builder is what init's entry carries).
O4 THE RECEIPT-KEEPING OPEN LEAF.  `UkRunSys.wp_uk_ecall_open_recv` beside `wp_uk_ecall_open` (this is a U-tier LEAF in a program-facing file, allowed -- it is the mold of `wp_uk_ecall_read_recv_body`: the deposit `udepwf N m pc USYS_open fdep` at an `xfam` whose open families are the pinned ones, and the continuation receives `spost_at uslot USYS_open fdep (uvis_of_run …) r M' fdv' cw' cs'` = `open_receipt …` at the exposed resume key, beside `urun` and the ledger moved to `fdv'` (find how `wp_uk_ecall_open` moves `ualloc`/`ustd` and keep that)). Do NOT change `wp_uk_ecall_open`.
O5 INIT.  `UkInit.wp_kinit_open` restated at the NAMED ledger: takes `ustd γfd l` with `l` all-closed (init's initial ledger: check `UkInitMain`'s entry -- `ustd_any` today; the entry constructor `UInitKernel.init_uexec_slot` hands `ustd_any`; the exec of /init gives `fdt0` (all closed) per `Hinit_boot`'s `init_boot_bundle ROOTINO fdt0` -- so the named ledger at `fdt0`'s first NSTD entries is available at init's entry: state it), the pinned open builder, and returns `ustd γfd (<[0 := FdOpen true true (FdDevice CONSOLE)]> l)` (fd 0 = the lowest free) OR the taint (`echo_taint` -- init's continuation on the taint is generic: state the disjunction and let `UkInitMain` case-split; on the taint arm init's later fork/exec are the generic ones -- if that makes init's loop unwieldy, STOP AND REPORT); `wp_kinit_dup` (twice) through the TRACKED `wp_uk_ecall_dup` yielding `ustd γfd [console; console; console]`-shaped; `UkInitMain`'s head carries the named ledger and hands it to the fork (the child gets the same list at a fresh name); `ush_std_cons` (UConsLine) is then payable at sh's entry -- this lane STATES that init's fork child arm carries `ustd (ukn_fd N') l ∗ ⌜l !! 0 = Some (FdOpen true true (FdDevice CONSOLE))⌝`; SH-LINE consumes it.
O6 SANITY: `grep -n "pinned_exec_bundle\|pinned_open_bundle" iris/PinnedExec.v iris/PinnedOpen.v iris/UInitSh.v iris/UkInit.v` shows both as instances of `PinnedObs`; `grep -rn "era0_cons_pins" iris/*.v` shows the pin in `echo_fs_pure` and the open builder; `grep -n "wp_uk_ecall_dup_untracked" iris/UkInit.v` EMPTY; `git diff --stat -- iris/Spec*.v iris/Proof*.v iris/ConsoleInv.v` EMPTY; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
O1's general lemma STATED (and `pinned_exec_bundle` restated as its instance, proof may be in progress), O2's pin file, O3's `pinned_open_bundle` and builder, O4's leaf and O5's `wp_kinit_open`/`wp_kinit_dup` as STATEMENTS; NO Admitted at report time. Build with -k. Report: (1) the general pinned-observation lemma, quoted; (2) `era0_cons_pins` and the image facts it rests on (inum, major, minor), quoted with file:line; (3) `pinned_open_bundle`'s statement and what `open_receipt_plain` says at the pinned node, quoted; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (the general lemma, exec's instance, the console pin's transport, the open bundle, the leaf, init's open/dups/ledger, `echo_fs_pure`'s extension, every red file), O6. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
