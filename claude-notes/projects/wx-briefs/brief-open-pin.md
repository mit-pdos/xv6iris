You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is OPEN-PIN, an APPLICATION-side lane: (a) the pinning construction is FACTORED so that exec and open are two instantiations of one general lemma rather than two copies; (b) an era-0 pin for the console device node; (c) a pinned OPEN bundle for init's `open("console")` and a user-tier open leaf that KEEPS its receipt, so init's descriptor ledger records fd 0 as the console device; (d) init's two dups go through the tracked dup leaf. Design of record: claude-notes/projects/app-echo.md "PINNING -- THE OWNER'S RULING" (binding: pinned lemmas are part of the APPLICATION proof and touch the kernel only by satisfying a spec's bundle precondition; no kernel contract names a pin; factor before a second instance), "SH-LINE PHASE 1 LANDED + TWO MORE KERNEL SEAMS RULED" item (2), "THE PINNED EXEC BUNDLE — design for ARM-c (1) and L6's init/sh", "ARM-c (1a) LANDED", "E1 ECHO-PRED LANDED" (`FsEchoPin` on `FsShPin`'s mold; `echo_pred = echo_taint ∨ pins`). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle" -- "A hoist proposed because two near-duplicates cannot see each other is usually a generalization in disguise", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; design/user-wp-slot.md; design/user-fd.md; iris/PinnedExec.v (whole: `pin_resolves` :123, `pex_P` :151, `pex_Pmiss` :156, `pex_recv` :162, `pex_Fo` :~168, `pex_slot` :~347, `pinned_exec_bundle_at` :419, `pinned_exec_bundle` :~459 -- the ingredients to factor); iris/UInitSh.v (the one instance: `init_sh_pin_resolves` :~110, `init_sh_slot` :~362, `init_exec_sup_of_sh_slot` :~425); iris/FsShPin.v + iris/FsEchoPin.v (the pin-file mold: `SH_INO`, `sh_path`, `era0_sh_pins`, the boot transport, the resource forms); iris/FsInitPinBoot.v (`era0_pins`, `era0_recovery`); iris/FsImgCheck.v (`fname_echo/init/sh/sync` :402-408, `fsimg_*_path` :434-446 -- there is NO console entry yet; the image's `console` is mkfs's device node: find its inum and `di_major`/`di_minor` in the tracked image (`FsAbsDefs.fn_major` :66, `abs_of` :249 gives `ADev major minor`; `ConsoleInv.CONSOLE = 1` :999)); iris/FsAbsDefs.v (`anode` :72 `ADev`, :100); iris/SpecSysOpen.v (`open_in` :863 -- `open_au_pre_plain Γ γfs cw P Pmiss Fo Ft` for a non-create open; `open_receipt` :1005 → `open_receipt_plain` :908: what it says about `fdv'`'s new row and the observed node; the families `P Pmiss Farm Fun Fok Fex Fo Ft`); iris/UexecExecInst.v (`xv6_sbundle` row 15 :~486-489, `xv6_spost` row 15 :~569-572, the `xfam` fields `of_P … of_Ft`); iris/UkRunSys.v (`wp_uk_ecall_open` :~718-790: the generic open leaf, descriptor type existential at :~727, `spost_at` bound as `_` at :~785; `wp_uk_ecall_dup` :~851 (tracked), `wp_uk_ecall_dup_untracked` :~1000); iris/UkInit.v (`wp_kinit_open` :135 -- calls the generic leaf at 0x3b4 and DISCARDS the handle at :~189; `wp_kinit_dup` :285 -- the untracked leaf at :325); iris/UkInitMain.v (open at 0x16, dups at 0x20/0x26, the fork at 0x38; the ledger `ustd_any` threaded); iris/UserFd.v (`ufd`, `ustd`, `ustd_any`, `ualloc`, `fd_lowest_closed`); iris/UConsLine.v (`ush_std_cons γfd l := ustd γfd l ∗ ⌜∃ wr, l !! 0 = Some (FdOpen true wr (FdDevice CONSOLE))⌝` -- what this lane must be able to establish for sh's entry); iris/AppEcho.v (`echo_fs_pure`, `echo_pred`, `echo_taint`; the pins the claim carries -- the console pin JOINS `echo_fs_pure`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `openpin1`, `openpin2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. New files into `iris/_CoqProject` in dependency order. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim -- this lane must not move the generic theorem); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. KERNEL FILES ARE OFF LIMITS: `Spec*.v`, `Proof*.v`, `ConsoleInv`, `UexecExecInst`'s bundle/post rows and the `uexecSG` class must not change (the bundle rows are what you SATISFY); if a kernel-side change seems needed, that is design friction -- STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact.

=== THE DESIGN (RE-SCOPED after phase 1; app-echo.md "OPEN-PIN FINDINGS -- RULED WITH THE OWNER" and "PATH-ARGS LANDED" are the design of record and WIN over anything older in this file; O1 is LANDED as `PinnedObs.v`) ===
FACTS: the tracked image has NO console device node -- init's first `open("console")` fails at era 0 and init's repair arm (`UkInitMain.wp_kinit_main_repair` @0x64) does `mknod("console", CONSOLE, 0)` then opens again; after PATH-ARGS, open's and mknod's bundles/receipts are stated AT THE ARGUMENT'S PATH (`arg_path_of M pv pl`), so a pinned walk at one path is sound and a receipt names WHICH file.
O2' THE TWO-STATE CONSOLE CLAIM.  `AppEcho.echo_fs_pure av := pins ∧ cons_state av` with `cons_state av := cons_absent av ∨ cons_present av`: `cons_absent av := (∀ i ma mi nl, av !! i ≠ Some (MkAnode (ADev ma mi) nl)) ∧ apath_at av ROOTINO cons_path = None` (no device node; no `console` entry in the root) and `cons_present av := ∃ i, apath_at av ROOTINO cons_path = Some i ∧ av !! i = Some (MkAnode (ADev CONSOLE 0) 1) ∧ (∀ j ma mi nl, av !! j = Some (MkAnode (ADev ma mi) nl) → j = i)` (the console entry resolves to a CONSOLE device node and it is the only device node). New `iris/FsConsPin.v` holds `cons_path := [fname_console]` (`FsImgCheck` gains `fname_console` = the bytes of "console"; NO inum -- there is none at era 0), `cons_absent`/`cons_present`/`cons_state`, `era0_cons_absent` (the image has no device node and no console entry: prove it off the tracked image the way `fsimg_*_path` are proved), and the transport lemma `cons_state_mknod : cons_absent av → (the mknod delta at the root: `delta_create ROOTINO fname_console i (ADev CONSOLE 0)` at a free `i`) → cons_present av'` (read `FsAbsDelta.delta_create` :~163 and `FsAbsMknodFire.v:~218`). `echo_init_img`/`echo_Happ_init` extend by `era0_cons_absent` (state 1 at boot); `echo_xfer` unchanged (pure ∨ persistent). A PERSISTENT `cons_made`: a `mono_nat_lb_own γcl 1`-style flag is WRONG (that is the taint's ghost); use a second ghost in `echo_fixed` (make `echo_fixed := gname * gname` or a record: the taint's mono_nat and a `cons_made` one-shot) -- `echo_cl` allocates both at 0; `cons_made γ := mono_nat_lb_own γ.2 1`; the claim law `cons_made γ -∗ echo_pred γ r av -∗ echo_pred γ r av ∗ (⌜cons_present av⌝ ∨ echo_taint γ)` requires the INVARIANT to tie the flag to the state: `echo_pred γ r av := echo_taint γ ∨ (⌜pins av⌝ ∗ (⌜cons_absent av⌝ ∨ (⌜cons_present av⌝ ∗ cons_made γ)))` -- i.e. state 2 CARRIES the flag (persistent, inside the durable claim), so a holder of `cons_made` refutes state 1 by `mono_nat_lb_own`'s agreement against… no: the flag being in the claim means "if state 2 then made"; the holder needs "made → state 2": make the flag EXCLUSIVE-at-birth: `echo_cl` holds `mono_nat_auth_own γ.2 1 0` (the authority) and the STEP that moves 1 → 2 (init's mknod, through `app_top_update`'s `▷ app_pred … -∗ ▷ app_pred …` premise) needs the authority to mint the lb -- but `app_top_update`'s step premise is a plain wand with no place to spend a resource: read `app_top_update_step` (:330) and `app_step_acc` (:~356) for the resource-carrying form (WX notes: "every view-moving commit's `app_step` is free" -- for echo the mknod step is NOT free: it must move the authority). DECIDE the exact ghost shape so that (a) `echo_pred` stays Persistent+Timeless, (b) `cons_made` is persistent, (c) `cons_made ∗ echo_pred γ r av ⊢ echo_taint γ ∨ (pins ∧ cons_present av)`, (d) init's mknod step can mint `cons_made` while moving the claim; PREFER: the claim's state-1 arm carries `mono_nat_auth_own γ.2 (1/2) 0`-style evidence that the flag is UNSET… (a half-authority inside a persistent predicate is not persistent -- so instead put the ONE-SHOT's unset token in the DURABLE claim only via the taint-style trick: the claim is `taint ∨ (pins ∧ (cons_absent ∧ ¬made) ∨ (cons_present ∧ made))` where `made` is `mono_nat_lb_own γ.2 1` (persistent) and `¬made` is NOT expressible persistently -- so drop `¬made` and let the step be the only minter: `cons_made` is minted by the step and NOTHING ELSE; then `cons_made` in hand + claim = (taint ∨ pins ∧ cons_absent ∨ pins ∧ cons_present): the `cons_absent` arm is not refuted by `made` alone. To refute it, state 1 must carry a resource incompatible with `made`: an EXCLUSIVE token `cons_unmade γ := mono_nat_auth_own γ.2 1 0` -- linear, so the claim is not persistent. ALTERNATIVE that works: make the claim Timeless but NOT Persistent? `app_pred` must be transportable (`Happ_xfer`), which for a linear conjunct means duplicable… CONCLUSION: state the exclusion through the DURABLE INVARIANT's own authority rather than the claim: `AppInv.app_body` holds `app_pred app_run (abs_view I)` at the CURRENT view; init's mknod step, holding the auth half `ghost_map_auth (fs_top γfs) (1/2) I` (`app_top_update`'s premise), knows the current view I and moves it; AFTER the step the claim at the new view is state 2; a LATER reader (init's second open) observes SOME view v and gets `echo_pred γ r v` = taint ∨ pins ∧ (absent v ∨ present v ∧ made) -- if `present v` carries `made` persistently inside the claim, then `made` in hand does NOT refute `absent v`. The honest fact init needs at the second open is "the view I observe has the console" -- a TEMPORAL fact (nothing unlinks it) that the claim alone cannot give. RULE: encode monotonicity in the claim: `cons_state av γ := ⌜cons_absent av⌝ ∨ (⌜cons_present av⌝ ∗ cons_made γ)` PLUS the mono_nat's AUTHORITY inside the durable invariant's BODY (not the claim): `app_body` for echo gains nothing -- instead put the authority in the claim's state-1 arm as `mono_nat_auth_own γ.2 1 0` (linear) and make `echo_pred` NOT persistent but TIMELESS and `Happ_xfer`-transportable by DUPLICATION FAILURE… -- STOP: this is the design friction. REPORT the ghost shape you find provable, or STOP AND REPORT with the two candidate shapes and where each fails. The requirement is exactly: after init's mknod, a persistent fact in init's hands + the claim of ANY later observed view ⊢ taint ∨ (that view has the console). Hint: the taint's own mechanism is the mold -- `echo_R`'s phase ledger `mono_nat_auth_own γ 1 (echo_phase h)` lives in `app_R` (the TRACE ledger, linear, in the theorem's obs ledger), not in the claim; the console flag can live in `app_R`'s twin for the fs: the DURABLE slot `app_dur_raw` (`AppDur.v:56`: `∃ r I, ghost_map_auth gt (1/2) I ∗ A r (abs_view I)`) -- the application's durable predicate `A r av` is what `app_top_update` steps; if `echo_pred γ r av` for state 1 holds `mono_nat_auth_own γ.2 1 0` LINEARLY, then `Happ_xfer` (which duplicates the claim for the transport at boot/crash) must be re-read: `app_xfer_raw A := □ ∀ r av, A r av ==∗ A r av ∗ ∃ r', ▷ A r' av`?? check `AppInv.app_xfer_raw` :141 -- it hands out a SECOND copy (for the era's slot) -- a linear token cannot be duplicated, but a `mono_nat_auth_own γ 1 0` CAN be split into two halves (`mono_nat_auth_own γ (1/2) 0` each) and the transport gives the new era a half… both halves needed to bump. DECIDE and report; if no shape closes, the fallback is to keep `echo_pred` as is and add a SEPARATE persistent-invariant-with-one-shot ("the console escrow": `inv consN' (cons_unmade ∨ (cons_made ∗ ⌜the console node is pinned⌝ …))`) -- but "the node is pinned" inside a plain invariant is not tied to the fs view either. The clean answer is the durable slot's linear authority: `A r av` linear in state 1 is fine IF `app_xfer_raw` is the boot/crash transport that does not need duplication of a linear part -- read it and rule.
O3' THE PINNED SECOND OPEN.  `PinnedOpen.v` (application side): `pinned_open_bundle` at open's families AT THE ARGUMENT'S PATH (PATH-ARGS: `open_au_pre_plain Γ γfs cw pl P Pmiss Fo Ft` and `open_receipt_plain … pl …`), answered from `PinnedObs.pinned_obs` at `Pin := fun av => cons_present av` (with `cons_made` in hand giving the claim law `□ (∀ v, app_pred … v -∗ app_pred … v ∗ (⌜cons_present v⌝ ∨ T))` from O2'), `hops := [ROOTINO; i]` -- `i` is EXISTENTIAL (the inum mknod chose): `pin_resolves_at` takes `ino`; instantiate at the `i` `cons_present` exhibits, or generalize `pinned_obs` to a pin that resolves to SOME node satisfying a predicate (`pin_resolves_pred Pin cw pl hops (fun a => a = MkAnode (ADev CONSOLE 0) 1)`) -- PREFER the generalization in `PinnedObs` (a small change; it is the same lemma with `∃ ino`), and `Ft` (the trunc piece) answered trivially (a device is not truncated: read `open_au_pre_plain`'s `Ft` role). The receipt's DEVICE arm at the pinned observation: `⌜arow_at av i (ADev CONSOLE 0)⌝`, `open_fd_rcpt true true (FdDevice CONSOLE) sts r fdv'` → `fdv' = <[fd := FdOpen true true (FdDevice CONSOLE)]> sts` at `fd = fd_lowest_closed sts` (check `open_fd_rcpt`), OR the taint; the FILE/DIR arms are REFUTED at the pinned node (`pobs_node`).
O4' THE RECEIPT-KEEPING OPEN LEAF.  `UkRunSys.wp_uk_ecall_open_recv_body` on `wp_uk_ecall_read_recv_body`'s mold BUT with the post stated at the TRAP key's `uvis_fd` for the entry table and the resume key's `fdv'` for the new one (the phase-1 report's item 5.5: `spost_at` at 15 reads `uvis_fd W` as the ENTRY table, so bind entry `fdv` and resume `fdv'` separately; `spost_at_cong` re-keys only across `skey_eq`); the deposit `udepwf N m pc USYS_open fdep` at an `xfam` whose open families are the pinned ones; the continuation receives `open_receipt … pl … sts r fdv'` and the ledger moved (`ualloc`/`ustd` as `wp_uk_ecall_open` moves them). `wp_uk_ecall_open` unchanged.
O5' INIT.  init's FIRST open (era 0, state 1): its receipt is the MISS arm (the walk of "console" dies: `cons_absent` says no entry) → `r = -1`; today `wp_kinit_open` uses the generic leaf and drops everything -- keep the generic leaf for the first open (init's C tests `< 0`); the repair arm's `wp_kinit_mknod` (UkInit.v:213) becomes the STEP: mknod's bundle at the argument's path "console" with `ma = CONSOLE, mi = 0` at the root; its AU step pays `app_top_update` moving the claim 1 → 2 (O2'; `cons_state_mknod`) and MINTS `cons_made`; then the SECOND open through O4' at the pin → the named ledger `ustd γfd (<[0 := FdOpen true true (FdDevice CONSOLE)]> fdt0-prefix)` (init's ledger is all-closed at entry: `fdt0`, `FdSlots.v:765`; `fd_lowest_closed_fdt0 = Some 0`) OR the taint; then the two dups through the TRACKED `wp_uk_ecall_dup`. THE FAILURE ARM (open can fail at `filealloc`/`fdalloc`): RULED BY THE OWNER -- acceptable. On it init owes only SAFETY: its continuation runs on the GENERIC slot (no console ledger, no pinned exec; sh never prints "$ ", the disciplined user never types, and the trace theorem holds vacuously on that run). State init's head as the disjunction (the named console ledger ∨ the taint ∨ the generic continuation) and do NOT add a room premise or try to prove the open succeeds. `UkInitMain`'s head carries the named ledger (or the taint) and hands it to the fork; the child arm carries `ustd (ukn_fd N') l ∗ ⌜l !! 0 = Some (FdOpen true true (FdDevice CONSOLE))⌝`; SH-LINE consumes it (`UConsLine.ush_std_cons`).
O6' SANITY: `grep -n "pinned_obs\|pinned_open_bundle" iris/PinnedExec.v iris/PinnedOpen.v` shows both as instances; `grep -rn "cons_present\|cons_made" iris/AppEcho.v iris/FsConsPin.v iris/PinnedOpen.v iris/UkInit.v` shows the claim, the flag, the step and the second open; `grep -n "wp_uk_ecall_dup_untracked" iris/UkInit.v` EMPTY; `git diff --stat -- iris/Spec*.v iris/Proof*.v iris/ConsoleInv.v` EMPTY (the `Ft`/`open_fd_rcpt` facts you need are READ, not changed); the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
O2''s ghost shape DECIDED and stated (`echo_pred` with `cons_state`, `cons_made`, the claim law, `era0_cons_absent`, `cons_state_mknod`), O3''s `pinned_open_bundle` (with `PinnedObs` generalized to a node PREDICATE if you take that route), O4''s leaf and O5''s init lemmas as STATEMENTS; NO Admitted at report time. Build with -k. Report: (1) the ghost shape and the claim law, quoted, and why it is provable (or the two candidates and where each fails -- then STOP); (2) `cons_absent`/`cons_present` and `era0_cons_absent`'s image evidence; (3) `pinned_open_bundle`'s statement and the receipt's DEVICE arm at the pin, quoted; (4) the failure-arm finding (can init's second open fail? what rules it out?); (5) the red files and why; (6) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (the general lemma, exec's instance, the console pin's transport, the open bundle, the leaf, init's open/dups/ledger, `echo_fs_pure`'s extension, every red file), O6. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
