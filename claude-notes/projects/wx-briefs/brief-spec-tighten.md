You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is SPEC-TIGHTEN, a small kernel-side lane: two syscall contracts over-approximate what the kernel does, in a way no constraining application can pay for, and are tightened to what the code does. Design of record: claude-notes/projects/app-echo.md "OPEN-PIN PHASE 1 LANDED + FINDINGS" item (c). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; design/fs-syscall-specs.md (the AU forms; "every view move on a dispatched path is an AU fire or a _step"); iris/SysOpenDefs.v (`open_au_pre_plain`/`_create` and `open_au_plain_at`/`_create_at` -- the `pf_at (atrunc_commit_at Γ appE) Ft` conjunct; `om_create`, `om_trunc`, `om_readable`, `om_writable`; `open_fd_rcpt` :~670); iris/SpecSysOpen.v (`open_in`, `open_receipt_plain`/`_create` -- where `Ft`'s piece is returned/spent on the FILE arm; `open_post_ok_*`/`_fail_*`; the ONE CONTRACT paragraph); iris/ProofSysOpen*.v (where the truncation AU fires: the `O_TRUNC` test in sys_open's C -- `if((omode & O_TRUNC) && ip->type == T_FILE) itrunc(ip)` -- find the block and the fire); iris/FsAbsCreateFire.v (`aunarm_commit_at` :~359 and the ARM `acre_commit_at_gen`/the arm leg; the design note calling them a do-then-undo PAIR); iris/SpecSysMknod.v (`mknod_au_pre`'s `cre_child_unfired Γ (ADev ma mi) Farm Fun` and the `Farm`/`Fun` families; `mknod_post_ok`/`_fail`) + iris/ProofSysMknod.v (the arm and unarm fires; `mkr_fail_arm`); iris/SpecSysOpen.v's create leg (`open_au_pre_create` carries the same `Farm Fun` -- the fix applies to both creators; check `SpecSysMkdir`/`SpecSysLink` if they share the pair); iris/FsAbsInvFire.v (the generic suppliers `fsabs_open_*`/`fsabs_mknod_*` pay `Ft`/`Fun` out of `app_sup` -- they must still be payable after the tightening, more easily); iris/FsConsPin.v (`file_pin_trunc`, `file_pin_unarm` -- what the pins NEED: `i <> ino` / `i <> ROOTINO`); iris/UexecExecInst.v (rows 15/17 -- unchanged in shape if the change is inside `open_in`/`mknod_au_at`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `tighten1`, `tighten2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group (sealed Parameters retyped in place listed); nothing Admitted; no new Axiom/Parameter. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact. NEVER weaken a contract: both changes make the kernel promise MORE (it demands less of the caller and says more precisely what it did).

=== THE DESIGN ===
T1 OPEN'S TRUNCATION COMMIT IS OWED ONLY WHEN THE CODE TRUNCATES.  sys_open truncates iff `(omode & O_TRUNC) && ip->type == T_FILE`. So `open_au_pre_plain`/`open_au_plain_at` (and the create forms if they carry `Ft`) carry `pf_at (atrunc_commit_at Γ appE) Ft` under the guard `if om_trunc vom then … else emp` (or take `Ft` only when `om_trunc vom = true`: PREFER the `if`, keeping the parameter list stable), exactly as `open_in` is guarded by `om_create vom`; and the commit, when owed, is keyed at the OPENED inum where possible (read what `atrunc_commit_at`'s `app_step` is stated over: if it is "every view row that is a FILE", state it at the row the walk resolved to -- the receipt already names `i`; if that needs the AU form to take `i`, do it: the fire site knows `ip`). The receipt's FILE arm returns `Ft`'s refund only when it was owed. `open_receipt_plain`/`_create`, `open_post_*`, the `_split` lemmas, `ProofSysOpen*`'s trunc block, `FsAbsInvFire.fsabs_open_*` (pays `emp` at `om_trunc = false` now), `PinnedOpen`'s `Ft` premise (drops when `om_trunc vom = false` -- init's `O_RDWR` open: state `pinned_open_bundle` at `om_trunc vom = false` with NO `Ft`).
T2 MKNOD'S UNARM IS THE UNDO OF ITS OWN ARM.  `aunarm_commit_at` fires at ANY nlink-1 row; the code only ever unarms the inode `ialloc` just returned (the arm), on the `dirlink` failure path. Tie them: the pair `Farm`/`Fun` share the inum -- the UNARM piece is stated at the inum the ARM's receipt named (`cre_child_unfired Γ node Farm Fun` becomes "arm at a fresh `i`, and the unarm, if fired, is at THAT `i`": read how `cre_child_unfired`'s two pieces are fired in `ProofSysMknod` (and `ProofSysOpen`'s create leg, `SpecSysMkdir`/`SpecSysLink` if they share it) and restate the unarm's AU at the armed inum -- the `Fun` family gains the inum as an index, or the pair becomes one piece with an ∃-bound `i` shared by both fires); `FsConsPin.file_pin_unarm`'s premises `i <> ino ∧ i <> ROOTINO` then follow from the arm's freshness (a fresh inum is not a pinned one and not the root). Every creator sharing the pair adapts; the generic suppliers pay as before.
T3 SANITY: `grep -n "om_trunc" iris/SysOpenDefs.v iris/SpecSysOpen.v` shows the guard; `grep -n "aunarm_commit_at" iris/*.v` shows every fire at the armed inum; `FsConsPin.file_pin_trunc`/`file_pin_unarm`'s premises are dischargeable from the contracts alone (state the two corollaries); the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
T1-T2 as STATEMENTS; NO Admitted at report time. Build with -k. Report: (1) the guarded `Ft` conjunct and the receipt's FILE arm, quoted; (2) the tied unarm, quoted, and which creators share the pair; (3) the red files and why; (4) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (sys_open's trunc block, the creators' arm/unarm fires, the generic suppliers, `PinnedOpen`/`FsConsPin`'s corollaries, every red file), T3. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 10 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
