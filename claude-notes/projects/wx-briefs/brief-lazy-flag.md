You are a KERNEL-and-U-tier lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel) in /shared/xv6iris-2. Your lane is LAZY-FLAG: the user-visible key `uvis` gains a boolean "this process may have lazily allocated pages", computed from the page table and the break at the trap boundary, so that a verified program whose key says `false` can refute consoleread's copyout-fault swallow (landed by lane CONS-SWALLOW) from its own byte ownership. Design of record: claude-notes/projects/app-echo.md "SH-LINE PHASE 2 -- THE SWALLOWED BYTE" and, inside it, "THE OWNER'S RULING ON THE FORK" with its REFINEMENT and TWO CORRECTIONS (read all three, first). The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; xv6-riscv/kernel/sysproc.c (sys_sbrk: SBRK_EAGER / n<0 → growproc; else lazy), proc.c (growproc), vm.c (vmfault, uvmalloc, uvmdealloc, uvmcopy), exec.c (the eager image: uvmalloc per segment from the running sz, then the guard+stack pages, uvmclear on the guard); iris/UexecSlot.v (`Record uvis` :92, `uvis_of` :183 -- THE key former from `ustate`; the PID-KEY precedent for adding a component is in this file's history: `git log -S uvis_pid -- iris/UexecSlot.v`); iris/UexecSG.v (`skey_eq` :196-232 -- MUST pin the new component; its five construction sites are listed in the CONS-SWALLOW note: UexecExecInst's two congruences, SpecUsertrap's `ut_sys_in_cong`/`ut_sys_out_cong` (+ ProofUservec), ProofUsertrapSys, ProofUserretClosed, UexecApply); iris/UexecRet.v (`uvis_of_run` :~169, `bump_at`/`bump` :~180-196, `urun_eq` :~391, `uslot_run` :~1554, `uslot_of_urun_all` :~1269); iris/UkRun.v (`urun` -- the run predicate over `uvis_of_run`; the U-tier leaves in iris/UkRunSys.v re-close it at the resume key from the rows); iris/UsysMemOk.v (`usys_mem_ok` :216 -- the per-syscall rows for M/π/sz; `usys_sbrk_perm` :205; `usys_sbrk_img` :183) and iris/UsysMemOkSpec.v (`perm_of_grow` :203, `perm_of_grow_below` :236 -- the table-free readings); iris/UserPerm.v (`perm_of` :106 = `omap perm_leaf um ∪ perm_fill um sz`; `perm_fill` = `uperm_rw` on `live_pages sz ∖ dom um`; `perm_of_lookup` :688; `perm_of_uptd_ext_sz` :810); iris/UserPtTree.v (`uva_wmapped` :269 and its lemmas, `uva_live` :711, `umem_lazy` :1245); iris/ProcPtOwn.v (`proc_pt_wf` :2005 -- `upt_map_wf`/`upt_acc_wf`: find the clause that makes every leaf in `ud_um` VALID (V=1), which `perm_leaf` (U and R only) does not test; `um_below` :2486; `uptd_ext`/`uptd_ext_sz` :536/:2515); iris/ProcDefs.v (`pprivate` :44, `ustate` :150); iris/ProcInv.v (`proc_priv_core` :1248 with `um_below`); iris/SpecSysSbrk.v (`sys_sbrk_ok` :109: the FAILED / EAGER (`sbrk_eager v1 ∨ n<0`, `growproc_ok`) / LAZY arms); iris/SpecGrowproc.v (`growproc_ok` :111: `dom (ud_um P') = dom (ud_um P) ∪ …` on grow, the dealloc arm on shrink); iris/SpecUvmalloc.v (:137 `dom P'.(ud_um) = dom P.(ud_um) ∪ vpn_run vpn0 n`); iris/SpecUvmcopy.v (the child's table vs the parent's -- find the dom/leaf relation); iris/SpecKexec.v + iris/KexecDefs.v + iris/ProofKexec*.v (how the NEW image's table is built and what its slot post says about the new key -- `exec_slot_pre` :~739 and `SpecSyscall.sysc_exec_out` :~620: the key `uvis_of U' sts' gn cs pid`); iris/SpecKfork.v (:368 the child's key `uvis_of (kfork_child Up) stsP g' ∅ pidc`) + iris/ProofKforkMain.v (:536 the child's projection equation); iris/SpecUsertrap.v + iris/ProofUsertrapSys.v + iris/ProofUserretClosed.v (the trap-out key and the rows' discharge; `Hpi0`/`Hsz0` at ProofUserretClosed :~372); iris/SpecSyscall.v + iris/ProofSyscall.v (where `usys_mem_ok` is proved per syscall -- find the one place the rows are discharged for the quiet syscalls and how `uptd_ext_sz` reaches it); iris/UexecExecInst.v (row 5 after CONS-SWALLOW: `∃ P, ⌜perm_of (ud_um P) (uvis_sz W) = uvis_perm W⌝ ∗ fileread_extra_core P …`; `spost_at_read_intro`); iris/SpecFileread.v (`console_receipt P Rd r M' addr` with the fault disjunct `⌜~ uva_wmapped P (uint (add_vec_int addr (Z.of_nat d)))⌝` after CONS-SWALLOW); iris/UkRunSys.v (`wp_uk_ecall_read_recv` :~2672 -- the receipt-keeping read leaf at the TRAPPING key); iris/UserHeap.v (`uheap` :563 -- `Md !! a = Some _ -> uw_addr pm a`; `ubytes` :256; `uheap_access`); iris/UkShMalloc.v (sh's sbrk at the U tier -- the one U-tier sbrk consumer: it is EAGER).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (names `lazy10`, `lazy11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. `UexecSlot`/`UexecSG`/`UsysMemOk` sit under most of the tree -- batch statements before each build. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. This lane may edit the kernel files named above (the key, the rows, the trap boundary, exec's/fork's/sbrk's slot posts and their proofs, row 5) and the U tier; everything else only to keep it compiling (report each such file). `ConsoleInv`, `FsAbs*`, `ChildTok`, the fs and device proofs are off limits: a needed change there is design friction, STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact. NO placeholder arms, NO premises nobody can discharge.

=== THE DESIGN ===
L1 THE PREDICATE.  `UserPerm.lazy_free (um : gmap (mword 27) (mword 64)) (sz : Z) : Prop := live_pages sz ⊆ dom um` (decidable), with the reading lemma the consumer needs:
   `lazy_free_wmapped : proc_pt_wf P -> lazy_free (ud_um P) sz -> perm_of (ud_um P) sz !! p = Some q -> up_W q = true -> exists w, ud_um P !! p = Some w /\ pte_vu w /\ pte_w w`
   (a fill entry cannot exist; a leaf entry's `up_W` is `pte_bit w 2` = `pte_w w`; U from `perm_leaf`; V from `proc_pt_wf` -- find the clause), and its byte form `lazy_free_uw_addr : … -> uw_addr (perm_of (ud_um P) sz) a -> uva_wmapped P a`.
L2 THE KEY.  `uvis` gains `uvis_lazy : bool`, LAST; `uvis_of` computes it: `bool_decide (~ lazy_free (ud_um (pv_upt (us_V U))) (uint (pv_sz (us_V U))))`; `bump_at`/`bump` take `lz'`; `skey_eq` pins it (`uvis_lazy W = uvis_lazy W'`), discharged at the five sites. `uvis_of_run` takes NO parameter and puts the literal `false` (the U tier's run is at `false`: L6).
L3 THE ROWS.  `usys_mem_ok` gains, beside `π' = π /\ szv' = szv` on every quiet row and on read/write/wait/pipe/fstat/fork, the clause `(lz = false -> lz' = false)` (state it once as `usys_lazy_keep lz lz'` and use it everywhere); exec's failure row: `lz' = lz`; sbrk's row: `usys_sbrk_lazy lz lz' tf szv szv'` := `(sbrk_eager (arg 1) \/ shrink) -> (lz = false -> lz' = false)` -- the LAZY-grow arm promises nothing about `lz'`. Discharge: quiet syscalls from `uptd_ext_sz` (the table only grows: `dom` grows, `live_pages` unchanged -- `lazy_free` is monotone in `um` and antitone in `sz`); sbrk eager grow from `growproc_ok`'s `dom` equation (the run of new pages is exactly `live_pages sz' ∖ live_pages sz`, up to `pgroundup`: state the arithmetic lemma once, `perm_of_grow` has the mold); sbrk shrink from dealloc's arm (`live_pages szv'` ⊆ the surviving domain: find `growproc_ok`'s shrink clause -- if it only says `dom P' ⊆ dom P` you will need what `uvmdealloc` removes; `um_below` bounds what CAN be mapped); fork's child from `SpecUvmcopy` (leaves copied at the same vpns); wait/pipe/read/write/fstat: their copyouts go through vmfault only (extension).
L4 EXEC'S NEW KEY.  The slot key after a successful exec (`sysc_exec_out`'s `uvis_of U' sts' gn cs pid`, reaching the program through `exec_slot_pre`/`PinnedExec`'s `S W'`) carries `⌜uvis_lazy W' = false⌝`: the fresh image covers `[0, sz')` -- every segment's `uvmalloc` runs from the CURRENT sz (gaps are filled), then the guard+stack pair; `uvmclear` clears U on the guard page but the page stays in `ud_um` (so `dom` covers it and `perm_of` shows nothing there, both fine). Find where exec's proof knows the final table's `dom` (KexecDefs / ProofKexecB*/Tail) and state the coverage lemma there; if the exec proof has lost `dom` by the tail, STOP AND REPORT with what it does keep.
L5 ROW 5'S TIE.  `∃ P, ⌜perm_of (ud_um P) (uvis_sz W) = uvis_perm W⌝ ∗ …` gains `⌜proc_pt_wf P⌝ ∗ ⌜uvis_lazy W = bool_decide (~ lazy_free (ud_um P) (uvis_sz W))⌝` (definitional at the dispatcher's instantiation `P := pv_upt (us_V U)`; `proc_pt_wf` from `proc_ptm`/`proc_priv_core`).
L6 THE U TIER.  `urun` is at `uvis_lazy = false` (`uvis_of_run`'s literal). Every leaf in `UkRunSys` that re-closes the run at the resume key proves `lz' = false` from `usys_lazy_keep` and the trapping key's `false`; the sbrk leaf (find it -- `UkShMalloc`'s consumer) proves it from `usys_sbrk_lazy` at the EAGER argument (sh's `sbrk` passes SBRK_EAGER: read the U-tier call's argument row); the exec leaf/`uslot_of_urun_all` takes `⌜uvis_lazy W = false⌝` (from L4); the fork leaf's child from the fork row. `wp_uk_ecall_read_recv`'s continuation gains `⌜uvis_lazy W = false⌝` (W the trapping key it already binds). THE DELIVERABLE: `UkRunSys.uk_read_nofault : ubytes (ukn_d N) (uint dst) k f -∗ urun-side facts (pm = perm_of (ud_um pt) sz from urun; the leaf has them) -∗ ⌜row-5 tie for P⌝ -∗ ⌜d < k⌝ -∗ ⌜~ uva_wmapped P (uint (add_vec_int dst (Z.of_nat d)))⌝ -∗ False` -- state it in whatever form lets SH-LINE 2b eliminate the receipt's fault disjunct inside `wp_uk_ecall_read_recv`'s post with the ∃ P of row 5 (you may instead fold it into `wp_uk_ecall_read_recv` itself: hand back `spost_at` AND the pure fact `⌜∀ P va, tie P -> uw_addr pm va-ish -> uva_wmapped P va⌝`; choose the form that keeps `UConsLine.ush_read_recv_leaf` (an unproved program-side statement owned by 2b; do not edit it) provable from the leaf, and say why).
L7 SANITY: `grep -n "uvis_lazy" iris/UexecSG.v` shows the pin; `grep -n "usys_lazy_keep\|usys_sbrk_lazy" iris/UsysMemOk.v iris/ProofSyscall.v iris/ProofUsertrapSys.v` shows the rows and their discharge; `grep -n "uvis_lazy W' = false\|uvis_lazy .* = false" iris/SpecKexec.v iris/SpecSyscall.v iris/UkRunSys.v` shows L4/L6; the audit unchanged; nothing Admitted; `git diff --stat -- iris/ConsoleInv.v iris/FsAbs*.v iris/ChildTok.v` EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
L1-L2 landed and proved (the predicate + lemmas, the key component, `skey_eq` and its five sites, the formers); L3-L6 as STATEMENTS with every downstream file compiling (rows added; their discharge sites may temporarily be closed by the OLD proofs only where the new clause is `True`-free -- if a row's new clause cannot yet be proved, do NOT weaken it: leave the statement out of the tree and report it verbatim as phase 2's target, exactly as CONS-SWALLOW phase 1 did). NO Admitted at report time. Report: (1) the new statements verbatim, file:line; (2) for each of exec / sbrk-eager-grow / sbrk-shrink / fork / quiet, which existing hypothesis gives `lazy_free` preservation, quoted (file:line), or what is MISSING; (3) the red files and why; (4) anything this brief got wrong about the landed code, with file:line evidence.

=== PHASE 2 -- after go-ahead ===
The proofs (L3-L6), L7, the gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, an as-landed note (≤ 20 lines, present tense), anything still owed with file:line. Do not commit or stage. If context runs short, STOP after a green build or at a clear milestone and report exactly what remains, file:line.
