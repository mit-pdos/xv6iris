You are a U-tier lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-sup (a sibling clone; the main checkout /shared/xv6iris-2 belongs to other lanes -- never touch it, never build it). Your lane is SUPPLY-SPLIT: today every verified program's entry slot takes `UkRun.udep`, whose only producer is the generic supply `□ app_sup` (`UexecExecMint.udep_gen`), and for the echo application `app_sup` is equivalent to the TAINT (`AppEcho.echo_taint_of_sup`); so init, sh and echo can only be entered tainted, which defeats the theorem. The intended design already exists as a comment: "a verified program with a weaker supplier declares its own `uprogSG` instance beside its constructor" (`UexecExecInst.v:~895`, `UexecSG.v:~554`). Build it. Design of record: claude-notes/projects/review-echo-plan-2026-09-12.md finding 1 (and 5, 9, 13 for context); claude-notes/projects/app-echo.md "The two options for the generic slot's supply -- RULED option 2", "CONS-ROUTE LANDED", "OPEN-PIN COMPLETE" (the claim-based deposits that already exist: `UkRun.udepwf_at` for open/mknod, `udepwf_std` for read), "GENERIC-PAY LANDED".

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; iris/UexecSG.v (`Class uprogSG` :558 -- `Dsup`, `psok`; :151-160 how `psok`'s discharge travels beside the supplier; `sbundle_of_supply_ne`; `sbundle_pay`, `sbundle_at`); iris/UexecExecInst.v (`uprogSG_gen` :~900 at `Dsup := xv6_ssupply := app_sup`, `psok := fun _ => True`; `xv6_sbundle` and the PER-NUMBER bundle definitions for every syscall number -- read each: which need an application resource (an fs AU precondition, a console credential, a pin) and which need NOTHING; `xv6_sbundle_of_supply` :~778 (now at a constant payload, GENERIC-PAY)); iris/UkRun.v (`udep` :302 -- `□ Dsup ∗ ⌜∀ n W Q, psok n -> n <> USYS_exec -> ⊢ □ Dsup ==∗ sbundle_pay uslot n Q W⌝`; `udep_dep`; `udepw`, `udepwf`, `udepwf_at` (:~600), `udepwf_std` (:~632), `uxsup_at`; `urun`'s `#Hdep` conjunct -- `urun` CARRIES `udep`); iris/UexecExecMint.v (`udep_gen` :71; `uslot_mint`/`uslot_mint_pay` :86/:104 -- how the generic slot gets its `udep`); iris/UkRunSys.v (every leaf: which mint from `udep` (`udep_dep`/`udepw_mint`) and at which number; `wp_uk_ecall_quiet`, `wp_uk_ecall_window`, `wp_uk_ecall_dup*`, `wp_uk_ecall_open_recv`, `wp_uk_ecall_read_recv`, `wp_uk_ecall_exit`, fork/wait/sbrk/close leaves); the 27 program files with `Hypothesis Hpsok : forall k, k <> USYS_exec -> psok k` (`grep -rln Hpsok iris/`): iris/UkInit*.v, iris/UInit*.v, iris/UkSh*.v, iris/UShKernel.v, iris/UkEcho*.v, iris/UEchoKernel.v, iris/USyncKernel.v, iris/UkSync*.v (read each header to see which leaves each program uses at which numbers); iris/UInitKernel.v (`init_uexec_slot` :~203 takes `udep`), iris/UShKernel.v (`sh_uexec_slot`), iris/UEchoKernel.v (`echo_uexec_slot` :~430), iris/USyncKernel.v (`sync_uexec_slot` :~167); iris/UexecCond.v (`cond_entry_slot` :~296: the gated entry -- sync's and echo's constructors are tried at the trivial payload with the GENERIC `udep`: this is where the verified constructors are handed `udep` today); iris/AppInv.v (`app_sup` :206, `app_sup_raw`, `app_inv`), iris/AppEcho.v (`echo_taint_of_sup` :932, `echo_sup_of_taint`); the user programs xv6-riscv/user/{init,sh,echo}.c + ulib.c/umalloc.c/printf.c (which syscalls each actually makes).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-sup <logname>` FROM /shared/xv6iris-2-sup (names `sup10`, `sup11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'` from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Your remote tree was seeded from the main tree; the first build may recompile a large cone. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-sup` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter. Kernel files (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`) are off limits; `UexecExecInst.v` (the per-number bundles and instances), `UexecSG.v`, `UexecExecMint.v`, `UexecCond.v`, `UkRun*.v` and the program files ARE yours. GIT: work on a branch `lane/supply-split` in your checkout; commit there when green; never push; never stash/reset/add -A/commit -a/amend; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. NO placeholder premises; a verified program's slot may NOT take `udep` at the generic supply when it is done.

=== THE DESIGN ===
P1 CLASSIFY.  For every syscall number's bundle in `UexecExecInst`, decide: FREE (`⊢ sbundle_pay uslot n Q W` for all `Q W`, or from `Q (-1)` alone -- e.g. getpid, exit, fork, wait, sbrk, kill, dup, close of a device?, pipe?), CLAIM (payable at the program's own keys from the application's claim/pins/payload -- open at a pinned path (landed `udepwf_at`), read of the console (landed `udepwf_std` + the payload wand), mknod (landed), write to the console device (NOT landed -- the E5 output lane's business: do not build it; report what the console-write bundle needs), chdir/close/fstat of an inode (say what they need)), or TAINT-ONLY (needs `app_sup` outright). Evidence per number: the bundle's definition, file:line. THIS TABLE IS THE MAIN PHASE-1 DELIVERABLE.
P2 THE FREE SUPPLY.  `UexecExecInst.uprogSG_free : uprogSG Σ := {| Dsup := True (or emp); psok := free_numbers |}` with the law `udep_free : ⊢ udep` at that instance (the proof is the FREE column of P1, per number). Keep `uprogSG_gen` for the generic slot untouched.
P3 THE PROGRAMS.  Each verified program's files are at the instance `uprogSG_free` (the section's `uprogSG` binder instantiated, `Hpsok` replaced by the free numbers' membership at each generic-leaf site -- a leaf at a CLAIM number must already be going through its claim-based supplier (open/mknod/read) or is flagged); `init_uexec_slot`/`sh_uexec_slot`/`echo_uexec_slot`/`sync_uexec_slot` take `udep` AT THE FREE INSTANCE, which their callers supply by `udep_free`, never from `app_sup`; `UexecCond.cond_entry_slot`'s gated arms hand the constructors `udep_free` (the generic arm keeps `udep_gen`). `urun`'s `#Hdep` conjunct is at the program's instance (check how `urun` is parameterised by the class -- if `urun` is stated at ONE class instance per section, the programs' sections simply bind `uprogSG_free`).
P4 THE FLAGGED SITES.  Every generic-leaf use at a CLAIM/TAINT-ONLY number in init/sh/echo that has no claim-based supplier yet: list them (expected: write(16) in all three -- init's banner, sh's prompt and diagnostics, echo's output -- and sh's `open("console")` at its start and its `close`; possibly chdir). For each, leave the program lemma taking a NAMED premise for that one deposit (e.g. `Hwr : udepwf_at … USYS_write …` shaped like open's), NOT `udep` -- so the taint is no longer a precondition of any verified slot, and the premises are exactly the work owed to E5 / the sh-open lane. Report the list with file:line.
P5 SANITY: `grep -rn "udep_gen" iris/*.v` shows only the generic slot's mint(s); `grep -rn "Hpsok" iris/*.v` EMPTY; `grep -rn "app_sup" iris/UInit*.v iris/UkInit*.v iris/UkSh*.v iris/UShKernel.v iris/UkEcho*.v iris/UEchoKernel.v` EMPTY except the taint arms (`echo_sup_of_taint` uses); the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
P1's table (complete); P2 stated with the free numbers' proofs done where short; P3 as statements where the sweep is mechanical, compiling. NO Admitted at report time. Report: (1) the table; (2) `udep_free`'s statement and which numbers are proved; (3) how `urun`/the leaves are parameterised by the class and what P3's sweep touches (file count); (4) P4's list; (5) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
P2-P5, the gate, commit on `lane/supply-split`. Report log name, the commit hash, `git diff --stat main..lane/supply-split | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), P4's final list.
