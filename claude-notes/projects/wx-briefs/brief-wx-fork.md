You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-FORK, the second of four lanes giving exit()/wait() a resource-transfer contract. Design of record: claude-notes/projects/app-echo.md, section "WAIT-EXIT — DESIGN OF RECORD" (the ESCROW shape) and its "WX-KEY LANDED" note -- read both in full first. WX-KEY landed the KEY's two readings (`uvis_gen`, `uvis_ch`), the children mirror in the Uk engine (`UserChildren`, `ukn_ch`, `urun`'s `uch_auth`), the quiet rows, `WaitInv.children_own_at`, and a PLACEHOLDER generation cell in `SchedCtx.proc_pub` (`proc_gen pa := ∃ γg, gen_tok γg ∗ gen_slot γg pa`, SchedCtx.v:251/:254; `gen_alloc` :227; `proc_gen_fresh` :264 re-mints at allocproc, ProofAllocproc.v:~1755; main's assembly mints every slot's first incarnation from `proc_pub_bare`, ProofMain.v:~1115) with the two readings threaded as ∀-parameters of the park package (`park_cap`'s `∀ sts gn cs`, paid by the parker). The tree is at `9117006fb` (`git log --oneline -2`: the notes commit, then "WX-KEY: …"). This lane makes fork MINT: the generation becomes a saved predicate, the parent gets `child_tok`, the child gets `my_pay`, the readings get RESOURCES behind them, and the generic fork bundle chooses the trivial payload. The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Proofmode & bitvector gotchas"); remote-build-gcp.md; design/user-wp-slot.md; design/proc-struct.md; the WX-KEY note's file:line list (WaitInv: `children_own_at γc cs` :171, `children_own_at_upd` :181, `children_wf` :197 stated not carried, `wait_res_at γc ξ` :209; SpecSyscall `sysc_fork_in` :404; SpecUsertrap `ut_fork_in` :~489; UexecRet `uexec_fork_parent_F` :668 / `_child_F` :712 / `uexec_fork_F` :718; UkFork `wp_uk_ecall_fork` :779; UexecExecInst `xv6_sbundle_of_supply` :618 -- the generic FORK deposit is built elsewhere (UexecCond's entry slot / the closed loop): grep `uexec_fork_F` for its generic payer); iris/ProcDefs.v (`pprivate` :39 -- `pv_fdg : gname` is THE MOLD for a ghost field naming a per-process resource; "LAST, so every positional MkPPriv only gained a trailing argument"); iris/FdSlots.v (`fd_frags (pv_fdg V) sts` :568 -- the per-process resource that rides the trap residue, `SpecUsertrap.v:~1140`, `UsertrapRes.v:~762`); iris/SpecAllocproc.v (`allocproc_post` found arm: where the private block is built); iris/SpecKfork.v (`kfork_post` :~193, the pid arm; kfork holds `wait_lock` when writing `np->parent`); iris/SpecSysFork.v; iris/SpecSyscall.v (`sysc_fork_in` -- after WX-KEY `∀ g', uslot (uvis_of (kfork_child U) sts g' ∅)`; the dispatcher's fork row in the returning post); iris/SpecUsertrap.v (`ut_fork_in`); iris/UexecRet.v (`uexec_fork_parent_F`/`_child_F`/`uexec_fork_F`); iris/UexecApply.v (`uexec_ret_round_slot`'s fork case); iris/UkFork.v (`wp_uk_ecall_fork` :~777); iris/UexecExecInst.v (`xv6_sbundle_of_supply` -- the generic bundles from a supply); iris/Xv6Cameras.v.

=== OPERATING RULES (hard) ===
- BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 under `run_in_background` with the log's `COMPILED=`/`EXIT=` lines as the sentinel; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`; ONE build at a time; never a remote top-level `make`; never local `coqc`. Never `simpl`/`cbn` a goal mentioning a dumped byte literal. `pprivate` is near the bottom: batch the statements before the first build. Pure files (`KforkChild`, `KexecBridge`) must not gain iris imports (ssreflect `rewrite` grammar -- WX-KEY's note).
- Gate: zero `Error`, `EXIT=0`, COMPILED>0; `make audit-only` on the VM (tree root `/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` clean (zero Admitted; no new Axiom/Hypothesis/Parameter; a sealed module's changed Parameter reported as such).
- GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`. DO NOT COMMIT OR STAGE. No other checkouts, no worktrees. Temp files in this scratchpad. Do not edit claude-notes/.
- DESIGN FRICTION = STOP AND REPORT. Never weaken a landed form. Comments state the current shape as fact (no dates, no "used to"/"now").

=== THE DESIGN (this lane's part) ===
F1. THE GENERATION IS A SAVED PREDICATE WITH ITS IDENTITY.  New `iris/ChildTok.v`: camera `savedAnythingG Σ (prodOF (constOF (leibnizO (nat * mword 32))) (Z -d> ▶ ∙))` (a generation carries its slot, its pid and its payload; add the instance to `Xv6Cameras`/`xv6G` the way the tree adds cameras); `gen_own γ dq j pid Q := saved_anything_own γ dq ((j, pid), Q)`. Readings: `gen_slot γ j`, `gen_pid γ pid` (persistent ONLY through a discarded fraction -- see `my_pay`; WX-KEY's `gen_slot γ pa` is keyed on the slot ADDRESS -- keep that key, it is `proc_pub`'s parameter); agreement `gen_agree : gen_own γ dq j pid Q -∗ gen_own γ dq' j' pid' Q' -∗ ⌜j = j' ∧ pid = pid'⌝ ∗ ▷ (∀ xs, Q xs ≡ Q' xs)` (the ▷ is the saved predicate's; state the combination rule so consumers strip it: `child_tok γ pid Q ∗ exit_tok γ pid xs ⊢ ▷ Q xs`, or prove the later-free form if `Q xs` is Timeless -- offer both). Definitions: `child_tok γ pid Q := gen_own γ (DfracOwn (1/4)) j pid Q` (∃ j; the PARENT's token), `gen_kq γ j pid Q := gen_own γ (DfracOwn (1/4)) j pid Q` (the KERNEL's quarter, kept in the child's private block until exit -- WX-EXIT moves it into the escrow), `my_pay γ Q := ∃ j pid, gen_own γ DfracDiscarded j pid Q` (persistent; the child's knowledge of its own payload); `exit_tok γ pid xs := ∃ j Q, gen_kq γ j pid Q ∗ Q xs` (WX-EXIT/WAIT produce it; state it now). Allocation: `gen_alloc j pid : ⊢ |==> ∃ γ, gen_own γ (DfracOwn 1) j pid (fun _ => True%I)`; update at full ownership `gen_set Q' : gen_own γ (DfracOwn 1) j pid Q ==∗ gen_own γ (DfracOwn 1) j pid Q'` (`saved_anything_update`); the split `gen_own γ 1 … ==∗ child_tok ∗ gen_kq ∗ my_pay` (1/4 + 1/4 + discard 1/2).
F2. THE PRIVATE BLOCK NAMES BOTH GHOSTS.  `ProcDefs.pprivate` gains TWO trailing ghost fields (the `pv_fdg`/`pv_cwi` mold: "LAST, so every positional MkPPriv only gained a trailing argument"): `pv_gen : gname` (this process's generation) and `pv_chg : gname` (the name of its children ghost_var). The key readings become READINGS OF THE BLOCK: `uvis_gen (uvis_of U sts …) = pv_gen (us_V U)`, and `uvis_ch` is read off the per-process resource `ch_frag (pv_chg (us_V U)) cs := ghost_var (pv_chg (us_V U)) (1/2) cs` that rides the trap residue BESIDE `fd_frags (pv_fdg (us_V U)) sts` (`UsertrapRes`, `SpecUsertrap.v:~1140/1153`, the park channel where `sts` is captured). CONSEQUENCE FOR WX-KEY's shape: `uvis_of U sts g cs` keeps its arity but `g` is now `pv_gen (us_V U)` at every kernel site (state `uvis_of_gen` and let the sites pass it), and `park_cap`'s `∀ gn cs` collapses to the block's `pv_gen` and the residue's `cs` -- decide whether to keep the ∀ (paid trivially) or remove the parameters; PREFER removing what the block now determines. The placeholder `gen_tok γg ∗ gen_slot γg j` in `proc_pub` GOES (the generation lives in the private block: `proc_priv`'s core gains `gen_kq (pv_gen V) j pid Q` for some Q -- ∃-bundled, the kernel's quarter -- and `ch_frag`'s OTHER half sits in the `wait_lock` cell: `WaitInv.children_own_at ξ cs` becomes, per slot j, `∃ γ, <the cell names γ> ∗ ghost_var γ (1/2) (cs !! j)` where the name is installed by allocproc/kfork when the block is built under `wait_lock` -- design the cell so that kfork, holding `wait_lock` AND the parent's residue (its own `ch_frag`), can `ghost_var_update` the parent's set with both halves, and kwait (WX-WAIT) likewise; reparent (WX-EXIT) touches a separate `orphans_own` and never the mirror).  `proc_priv_core`'s new conjuncts and every positional `MkPPriv`/`proc_priv` destruct adapt (a wide but mechanical sweep; list the files).
F3. ALLOCPROC MINTS; KFORK SETS THE PAYLOAD AND SPLITS.  `allocproc_post`'s found arm: the new block has `pv_gen U = γ` for a fresh γ with `gen_own γ 1 j pid (fun _ => True)` (`gen_alloc` at the slot and the pid allocpid chose) and `pv_chg U = γc` fresh with `ghost_var γc (1/2) ∅` in the block's residue-side resources and the other half installed in the `wait_lock` cell (allocproc does NOT hold `wait_lock` -- so the cell's install must happen in kfork (which does) and in userinit (which takes `wait_lock`? check; if neither holds it at the right moment, the cell is `option`-valued: `None` until the first fork/wait by that slot installs, and `children_wf` treats `None` as `∅` -- decide and report). kfork: `gen_set Q` at full ownership, then the split: `child_tok γ pid Q` goes to the parent (kfork_post's pid arm: `∃ pidv γ, ⌜rv = sign_extend' 64 pidv⌝ ∗ ⌜1 <= pidv <= PIDMAX⌝ ∗ child_tok γ pidv Q ∗ ⌜γ ∉ cs⌝` with the parent's children set moved `cs → cs ∪ {γ}` in BOTH halves under `wait_lock`), `gen_kq` stays in the child's block, `my_pay γ Q` is handed to the child's slot: the slot premise kfork takes becomes `∀ γ, my_pay γ Q -∗ uslot (uvis_of (kfork_child U) sts γ ∅)`; `Q` reaches kfork from the fork bundle (`sysc_fork_in`/`ut_fork_in` gain `∃ Q, …` with the child slot under `my_pay`; the dispatcher's fork row relays `child_tok γ pid Q` and `cs' = cs ∪ {γ}` to the round; `uexec_fork_F X W := ∃ Q, uexec_fork_parent_F X W Q ∗ (∀ γ, my_pay γ Q -∗ uexec_fork_child_F X W γ)` with the parent arm `∀ r fdv' cw' cs' γ, ⌜r <> 0⌝ -∗ … -∗ ⌜cs' = uvis_ch W ∪ {γ}⌝ -∗ child_tok γ (pid_of r) Q -∗ X (bump W r … cs')`). `UsysMemOk.usys_ch_ok` gains the fork row (`∃ γ, cs' = cs ∪ {γ}` at `USYS_fork`; quiet elsewhere); `UexecApply.uexec_ret_round_slot`'s fork case relays it (WX-KEY's `eq_refl` premises go).
F4. THE U TIER.  `UkFork.wp_uk_ecall_fork` takes `(Q : Z -> iProp Σ)`; its parent arm gains `child_tok γ r Q -∗ uch (ukn_ch N) (S ∪ {γ}) -∗` (the program hands its `uch (ukn_ch N) S` fragment in, as it hands `ucwd`), its child arm gains `my_pay (ukn_gen? no -- γ') Q -∗ uch (ukn_ch N') ∅ -∗`; `uch_update` in the leaf's proof. Every caller passes `Q := fun _ => True%I` this lane (UkInitMain's `wp_kinit_fork`, UkShFork's, UkShDiag's) and drops the token/`my_pay` -- statements of the program lemmas unchanged except where they hand `uch` in; init/sh's payloads are L7's.
F5. THE GENERIC SLOT.  `UexecExecInst.xv6_sbundle_of_supply` (and whatever builds the generic fork deposit from a supply) instantiates `Q := fun _ => True%I` and a child slot that ignores `my_pay`; `InitBoot.init_boot_bundle_triv` unaffected (exec, not fork). Exit's deposit stays `emp` this lane.
F6. SANITY.  `grep -n "gen_tok\|gen_slot" iris/*.v` names only ChildTok's readings (the placeholder is gone); `grep -n "pv_gen\|pv_chg" iris/ProcDefs.v iris/UexecSlot.v iris/SpecUsertrap.v` shows the block determining the key; `grep -rn "child_tok" iris/*.v` names ChildTok, SpecKfork, SpecSysFork, SpecSyscall, UexecRet, UexecApply, UkFork; every `Uk*.v` program file compiles with statements unchanged except `Q := fun _ => True` at fork sites.

=== PHASE 1 -- then STOP AND REPORT ===
F1 proved (ChildTok.v is small); F2's field additions and the readings' redefinition as STATEMENTS with the residue's `ch_frag` and the `wait_lock` cell's shape decided; F3's statements (allocproc_post, kfork_post, the bundle rows, the round's fork case); F4's leaf statement; NO Admitted in the tree at report time. Build with -k. Report: (1) ChildTok's definitions and the combination rule, quoted; (2) how `uvis_gen`/`uvis_ch` are now read (the block field, the residue resource, the cell) and what happened to `park_cap`'s `∀ gn cs`; (3) the cell-install question (who installs the `wait_lock` half for a fresh block; `option`-valued or not); (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (allocproc's mint, kfork's set/split/children move under wait_lock, sys_fork, the dispatcher's fork row, the round, the u-tier leaf), F5, every red file, F6. Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, new files, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build and report exactly what remains, file:line.
