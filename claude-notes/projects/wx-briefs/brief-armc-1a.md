You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is ARM-c (1a): THE KERNEL NEVER MINTS. The first process's user-execution slot stops being minted by userinit out of the application's supply `app_sup`; instead userinit is handed, from the theorem, the EXEC BUNDLE forkret's boot arm spends on `kexec("/init")`, whose slot piece yields the slot at the key kexec builds. `app_sup` then leaves every kernel contract and `Happ_sup` leaves the theorems; the generic corollary re-discharges the new hypothesis from the trivial bundle and the trivial mint. Design of record: claude-notes/projects/app-echo.md ("ARM-c — DESIGN", "THE PINNED EXEC BUNDLE — design", "PID-ROW LANDED" (the freed `app_sup` consumers), "PINNED-EXEC D LANDED") and this brief. The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1. Echo's own discharge of the new hypothesis (the pinned bundle at "/init", the taint) is lane (1b), NOT yours; your lane ends with the generic corollary green and the tree free of `app_sup` in kernel contracts.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle" -- the GAP-premise trap, "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Proofmode & bitvector gotchas"); remote-build-gcp.md; design/user-wp-slot.md (SS4c, the park modes); the headers of iris/ParkCap.v, iris/SyscParkEnv.v, iris/SpecForkret.v, iris/SpecUserinit.v, iris/AppInv.v (`app_sup_raw`), iris/App.v, iris/SystemAdequacy.v (~375-395, the `Happ_sup` paragraph), iris/SpecKexec.v (`exec_au_pre`, `exec_au_pre_triv` ~699-740).

=== OPERATING RULES (hard) ===
- BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 under `run_in_background` with the log's `COMPILED=`/`EXIT=` lines as the sentinel; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`; ONE build at a time; never a remote top-level `make`; never local `coqc`. Never `simpl`/`cbn` a goal mentioning a dumped byte literal.
- Gate: zero `Error`, `EXIT=0`, COMPILED>0; `make audit-only` on the VM (tree root `/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" -- THIS LANE TOUCHES THE THEOREM, so the audit is the gate that matters most; `tools/lemma_diff.py` clean (zero Admitted; no new Axiom/Hypothesis/Parameter; the theorem's hypothesis SWAP is reported as such).
- GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`. DO NOT COMMIT OR STAGE. No other checkouts, no worktrees. Temp files in this scratchpad. Do not edit claude-notes/.
- DESIGN FRICTION = STOP AND REPORT. Never weaken a landed form. Comments state the current shape as fact (no dates, no "used to"/"now"). The sweep of `app_sup` PROSE is part of the lane (every "THE APPLICATION'S SUPPLY (the ARM…)" paragraph at a deleted row goes; `AppInv`'s `app_sup_raw`/`app_sup` DEFINITIONS and the supply-parametric construction (`xv6_ssupply`, `uslot_mint`, the `fsabs_*`/`caf_*`/`lnk_*` dischargers) STAY -- they are how a generic slot's bundles are built from a supply, used by whoever holds one).

=== WHERE THE SUPPLY IS TODAY (file:line verified 2026-09-09) ===
- The ONE kernel spend: `ProofUserinit.v:806-808` -- `iPoseProof UG.uexec_wp_gen as "#Hgen"; iDestruct (UexecExecMint.uslot_mint with "Hsup Hgen") as "#Hmkgen"` building `Hjslot : ∀ W, ⌜uvis_fd W = fdt0 ∧ uvis_cwd W = <ROOTINO cwd>⌝ -∗ uslot W` (:~795-805), handed to `park_token_park … Hjslot` (:812). `park_token_park` (ParkCap.v:323) takes `app_sup` (:332) and the slot FAMILY beside `fd_frags (pv_fdg (us_V U)) sts` (:~350). `park_pkg` (ParkCap.v:85) carries `app_sup` (:122) and the mode `Wk : option uvis` (:114): `None` = "captured a slot FAMILY, because the resume may still run the boot arm's kexec("/init")"; `Some Wk` = steady, one slot at `Wk` with `first_done`. `forkret_closer` (SpecForkret.v:273) mirrors the mode (:281-289).
- forkret's boot arm: `ProofForkret.v:1488-1531` hands kexec the TRIVIAL bundle `exec_au_pre_triv (fs_gamma_L fsc_fs) fsc_fs (pv_cwi (us_V U)) (DirentEnc.bview 5 fkr_init_bytes) 1 (fun _ => 5) (fun _ => fkr_init_bytes) nil` with `MkPfam (fun _ => emp) True` ("forkret's own user WP comes from the park closer at the tail, not from exec"; `sts := nil` because "the descriptor view the key would be built at is unconstrained here"); the tail (:770-795) re-keys the closer's slot (`uslot_ukc`) at the resumed state, `sts` being the closer's own reading of the parked `fd_frags`.
- Standing, unspent rows: `SpecUserretClosed.v:169` + `ProofUserretClosed.v:255` (loop premise; `Require Import AppInv` :104); `SyscParkEnv.v:126` (`park_world`'s row) + `park_world_sup` :145; `ParkCap.v:122, :332, :468`; `ProofForkret.v:~239, ~884`; `SpecUserinit.v:226`; `SpecMain.v:~651`; `BootShared.v:~1471, ~1503`; `BootChain.v:~406`; `SystemAdequacy.v:564` (`Hsup0 := Happ_sup r`), `:676` (threaded into main's premise), the hypothesis at `:394`; `App.v:202` (`Happ_sup`), `:248` (passed through), `:317-330` (`xv6_app_adequacy_triv_xv6Σ` discharges it by `app_sup_raw_triv`).
- What the application CAN supply: `SpecKexec.exec_au_pre (MkPfam uslot R) Γ γfs cw P Pmiss Fo pl na alen afun sts` at ONE path (:719, `exec_au_pre_triv` :~735 is the trivial one at a `True` cursor/receipt and slot `S := fun _ => emp`); `PinnedExec.pinned_exec_bundle_at` yields `sys_exec_au_pre …` (the ∀-over-readings form; a kexec-tier `pinned_exec_au_pre` at one `pl` is (1b)'s, not yours). `UexecExecInst.xv6_sbundle_of_supply`/`UexecExecMint.uslot_mint` build the GENERIC slot `∀ W, uslot W` from a supply.

=== THE DESIGN ===
A. THE BOOT BUNDLE. In a kernel-tier file forkret and userinit both see (SpecKexec.v is the natural home; otherwise a new `InitBoot.v` in `_CoqProject`), define
   `init_boot_bundle (cw : Z) (sts : list fdstate) : iProp := ∃ (P Pmiss : nat -> Z -> iProp) (Fo : pfam …) (R : iProp), exec_au_pre (MkPfam uslot R) (fs_gamma_L fsc_fs) fsc_fs cw P Pmiss Fo init_boot_path 1 (fun _ => 5) (fun _ => fkr_init_bytes) sts`
   where `init_boot_path := DirentEnc.bview 5 fkr_init_bytes` (name the path constant once; forkret's boot arm spells it today). It is LINEAR (one-shot pieces), Timeless if its parts are (check; `exec_au_pre`'s pieces are AUs -- if not timeless, the park channel must carry it without a later, as it carries `fd_frags`). Also `init_boot_bundle_triv : (∀ W, uslot W) -∗ init_boot_bundle cw sts` (from `exec_au_pre_triv` with the slot piece `S := uslot` paid by the family and `R := True`; check `exec_au_pre_triv`'s statement admits `S` other than `emp` -- if it is stated at `emp`, generalise it or add the sibling).
B. THE PARK'S BOOT MODE. `park_pkg`/`forkret_closer`/`park_token_park`'s mode gains what the `None` family mode really carries: the parker captures `init_boot_bundle cw sts` (at the parked cwd and the parked `fd_frags` states -- `fdt0` for userinit) ON THE PARK CHANNEL, the linear route `fd_frags` already takes, IN PLACE OF the slot family; the closer, on this mode, hands the RESUMER the bundle and asks nothing about a key. Shape: `Wk : option uvis` stays the discriminator; `None`'s payload becomes the bundle (rename the mode's prose: BOOT mode). `park_token_park` takes `init_boot_bundle (pv_cwi (us_V U)) sts` where it took the family; its `app_sup` premise (:332) goes; `park_pkg`'s `app_sup` row (:122) goes; `park_token_park_steady` (:~468) loses `app_sup`. The steady mode is untouched.
C. FORKRET'S BOOT ARM SPENDS IT. `ProofForkret.v:~1488-1531`: the boot arm takes the bundle out of the closer and hands kexec `MkPfam uslot R` with the bundle's `P Pmiss Fo` at `sts` (the parked table, NOT `nil`; the closer already names it), path `init_boot_path`; `exec_arms` arm (a) then returns `uslot W'` at `exec_key U' sts 1` (the slot piece's receipt, `Fs.(pf_recv)`), and the tail's `Hslot` is THAT slot, keyed as the tail already re-keys (`uslot_ukc`); the family instantiation at the tail goes. Arm (b) (`¬ anode_loadable` success) is REFUTED or paid by the bundle's own slot piece -- the bundle carries both wands, so the tail just reads `S W'` out of whichever arm fired; check `exec_arms_landed` (:1531) delivers the receipt beside `kexec_ok`. kexec FAILURE at the boot arm: whatever the landed proof does today (panic path or refuted) -- unchanged; the refund `R` is dropped there.
D. USERINIT TAKES THE BUNDLE. `SpecUserinit` (:226) replaces `app_sup -∗` by `init_boot_bundle (bv_unsigned InodeInv.ROOTINO) fdt0 -∗` (LINEAR; the header paragraph rewritten: userinit parks the first process with the exec bundle the boot arm spends, minting nothing); `ProofUserinit` deletes the mint (:806-808, the `UG.uexec_wp_gen`/`uslot_mint` imports if unused) and hands the bundle to the park. `SpecMain`/`ProofMain`, `BootShared` (:~1471, ~1503 -- the per-hart bundles: only the BOOT hart's chain carries it; the others never call userinit), `BootChain`, `SystemAdequacy` thread it in place of `app_sup`. `SyscParkEnv.park_world` loses its row and `park_world_sup`; `SpecUserretClosed`/`ProofUserretClosed` lose the premise (and the `AppInv` Require if nothing else needs it); `ProofForkret` :~239/~884 rows go.
E. THE THEOREMS. `SystemAdequacy.xv6_power_adequacy_gen`: `Happ_sup` is REPLACED by `Hinit_boot`, stated so both instances can discharge it -- the application receives what the boot gives it and returns the bundle: the shape to aim for is `Hinit_boot : ∀ <the ghost/instance binders the theorem allocates -- mirror how Happ_xfer/Happ_init are quantified>, app_inv fsc_fs -∗ |==> init_boot_bundle (bv_unsigned ROOTINO) fdt0` (with `fdt0` the all-closed NOFILE table userinit's allocproc mints -- name it in SpecUserinit if it is not named); the bupd is there so a discharge may mint ghosts. Its paragraph replaces the `Happ_sup` paragraph (:375-395): the first process's exec bundle is the ONE thing the application owes the kernel about user execution; the generic theorem's is the trivial bundle over the trivial mint; a constraining application's is its pinned bundle (the GAP-premise trap is not this: the hypothesis is discharged by proof for every instance). `App.xv6_app_adequacy` likewise (`:202`, `:248`); `xv6_app_adequacy_triv_xv6Σ` (`:317`) discharges it: `app_sup_raw_triv` → `app_sup` → `uslot_mint` at `UG.uexec_wp_gen` → `∀ W, uslot W` → `init_boot_bundle_triv`. `AppInv`'s prose naming `Happ_sup` (:37-43, :101-109) and `App.v`'s (:26-30, :49-55) are rewritten to the landed shape. `AppEcho.v`'s header (:47-56) then says echo's remaining owed items are `Hinit_boot` (lane 1b) and `Hphi`.
F. SANITY. `grep -rn "app_sup" iris/*.v` names only AppInv (definitions + `app_sup_of_triv`), UexecExecInst/UexecCond's supply-parametric construction, `App.v`'s triv corollary discharge, and UShKernel/UkRun's `uxsup` prose if it mentions it; `grep -rn "Happ_sup" iris/*.v` names nothing; `grep -rn "exec_au_pre_triv" iris/*.v` names SpecKexec and the triv discharge only (forkret's boot arm no longer); `make audit-only` = the thirteen.

=== PHASE 1 -- then STOP AND REPORT ===
A and B as STATEMENTS (`init_boot_bundle`, `init_boot_bundle_triv` proved, the park mode's payload), C's read of `exec_arms` (which conjunct carries the slot receipt), D's statements (SpecUserinit, SpecMain, BootShared, BootChain, SystemAdequacy's premise threading), E's hypothesis STATED in both theorems with the corollary's discharge sketched; NO Admitted in the tree at report time. Build with -k. Report: (1) `init_boot_bundle`, the park's boot-mode row and `Hinit_boot`, quoted; (2) forkret's boot arm: where the slot receipt comes out of kexec's arms (file:line) and what the tail needs; (3) the boot-hart threading of a LINEAR premise (which BootShared/BootChain bundles change); (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (ProofUserinit, ParkCap, ProofForkret + ParkPaid, ProofMain, BootShared/BootChain, SystemAdequacy, App's corollary), D's deletions, the prose sweep, F. Gate (audit = thirteen). Report log name, `git status --porcelain`, `git diff --stat -- iris/`, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build and report exactly what remains, file:line.
