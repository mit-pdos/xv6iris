You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is the CONTINUATION of WX-ROW + WX-RES FINISH phase 2: read `brief-wx-row-res-finish.md` beside this file FIRST (design, rulings, operating rules -- all of it binds you), then `brief-wx-res.md`, then claude-notes/projects/app-echo.md "WAIT-EXIT — DESIGN OF RECORD" and the "WX-KEY LANDED" / "WX-FORK LANDED" notes, then claude-notes/README.md + durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop") + remote-build-gcp.md. The previous agent ran out of session mid-edit; the working tree is DIRTY ON PURPOSE with its work (284 modified iris/*.v; a backup of the tree as you found it is in the coordinator's scratchpad -- you do not need it). Do not revert, stash, reset or `git checkout --` anything; a file you did not touch appearing modified is expected here (the whole sweep is uncommitted).

=== PHASE-2 RULINGS (from the coordinator; already implemented as STATEMENTS in the tree -- do not reopen) ===
- The children map's name is CANONICAL: class-carried `wch_name` on `Xv6Cameras.wchG`, minted in the boot fupd (`WaitInv.children_res_alloc`, `BootShared`), the `fdslot_name`/`pav_name`/`bioslot_name` precedent.  `γc` is GONE from `wait_res_at`/`ch_frag`/`un_ch`/`park_globals` and every lemma that threaded it (the deleting sweep -- that is why 284 files are dirty).  `ch_frag γ0 pa S := γ0 ↪[wch_name] (pa, S)` carries the OWNER'S SLOT ADDRESS in the value.
- ROWS ARE PER-SLOT, BORN AT BOOT, RIDING THE DORMANT BLOCK on the `kstack_free`/`bslots 3` mold: `ProcDefs.proc_dormant`/`proc_dormant_noctx` carry `(if bool_decide (st = ZOMBIE) then ∃ S, ch_frag (pv_chg V) pa S else ch_frag (pv_chg V) pa ∅)`; the row enters at `ProcInv.proc_dormant_seal`/`proc_dormant_prestk_seal` (SpecProcinit's pass 3 deposits row i into slot i; `WaitInv.children_boot` = authority + the NPROC rows, carried unopened through BootShared/BootChain/SpecMain to ProofMain), leaves at `ProcInv.proc_dormant_unused` (allocproc: `allocproc_post`'s found arm carries `ch_frag (pv_chg (us_V U)) (proc_addr j) ∅`), returns at `SpecFreeproc.wp_freeproc_sconf` (new premise `ch_frag (pv_chg V) pa ∅`; `fp_of_dormant_zombie` yields `∃ S`).
- `∅` at UNUSED, `∃ S` at ZOMBIE THIS LANE: kwait resets the reaped zombie's row to `∅` with `children_own_upd` under the `wait_lock` it holds, BEFORE `freeproc`; WX-EXIT tightens ZOMBIE to `∅` later.  kexit parks its OWN row (off its residue, at whatever set) into the ZOMBIE block.
- `wait_res_alloc` is now the pure pairing `parents_res -∗ children_res -∗ wait_res`; `children_own_install` STAYS (boot's primitive -- `ch_rows_alloc` uses `ghost_map_insert` directly; keep `children_own_install` only if something uses it, else it is a justified GONE); `children_own_del` and `children_wf` are DEAD (justified GONEs).  `SpecUserinit` needs NO row premise.
- The dispatcher relay (`ProofSyscall`: `sysc_hcont_ty`/`sysc_ret_tail`/`sysc_epilogue_tail`/`sysc_fallback` with `cs'`, the row and `sysc_ch_ok`; `ProofUsertrapSys.ut_90` relays `cs2`; `ProofUsertrap`) was reported GREEN in phase 1 (build `wxrow` series) -- it will be re-verified by your build since ProcInv is below it.

=== STATE (build `wxrow7`, this checkpoint: COMPILED=720, EXIT=2, ONE error; everything above ProcInv was SKIPPED) ===
The ONLY error: `ProcInv.v:2748` in `proc_priv_to_dormant_zombie` -- `iPureIntro: (∃ S : gset gname, ch_frag (pv_chg (us_V U)) pa S)%I not pure`.  The ZOMBIE block now carries the row (ruling above) and the lemma takes none in.  ProcInv has ~316 dependents, so the true red list above it is UNKNOWN; your first build reveals it.  Expect at least the sites below, which grep shows are NOT yet done (no `ch_frag`/`Hrow` in them):

R1. `ProcInv.proc_priv_to_dormant_zombie` (:~2725): gains `(S : gset gname)` and the premise `ch_frag (pv_chg (us_V U)) pa S -∗` (position: beside `kstack_free pa`, LAST among the spatial premises -- durable-notes "a new conjunct goes LAST"); the proof frames it into the ZOMBIE arm's `∃ S`.  Then `SpecKexit.kexit_park_pay` (:~287) gains the same premise and relays it; `SpecKexit.wp_kexit_sconf_body` (:~149; it already takes `fd_frags_any (pv_fdg (us_V U))` at :~257 -- the row goes beside it: `ch_frag (pv_chg (us_V U)) pj cs`, with `cs` a new ∀-parameter of the body, or `∃`-bundled if the body's callers cannot name it -- PREFER the explicit `cs`: the dispatcher's exit arm `ProofSyscall.sysc_exit_ty` (:~1793) already binds `cs`), the sealed `KEXIT` Parameter retyped in place, `ProofKexit`'s park (:~1069-1081 region, `kx_park`) hands the row to `kexit_park_pay`, `SpecSysExit.v` (:~172 takes `fd_slots FDSPARE`/`proc_priv`; add the row beside them) relays it, `ProofSysExit`, and the dispatcher's exit arm (`ProofSyscall.v:~4970` `SysExit.wp_sys_exit_sconf`) passes the row it holds off the residue (`UsertrapRes.ut_own … ∗ ch_frag (pv_chg (us_V U)) (un_pj N) cs`).  This is the row's ONLY route into a ZOMBIE block; WX-EXIT later builds on it (X3).
R2. `ProofUserinit.v:~438`: the `Hgot` destruct pattern of `allocproc_post` lacks the new row conjunct (it sits right after `fd_frags`: `… & Hfrag & Hrow & #Hmk & …`); at the park (:~809) pass `Hrow` to `park_token_park` as its `ch_frag (pv_chg (us_V U)) (un_pj N) cs` premise at `cs := ∅` (the row is at `proc_addr j` = `un_pj N` by construction; `pv_chg` is preserved by the `upd_cwi`/`upd_cwd` the park's `U` applies -- `ProcDefs`' `upd_*` lemmas).
R3. kfork, `ProofKforkB5.kfk_b5` (:~161): TWO new premises and one new move.  (a) the CHILD's row `ch_frag (pv_chg (us_V Uc)) (proc_addr j) ∅` (from `allocproc_post`, threaded through `ProofKforkMain` -- find where `Hcfrag` = the child's `fd_frags` is carried from allocproc's return to the `B5.kfk_b5` call at :~764 and carry the row beside it, through whichever of B1..B4/B6 hold it; every `upd_*` between allocproc and the park preserves `pv_chg` -- `ProofKforkMain.v:~735` proves the same for `pv_gen`, copy it) goes to `park_token_park_steady` (:~320) as its `ch_frag … (un_pj N) cs` premise at `cs := csP`?? NO -- at `∅`: the CHILD is parked with an empty set; `kfk_b5`'s `uvis_ch Wk = csP` premise names the PARENT's set for the key -- check `ProofKforkMain.v:~764` passes `∅` there (it does: `B5.kfk_b5 … stsP ∅ (uvis_of … ∅)`), so the child's row at `∅` matches the key and the premise `uvis_ch Wk = csP` is at `csP = ∅`.  Rename nothing; just add the row premise.  (b) the PARENT's row `ch_frag (pv_chg (us_V Up)) pme csP'` where `csP'` is the caller's set (`wp_kfork_sconf_body` :~373 takes `ch_frag (pv_chg (us_V Up)) pme csP -∗` -- NOTE `kfk_b5`'s existing `csP` parameter is the CHILD key's set (= ∅ at the call); the parent's set needs its OWN name in `kfk_b5` -- add `(csPar : gset gname)` or similar and keep the statement honest), moved at +0xd4 where `Hch` (`children_res`, i.e. `∃ m, children_own_at m`) and the lock are in hand (:~480-495): `iDestruct "Hch" as (m) "Hch"; iMod (WaitInv.children_own_upd m _ pme csPar (csPar ∪ {[γ]}) with "Hch Hprow") as "[Hch Hprow]"` with `γ` the child's generation `pv_gen (us_V Uc)` (the token `child_tok γ pidv Q` in `kfork_post`'s arm is at that name -- `ProofKforkMain.v:~750` `gen_split`), re-seal `wait_res` with the new `m`, and return the moved row through `kfk_b5`'s continuation (add `ch_frag (pv_chg (us_V Up)) pme (csPar ∪ {[pv_gen (us_V Uc)]})` to the `wp_next` continuation's premises) so `ProofKforkMain` can build `kfork_post`'s pid arm (:~267 `ch_frag (pv_chg (us_V Up)) pme (csP ∪ {[γ]})`) and its failure arms return the row unmoved.  The freshness `γ ∉ csP` is NOT needed (set union).
R4. `ProofKwait.v:~1384` (`kw_reap`): `fp_of_dormant_zombie` now yields `(Hrest & (%S & Hrow) & Hpt & Htf)`; before `Freeproc.wp_freeproc_sconf` (:~1386, which gained the premise `ch_frag (pv_chg V) pa ∅` between `fp_rest` and `fp_pt`) reset the row: `iDestruct "Hch" as (m) "Hch"; iMod (children_own_upd m _ (proc_addr k) S ∅ with "Hch Hrow") as "[Hch Hrow]"` (you hold `locked γw` and `Hch` = `children_res` here -- see the lemma's premises :~1287) and re-close `children_res` with `iExists _`.  Check the other freeproc caller(s) (`ProofAllocproc.v:~1996/:~2368` already pass `Hrow`) and any other `proc_dormant … ZOMBIE`/`UNUSED` producer/consumer the build flags (`SchedCtx.park_pay`, `ProcInv.proc_dormant_unused`, `SpecProcinit`, `ProofScheduler`, `ProofKexit`).
R5. Anything else the build turns red above ProcInv.  Fix statements the ruling forces; STOP AND REPORT on anything that needs a design decision (a row demanded somewhere the ruling does not put one; a `cs` that cannot be named; a fraction that does not add up).

=== OPERATING RULES (hard; as brief-wx-row-res-finish.md) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 (run_in_background; the summary prints `COMPILED=`/`EXIT=`; error context: `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); log names `wxrow8`, `wxrow9`, …; ONE build at a time; never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal.  A full cone above ProcInv is ~25-40 minutes: batch every statement change you can foresee BEFORE the first build (R1-R4 above), then build, then iterate on the red list.  Gate = zero `Error`, `EXIT=0`, COMPILED>0; VM `make audit-only` from the tree root `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen assumptions in durable-notes.md "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` clean except the justified GONEs (`children_wf`, `children_own_del`, `children_own_install` if unused, and `SchedCtx`/`WaitInv` renames you can name) and sealed Parameters retyped in place (list them); nothing `Admitted`; no new Axiom/Parameter.  GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/.  DESIGN FRICTION = STOP AND REPORT.  Comments state the current shape as fact (no dates, no "now"/"used to").

=== DELIVERABLE ===
Every red file green; R3's greps from brief-wx-res.md; the comment sweep the previous brief asked for (`WaitInv`'s header says rows are born at boot per slot -- verify; `kfork_post`'s note at SpecKfork.v:~250 still says kfork "installs the child's own row" -- it does NOT any more (the row came from allocproc); `ParkCap`'s note).  Gate.  Report: log name, `git status --porcelain | grep -v '^ M iris/'` (must be only `?? vtest-rocq/.VIcache.v.swp`), `git diff --stat -- iris/ | tail -1`, the `make audit-only` output verbatim, the `lemma_diff` report with each line justified, and an as-landed note (≤ 15 lines, present tense) covering: the canonical name, the per-slot rows' route (boot fupd → children_boot → procinit pass 3 → dormant block → allocproc → residue → park; freeproc/kexit back), `∅`/`∃ S`, kwait's reset, kfork's parent move, kexit's row premise, what the dispatcher relays, and the GONEs.  Do not commit or stage.  If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
