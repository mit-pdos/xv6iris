You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is OPEN-PIN PHASE 4, the LAST item of the console-open work (a fresh agent): the ERA-LEVEL DISCHARGE of init's three console leaves -- `⊢ □ UkInit.uki_open_absent_leaf T K` and `⊢ □ UkInit.uki_mknod_leaf T K stc` (i.e. `UkInit.init_cons_leaves N T K stc`) at echo's era, `T := AppEcho.echo_taint γcl`, `K := AppEcho.cons_key r`, `stc := UInitCons.init_cons_fd`. Design of record: claude-notes/projects/app-echo.md "OPEN-PIN PHASE 2 MILESTONE LANDED" and the commit message of the phase-3 landing (`git log -1 --format=%B <commit of "OPEN-PIN phase 3">`), "OPEN-PIN PHASE 1 LANDED + FINDINGS", "PINNING -- THE OWNER'S RULING". The coordinator (me) owns the design; you do the proofs. ONE PHASE: build the discharge, gate, report.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; iris/UkInit.v (`uki_open_absent_leaf`, `uki_open_console_leaf`, `uki_mknod_leaf`, `init_cons_leaves` :~337; the ecall sites 0x3b2/0x3b4/0x3b8 (open) and 0x3ba/0x3bc/0x3c0 (mknod) -- read `wp_kinit_open`'s old walk for the instruction shapes); iris/UkSh.v `ush_read_leaf_holds` :~5819 and iris/UShKernel.v (THE MOULD: how a program-tier leaf body is discharged at the era from the concrete bundle); iris/UInitSh.v `init_exec_sup_of_sh_slot` :~424-508 (THE WORKED PRECEDENT for a `udepwf_at`-style supplier that reads a path off the LOANED heap: `uimg_sub`, `uheap_text`, `init_rodata`; here the path "console" lives at 0x970 in init's rodata -- find the literal and its bytes, `init_cons_pl`); iris/UInitCons.v (`init_cons_laws` :~953 (nine laws, Persistent), `init_cons_laws_mknod_bundle` :~1001, `init_cons_laws_open_absent` :~1019, `init_cons_laws_open_console` :~1042, `init_cons_laws_echo` :~1073, the receipt readings `init_cons_open_recv_absent`, `init_cons_recv` + `init_cons_open_fd`, `init_cons_mknod_recv`, `init_cons_mknod_fail_recv`, `init_cons_open2_of_mknod_fail`, the `xfam` pieces `init_mk_*`, `cons_pin_resolves_at`, `cons_pin_misses_at`); iris/PinnedObs.v (`pobs_P`, `pobs_P_dead`, `pobs_Fo`, `pobs_miss_free`); iris/UkRunSys.v (`wp_uk_ecall_open_recv` :~2744, `wp_uk_ecall_quiet_recv`, their `udepwf_at` premise and the pure rows they hand back); iris/UkRun.v (`udepwf_at` :~569, `udepwf_at_of_udepwf`, `udepwf_at_udepw_at`; how a deposit's `xfam` is built: `sbundle_pay`, `xfam_at`/`xfam_pt`); iris/UexecExecInst.v (`xfam`'s fields `of_P … of_Ft`, `nf_*`; rows 15 and 17 of `xv6_sbundle`/`xv6_spost`; `sbundle_at_open_elim`/`_mknod_elim`, `spost_at_open_intro`/`_mknod_intro`); iris/ArgPath.v (`arg_path_of`, `arg_path_of_bview`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `openpin70`, `openpin71`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter. KERNEL FILES ARE OFF LIMITS (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`, `UexecExecInst`, `UexecSG`, `UkRunSys`'s existing lemmas -- a NEW lemma beside them is allowed if the three leaves need one); a needed kernel change is design friction: STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact.

=== THE WORK ===
A new file `iris/UInitConsK.v` (after `UInitCons.v`; requires `UkInit` and `UInitCons` -- neither can see the other, this file sees both) with: (1) the `udepwf_at` suppliers for init's open at the miss pin, the open at the resolving pin, and the mknod -- each reads `arg_path_of M 0x970 init_cons_pl` off the loaned heap (init's rodata holds "console" at 0x970: verify the address and bytes against `UCodeInit`/`init_rodata`) and builds the `xfam` whose row-15/17 families are the pinned ones (`of_P := pobs_P_dead …` / `pobs_P …`, `of_Fo := pobs_Fo …`, `of_Ft` trivial at `om_trunc = false`, the create families trivial at `om_create = false`; `nf_* := init_mk_*`), paying the bundle from `init_cons_laws` + `app_inv fsc_fs` + `K` (`init_cons_laws_open_absent`/`_open_console`/`_mknod_bundle` are the proved bundles; check their exact premises); (2) the three leaf discharges `init_open_absent_leaf_holds`, `init_open_console_leaf_holds`, `init_mknod_leaf_holds` walking the ecall through `wp_uk_ecall_open_recv`/`wp_uk_ecall_quiet_recv` and reading the receipt with `init_cons_open_recv_absent`/`init_cons_recv` + `init_cons_open_fd`/`init_cons_mknod_recv` + `_fail_recv` (the mknod's success arm hands back `uki_open_console_leaf` -- built from (2b) under `cons_made r i`); (3) `init_cons_leaves_echo : file_app = MkAppcfg echo_names (echo_pred γ) r -> ⊢ □ (∀ N, init_cons_leaves N (echo_taint γ) (cons_key r) init_cons_fd)` (or whatever quantification `UInitKernel.init_uexec_slot`'s new premise takes -- match it exactly). If the ecall walks at 0x3b2-0x3c0 need instruction facts, they are in `UCodeInit`/`UkInit`'s old `wp_kinit_open`/`wp_kinit_mknod` proofs -- reuse them.
SANITY: `grep -n "init_cons_leaves" iris/UInitKernel.v iris/UInitConsK.v` shows the premise and its discharger; the audit unchanged; nothing in `UkInit`/`UkInitMain`/`UInitCons` changed in statement (proof-only edits allowed if a leaf body needs a pure row you can add WITHOUT weakening).

=== REPORT ===
Log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, the three discharges' statements quoted, and an as-landed note (≤ 8 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
