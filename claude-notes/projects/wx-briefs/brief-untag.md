You are a MAINTENANCE-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel) in /shared/xv6iris-2 (the MAIN checkout, on branch main, clean). Your lane is UNTAG: REMOVE lane TX-TAG's per-byte source labels from the UART ghost state and every contract that threads them, keeping the two things landed with them that are still wanted. The owner: "please ditch the per-byte labels yourself" -- the theorem is about raw bytes, attribution will be the application's private knowledge inside the view shifts every UART output will take (lane OUT-FUPD, after the machine model gains a second UART), and a per-byte label in the invariant is not a useful way to think about UART output. Read first: claude-notes/projects/app-echo.md "THE OWNER'S REDESIGN OF THE UART OUTPUT SIDE", "TWO UARTS", "TX-TAG LANDED" (what to remove; the commits `4e4d9bc8f` (the tags) and `626a9b95f` (the rider)), "TX-TAG PHASE 1"; durable-notes.md ("Build", "Shared-checkout discipline", "The dev loop", the dirty-bottom-file note, the never-collapse-tokens note); remote-build-gcp.md.

WHAT GOES (all of TX-TAG's labels): `Xv6Cameras.txsrc` (`TxK`/`TxE`/`TxW`) and `uart_ghost_tagG`; `UartNames.un_tag`; `WpUart.uart_tags_auth`/`uart_sent_tagged`/`uart_tag_at`/`uart_tagsE` (out of `uart_ghosts`), `uart_tags_*` algebra, `uart_pop_tag`, `uart_ghosts_alloc`'s tag founding; `WpSconfUartAccess.wp_uart_thr_write_s_sconf`'s `src`/`tg0` parameters and tagged post; `UartTxInv.uart_sent_sub_at` and its lemmas; `UartSentLoc.uart_sent_from_at`/`uart_sent_from_tag`/`tag_proj`/`uart_sent_exact_at`; `SpecConsolewrite.cons_sent_cnt_at`/`cons_sent_cnt_of_at`; the tag threading in `SpecUartPutc`/`ProofUartPutc`, `SpecConsputc`/`ProofConsputc`, `SpecPrintint`/`ProofPrintint`, `SpecPrintk`/`ProofPrintk`, `SpecConsoleintr`/`ProofConsoleintr` (`console_caps` back to the untagged baseline), `SpecUartwrite`/`SpecUartwriteLoc` and their proofs, `ProofConsolewrite`, `ProofFilewrite`'s one projection, `ProofUartinit`'s destructurings, `BootShared`'s founding, `SpecMain.boot_k_shape` and its header, `LinkPrintk`'s comment.
WHAT STAYS: (a) THE RIDER `626a9b95f` -- `uart_col_ok`'s clause `u_wire u = u_out u`, `uart_colE_wire_out`, `uart_colE_tx_pop`, the maintainers and the six WpSconfUartAccess callers, ProofUartinit's stores, the boot mint base case (E5 needs it); (b) consputc's PINNED BYTES post (`⌜if eq_vec a00 cp_backspace then cs = consputc_bs else cs = [cp_byte a00]⌝`, `cp_byte`, `cp_byte_sb`, `consputc_bs`) and consoleintr's pinned echo `⌜hh' = Some hb -> cs = [echo_of cb]⌝` with `echo_of`/`cons_echo` -- RE-EXPRESSED over the untagged `uart_sent_sub γu cs` (the pre-TX-TAG baseline claim), not over a tagged one; (c) the UNTAGGED baseline receipts (`uart_sent`, `uart_sent_sub`, `uart_sent_from`, `cons_sent_cnt`) stay exactly as they were before TX-TAG (lane OUT-FUPD replaces them later; do not remove them here); (d) the printk inventory's facts live in the notes, nothing to keep in code.
METHOD: `git revert --no-commit 4e4d9bc8f` will conflict with the rider's hunks in WpUart/WpSconfUartAccess/ProofUartinit/BootShared -- resolve by hand keeping the rider; then re-apply (b) over the untagged claim; check every file TX-TAG touched (`git show --stat 4e4d9bc8f`) ends at the intended state; NEVER a tree-wide textual substitution. BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 untag10` (then untag11, …; run_in_background, detached; one at a time; WpUart is at the bottom: expect a ~1000-file rebuild; the VM is shared -- builds queue; a bogus "Cannot find library Xv6Cameras/WpUart" cascade means re-run once). Gate: zero Error, EXIT=0; VM `make audit-only` from /mnt/rocq/trees/_shared_xv6iris-2 = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified (expected: the tagged lemmas GONE, nothing else); nothing Admitted; no new Axiom/Parameter; `grep -rn "TxK\|TxE\|TxW\|txsrc\|un_tag\|uart_tag\|uart_sent_sub_at\|uart_sent_from_at\|uart_sent_from_tag\|tag_proj\|uart_sent_exact_at\|cons_sent_cnt_at" iris/*.v` EMPTY; `grep -n "u_wire u = u_out u" iris/WpUart.v` shows the rider; `grep -n "echo_of" iris/SpecConsoleintr.v` shows the pinned echo. GIT: DO NOT COMMIT OR STAGE (the coordinator commits by path; `git revert --no-commit` leaves changes unstaged after `git reset -q` of the index -- if you must unstage, use `git restore --staged -- <file>` only); never stash/reset --hard/add -A/commit/amend/checkout -- <file>; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. Report: `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, the log name, the audit verbatim, lemma_diff justified, the resolutions in the four rider files, and the untagged spelling of consoleintr's pinned echo, verbatim.
