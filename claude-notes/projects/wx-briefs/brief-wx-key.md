You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-KEY, the first of four lanes giving exit()/wait() a resource-transfer contract (a child returns resources to its parent at exit; the parent reclaims them at wait). Design of record: claude-notes/projects/app-echo.md, section "WAIT-EXIT — DESIGN OF RECORD" -- read it in full first; this lane is its "WX-KEY" item: the KEY gains two pure readings of kernel state and the kernel gains the ghost cells they read, with NO semantic change yet (every child set empty, no token minted; every process has a generation). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Proofmode & bitvector gotchas"); remote-build-gcp.md; design/user-wp-slot.md; design/proc-struct.md §2; the headers of iris/UexecSG.v, iris/UexecRet.v, iris/SpecUsertrap.v (~395-500, `ut_sys_in`/`ut_fork_in`), iris/SpecSyscall.v (~340-420, `sysc_sys_in`/`sysc_fork_in`), iris/UkRun.v (`urun` ~276-330, `uk_names`), iris/UserCwd.v (THE MOLD for a per-process ghost: `ucwd_auth`/`ucwd`/`ucwd_any`/`ucwd_alloc`/`ucwd_update`), iris/UsysMemOk.v (`usys_cwd_ok`, `_quiet`), iris/UkStepGen.v (~109, the round hands the program `⌜uvis_cwd W' = cw⌝`), iris/WaitInv.v, iris/PidLock.v, iris/SchedCtx.v (`proc_pub` :162), iris/SpecAllocproc.v (`allocproc_post` :141), iris/SpecFreeproc.v.

=== OPERATING RULES (hard) ===
- BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 under `run_in_background` with the log's `COMPILED=`/`EXIT=` lines as the sentinel; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`; ONE build at a time; never a remote top-level `make`; never local `coqc`. Never `simpl`/`cbn` a goal mentioning a dumped byte literal. THIS LANE TOUCHES `uvis`, which is near the bottom of the user tier: expect long rebuilds; do the statements in ONE pass before the first build.
- Gate: zero `Error`, `EXIT=0`, COMPILED>0; `make audit-only` on the VM (tree root `/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline"; `tools/lemma_diff.py` clean (zero Admitted; no new Axiom/Hypothesis/Parameter; a changed Parameter of a sealed module replacing the old is reported as such).
- GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`. DO NOT COMMIT OR STAGE. No other checkouts, no worktrees. Temp files in this scratchpad. Do not edit claude-notes/.
- DESIGN FRICTION = STOP AND REPORT. Never weaken a landed form. Comments state the current shape as fact (no dates, no "used to"/"now").

=== THE DESIGN (this lane's part) ===
K1. THE KEY.  `uvis` (find its Record -- UexecSG.v or below) gains `uvis_gen : gname` (this process's own generation ghost) and `uvis_ch : gset gname` (the generations of this process's live children). Every constructor/reader adapts: `uvis_of U sts` becomes `uvis_of U sts g cs` (or the shape that fits: the kernel reads `g`/`cs` off its state exactly as it reads `sts` off `fd_frags`); `bump` keeps both; `KforkChild.kfork_child` is about `ustate` (unchanged) but the child KEY at fork has `uvis_gen := γ'` for the child's own fresh generation (`∀ γ'` in the child arm this lane -- the round learns it from kfork's post) and `uvis_ch := ∅`; `SpecKexec.exec_key U' sts na` keeps both from the exec'ing key; `UexecSG.skey_eq`, `UexecRet.urun_eq`/`uslot_of_urun_eq`, `UexecRet.uexec_fork_parent_F`/`_child_F` (the parent's `uvis_ch` unchanged THIS lane; `∀ fdv' cw'` gains `∀ cs'` with `⌜cs' = uvis_ch W⌝` -- the quiet row), `uexec_ret_cont_F`, `sbundle_at`/`spost_at` (their arities grow by the fields the key carries -- check how `uvis_fd`/`uvis_cwd` appear there and mirror; `f_equiv_wide`/`solve_contractive_wide` handle arities 6-8, extend if needed, durable-notes "stdpp f_equiv arity cap").
K2. THE ROUND'S PURE ROWS.  `UsysMemOk.usys_gen_ok n g g'` := `g' = g` at every number (the identity survives every syscall; fork's child key is built separately); `usys_ch_ok n r cs cs'` := `cs' = cs` at every number (this lane; fork/wait rows come in WX-FORK/WX-WAIT), with `usys_ch_ok_quiet`. The round hands the program `⌜uvis_gen W' = g⌝ ∗ ⌜uvis_ch W' = cs⌝` beside `⌜uvis_cwd W' = cw⌝` (UkStepGen ~109).
K3. THE PROGRAM'S MIRROR.  New `iris/UserChildren.v` (UserCwd.v's mold, at `gset gname`): `uch_auth γ S`, `uch γ S` (the fragment), `uch_any`, `uch_alloc`, `uch_update`, agreement, Timeless. `UkRun.uk_names` gains `ukn_ch : gname`; `urun` gains `uch_auth (ukn_ch N) cs` beside `ucwd_auth (ukn_cwd N) cw` at the SAME `cs` the key carries; `urun_close`/`_upd` take it; the three entry constructors `uslot_of_urun{,_all,_ro}` allocate it (`uch_alloc` at `uvis_ch W`) and hand the program `uch (ukn_ch N) (uvis_ch W)`; `UkFork` mints the child's pair at `∅` beside `γfd'`/the cwd pair (the child's `uvis_ch = ∅`), the parent keeps its fragment (its set unchanged this lane); every leaf's post re-introduces `urun` at `cs' = cs` via `usys_ch_ok_quiet` FOLDED INTO the generic leaf machinery (UkRunSys/UkRunLeaf) so NO per-leaf statement grows -- the CWD-RESOURCE lane did exactly this for `cw`; read its landed shape (`git log --grep=CWD`, `git show <commit> --stat`) and mirror. Kernel-side constructors (`UShKernel`, `UInitKernel`, `UEchoKernel`) weaken the fragment to `uch_any` for the program's pstate (as `ucwd_any`).  `uvis_gen` needs NO program mirror (it is read by the exit row and by `my_pay`, WX-FORK/WX-EXIT).
K4. THE KERNEL CELLS.  (a) THE GENERATION: a per-slot ghost cell `slot_gen j : gname` -- fresh at allocproc (`allocproc_post`'s found arm gains `∃ γg, slot_gen_at (proc_addr j) γg` or the ghost-map form `j ↪[γgen] γg`; decide against how `proc_pub`'s rows are owned and where allocproc builds the slot), kernel-exclusive (no agreement needed: nothing shares it this lane), freed/re-minted at freeproc → allocproc.  Place it in `SchedCtx.proc_pub`'s payload (p->lock) if allocproc/kexit/kwait all hold p->lock at the points that read it (they do: allocproc builds under np->lock; kexit parks ZOMBIE under p->lock; kwait reaps under pp->lock) -- state the ghost in `ProcDefs`/`SchedCtx` beside the pid quarter. (b) CHILDREN under `wait_lock`: `WaitInv.parents_own ps` gains the sibling `children_own cs : list (gset gname)` (NPROC; `wait_res_at` closes both existentially); the boot carve founds it at `∅` (BootCarveMain/BootShared, the parent cells' carve is the mold -- find how `parents_own` is founded: `WaitInv.v:~147` "THE BOOT CARVE'S SHAPE"); `SpecReparent`'s `rp_map` is about the parent cells only (unchanged this lane; the children move comes in WX-EXIT). The KEY's `uvis_gen`/`uvis_ch` for slot j are READ from the slot's generation cell / `cs !! j` where the trap route reads `sts` (find how `sts` reaches `uvis_of U sts` at usertrap's entry -- `fd_frags` -- and route `tk cs` from a `wait_lock`-free reading: since nothing writes them this lane, hand them as PURE parameters through `proc_priv`-adjacent state or as a persistent snapshot; if the only sound route is under `wait_lock`, STOP AND REPORT with the exact obstacle -- the alternative is the `pid`-style route: a per-process cell in `proc_priv`/`pprivate` mirroring the wait-lock cell, which WX-FORK/WX-WAIT would then have to keep in step).  (c) PID UNIQUENESS: `PidLock.nextpid_res_at` gains `⌜NoDup (live pids)⌝` over the quarters it owns -- state it as the payload's own reading of the NPROC pid quarters (`pid_lock_share_at`): "no two slots with nonzero pid share a value"; prove it inductive across allocpid's scan (the scan chooses a pid no live slot holds -- `ProofAllocproc.wp_ap_pidsec`'s scan is exactly the check) and across freeproc's `p->pid = 0`; found at boot (all zero).  If (c) does not fit this lane's budget, STOP AFTER K1-K4(b) GREEN and report; (c) then becomes WX-PID.
K5. SANITY.  `grep -n "uvis_ch\|uvis_gen" iris/UkRun.v iris/UkRunSys.v iris/UkStepGen.v iris/UsysMemOk.v` names the mirror, the quiet rows and the round's handoff; every `Uk*.v` program file compiles with statements unchanged except the record's new field; `grep -rn "exec_key" iris/SpecKexec.v` shows both fields carried.

=== PHASE 1 -- then STOP AND REPORT ===
K1-K3 as STATEMENTS (UserChildren.v proved; the Record's fields; `uvis_of`'s new arity threaded through the trap route's rows and the round; `urun`'s conjunct; the constructors); K4's placement decided (file:line for each cell) with (a) stated; NO Admitted in the tree at report time. Build with -k (long). Report: (1) the Record and `uvis_of`'s new statement, quoted; (2) how `tk cs` reach `uvis_of` at usertrap's entry (the route, file:line) and whether `wait_lock` stands in the way; (3) `urun`'s new body and the round's handoff, quoted; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs, K4 (b)(c), every red file, K5. Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, new files, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build and report exactly what remains, file:line.
