You are a REVIEWER on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel, checkout /shared/xv6iris-2). You change NOTHING: no edits to iris/, no builds, no git operations other than read-only ones (`git log`, `git show`, `git diff`). Your one output is a written report at claude-notes/projects/review-echo-plan-2026-09-12.md (create it; that is the only file you write). Budget your reading: the design note is 3400 lines and the tree is ~1000 files; read what the trace below needs, and say what you did not read.

THE QUESTION.  The project's goal is a closed instance of the top-level theorem for the "echo hello world" scenario. For weeks the arc has gone: launch a lane, discover ONE contract or model gap when a proof is attempted, rule it with the owner, fix it, launch the next lane, discover the next gap. Recent examples: the exit payload had to be additive (Q xs ∧ Q (-1)); the fork's child could not receive a ghost token (Forkable is address-space-only); the read receipt was stated at the wrong key; consoleread's window allowed a swallowed byte (dc = d+1) which breaks the input line; the copyout-fault swallow could not be refuted because the U tier's permission projection cannot see lazy pages; the generic user slot existed only at the trivial exit payload so a verified sh could not carry a real one; the fs image has no console device node; open/mknod could not name their path argument. The owner wants the REMAINING gaps found NOW, in a batch, by tracing the whole plan from the theorem down to the landed lemmas, instead of one per lane.

THE TARGET (read these first, in this order):
1. claude-notes/projects/app-echo.md sections "## The target statement" (:10), "THE REMAINING ARC" (grep it), "E1 ECHO-PRED"…"E5 L7 — THE OUTPUT SIDE" (:~2888-2935), "E3 — THE INPUT LINE: DESIGN PROPOSAL", "SH-LINE RULING", "CONS-CURSOR RULINGS" (the LEASE), "PINNING -- THE OWNER'S RULING", "OPEN-PIN FINDINGS -- RULED WITH THE OWNER" (FACT 1-3), "SH-LINE PHASE 2 -- THE SWALLOWED BYTE" + "THE OWNER'S RULING ON THE FORK", "SH-LINE PHASE 2a LANDED" + "SECOND SEAM FOUND", "GENERIC-PAY LANDED", "OPEN-PIN COMPLETE". Then skim every "… LANDED" note for what is actually in the tree.
2. claude-notes/projects/CHECKPOINT.md (the pipeline order: CONS-SWALLOW (in flight) → LAZY-FLAG → SH-LINE 2b → E4 SH-ECHO → E2 INIT-BOOT → E5); the briefs in claude-notes/projects/wx-briefs/ for the lanes not yet landed: brief-cons-swallow.md, brief-lazy-flag.md, brief-sh-line-2.md (S4/S5 are 2b's), and the older brief-sh-echo.md / brief-init-boot.md if present.
3. iris/App.v (`Record xv6_app` :117, `app_triv`, `xv6_app_adequacy` -- THE theorem shape: read its full statement and every hypothesis), iris/AppEcho.v (the header :1-90 says exactly what is discharged and what is open: `Hinit_boot` (E2) and `Hphi` (E5); `disc` :211 is the input discipline as currently stated; `echo_pred`, `echo_taint`, `echo_tag`, `echo_line`), iris/SystemAdequacy.v (`xv6_trace_adequacy` :1210 and `xv6_power_adequacy_gen` :793: what the trace theorem quantifies over and what an observation `mobs` is -- ObsUartIn / ObsUartOut / ObsPowerOn/Off).
4. The programs: xv6-riscv/user/init.c, sh.c, echo.c, ulib.c (gets, printf → write), umalloc.c, xv6-riscv/kernel/console.c (consoleintr ECHOES typed bytes with consputc, handles backspace/^U/^D, the 128-byte ring; consoleread; consolewrite), uart.c (uartputc / uartintr; the tx buffer), and the kernel's printf (init's "init: starting sh" goes through the user printf → write(1); the KERNEL's printf/panic goes to consputc directly).
5. What is proved about the programs today: iris/UkInit*.v, iris/UInit*.v (init), iris/UkSh*.v, iris/UShKernel.v, iris/UkShParse*.v, iris/UkShRun.v, iris/UkShDiag.v (sh -- read the section headers and the main theorems' statements, especially every Hypothesis/premise a program lemma still takes, e.g. `UkShFork.ushf_lexable`, `UkSh.ush_rest`, `UkSh.ush_read_leaf`), iris/UEchoKernel.v + iris/UkEcho*.v (echo), iris/UConsLine.v and iris/UserConsole.v (the input-line statements), iris/PinnedExec.v / iris/PinnedObs.v / iris/PinnedOpen.v / iris/FsEchoPin.v / iris/FsShPin.v / iris/FsConsPin.v (the pins), iris/ConsoleInv.v + iris/SpecConsoleread.v + iris/SpecConsoleintr.v + iris/SpecConsolewrite.v + iris/SpecUartputc*.v (what the console/UART contracts say about INPUT and, crucially, about OUTPUT: is there an output history? tags on output bytes? how does the theorem's `app_phi` observe the UART tx stream?), iris/ChildTok.v + iris/WaitInv.v + iris/SpecKwait.v + iris/SpecKexit.v (exit/wait payloads), iris/UexecRet.v + iris/UexecExecInst.v + iris/UkRun.v + iris/UkRunSys.v (the U-tier key, rows, leaves).

OWNER'S RULINGS you must take as fixed (do not re-litigate; do check the plan honours them): the trace discipline is "wait for the `$ ` prompt, then type each character of `echo hello world\n` after the previous one is echoed back" (per-whole-command form acceptable; fallback if the ring overflows: UART flow control in the kernel); verified processes hold LEASES on kernel tokens (reclaimed at the taint, accessed through a fupd); the generic tainted slot must remain constructible; pinned bundles are application-side only and may touch kernel proofs only via a spec's precondition; init proves NOTHING about allocation succeeding -- on failure sh starts with closed descriptors, never prints the prompt, the user never types, and the theorem is vacuously true; on success sh's proof must say what reaches stdout; a verified program never drops to the generic slot except under the taint; the copyout-fault swallow is handled by a lazy flag on the key (LAZY-FLAG).

WHAT TO PRODUCE.  Trace the theorem's obligations end to end for the concrete scenario -- power on; kernel boot; init: mknod/open console, dup, fork, exec sh; sh: prints "$ ", gets a line one byte per read, parses "echo hello world", fork, exec echo (path "echo", argv), wait; echo: write(1, "hello world\n") in pieces (read echo.c: one write per argument plus the separator/newline), exit; sh: wait returns, prints "$ " again; meanwhile consoleintr echoes every typed byte and the top-level trace records UART in/out bytes -- and for EACH hop write down: (a) which landed lemma/contract carries it today (file:line), (b) which planned lane is supposed to carry it, and (c) whether you can see a reason the planned statement will be FALSE, UNPROVABLE from what the kernel contracts hand over, VACUOUS, or missing an owner decision. Look especially hard at the parts with the least design: E5 (the OUTPUT side -- how "good_out" is stated over the UART tx stream when output comes from three processes (init's banner, sh's prompt, echo's text) plus the console's own echo of the input and the kernel's own printf/panic; whether the console's output path has any ghost history/tag machinery comparable to the input side; whether write(2) to the console device has a receipt; the interleaving of the input echo with sh's output; the `$ ` prompt detection in the discipline; backspace/^U/^D handling in consoleintr; the 128-byte ring and the "type after echo" discipline; what happens on the SECOND and later commands; power cycles), E4 (sh's exec of "echo": the relative path lookup through sh's cwd, argv construction in sh's memory, the fs pin under the mknod modification, echo's fd 1/2 inheritance, echo's own verified slot and its exit status, sh's wait and the payload), E2 (init's boot obligation: the era-0 claim with the key, the record equation, the ledger fact, the reader token through the boot bundle's Pay, the pid), and sh's error paths (fork failure → panic prints "fork" and exits; exec failure prints "exec %s failed"; what the theorem says then; whether kalloc/allocproc failure arms exist in the kernel contracts sh's leaves consume, and whether they force output the theorem must account for). Also examine: kill/exit interactions (sh killed while holding the reader lease), the taint escrow's actual constructibility (is there a landed lemma that moves the token INTO the escrow at the taint, and one that lets the generic slot borrow it?), `echo_tag`'s dependence on the top theorem's `riscv_rx_tag = app_tag` equation, the "per-character echo" version of `disc` vs the currently landed prefix predicate, and anything in the pipeline order that is out of dependency order.

FORMAT.  A numbered list of FINDINGS, most severe first. Each: title; one-paragraph statement of the problem in precise separation-logic / xv6 terms (no project jargon without a file:line pointer); evidence (file:line quotes, at most ~10 lines each); which lane it lands in; a proposed fix (kernel contract change / model change / design decision needed from the owner / just work); confidence (certain / likely / possible) and what you did NOT check. Then a short section "Things I checked that look fine" (so the coordinator does not re-check them) and "What I did not read". Aim for the ten to twenty findings that matter, not a catalogue. Do not pad. Do not propose re-architectures; find the gaps in THIS plan.
