You are a KERNEL-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-sup (a sibling clone; never touch or build /shared/xv6iris-2, /shared/xv6iris-2-tlw, /shared/xv6iris-2-disc; do not touch the branches lane/supply-split or lane/sh-open). Your lane is PRINTK-LEDGER-2: continue the previous agent's UNCOMMITTED work on branch `lane/printk-ledger` (19 files dirty, +404/-71; build `pkl32`: everything green except `ProofMain.v` and `ProofPrintk.v`) to the first green commit (MILESTONE A), then milestones B, C, D as context allows. Read first: claude-notes/projects/app-echo.md "PRINTK-LEDGER MILESTONE A, IN PROGRESS" (the exact remaining work, the soundness correction, the boot routing) and "PRINTK-LEDGER PHASE 1" (what is landed, the staging ruling, the design), "O5' RULED", "TX-TAG LANDED"; then wx-briefs/brief-printk-ledger.md (P1-P5; superseded where the notes say); durable-notes.md ("Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the bundle-intro note, the never-collapse-tokens note); remote-build-gcp.md ("Sharing the VM": the VM is shared by five trees right now -- builds queue; be patient, never kill anything).

=== OPERATING RULES (hard) ===
Start: `cd /shared/xv6iris-2-sup && git status --porcelain` shows ~21 dirty paths on branch `lane/printk-ledger` (if clean, STOP and report). Do NOT revert anything; the tree is the state. BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-sup <logname>` (names `pkl40`, `pkl41`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate for each milestone: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-sup` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref main` justified; nothing Admitted; no new Axiom/Parameter. Sanctioned files: the printk cone (SpecPrintk/ProofPrintk/SpecPrintint/ProofPrintint/PrintkFmt/PrintkArgs/PrintkRender/KLedger/LinkPrintk), SpecMain*/ProofMain*/BootShared/BootChain, Xv6Cameras/UartNames/WpUart/UartTxInv/UartSentLoc, WpSconfUartAccess + its THREE THR callers (ProofUartPutc, ProofUartwrite, ProofUartwriteLoc), Spec/ProofUartputcSync, Spec/ProofConsputc, the GEN callers only for the one pure premise. Off limits: App.v, SystemAdequacy.v, EchoDisc.v, AppEcho.v, ConsoleInv, FsAbs*, the U tier. GIT: commit on `lane/printk-ledger` ONLY when a milestone's gate is met (never a red commit); never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NEVER a tree-wide textual substitution.

=== THE WORK ===
A-1: `ProofMain.v:2387` -- `iEval (rewrite Hcnu) in "Hkled"` / `"Hkfl"` before the group call (the resource arrives at `cn_uart cn`, the group wants `γd`); build (`pkl40`) to confirm.
A-2: `ProofPrintk.v` -- `pk_held` (:681) gains `(γd : uart_names)` and `∗ pr_res γd` (12 use sites `pk_held γpr {h,hh,CIDacq} n eb pcur`); `wp_printk_epi` (:684), `wp_printk_exit` (:953), `wp_printk_exit2fe` (:1035), `wp_printk_setup` (:1174) gain `γd` after `γpr` (applications :1027, :1115, :1394, :7822, :8108, :8267, :8282); acquire (:8237) and release (:768) at `<{ pr_res γd }>`; the `iAssert` at :8248 frames the payload. THE COMMIT PLACEMENT IS RULED: pass `ms`/`e` down -- `wp_printk_epi` takes `k_ledger_lb γd ms` and its post is `∀ mf ms0, … ⌜ms prefix_of ms0⌝ -∗ k_ledger_lb γd (ms0 ++ [e]) -∗ …` (via `k_ledger_lb_le` and `k_ledger_commit`, WpUart.v:427); the three exits of the top lemma (:8276-8279, :8328-8330, :8339) consume it. Then the milestone-A gate and COMMIT ("PRINTK-LEDGER A: the K ledger under pr.lock; cs still existential").
B: printint's digits pinned -- `digits_tbl` (ProofPrintint.v:267) at NAMED contents (`digits_from_data` :1354 already fetches `nth_byte digits_word j`, stop weakening at :1366; `pi_digit d = nth_byte digits_word (Z.to_nat d)` for 0 ≤ d < 16 from `digits_bytes` :1345); the buffer as a content-bearing `StackBytes.bytes_own_name` run in `wp_printint_dbody` (:278), `dloop` (:518), `ploop` (:658), the body (:820), the epilogue's `bytes_own_slots3` (:1013); the row `uint x = uint x0 / base^i` beside `PrintintArith.digit_step` (:190); the sign store; the print loop closed by consputc's pinned post; post `⌜cs = printint_bytes (sint xx) (uint base) (sign ≠ 0)⌝`. Gate, COMMIT ("PRINTK-LEDGER B").
C: printk's post pinned -- `cs = pk_render f (pk_vals m0 descs)` through the 23 block lemmas (wp_printk_char :1822 … wp_printk_dispatch :6904; a rendering accumulator instead of the existential); `k_led_ok` regains `template_match`; `k_ledger_kernel_msgs` loses its explicit `Forall template_match` hypothesis. Gate, COMMIT ("PRINTK-LEDGER C").
D: the tie -- copy TX-RECEIPT's Xv6Cameras block character-for-character (`git -C /shared/xv6iris-2-tlw show 6ac4b8626:iris/Xv6Cameras.v`, lines 373-423: `tx_free`, `tag_proj`, the two snoc laws); the THR leaf `WpSconfUartAccess.wp_uart_thr_write_s_sconf` takes a `match src` claim (the `k_flight γ (1/2) fl` half for `TxK`, `emp` otherwise) AND the pure `k_push_ok src fl b := match src with TxK => k_flight_ok (fl ++ [b]) | _ => True end`, threaded uartputc_sync → consputc → printint → printk and discharged at printk from its rendering (`I` at uartwrite/consoleintr); `k_tags_ledger` juxtaposed as conjuncts 5-6 of `uart_tagsE` (TX-RECEIPT's are 3-4; shape yours like its `tx_claim`/`tx_push_map`); `k_bytes_of_ledger` produced; the hart lines (a per-hart one-shot in `boot_hart_pre` spent at main.c:38's printk; `k_ledger_lb γd [banners]` on the `started` invariant; cpuid's pinned value through a value-pinning corollary so the hart line is byte-exact); delete `boot_k_shape`; fix SpecMain.v:219's "eight". Gate, COMMIT ("PRINTK-LEDGER D").

=== REPORT after each milestone's commit ===
Hash, log name, `git diff --stat main..lane/printk-ledger | tail -1`, the audit verbatim, lemma_diff justified, an as-landed line. When context runs short, STOP after the last green commit and report the next milestone's exact remaining work file:line.
