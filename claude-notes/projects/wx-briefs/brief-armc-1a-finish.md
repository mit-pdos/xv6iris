You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is ARM-c (1a) FINISH: you take over a lane whose previous agent landed everything but ONE open goal and stopped on a design question that is now ruled. Read brief-armc-1a.md in this scratchpad for the lane's design and operating rules, then this file. The working tree is DIRTY ON PURPOSE with the lane's phase-2 WIP (27 modified iris/*.v + iris/_CoqProject, new iris/InitBoot.v; backup `armc1a-wip.patch` + `InitBoot-wip.v` here); do not revert, stash, reset or `git checkout --` anything.

=== STATE (the previous agent's report, verified by build `armc1a11`: COMPILED=120, EXIT=2, ONE Error) ===
- Landed and green: `InitBoot.v` (`init_boot_bundle cw sts`, `init_boot_bundle_triv` over `□ (∀ W, uslot W)`, `init_boot_path`/`init_boot_bytes` named once, `SpecKexec.exec_au_pre_triv_at`); `ParkCap.park_pkg`'s mode row `match Wk with Some _ => first_done | None => init_boot_bundle cw sts end` (EAGER row; `sts` is now an explicit parameter of `park_pkg`/`park_cap`/`forkret_closer`/`wp_forkret_gen_body`/`forkret_park_pkg`/`forkret_park_paid_body`/`fkr_tail`/`fkr_boot`/`wp_forkret`; the closer yields `uslot` on the steady mode only); `park_token_park` takes the bundle where it took the family and both parkers lost `app_sup`; `fkr_boot` spends the bundle: `KX.wp_kexec_sconf` at `MkPfam uslot Rrf`, `init_boot_path`, the parked `sts`; `Harms` kept as a resource (`SpecKexec.exec_arms_landed_keep` :~900) and split at the branch; `SpecKexec.exec_post_ok_recv` (:~924) reads `uslot (exec_key U' sts 1)` out of either success arm and it is re-keyed onto the tail's record; `app_sup` gone from userinit/main/boot chain/trap loop/`park_world`/`UsertrapRes.park_world_open`; `FsCfgBoot.fs_boot_supply_app_inv` (the persistent `app_inv fsc_fs` peeled off kit 2) used at `SystemAdequacy.v:~595`; `Hinit_boot` replaces `Happ_sup` in `SystemAdequacy.xv6_boot_era` (:~408), `xv6_power_adequacy_gen` (:~788), `App.xv6_app_adequacy` (:~205); `App.app_triv_init_boot` replaces `app_triv_sup`; discharge `SystemAdequacy.init_boot_of_sup`/`init_boot_of_triv` (:~756/:~768); `ProofUserinit` lost the `(UG : UEXEC_GEN)` functor argument; prose sweep done.
- THE ONE RED GOAL: `ProofForkret.v:~2196` (`OPEN:` comment at :~2185) -- `wp_forkret`'s STEADY arm at `steady = false`: a `None`-mode (boot) package carries the exec bundle and no slot, its closer yields none, and the steady arm never runs kexec, so `fkr_tail`'s boot-mode slot premise (`if steady then emp else uslot (uvis_of U sts)`, SpecForkret.v:~456 / ProofForkret.v:~267) has no payer. Unreachable in the kernel -- the only `None` park is userinit's, whose block holds `FirstTok.first_tok`'s BOOT disjunct (`ProofUserinit.v:~698`, `first_tok_boot`), so the record takes the boot arm -- but the contract does not correlate `first_tok`'s state (inside `ProcInv.proc_priv`, carried whole by `ParkCap.park_child`) with `park_pkg`'s mode.
- Skipped behind it, written but unverified: `LinkUserinit`, `BootChain`, `SystemAdequacy`, `App`, `AppEcho`.

=== THE RULING: D1, the block-split seam ===
The boot mode IS "the parked record holds the first-process boot token": make the contract say so. `ParkCap.park_child` gains the mode (`Wk : option uvis`, or the bool the parkers already pass) and, on the BOOT mode, carries the block SPLIT -- `proc_priv_nocwd ∗ cwd_ref_at` (or whatever `ProcInv` names the block-minus-the-first-token split; find the seam `ProofUserinit`'s comment at ~698 names: "the seam your userinit comment already names") together with `first_tok`'s boot rows (`first_addr ↦₄ 1` and its companions -- `FirstTok.first_tok` :~538, `first_tok_boot` :~593) as SEPARATE rows of `park_child`; on the steady mode `park_child` is unchanged (the block whole). Consequences: `SpecForkretParkPaid`/`SpecForkret`/`ProofForkretPark` carry the mode-dependent shape (the cap's `ctx_move` of the boot rows needs their `CtxMorph` instances -- `ctx_morph_word4` exists and is dfrac-indexed; add instances for the companions if missing); `ProofUserinit` parks with the split block (it has the boot disjunct in hand); `wp_forkret` at `steady = false` holds `first_addr ↦₄ 1` beside `first_done`'s `first_addr ↦₄□ 0` on the steady arm and REFUTES it by `FirstTok.first_tok_boot_excl` (:~679); `fkr_boot` reassembles the block (`first_tok` closes to `first_done` at the release store exactly as today). NOT the half-cell variant (it changes `FirstTok.first_tok`'s cone) and NOT the `first_fsinit` variant (it needs an exclusion lemma that does not exist). NOT an application-paid slot (a GAP premise for echo).

=== OPERATING RULES (hard) ===
As brief-armc-1a.md: BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (run_in_background, sentinel `COMPILED=`/`EXIT=`); ONE build at a time; gate = zero Error, EXIT=0, `make audit-only` on the VM = EXACTLY the thirteen (print the list verbatim in the report -- this lane touches the theorem), `tools/lemma_diff.py` clean (the four justified GONEs: `App.app_triv_sup`, `ProofForkretParts.fkr_init_bytes`, `ProofSyscall.syscall_env_sup`, `SyscParkEnv.park_world_sup`; the hypothesis swap `Happ_sup` → `Hinit_boot` reported as such; nothing admitted; no new assumption). GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files here; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT. Comments state the current shape as fact.

=== DELIVERABLE ===
D1 landed and the tree green through `App`/`AppEcho`; the `OPEN:` comment gone (the site's comment states the refutation); sanity F of brief-armc-1a.md (as corrected by the previous agent: `app_sup` survives only in AppInv's definitions, the fs dischargers' premise positions, UexecExecInst/UexecCond, `SystemAdequacy.init_boot_of_sup`, UexecWp/UexecSG prose; `Happ_sup` names nothing; `exec_au_pre_triv` names SpecKexec only). Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, new files, the audit list verbatim, and the ≤15-line as-landed note (the previous agent's draft is in its report; correct it to what landed, adding the D1 sentence). If context runs short, STOP after a green -k build and report exactly what remains, file:line.
