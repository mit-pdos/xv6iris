You are a KERNEL-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-tlw (a sibling clone at current main; never touch or build /shared/xv6iris-2). Your lane is TX-RECEIPT: a user process's console write gets an EXACT receipt -- "the bytes tagged with my pid accepted since my seed are exactly my bytes, in order" -- by way of an exclusive per-pid TRANSMIT TOKEN that the device leaf demands for every pid-tagged push; and the exact receipt is relayed through filewrite and the dispatcher to row 16 of the user-execution key, with a receipt-keeping U-tier write leaf, so E5 can prove what init, sh and echo put on the wire. Design of record: claude-notes/projects/app-echo.md "TX-TAG LANDED" + "TX-TAG PHASE 1" (facts (i)-(iii); the ruling: an exclusive per-pid transmit token), "E5 -- THE OUTPUT SIDE" O4 TX-RECEIPT, "SUPPLY-SPLIT PHASE 1" (the console write's deposit costs nothing; `udepw_law 16` is what init/sh/echo owe), "CONS-ROUTE LANDED"/"OPEN-PIN COMPLETE" (the receipt-keeping leaf moulds), and `review-echo-plan-2026-09-12.md` findings 4, 5. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the bundle-intro note, the pinned-C note: read C via `git -C /shared/xv6iris-2/xv6-riscv show 06ea57f:kernel/<file>`); remote-build-gcp.md; iris/UartSentLoc.v (:48-69 the header on what the token must do; `tag_proj` :~104, `uart_sent_exact_at` :~200 -- stated without a producer; `uart_sent_from_tag` :~281); iris/WpUart.v (`uart_tagsE`, `uart_ghosts`, `uart_tags_update`, `uart_sent_tagged`, `uart_tag_at`, `uart_ghosts_alloc`); iris/WpSconfUartAccess.v (`wp_uart_thr_write_s_sconf` :~327 -- the ONE push site, taking `src` and `tg0`); iris/UartTxInv.v (`tx_res`, `is_txlock`, `uart_sent_sub_at`); iris/SpecUartPutc.v, iris/SpecUartwrite.v + iris/SpecUartwriteLoc.v, iris/SpecConsolewrite.v (`cons_sent_cnt_at` :~150; the post at `TxW pid`; `pid` is a parameter), iris/ProofConsolewrite.v, iris/SpecFilewrite.v + iris/ProofFilewrite.v (the console arm consuming `cons_sent_cnt`; `filewrite_in … (wf_Q f) (wf_tr0 f)` -- the family fields a write's receipt is stated through), iris/SpecSysWrite.v + iris/ProofSysWrite.v, iris/SpecSyscall.v + iris/ProofSyscall.v (the write arm; how row 16 of `UexecExecInst.xv6_sbundle` is instantiated: `filewrite_in (fd_st_of_key …) (sys_rw_count …) (uvis_M W) (xk_a W 1) (wf_Q f) (wf_tr0 f)`), iris/UexecExecInst.v (row 16 :~511-513, the `xfam` fields `wf_Q`/`wf_tr0`; row 5's receipt shape as the mould; `spost_at_read_intro`), iris/FsAbsInvFire.v (`fsabs_filewrite_in` :~325-340: at a CONSOLE row the payload is `uart_sent γu []` from unit), iris/UkRunSys.v (`wp_uk_ecall_open_recv` :~2889, `wp_uk_ecall_read_recv` :~2672, `wp_uk_ecall_quiet_recv` :~3053 -- the receipt-keeping leaves at the TRAPPING key: the mould for `wp_uk_ecall_write_recv`), iris/UkRun.v (`udepwf_std` -- the ledger-fixed deposit; `udepw_law`); iris/ProcInv.v (`proc_priv_core` :~1248 -- where a per-pid token lives), iris/ProcDefs.v, iris/SlotGen.v (`pid_reg`: pids unique per generation; where a pid is born: `ProofAllocproc.v`; where it dies: `ProofFreeproc.v`), iris/UkInitPrintf.v / iris/UkInitPutc.v (init's banner: 18 one-byte writes through `wp_kinit_write`), iris/UkSh.v (`wp_ksh_write` :~770 at `sh_deps`' 16 conjunct), iris/UkEcho.v (`wp_kecho_write` :~1012: echo's four writes).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-tlw <logname>` (names `txrcpt10`, `txrcpt11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. `WpUart`/`ProcInv` sit under most of the tree: batch, run detached, wait. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-tlw` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref main` justified; nothing Admitted; no new Axiom/Parameter. Files: the UART/console/filewrite/syscall/row-16 chain named above, `ProcInv`/`ProcDefs`/`ProofAllocproc`/`ProofFreeproc` for the token, `UkRunSys`/`UkRun` for the U-tier leaf; `App.v`, `SystemAdequacy.v`, `ConsoleInv`, `FsAbs*` beyond `FsAbsInvFire`'s write arm, `Uk{Init,Sh,Echo}*` program files are off limits (report what they will consume). GIT: branch `lane/tx-receipt`; commit there when green; never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NO placeholder arms.

=== THE DESIGN ===
R1 THE TOKEN.  `tx_mine (pid : mword 32) (l : list (bv 8))`: the process's own accepted bytes, as a ghost-map (pid ↦ list) whose AUTHORITY lives in the UART invariant beside `uart_tagsE` with the tie `∀ pid l, mine !! pid = Some l -> tag_proj (TxW pid) tg l` (the TxW-pid projection of the tagged accepted list IS the pid's list) and whose FRAGMENT `tx_mine pid l` (exclusive, `ghost_map_elem` at `DfracOwn 1`) lives in the process's private block (`proc_priv_core`; minted at `ProofAllocproc` with `l := []` when the pid is born -- the tie holds because a fresh pid has no bytes: PROVE that from pid uniqueness (`SlotGen.pid_reg`) and the invariant, or by keeping dead pids' entries in the map with their final lists; state which); `ProofFreeproc` returns it (or leaves it, if dead entries stay). The THR leaf `wp_uart_thr_write_s_sconf` at `src = TxW pid` DEMANDS `tx_mine pid l` and returns `tx_mine pid (l ++ [sb])` (the other tags demand nothing). Consequence: every TxW-pid push is by the holder of `tx_mine pid`, so the projection tie is maintained by the one push site.
R2 THE EXACT RECEIPT.  `uart_sent_exact_at γu n0 (TxW pid) bs` is PRODUCED: a caller holding `tx_mine pid l0` at seed `tr0` (with `length l0` = the number of its bytes before the seed… simpler: the receipt is stated relative to the token: `tx_mine pid (l0 ++ bs)` after, and the tie gives "the TxW-pid projection of the accepted list = l0 ++ bs"; the located form follows). consolewrite's post: `cons_sent_cnt_exact γu pid l0 M ua r := ∃ bs, ⌜length bs = r⌝ ∗ ⌜ubytes_at M ua bs⌝ ∗ tx_mine pid (l0 ++ bs)` beside (or replacing, if every consumer moves) `cons_sent_cnt_at`; uartwrite/uartputc thread the token; consolewrite takes it from `proc_priv_core` (it has the block) -- or, if the block is packed where consolewrite runs, from a premise its caller (filewrite ← sys_write ← the dispatcher, which HAS the block) supplies: find the shortest route.
R3 THE RELAY TO ROW 16.  `filewrite_in`'s console arm hands the exact receipt through `wf_Q f`/`wf_tr0 f` (read what those fields are for -- `wf_tr0` is the seed, `wf_Q` the caller's post about the receipt): the U-tier caller chooses `wf_Q` = "my token advanced by exactly my bytes"; the dispatcher's write arm relays; row 16's post carries it at the trapping key (as row 5 does for read: `spost_at_read_intro` is the mould, incl. the `∃ P` tie only if needed -- a write needs no table). The token must be REACHABLE from the U tier: the process's `tx_mine pid l` is in the KERNEL's block, but the user program needs to know `l` (its output so far) -- so the key exposes it or the program holds a lower-bound/agreement copy: PROPOSE the shape (a `uvis_tx : list (bv 8)` component on the key is the PID-KEY/LAZY-FLAG precedent, rows: `tx' = tx` everywhere except write's `tx' = tx ++ bs`; the program then reads its own transcript off the key exactly as it reads the pid), and STATE it; discuss the cost (skey_eq, bump, uvis_of, urun) before building -- phase 1 reports the proposal, phase 2 builds what the coordinator rules.
R4 THE U-TIER LEAF.  `UkRunSys.wp_uk_ecall_write_recv` at the TRAPPING key (mould: `wp_uk_ecall_open_recv`), handing back the argument words, the ledger row (fd 1 or 2 = console), the returned count `r` and the exact receipt "my transcript grew by exactly the first `r` bytes of my buffer" -- and the deposit it needs is ledger-fixed (`udepwf_std`-shaped at 16, because the console arm is free only when the fd row IS the console) so that `udepw_law 16` (what init/sh/echo owe today) is REPLACED by this leaf's own supplier `udepwf_std … 16` that the programs build from their ledger. Report what `UkInitPutc.wp_kinit_write` / `UkSh.wp_ksh_write` / `UkEcho.wp_kecho_write` will consume (do not edit them).
R5 SANITY: `grep -n "tx_mine" iris/ProcInv.v iris/WpSconfUartAccess.v iris/SpecConsolewrite.v` shows the token's home, the demand, the receipt; `grep -n "uart_sent_exact_at" iris/*.v` shows a PRODUCER; the audit unchanged; `git diff --stat -- iris/App.v iris/SystemAdequacy.v iris/ConsoleInv.v iris/Uk{Init,Sh,Echo}*.v` EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
R1 landed (the ghost, the tie in the invariant, the block's fragment, the THR leaf's demand, allocproc/freeproc) with proofs where short; R2 STATED (consolewrite's post) with the projection lemma proved; R3 PROPOSED (the key component or the alternative, with cost) and row 16 STATED; R4 STATED. Compiling, NO Admitted. Report: (1) statements verbatim, file:line; (2) the token's route allocproc → block → consolewrite → THR leaf, and how freeproc/pid reuse is handled; (3) R3's proposal and cost; (4) the red files; (5) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs, the relay as ruled, the leaf, R5, the gate, commit on `lane/tx-receipt`. Report log name, commit hash, `git diff --stat main..lane/tx-receipt | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), what the three programs' write sites will consume (file:line).
