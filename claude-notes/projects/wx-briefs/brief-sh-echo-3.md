You are an APPLICATION-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-tlw (a sibling clone; never touch or build /shared/xv6iris-2, /shared/xv6iris-2-sup, /shared/xv6iris-2-disc; never touch lane/tx-receipt, lane/tx-tag, lane/text-lw). Your lane is E4 SH-ECHO PHASE 3: finish lane E4 on branch `lane/sh-echo` (= e01a8d9df, green, on lane/supply-split-r1). Read first: claude-notes/projects/app-echo.md "E4 SH-ECHO PHASE 2 ON ITS BRANCH" (what is proved, what is owed, the hang's three suspects) and "E4 SH-ECHO PHASE 1"; wx-briefs/brief-sh-echo.md (the design S1-S5; superseded where the notes say); durable-notes.md ("Build", "Vacuity", the bundle-intro note ("iIntros \"#H\" on a bundle of wands ... never returns"), the "definition nobody computes but the unifier will" note, the ELF-bytes note); remote-build-gcp.md ("Sharing the VM": builds queue; never kill anything but your own worker by PID). The coordinator owns the design; you do the proofs. ONE PHASE: finish and gate.

=== OPERATING RULES (hard) ===
Start: `cd /shared/xv6iris-2-tlw && git status --porcelain` empty, `git checkout lane/sh-echo`, `git rev-parse --short HEAD` = e01a8d9df. BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-tlw <logname>` (names `shecho40`, `shecho41`, …; run_in_background; one at a time; never local coqc; never a remote top-level make). A proofmode HANG is detected by a build that does not finish a 700-line file in ~10 minutes while siblings progress: kill ONLY your own `rocqworker … UkShEcho.v` by PID, never pkill. Gate: zero Error, EXIT=0; VM `make audit-only` from /mnt/rocq/trees/_shared_xv6iris-2-tlw = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref lane/supply-split-r1` justified; nothing Admitted; no new Axiom/Parameter; `grep -n "PHASE 3" iris/UkShEcho.v iris/UShEcho.v` EMPTY at the end. Files: UkShEcho.v, UShEcho.v, UkShRun.v/UkShDiag.v/UkShFork.v (only for the cwd threading), UEchoKernel.v (if echo's bridge needs an entry lemma restated), UShKernel.v (read-only mould). Kernel files, App.v, SystemAdequacy.v, UexecExecInst.v off limits. GIT: commit on lane/sh-echo when green; never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/.

=== THE WORK ===
W1 THE HANG: restore the two walks from /tmp/claude-0/-shared-xv6iris-2/90bbdadb-72f8-4d74-adc1-7dbe26702b13/scratchpad/e4/UkShEcho-with-walks.v (if the scratchpad is gone, the `PHASE 3:` markers at UkShEcho.v:575/:627 say what goes there) and try the suspects IN ORDER, one build each: (1) intro `sh_exec_sup_echo` LINEARLY (`iIntros "Hexs"`) and re-`iDestruct`/`iPoseProof` its □ where it is spent, or destructure per conjunct -- and `Set Typeclasses Debug` once to confirm whether the Persistent search is the culprit; (2) `Global Opaque echo_cmd` (and `ush_args` if needed) with explicit rewrites `ush_jarm (echo_cmd s0 g) = 0xce`-style equations proved by `vm_compute`/`reflexivity` BEFORE the proofmode; (3) replace the `replace … by lia` over `ush_Dg` by a `pose proof`'d equation outside the proofmode. Report which suspect it was.
W2 `echo_slot_of_kexec` (UShEcho.v:589): echo's entry at the exec channel -- from `kexec_image_ok echo_elf na alen afun sts W'` (with na = 3, alen 4/5/5, afun the three strings) derive `UEchoKernel.echo_uexec_slot`'s key-level premises: the argv block kexec BUILT (`SpecKexec.kexec_args_at`: strings at `kxc_sp top alen (S i)`, NULs after them, the (na+1)-word vector at `kxc_sp_final`) → `UkAbi.uk_args_c` (the block echo READS) + the two presence rows; `UShKernel.sh_slot_of_kexec` (UShKernel.v:436) is the mould; `echo_kexec_sz = 0x4000`, `kxc_sp_final 0x4000 [4;5;5] 3 = 0x3FB0`, `echo_room` are already proved.
W3 `echo_key_args` (UShEcho.v:708): echo's key spells the three strings (from W2's geometry).
W4 THE CWD THREADING: sh's fork arm preserves the cwd VALUE (the engine leaf `UkFork.wp_uk_ecall_fork` :831/:893 already hands the child `ucwd (ukn_cwd N') c`): `UkShDiag.wp_kshr_fork1_final` / `UkShFork.wp_kshf_fork` at `ucwd c` instead of `ucwd_any` (keep an `ucwd_any` corollary for the untouched consumers), so `wp_kshm_child_echo`'s `ucwd ROOTINO` premise is discharged from sh's entry fact `uvis_cwd W = ROOTINO` (SH-OPEN's; take it as the entry's premise as UShKernel now does).
W5 `wp_kshm_child_echo` dispatch proved; S5's greps; the gate; commit ("E4 SH-ECHO phase 3: echo's exec entry, the specialised walks, the cwd").

=== REPORT ===
Log name, commit hash, `git diff --stat lane/supply-split-r1..lane/sh-echo | tail -1`, the audit verbatim, lemma_diff justified, which suspect caused the hang, an as-landed note (≤ 15 lines, present tense), the named premises left and their owners. If context runs short, commit a green milestone and report what remains with file:line.
