You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is E1 ECHO-PRED: the echo application's claim becomes "tainted or pinned", gains the /echo pin, meets the top theorem's exact hypothesis shapes, and is assembled into an `xv6_app` record -- with every hypothesis but `Hinit_boot` and `Hphi` discharged as lemmas, and STILL NO THEOREM (a theorem taking the open ones as hypotheses is the GAP-premise trap; durable-notes "Vacuity"). Design of record: claude-notes/projects/app-echo.md "THE REMAINING ARC TO xv6_app_adequacy FOR ECHO — DESIGN" (E1), then "What is in iris/AppEcho.v today", "ARM-c — DESIGN" (why `taint ⊢ app_sup`), "The target statement". The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "Vacuity", "Maintaining these notes"); remote-build-gcp.md; design/applications.md; iris/App.v (whole: the record :117-144, the theorem :158-245, the generic instance :286-380); iris/AppInv.v (`app_sup_raw` :111, `app_xfer_raw` :141 and its two laws :151/:160, `app_sup` :190); iris/AppEcho.v (whole, 376 lines); iris/FsShPin.v (whole: THE MOLD for the /echo pin -- `SH_INO`, `sh_path`, `sh_bytes`, `sh_bytes_elf`, `era0_sh_pins` :286, `era0_recovery_sh_pins`); iris/FsInitPinBoot.v (`era0_pins` :109, `era0_recovery` :229); iris/FsImgCheck.v (`fname_echo` :402, `fsimg_echo_path` :434 -- /echo is inum 4, `fsimg_echo_type` :474, `ElfUser.echo_elf`); iris/FsDurImg.v (`img_snap_ok` :~1381) and iris/SystemAdequacy.v (`fsimg_snap_ok` :~1445; §2b `init_boot_of_sup`/`_triv` :757-777 -- how the GENERIC instance discharges `Hinit_boot`, which E2 mirrors for echo).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 (log names `echopred1`, `echopred2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. New files go into `iris/_CoqProject` in dependency order. Gate: zero `Error`, `EXIT=0`, COMPILED>0; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim -- the audited theorem is the GENERIC one, so this lane must not move it); `tools/lemma_diff.py` with every line justified. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact (no dates, no lane markers). Pure files must not gain iris imports.

=== THE DESIGN ===
P1. THE /echo PIN: new `iris/FsEchoPin.v` on `FsShPin.v`'s mold, exactly: `ECHO_INO : Z := 4` (read off the image: `FsImgCheck.fsimg_echo_path`), `echo_path := [fname_echo]`, `echo_bytes := ElfUser.echo_elf` with the joining lemma, `era0_echo_pins (av : aview) : Prop` (the path pin `apath_at av ROOTINO echo_path = Some ECHO_INO`, the content pin `av !! ECHO_INO = Some (MkAnode (AFile echo_bytes) 1)` -- copy whatever `era0_sh_pins` states for sh, including its `arun` clause if it has one), and `era0_recovery_echo_pins` (the image-recovery instance, on `era0_recovery_sh_pins`'s proof). Whatever `FsShPin` proves about durability/transport of its pin (grep its lemmas and their consumers in `UInitSh.v`/`PinnedExec.v`), prove for echo.
P2. THE PREDICATE: `AppEcho.echo_fs_pure av := era0_pins av /\ era0_sh_pins av /\ era0_echo_pins av`; `echo_pred (γ : echo_fixed) (_ : echo_names) (av : aview) : iProp Σ := (mono_nat_lb_own γ 1 ∨ ⌜echo_fs_pure av⌝)%I` -- `echo_names` is whatever `app_names` echo uses today (unit if so; keep it); the taint is `echo_tag`'s right disjunct (:250) -- state ONE definition `echo_taint γ := mono_nat_lb_own γ 1` and use it in `echo_tag`, `echo_pred`, `echo_R_untainted`. Persistent/Timeless instances.
P3. THE LAWS: `echo_xfer : ⊢ app_xfer_raw (echo_pred γ)` for every γ -- prove a general `AppInv.app_xfer_raw_pers_or_pure` ("a persistent-or-pure claim duplicates") beside `app_xfer_raw_pure`, or prove `echo_xfer` directly (PREFER the general law: it is a lemma about the transport, not about echo). `echo_sup_of_taint : echo_taint γ -∗ app_sup_raw (echo_pred γ) r` (immediate). `echo_init_img`: `Happ_init`'s LITERAL shape (App.v:204-207: `⊢ |==> ∃ r, echo_pred γ r (abs_view (fss_inodes (FsDurImg.img_state (fs_blocks (v_disk (g.(gdev).(dvirtio)))) sb nib)))` under the theorem's `Himg`-style premise -- quote it exactly as the theorem binds it) composed from today's `echo_init` (:367) + `FsDurImg.img_snap_ok` + `FsInitPinBoot.era0_recovery` + the sh/echo recovery pins + `SystemAdequacy.fsimg_snap_ok` -- find the composition the GENERIC instance's `app_triv_init` (App.v:302) sidesteps and the pins need; it takes the RIGHT disjunct (pins) -- no taint at boot. `echo_R_untainted` restated at `echo_taint`. Keep `echo_birth`, `echo_R_*`, `echo_tag_*` as they are (retype where `echo_taint` replaces the literal).
P4. THE RECORD: `Definition app_echo : xv6_app Σ := MkApp echo_fixed echo_cl echo_names echo_pred echo_R echo_tag echo_phi` with `echo_phi : gstate -> list mobs -> Prop := fun _ _ => True` FOR NOW (E5 writes `good_out`; a `True` `app_phi` makes `Hphi` trivially dischargeable but the theorem then says only safety -- that is honest and is stated in the header; do NOT prove an `xv6_app_adequacy` instance for `app_echo` in this lane even though `Hphi` would go through, because `Hinit_boot` does not). Then the dischargers AS LEMMAS at `app_echo`'s fields, one per hypothesis of `App.xv6_app_adequacy` except `Hinit_boot` and `Hphi`: `echo_Hbirth`, `echo_HRt`, `echo_Htagp`, `echo_Htagt`, `echo_HR0`, `echo_Hpow`, `echo_Htx`, `echo_Hrx`, `echo_Happ_xfer`, `echo_Happ_init` -- each stated EXACTLY as the theorem's binder (copy the statement from App.v:166-207 with `A := app_echo`), so E2 and E5 only add the last two and apply the theorem. A header note listing the two open hypotheses and which lane closes each (E2: `Hinit_boot`; E5: `Hphi` at the real `good_out`).
P5. SANITY: `grep -n "echo_taint\|echo_pred\|app_echo" iris/AppEcho.v`; `grep -n "ECHO_INO\|era0_echo_pins" iris/FsEchoPin.v iris/AppEcho.v`; `grep -rn "Happ_sup" iris/*.v` still only AppInv/SystemAdequacy; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
P1-P4 as STATEMENTS (proofs may be in progress; NO Admitted at report time -- leave a lemma out rather than admit it). Build with -k. Report: (1) `echo_pred`, `echo_taint`, `era0_echo_pins`, quoted; (2) `echo_init_img`'s statement, quoted, and the composition you found; (3) the record and the list of discharger lemmas with their exact statements; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs, P5. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 12 lines, present tense). Do not commit or stage.
