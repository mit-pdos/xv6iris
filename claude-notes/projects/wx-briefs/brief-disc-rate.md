You are an APPLICATION-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-disc (a sibling clone; never touch or build /shared/xv6iris-2). Your lane is DISC-RATE: restate the echo application's input DISCIPLINE and state its OUTPUT CLAIM, as pure definitions over the observation trace, with the bridge lemmas that keep every landed consumer of the old discipline working. Design of record: claude-notes/projects/app-echo.md "E5 -- THE OUTPUT SIDE: DESIGN PROPOSAL" -- O1, O2, and O3 AS RULED BY THE OWNER (D0-D3), O5 -- and "E3 -- THE INPUT LINE: DESIGN PROPOSAL" (R1-R5) for the older per-character ruling; the pre-mortem review `review-echo-plan-2026-09-12.md` findings 2, 7, 8, 12. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Vacuity"); remote-build-gcp.md; iris/AppEcho.v (the whole header :1-90; `disc` :~211 and everything it feeds: `disc_seg`, `cycles_of`, `disc_nil`, the three closure laws (out / power / in), `echo_tag`, `echo_R`, `echo_line`, `echo_phi` (today `True`), the `Hphi` placeholder, `echo_taint`); iris/App.v (`xv6_app` :117: `app_R`, `app_tag`, `app_phi : gstate -> list mobs -> Prop`; `xv6_app_adequacy`'s `HR0`/`Hpow`/`Htx`/`Hrx` -- what the ledger must be closed under: an out byte, a power event, an in byte); iris/SystemAdequacy.v (`xv6_trace_adequacy` :~1210: `trace_shape`, `open_seg`, `obs_wire`, the shape of `mobs` (ObsUartIn/ObsUartOut/ObsPowerOn/Off) -- find their definitions); iris/UConsLine.v (`ush_line_is`, `ush_line_lexable`, `ush_disc_line`, `ush_echo_tokens`, `ush_tag_law` -- the consumers of `disc` that SH-LINE 2b will prove: they must keep holding under the NEW `disc` through a projection lemma); iris/SpecMain.v :1-20 and iris/SpecMainSecondary.v :1-12 (the boot's printk strings, verbatim: "\n", "xv6 kernel is booting\n", "\n" on hart 0; "hart %d starting\n" on harts 1..7 -- read `PrintkFmt` for how "%d" of `cpuid()` renders; `RiscvLang.NCPU = 8`); xv6-riscv/user/init.c (the banner "init: starting sh\n" and the three failure printfs), sh.c (the prompt `fprintf(2, "$ ")`; `panic` prints "%s\n" of its argument and `exit(1)`; runcmd's EXEC arm prints "exec %s failed\n" then falls to `exit(0)`; `fork1` panics "fork"), echo.c (writes each argument, a space or newline after it: "hello"," ","world","\n"), kernel/console.c (`consoleintr`: echo of a typed byte is the byte itself, '\r' echoed as '\n'; BACKSPACE = "\b \b"; nothing echoed when the ring is full).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-disc <logname>` FROM /shared/xv6iris-2-disc (names `disc10`, `disc11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'` from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-disc` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter. ONLY `iris/AppEcho.v` (and a new `iris/EchoDisc.v` if you want the pure combinatorics separate) and, for the bridge, `iris/UConsLine.v` are yours; `App.v`/`SystemAdequacy.v` are the TRUSTED STATEMENT: do not touch. GIT: branch `lane/disc-rate` in your checkout; commit there when green; never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. Every definition is decidable where the old one was (`disc_dec` is used).

=== THE DESIGN ===
R1 THE BOOT MESSAGES.  `boot_msgs : list (list (bv 8))` -- the eight strings, verbatim from the specs (hart 0's three, then "hart 1 starting\n" … "hart 7 starting\n" as the model renders them); `hart_lines` the seven.
R2 THE PURE-SUFFIX POINT.  `k_done (w : list (bv 8)) : Prop` -- every one of the seven hart lines is a SUBSEQUENCE of `w` (stdpp `sublist`); `k_point w` its least index (decidable; a `nat` or `None`). Lemma: the U prefix "init: starting sh\n$ " (and any prefix of `expected`) has no hart line as a subsequence -- state and prove what D0's unambiguity needs (at least: `hart_lines` are not sublists of any prefix of `expected n`).
R3 THE EXPECTED SESSION.  `expected_rel ins out` for `ins` a prefix of `(echo_line)*`: `out` is a prefix of "init: starting sh\n$ " ++ for each input byte its echo, and after each complete `echo_line` one of `line_alts` (see O5 below). Where the rest of this brief says `expected`, read the relation. (Echo of '\n' is '\n'; the user types '\n' not '\r' -- if `echo_line` ends in '\r' today, say so.) Prove `expected` monotone in `ins` (prefix to prefix) and `length`/decomposition lemmas the proof will want.
R4 THE DISCIPLINE.  Over one power cycle's segment `h : list mobs` (reuse `cycles_of`/`disc_seg`): D3 `disc_in h` := today's content condition (inputs a prefix of `(echo_line)*`); D0 := every ObsUartIn occurs after `k_point (obs_wire h)`; D1/D2 := for the i-th input byte, the wire between the previous input (or the k-point) and it contains the previous byte's echo (D2) and, at a line start, the prompt "$ " (D1) -- state D1/D2 positionally over the pure suffix: "the out-bytes of `h` after the k-point and before input i equal `drop k (expected (take i ins))`-prefix-wise" is the cleanest single condition that implies both (the suffix is pure U, so the wire before input i IS the expected stream up to the previous echo). Choose the formulation that makes the proof of `Hphi` a simulation; explain your choice. `disc h := Forall disc_seg' (cycles_of h)` with `disc_seg'` = D0 ∧ D1/D2 ∧ D3. Closure laws as today (out byte: `disc` of the extension is decided by the new byte's position -- state exactly what remains true; power; in byte: `disc (h ++ [In b]) -> disc h`). PROJECTION: `disc h -> disc_old h` (the landed prefix predicate), so `echo_tag`, `echo_R`, `UConsLine`'s line lemmas keep their proofs: re-prove the three closure laws for the new `disc` and derive the old ones' USES from the projection where the old law does not survive verbatim (report which).
R5 THE CLAIM.  `good_out (h : list mobs) : Prop := ∃ k, k = k_point (obs_wire h) ∧ ∃ pre, obs_wire h = pre ++ drop_k_part … ∧ pre ∈ shuffle boot_msgs (take (length pre - boot_len) (expected ins)) ∧ (the suffix) `prefix_of` drop (…) (expected ins)`, with `ins := the ObsUartIn bytes of the cycle` -- write it as a clean definition (`shuffle` = interleaving of a list of lists with one list, order preserved within each; define it, or use stdpp's `interleave`/`Permutation`-free formulation; keep it decidable if cheap). `echo_phi g h := Forall (fun seg => disc_seg' seg -> good_out seg) (cycles_of h)`. ALLOCATION FAILURE (O5, RULED): `expected` is a RELATION per line: after a complete line's '\n' echo the continuation is ONE OF `line_alts := ["hello world\n$ "; "exec echo failed\n$ "; "$ "; "fork\n" ++ "init: starting sh\n$ "]` (good; exec failed in the child; the child died silently; sh's fork1 panicked and init restarted sh). So `expected` becomes `expected_rel (ins : list (bv 8)) (out : list (bv 8)) : Prop` (inductive or a decidable checker over the line structure), monotone in both, with the D1 prompt-wait reading "a "$ " has appeared since the previous '\n' echo" (every alternative ends in "$ ", so D1 is well-defined for all of them). Keep `line_alts` a named constant so a later ruling changes one list.
R6 SANITY: `AppEcho.echo_phi` is no longer `True`; `grep -n "disc_old\|disc_in" iris/AppEcho.v iris/UConsLine.v` shows the projection and its uses; nothing else in the tree references a changed name (`grep -rn "AppEcho.disc\b" iris/`); the audit unchanged; `Hphi` (the theorem's obligation at `echo_phi`) is NOT proved by this lane -- leave it open and say so at the site.

=== PHASE 1 -- then STOP AND REPORT ===
R1-R5 as definitions + the statements of every lemma, the short proofs done (R2's unambiguity, R3's monotonicity, the projection), compiling. NO Admitted. Report: (1) the definitions verbatim; (2) the D1/D2 formulation chosen and why it makes `Hphi` a simulation; (3) which old closure laws survive verbatim and which consumers need the projection; (4) anything this brief got wrong (e.g. how `mobs`/`obs_wire`/`cycles_of` are really shaped), with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs, R6, the gate, commit on `lane/disc-rate`. Report log name, the commit hash, `git diff --stat main..lane/disc-rate | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense).
