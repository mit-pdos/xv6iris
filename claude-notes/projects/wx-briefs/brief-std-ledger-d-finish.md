You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane has TWO PARTS, run in order: (A) STD-LEDGER -- sh is verified at ANY standard-stream ledger (its entry stops assuming its three standard streams are open); (B) PINNED-EXEC D FINISH -- the previous agent's work-in-progress (init execs sh on its own pinned bundle) is closed on top of (A). The working tree is DIRTY ON PURPOSE with D's phase-2 WIP: `git status --porcelain` = ` M iris/UCodeInit.v  M iris/UInitKernel.v  M iris/UShKernel.v  M iris/UkFork.v  M iris/UkInit.v  M iris/UkInitMain.v  M iris/_CoqProject  ?? iris/UInitSh.v` (+ a pre-existing `?? vtest-rocq/.VIcache.v.swp`). Do not revert, stash or reset any of it; a backup is `pexd-wip.patch` + `UInitSh-wip.v` in this scratchpad. Design of record: claude-notes/projects/app-echo.md ("PINNED-EXEC D — brief-pinned-exec-d.md" for D's design; "D-4 RULING: STD-LEDGER" for this lane) and brief-pinned-exec-d.md in this scratchpad (D's design). The coordinator (me) owns the design; you do statements and proofs. STOP AND REPORT after (A) is green; then (B) after go-ahead.

=== THE STATE OF D (what the previous agent landed; read its diff: `git diff -- iris/`, and iris/UInitSh.v) ===
- `UCodeInit.init_argv_map`/`init_argv` (persisted argv bytes); `UkFork.forkable_ubyteq_map`; `UkInitMain.forkable_init_img` carries `init_argv`; init's entry (`UInitKernel.init_uexec_slot`) carves with `uslot_of_urun_all`, persists the sixteen bytes (`ubyte_map_sub`, `Local Opaque init_argv_map` -- inlining `big_sepM_subseteq` at that altitude hangs), and hands `init_exec_sup` down; `UkInit.init_exec_sup` (UkInit.v:~486) is the u-tier supplier: `□ (∀ N' m pc, ⌜a0 = 0x9a8⌝ -∗ ⌜a1 = 0x1000⌝ -∗ init_rodata -∗ init_argv -∗ ustd_open (ukn_fd N') -∗ udepw_at N' m pc USYS_exec ROOTINO)`; `init_exec_sup_of_uxsup`; `UkInit.wp_kinit_exec` is cwd-indexed (`wp_uk_ecall_exec_at_cwd`); every init lemma carries `ucwd γcwd ROOTINO`; `UInitSh.v` (new): `init_sh_slot T Pay`, `sh_pay`, `init_args_det` (na = 1, alen 0 = 2 off init's image), `init_sh_room`, `init_exec_sup_of_sh_slot : psok -> 8*(2+(8+(16+(ush_Dbody+n0)))) <= 0xFE0 -> udep -∗ init_sh_slot T (sh_pay Rsh n0) -∗ init_exec_sup` -- ALL GREEN except the one red line below. `UInitKernel.init_slot_of_kexec : kexec_image_ok init_elf … -> room -> length sts = NOFILE -> uvis_cwd W' = ROOTINO -> psok -> udep -∗ init_exec_sup -∗ uslot W'`.
- THE ONE RED LINE: `UkInitMain.v:~1346` -- `wp_kinit_main_from_1e` enters the loop after the two dups with `ustd_any γfd` where the loop wants `UkInit.ustd_open γfd := ∃ l, ⌜fd_lowest_closed l = None⌝ ∗ ustd γfd l`. It is UNPROVABLE, not merely undone: init's dups go through the untracked leaf (`UkInit.wp_kinit_dup` → `UkRunSys.wp_uk_ecall_dup_untracked`, post binds a bare `l'`), and xv6's init never tests its second `open` (repair arm) nor its two `dup`s, so on real paths sh starts with a closed standard stream. sh's entry premise `fd_lowest_closed (take NSTD sts) = None` (UShKernel.v:~325, ~391; `UkSh.ush_std` UkSh.v:4117) was therefore a GAP premise. RULING: the premise goes; sh is verified at any ledger. `ustd_open` is then deleted and `init_exec_sup` takes `ustd_any`.

=== OPERATING RULES (hard) ===
- BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 under `run_in_background` with the log's `COMPILED=`/`EXIT=` lines as the sentinel; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`; ONE build at a time; never a remote top-level `make`; never local `coqc`. Never `simpl`/`cbn` a goal mentioning a dumped byte literal. Build with -k (the script does); during (A) the ONLY acceptable red is UkInitMain.v's line above and UInitKernel.v skipped behind it.
- Gate (end of B): zero `Error`, `EXIT=0`, COMPILED>0; `make audit-only` on the VM (tree root `/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline"; `tools/lemma_diff.py` clean (zero Admitted; no new Axiom/Hypothesis/Parameter; `UInitSh.v` is untracked so check it by grep).
- GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`. DO NOT COMMIT OR STAGE. No other checkouts, no worktrees. Temp files in this scratchpad. Do not edit claude-notes/.
- DESIGN FRICTION = STOP AND REPORT. Never weaken a landed form (dropping a GAP premise is a STRENGTHENING). Comments state the current shape as fact (no dates, no "used to"/"now").

=== (A) STD-LEDGER: sh at any standard-stream ledger ===
The facts (UkSh.v, verified 2026-09-09): the open-streams fact is used at exactly ONE program point -- sh's console preamble `wp_ksh_console` (UkSh.v:5116, the loop 0x900..0x910 under iLöb: `while((fd = open("console", O_RDWR)) >= 0){ if(fd >= 3){ close(fd); break; } }`), through `wp_ksh_open` (:602, premise :604) → `wp_ksh_ostub` (:450, premise :452, `ualloc_hi … Hnone` at :506) and the destruct at :5172-5179. `UkShRun`/`UkShFork`/`UkShCd` never name it (REDIR is REFUTED in the verified command set, UkShRun.v:2872). `UserFd.ualloc γf l fd st := ustd γf (ustd_after l st) ∗ ualloc_at γf l fd st` with `ualloc_at := match fd_lowest_closed l with Some k => ⌜fd = k⌝ | None => ⌜NSTD <= fd⌝ ∗ ufd γf fd st end` (UserFd.v:472-481) -- the general post ALREADY says where an open lands.
A1. `UkSh.ush_std l := ustd γfd l` (the pure conjunct goes; header :4100-4116 rewritten: what the ledger buys is the right to say where an open lands and the right to close a standard stream; sh assumes nothing about which are open -- its console preamble is xv6's own repair of closed ones). `ush_pstate` unchanged in shape.
A2. `wp_ksh_ostub`/`wp_ksh_open` at ANY `l`: drop the `fd_lowest_closed l = None` premise; the post becomes the general one -- `(∃ fd rd wr t, ⌜ret = fd ∧ fd < NOFILE⌝ ∗ ualloc γfd l fd (FdOpen rd wr t)) ∨ (⌜ret = -1⌝ ∗ ustd γfd l)` with the ledger NOT returned separately (it is inside `ualloc`). Check `wp_uk_ecall_open`'s exact post (UkRunSys.v:~638) and mirror it.
A3. `wp_ksh_console` at any `l`: the Löb induction generalises over the ledger (`iLöb as "IH" forall (h m l)`); after the open: the failure arm leaves the loop at 0x914 with `ush_pstate l`; the success arm has `ret = fd`, so `bltz a0` is NOT taken and `bge s1,a0` (s1 = O_RDWR = 2) is decided by `fd <= 2` -- decide it (case on `fd_lowest_closed l`: `Some k` gives `fd = k < NSTD = 3`, the back edge with `ush_pstate (ustd_after l st)`; `None` gives `3 <= fd` with the handle `ufd γfd fd st`, the fall-through `close(fd)` at 0x910 spends the handle exactly as today, then 0x914 with `ush_pstate l` since `ustd_after l st = l`). If the landed proof takes the two branches on ABSTRACT booleans (header :5111-5113) and you cannot decide them from `ret = mword_of_int (Z.of_nat fd)` with `fd < NOFILE` cheaply, the abstract route still closes: the back edge always carries `ush_pstate (ustd_after l st)` (drop the handle if any), and the fall-through's `close(fd)` needs a handle -- in the `Some k` case there is none, so THAT combination must be refuted by the branch condition; state the small lemma on `bge`'s condition at `mword_of_int` of a small natural. Everything downstream of 0x914 threads `ush_pstate l` at the ledger the loop left -- no other lemma changes statement (confirm by grep after the edit).
A4. `UShKernel.sh_uexec_slot` (:~318-325) and `sh_slot_of_kexec` (:~391) drop `fd_lowest_closed (take NSTD sts) = None`; `fd_lowest_closed_take_none` (:209) is deleted if unused; the header :44-49 is rewritten (sh's entry says nothing of its standard streams).
A5. SANITY: `grep -rn "fd_lowest_closed" iris/UkSh*.v iris/UShKernel.v` names nothing; `grep -n ualloc_hi iris/UkSh.v` names nothing (or only a still-needed use in the `None` case).
STOP AND REPORT: the new `wp_ksh_console` proof shape (how the branch is decided), the red files (must be exactly D-4's line), and anything this brief got wrong (file:line).

=== (B) D FINISH -- after go-ahead ===
B1. `UkInit.ustd_open` DELETED; `init_exec_sup` takes `ustd_any (ukn_fd N')`; `init_exec_sup_of_uxsup` adapts; `UInitSh.init_exec_sup_of_sh_slot` needs no fd fact beyond `length sts = NOFILE` (from `UserFd.ufd_auth_len` at the loan) -- the `sh_slot_of_kexec` premise is gone by A4.
B2. UkInitMain: `ustd_open` → `ustd_any` in `wp_kinit_fork`, `wp_kinit_main_loop`, `wp_kinit_main_child`; the fork's child arm KEEPS handing the child's ledger (`ustd_any (ukn_fd N')`) -- the previous agent's stop-dropping change stays; `wp_kinit_main_from_1e`'s red line closes.
B3. D-e sanity: `grep -n uxsup iris/UInitKernel.v iris/UkInitMain.v iris/UkInit.v` names only `init_exec_sup_of_uxsup`'s statement/proof/note; `grep -rn "exec_au_pre_triv" iris/*.v` names SpecKexec and ProofForkret only.
B4. Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, new files, and TWO as-landed notes (≤ 12 lines each, present tense): STD-LEDGER and PINNED-EXEC D (the previous agent's D note is in the worklist's "D-4 RULING" section as a draft -- correct it to what landed). Do not commit or stage. If context runs short, STOP after a green -k build and report exactly what remains, file:line.
