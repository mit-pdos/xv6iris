You are a MAINTENANCE-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-disc (a sibling clone; never touch or build /shared/xv6iris-2 or /shared/xv6iris-2-sup). Your lane is REBASE-SUPPLY-SPLIT: the green branch `lane/supply-split` (= `d2708e5b7`, based on `a992d2818`) must be rebased onto the current `main` (which has since landed TEXT-LW `290dbdf3a`, CONS-SWALLOW `150df4ddf`, DISC-RATE, APP-IFACE `0c0624be1`), producing `lane/supply-split-r1`, green and gated. A trial merge conflicted in exactly three files -- `iris/UkShDiag.v`, `iris/UkShFork.v`, `iris/UkShMain.v` -- where TEXT-LW DELETED the `(Hclw : UkShDiag.ushd_clw_text_ty)` binders / `Hypothesis` (the four-byte text load is now the proved leaf `UkRunMem.wp_uk_clw_text`) and SUPPLY-SPLIT, in the same lemma headers and sections, RENAMED the blanket `Hypothesis Hpsok : forall k, k <> USYS_exec -> psok k` to `Hpsok_free : forall k, free_num k -> psok k` and threaded named deposits (`sh_deps`, `init_deps`, `udepw_law n`). BOTH edits must survive: no `Hclw` binder anywhere, no `Hpsok` anywhere. Read /shared/xv6iris-2/claude-notes/projects/app-echo.md "TEXT-LW LANDED", "SUPPLY-SPLIT PHASE 2 GREEN ON ITS BRANCH" and durable-notes.md ("Build", "Shared-checkout discipline", the `iIntros "#H"` bundle note) first.

RULES: `cd /shared/xv6iris-2-disc`; `git status --porcelain` must be empty at start; the branches `main` (current) and `lane/supply-split` already exist locally. Do `git checkout -b lane/supply-split-r1 lane/supply-split && git rebase main`, resolve the conflicts by hand keeping both sides' intent (prefer re-deriving a lemma header from its two parents over guessing; if a proof body breaks because a binder is gone, adapt the application), continue the rebase to completion (two commits, phase 1 and phase 2, or squash into one -- your call; say which). BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-disc rebase10` (then `rebase11`, …) from your checkout; ONE build at a time; never local `coqc`; never a remote top-level `make`; expect the SUPPLY-SPLIT cone (~120 files) plus whatever the rebase touches. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-disc` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref main` justified (expected: the SUPPLY-SPLIT items -- 29 `Hpsok` GONE / 24 `Hpsok_free` NEWAXIOM, `xv6_free_dec` moved, `wp_kshr_qstub` deleted -- and nothing else); nothing Admitted; no new Axiom/Parameter; `grep -rn "Hclw\|ushd_clw_text_ty\|Hpsok\b" iris/*.v` shows only ProofMain.v's unrelated `Hclw` name. Never push; never touch `main`/`lane/supply-split`/`lane/app-iface`; never stash/reset/add -A/commit -a/amend outside the rebase's own mechanics; do not edit claude-notes/. Report: the conflict resolutions (per file, what each side wanted and what you kept), the final commit hash of `lane/supply-split-r1`, `git diff --stat main..lane/supply-split-r1 | tail -1`, log name, the audit verbatim, lemma_diff justified.
