You are a MAINTENANCE-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-tlw (a sibling clone; absolute paths; never touch /shared/xv6iris-2 or any other sibling except read-only fetches). Your lane is REBASE-SH-ECHO: rebase E4's green branch `lane/sh-echo` (= `c2879f176`, eight commits `5b4c88ef8..c2879f176`, on the OLD base `lane/supply-split-r1` = `69543d942`, files UShEcho/UkShEcho/UkShRun/UkShDiag/UkShFork/_CoqProject) onto the current `main` (fetch it: `git fetch mainwt main` or `git fetch origin main`; main has SUPPLY-SPLIT r2 = `f4b39cdaf` (the r1 content re-based: `uprogSG_free` is a Definition, the LAZY-FLAG rows `⌜uvis_cwd W' = cw⌝ -∗ ⌜uvis_lazy W' = false⌝ -∗` on `SpecKexec.exec_slot_pre`'s wands), SH-OPEN = `6f959f529` (UkShRun/UkShDiag changed: sh's pinned console preamble, `ush_fd0p`, `UConsOpen.v`, `UShConsK.v`; entry premise order free_num-psok → `uvis_cwd W = ROOTINO` → `uvis_lazy W = false` → named deposits), TX-TAG, APP-IFACE, LAZY-FLAG). Read first: claude-notes/projects/app-echo.md "E4 SH-ECHO PHASE 3 DONE ON ITS BRANCH", "SH-OPEN ... LANDED", "SUPPLY-SPLIT ... LANDED", "LAZY-FLAG LANDED"; durable-notes.md ("Build", "The dev loop", the bundle-intro hang note -- E4's walks intro `UkSh.sh_deps` LINEARLY for that reason; keep it so).

DO: `git rebase --onto main 69543d942 lane/sh-echo` (new branch name: first `git branch lane/sh-echo-r2 lane/sh-echo` and rebase THAT, leaving `lane/sh-echo` as history). Expected conflicts: UkShRun.v / UkShDiag.v / UkShFork.v (SH-OPEN and SUPPLY-SPLIT r2 vs E4's cwd-INDEXED fork arms `wp_kshr_fork`/`wp_kshr_fork1`/`wp_kshr_fork1_final`/`wp_kshf_fork` + their `_any` corollaries) -- both sides' intent must survive: SH-OPEN's preamble and premise order, r2's names, E4's index. Resolve by hand; NEVER a tree-wide textual substitution; never collapse literal token sequences. Where E4's phase-1/2 statements referenced r1-only names, use r2's. BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-tlw shechoR10` (then shechoR11, …) from /shared/xv6iris-2-tlw (the remote tree is `/mnt/rocq/trees/_shared_xv6iris-2-tlw`; its .vo cache is E4's -- the sync brings main's sources and make rebuilds the cone; a bogus "Cannot find library" cascade from a torn cache means re-run once); ONE build at a time; never local coqc; never a remote top-level make. If a hang appears in a walk (build wedged > 12 min with `UkShEcho` still compiling), the locator is: wrap the walk's top-level tactics in `timeout 120 (...)` to find the line, then intro the offending bundle linearly; remove the instrument after.

GATE: zero Error, EXIT=0; VM `make audit-only` from /mnt/rocq/trees/_shared_xv6iris-2-tlw = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref main` justified (expected: E4's additions in the five files; `Hpsok_free` NEWAXIOM is a section hypothesis, known); nothing Admitted (comments mentioning it are fine); no new Axiom/Parameter; `grep -n "PHASE 3" iris/UkShEcho.v iris/UShEcho.v` empty; `make -f CoqMakefile -n` on the VM shows nothing left; the kernel-side diff vs main is EMPTY outside UShEcho/UkShEcho/UkShRun/UkShDiag/UkShFork/_CoqProject. Never push; never touch `main`; never stash/reset --hard/add -A/commit -a/amend outside the rebase's own mechanics; do not edit claude-notes/. Leave the checkout ON `lane/sh-echo-r2` with a clean tree.

REPORT: the conflict resolutions (file, what each side wanted, what you kept), the final commit hash, `git diff --stat main..lane/sh-echo-r2 | tail -1`, the log name, the audit verbatim, lemma_diff justified, and the list of E4 named premises still open (from the E4 note) confirming none was discharged or added by the rebase.
