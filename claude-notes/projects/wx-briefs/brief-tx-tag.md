You are a KERNEL-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-tlw (a sibling clone at main 2c0d32048; never touch or build /shared/xv6iris-2). Your lane is TX-TAG: every byte the UART accepts for transmission is tagged, in the ghost state, with WHO wrote it -- the kernel's printk, the console's echo of a typed byte (carrying that byte's receive history), or a user write from a given pid -- chosen by the caller that holds the transmit lock for that byte. Design of record: claude-notes/projects/app-echo.md "E5 -- THE OUTPUT SIDE: DESIGN PROPOSAL" (O1, O2, O3 AS RULED, O4's TX-TAG paragraph, O5 as ruled) and `review-echo-plan-2026-09-12.md` findings 2, 4, 5. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; xv6-riscv sources READ-ONLY from /shared/xv6iris-2/xv6-riscv/kernel/ (uart.c: uartputc, uartputc_sync -- no software tx ring in this fork, both spin on LSR_TX_IDLE under tx_lock and store THR; console.c: consputc, consolewrite, consoleintr's echo; printk.c: pr.lock per message, consputc per byte; main.c: the boot's printks); iris/WpUart.v (§ "the accepted trace": `uart_sent_auth` :316, `uart_sent` :318 (`own γ.(un_acc) (◯ML l)`), `uart_ghosts` :567, the record `uart_names` (find it: `un_acc`, `un_out`, `un_tx`, `un_dlab`, and the RX-side `un_*` for the column/tags -- the rx side ALREADY tags each received byte with its history via `riscv_rx_tag`/`cons_tags`: that is your mould), the THR-write device step where `uart_acc` grows (":291 grows ONLY when a CPU pushes to THR") and its leaf; the rx wand `Hrx` machinery in `uart_col_ok` :746 for how a per-byte annotation is kept in lockstep with a mono_list); iris/UartTxInv.v (`tx_res`, `is_txlock` :130, `uart_sent_sub` :160, `uart_sent_nil`; the header on why sublist); iris/UartSentLoc.v (`uart_sent_from` :71, the located receipt and its chain lemma); iris/SpecUartputc.v + iris/SpecUartputcSync.v (the two writers' leaves: find where each extends `uart_sent`/`uart_sent_sub`); iris/ProofUartputc*.v; iris/SpecConsputc.v (:68-95: threads `uart_sent_sub γd bs` in, `bs ++ cs` out); iris/SpecPrintk.v (:40-50, :150-165: `uart_sent_sub` threaded per message; `pr_res`; :181-230 the "one bundle" note); iris/SpecConsoleintr.v (:14-33 the echo through consputc, :118 `uart_sent_sub γu []` discarded -- "consoleintr's echo is of no interest to any caller": it is now; :170 THE BYTE'S TAG a0 carries -- the rx history `h` is in scope where the echo happens); iris/SpecConsolewrite.v (:210-260: the seed `uart_sent γu tr0`, the receipt at the returned count via `cons_sent_cnt`/`uart_sent_from`; `pid` is a parameter); iris/SpecUartwrite.v + iris/SpecUartwriteLoc.v; iris/SpecMain.v (:684 the boot seeds `uart_tx_own γd l0 ∗ uart_sent γd l0 ∗ uart_out_lb γd l0`; the boot's printk calls) + iris/SpecMainSecondary.v; iris/ProofMain.v :450-600; iris/App.v (:35-47 `Htx`: the theorem's transmit wand receives `uart_ghosts γ u'` at each `uart_tx_pop` -- the tag must be READABLE off `uart_ghosts` there, which is why it lives in the UART ghost and not in a side invariant); iris/SystemUartAccepted.v (:32-43 the era-identification note -- NOT your lane, but know it).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-tlw <logname>` FROM /shared/xv6iris-2-tlw (names `txtag10`, `txtag11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'` from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. `WpUart` sits under the whole tree: batch statements before each build; expect ~1000-file rebuilds; run them detached and wait. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-tlw` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter. SCOPE (to avoid colliding with lanes in flight): the UART/console/printk layer and the boot -- `WpUart`, `UartTxInv`, `UartSentLoc`, `SpecUartputc*`/`ProofUartputc*`, `SpecUartwrite*`/`ProofUartwrite*`, `SpecConsputc`/`ProofConsputc`, `SpecPrintk*`/`ProofPrintk*`, `SpecConsoleintr`/`ProofConsoleintr`, `SpecConsolewrite`/`ProofConsolewrite`, `SpecMain*`/`ProofMain*`, `SystemUartAccepted`, `App.v` ONLY if `Htx`'s type must see the tag (report before touching it: it is the trusted statement). DO NOT touch `SpecFileread`/`ProofFileread`/`SpecSysWrite`/`ProofSyscall`/`UexecExecInst`/the U tier -- the relay of the tagged receipt through filewrite and row 16 is the NEXT lane (TX-RECEIPT). Everything else only to keep it compiling (report each). GIT: branch `lane/tx-tag` in your checkout; commit there when green; never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact. NO placeholder arms.

=== THE DESIGN ===
X1 THE TAG.  `Inductive txsrc := TxK | TxE (h : list mobs) | TxW (pid : mword 32)` (K = kernel printk; E = the console's echo of the typed byte whose receive history is `h`; W = a user write by `pid`). `uart_names` gains `un_tag`; `WpUart` keeps `uart_tags γ : list txsrc` in lockstep with `uart_acc` (a second mono_list, `uart_tags_auth` inside `uart_ghosts`, invariant `length tags = length (uart_acc u)`), and the persistent lower bound `uart_sent_tagged γ (l : list (txsrc * bv 8))` (or the pair of bounds with a length tie -- choose the shape that makes `Htx`'s reader simplest: at a pop of byte `b` at wire position `i`, the reader must be able to read `tags !! i`). The THR-write leaf takes `src : txsrc` from the caller and pushes `(src, b)`.
X2 THE WRITERS.  `uartputc_sync`'s and `uartputc`'s specs take `src`; `consputc` takes `src` and threads the tagged sublist (`uart_sent_sub` becomes tag-aware: `uart_sent_sub_at γ src bs` = "these bytes, each tagged `src`, are a sublist of the accepted tagged list"); `printk` passes `TxK` (all its bytes; `pr_res` unchanged); `consoleintr`'s echo passes `TxE h` with `h` the received byte's history (the rx tag in scope at :170) and KEEPS the resulting claim in its post (`uart_sent_sub_at γu (TxE h) [c]` for the echoed byte, or the BACKSPACE triple for ^H/^U -- state what the arms produce; a full ring echoes nothing); `consolewrite`/`uartwrite` pass `TxW pid` and their located receipt `uart_sent_from` becomes tag-aware: `uart_sent_from_at γu tr0 (TxW pid) bs` = "since the seed, the bytes tagged `TxW pid` are EXACTLY `bs`, in order" -- exact, not sublist, because a pid's writes are sequential (state the lemma that makes it exact: no other writer uses this tag while this call runs; if that needs a per-pid token or the process's own `proc_priv` and the tier does not have one, say what it needs and land the SUBLIST-on-own-tag form instead, flagged).
X3 THE BOOT.  `SpecMain`'s seeds gain the tag list at `[]` alongside `l0` (or the tagged bound at the empty list); hart 0's three printks and the seven "hart N starting" (`SpecMainSecondary`) are `TxK`; state once, as a lemma over the boot's posts, WHAT THE K-TAGGED BYTES ARE in this run: `k_bytes_boot : the K-tagged subsequence of any accepted list is a prefix of a shuffle-free concatenation of the eight boot messages in an order compatible with (banner, then hart lines in any order), each at most once` -- or the weaker, cheaper form the boot proof can pay (each printk contributes its whole message as a contiguous run of K-tagged bytes under `pr.lock`; the set of printk call sites in the run is these eight plus panics); report which form you can prove from the landed `SpecPrintk` post ("SOME byte list cs was appended" -- if `cs` is not pinned to the format string's rendering, say so: pinning it is `PrintkFmt`'s business and may be its own lane).
X4 THE READER.  `WpUart`'s `Htx`-side lemma: at `uart_tx_pop u = Some (b, u')` with `uart_ghosts γ u'` and `⌜obs_wire (open_seg h) = u_wire u⌝`, the popped byte is the `i`-th accepted byte (`i = length (u_wire u)` -- find the exact index law in `uart_col_ok`/the out ledger) and `uart_tags γ !! i = Some src` is readable: `uart_pop_tag : uart_ghosts γ u' -∗ ⌜tags at i = Some src⌝ ∗ uart_ghosts γ u'` (persistent bound). `App.Htx`'s type does NOT change in this lane -- the application will use `uart_pop_tag` inside its own `Htx` proof.
X5 SANITY: `grep -rn "uart_sent_sub γu \[\]" iris/SpecConsoleintr.v` EMPTY (the echo's claim is kept); `grep -rn "TxK\|TxE\|TxW" iris/Spec*.v` shows printk/consoleintr/consolewrite passing their tags; the audit unchanged; nothing Admitted; `git diff --stat -- iris/SpecFileread.v iris/ProofFileread.v iris/UexecExecInst.v iris/Uk*.v` EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
X1 landed (the ghost, the lockstep invariant, the bound, the THR leaf's `src`), X2-X4 as STATEMENTS with every downstream file compiling (the writers' specs carry the tag; proofs that need only threading done; the exactness lemma of X2 and X3's boot lemma STATED), NO Admitted at report time (leave a hard statement out and report it verbatim rather than admit). Report: (1) the statements verbatim, file:line; (2) where in the device model the THR push extends the accepted list and how the tag rides it; (3) what `consolewrite`'s exact-on-own-tag receipt needs and whether you landed exact or sublist; (4) what X3 can be proved from `SpecPrintk`'s post; (5) the red files and why; (6) anything this brief got wrong, with file:line evidence.
=== PHASE 2 -- after go-ahead ===
The proofs, X5, the gate, commit on `lane/tx-tag`. Report log name, the commit hash, `git diff --stat main..lane/tx-tag | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), anything still owed with file:line.
