You are a proof-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-sup (a sibling clone at current main; never touch or build /shared/xv6iris-2, /shared/xv6iris-2-tlw, /shared/xv6iris-2-disc; do not touch the branch lane/printk-ledger). Your lane is SH-LINE 2b: sh's input line. sh reads fd 0 one byte per read through the receipt-keeping read leaf; its line buffer carries "these are the consecutive next input bytes at my position, each tagged" or the taint; the ^D swallow is refuted into the taint from the byte's tag, the copyout-fault swallow is refuted from byte ownership under the lazy flag; `ushf_lexable` is DELETED and the line is proved to be exactly "echo hello world\n" under the discipline. Design of record: claude-notes/projects/app-echo.md "SH-LINE RULING", "SH-LINE PHASE 1 LANDED + TWO MORE KERNEL SEAMS RULED", "SH-LINE PHASE 2a LANDED" (and its "2b still owes" list), "CONS-SWALLOW LANDED" (`cons_swallow`, `cons_swallow_range`, `console_recv P addr`), "LAZY-FLAG LANDED" (`UkRunSys.uk_read_nofault`, `wp_uk_ecall_read_recv` handing back `⌜uvis_lazy W = false⌝`), "SUPPLY-SPLIT AND SH-OPEN LANDED" (`sh_deps := udepw_law 5 ∗ udepw_law 16`; `ush_fd0p` as the preamble's loop invariant; the entry premises' order), "DISC-RATE LANDED" (`disc`, `disc_seg`, `disc_old`/`disc_proj`, `ush_line_is`'s consumers), "GENERIC-PAY LANDED" (sh's payload `Qsh`), and the ORIGINAL brief wx-briefs/brief-sh-line-2.md sections S4 and S5 (superseded where the notes say: the swallowed byte is now NAMED by the kernel, so the "taint-style escape" for r = 0 is the ^D refutation + the fault refutation, not a stated escape). The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the bundle-intro note, the never-collapse-tokens note); remote-build-gcp.md; iris/UConsLine.v (the phase-1 statements: `ush_read_recv_leaf` :~244 with its `⌜d <= dc <= d+1⌝` arm (to be RESTATED at `cons_swallow`'s two arms), `ush_gets_line`, `ush_gets_line_0`, `ush_std3` (to COLLAPSE into `UkSh.ush_fd0p`), `ush_tag_law`, `ush_rest_line`, `ush_exec_pay`, `ush_line_is`/`ush_line_lexable`/`ush_disc_line`/`ush_echo_tokens`/`ush_line_full`, `disc_seg`'s uses); iris/UkRunSys.v (`wp_uk_ecall_read_recv` :~2672 at the TRAPPING key; `udepwf_std`; `uk_read_nofault`; `wp_uk_ecall_read_win` (what sh uses today: discards the post)); iris/ConsoleInv.v (`cons_swallow` :~1260, `cons_swallow_range`, `cons_window`, `cons_chain`, `cons_stored_lb`, `cons_acc`, `cons_acc_reader`, `cons_acc_cred`, `cons_out`); iris/SpecFileread.v (`console_receipt P Rd r M' addr` :~967, `console_recv P addr` :~1113 -- the program-facing spelling with the fault arm at `~ uva_wmapped P (addr + d)`; `fileread_extra_core P …`); iris/UexecExecInst.v (row 5: `∃ P, ⌜perm_of (ud_um P) (uvis_sz W) = uvis_perm W⌝ ∗ ⌜proc_pt_wf P⌝ ∗ ⌜uvis_lazy W = false -> lazy_free (ud_um P) (uvis_sz W)⌝ ∗ fileread_extra_core P …`); iris/UserConsole.v (`upos`, `upos_a`, `upos_agree`, `upos_update`, `ucons_pay`, `ucons_reader`); iris/UkSh.v (`wp_ksh_read`, `wp_ksh_gets_loop` (one byte per read into the frame slot; `s5 = 1`), `wp_ksh_gets`, `wp_ksh_getcmd`, `ush_pstate` (with `ush_pos` last), `ush_fd0p`/`ush_fd0`, `ush_std`, `ush_rest`, `sh_deps`, `wp_ksh_start`, `ush_read_leaf` Hypothesis :~786 and `ush_read_leaf_holds`); iris/UShKernel.v (`ush_read_leaf_of_win` -- to be REPLACED by the discharge through `wp_uk_ecall_read_recv`; `sh_uexec_slot`'s premises incl. `ush_tag_law`'s and the console state); iris/UkShFork.v (`ushf_lexable` :~816 -- DELETE; `ushf_rest_of_body` :~835 its only consumer; `wp_kshm_body`'s line hypotheses); iris/UInitSh.v (`init_sh_slot`, `sh_pay` -- where `ush_rest`'s discharge is owed; `init_exec_sup_of_sh_slot`); iris/AppEcho.v + iris/EchoDisc.v (`echo_tag γ h = ⌜disc h⌝ ∨ echo_taint γ`; `echo_taint_of_sup`; `echo_sup_of_taint`; `disc`, `disc_seg`, `disc_proj`, `echo_line`, `cons_xlate`); iris/FsConsPin.v; the C: /shared/xv6iris-2/xv6-riscv/user/sh.c (`getcmd`, `gets` in ulib.c: `read(0, &c, 1)` per byte, stops at '\n' or '\r' or cc < 1), kernel/console.c (`consoleread`: ^D with nothing delivered is swallowed).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-sup <logname>` (names `shline40`, `shline41`, …; run_in_background; one at a time; the VM is shared -- builds queue; if a bottom-of-tree dirty file yields bogus "Cannot find library" failures, re-run once); your remote tree was last built at another branch's content, so your FIRST build recompiles a large cone: run it detached BEFORE editing. Never local coqc; never a remote top-level make. Gate: zero Error, EXIT=0; VM `make audit-only` from /mnt/rocq/trees/_shared_xv6iris-2-sup = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref main` justified; nothing Admitted; no new Axiom/Parameter; kernel diff (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`, `UexecExecInst`, `UexecSG`) EMPTY; the UART layer (`WpUart`, `WpSconfUartAccess`, `Uart*`) UNTOUCHED (another agent owns it). GIT: branch `lane/sh-line-2b`; commit there when green; never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NO placeholder arms; a verified program never drops to the generic slot except under the taint.

=== THE DESIGN ===
L1 THE LEAF, RESTATED AND DISCHARGED.  `UConsLine.ush_read_recv_leaf`'s window disjunct adopts `cons_swallow`'s two arms: `dc = d` (the window `cons_window sl n d g hs`, `cons_chain sl`, `ucons_stored_lb`, the tags, `upos γp (n + d)`) ∨ (`dc = d + 1` with the swallowed byte's `(h, b)` at `n + d`, its tag, and `⌜d = 0 ∧ cons_xlate b = 4⌝` -- the fault arm is ELIMINATED inside the leaf by `uk_read_nofault` from row 5's `∃ P` tie and the buffer's `ubytes`), plus the TAINT arm (`T ∗ ∃ n', upos γp n'`). The discharge (`ush_read_leaf_holds`'s replacement, at `UShKernel`'s altitude or a new `UShLine.v`) goes through `wp_uk_ecall_read_recv` at an `xfam` whose console arm is the wand from the payload `Qsh γ (-1)` (S4 of the original brief: `upos_agree`, `cons_acc_reader`, `cons_out`, `upos_update` both halves, rebuild `Qsh γ (-1)`; the credential disjunct via `echo_sup_of_taint`), reading `console_recv P addr` (or the receipt) at row 5's `P`. `ush_read_leaf_of_win` and `wp_uk_ecall_read_win`'s use in sh are retired.
L2 GETS.  `wp_ksh_gets_loop` carries `ush_gets_line cn γp T n0 i g` (holder branch: `upos γp (n0 + i)`, the window so far, the tags of every byte so far; taint branch); each round: `r = 1` → `d = 1`, the arm `dc = d` (since `dc = d+1` needs `d = 0`), the window grows by one byte with its tag; `r = 0` → `d = 0`, the swallowed byte is `^D` (`cons_xlate b = 4`) with tag `riscv_rx_tag h` → through `ush_tag_law` the tag yields `⌜disc h⌝ ∨ T`, and `disc h` is FALSE of a history ending in 0x04 (`disc_seg`/`star_prefix` over `echo_line`'s bytes: 0x04 ∉ echo_line -- state and prove `disc_no_ctrl_d`) → the taint arm; `r < 0` is not observable at the U tier (a killed process is never resumed). The '\n' test is decided from the window's last byte. `wp_ksh_gets` / `wp_ksh_getcmd` restated on `ush_gets_line`; `getcmd`'s `buf[0] == 0` test decided (under the holder branch the first byte is 'e').
L3 LEXABILITY.  `ushf_lexable` DELETED; `ushf_rest_of_body` takes `ush_rest_line` (the line IS `echo_line` ∨ T): under the holder branch at '\n', the tags of the line's bytes and `disc` of the LAST byte's history give `ins h` a prefix of `(echo_line)*` (`disc_proj` to the landed `disc_seg`), the window's bytes are its last line (`cons_window` + `cons_chain`), so the buffer is exactly `echo_line` (`ush_line_is`), whence `ush_echo_tokens` (proved by E4 already as `UkShEcho.ush_echo_tokens_holds`), no symbols, three tokens; under T the continuation is generic (`ush_gen_run`, only under T).
L4 THE ENTRY.  `ush_std3` collapses into `UkSh.ush_fd0p`; sh's entry (`UShKernel.sh_uexec_slot`) takes `ush_tag_law` (the persistent premise `□ (∀ h, riscv_rx_tag h -∗ ⌜disc h⌝ ∨ T)`, E2 discharges it from `riscv_rx_tag = app_tag`) and the payload `Qsh γ` (landed); `ush_rest` is DISCHARGED from `ushf_rest_of_body` at sh's own `N` and connected to `wp_ksh_start` (review finding 10: today no call site); `UInitSh.sh_pay`'s second conjunct `∀ γp N, ush_rest N γp (Rsh …)` becomes provable -- prove it or state exactly what remains (E4's `Rsh` static-state conjunct is E4's).
L5 SANITY: `grep -n "ushf_lexable" iris/*.v` EMPTY; `grep -n "ush_read_leaf_of_win\|wp_uk_ecall_read_win" iris/UkSh.v iris/UShKernel.v` EMPTY; `grep -n "ush_std3" iris/*.v` EMPTY (collapsed); `grep -n "d <= dc <= d + 1" iris/UConsLine.v` EMPTY; `grep -n "ush_gets_line" iris/UkSh.v` shows the loop; the audit unchanged; kernel diff EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
L1's leaf restated and its discharge STATED (proved if short), L2's loop invariant restated with `disc_no_ctrl_d` proved, L3's `ushf_rest_of_body` restated (ushf_lexable gone from the statement), L4's entry; compiling, NO Admitted. Report: (1) statements verbatim, file:line; (2) the r = 0 arm's refutation chain, quoted; (3) what `ush_rest`'s discharge still needs; (4) the red files; (5) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs, L5, the gate, commit on `lane/sh-line-2b`. Report log name, commit hash, `git diff --stat main..lane/sh-line-2b | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), the named premises left and their owners.
