You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-GEN: two exclusive ghosts that let the wait-lock invariant (next lane, WX-INV) and kwait's post (WX-WAIT) speak about a slot's CURRENT generation and about pid uniqueness without opening pid_lock. Design of record: claude-notes/projects/app-echo.md, section "WX-GEN / WX-INV / WX-WAIT — DESIGN" (read it first, whole), then "WAIT-EXIT — DESIGN OF RECORD" and the LANDED notes above it (WX-KEY, WX-FORK, WX-RES + WX-ROW, WX-EXIT -- take file:line anchors from the newest). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; iris/ChildTok.v (whole); iris/PidLock.v (whole; `nextpid_res_at`); iris/SchedCtx.v (`pid_lock_share_at`, `proc_pub`); iris/ProcDefs.v (`pprivate`, `proc_dormant`/`_noctx` -- the row conjunct is the mold for the new ones); iris/ProcInv.v (`proc_priv_core`, `proc_priv_split_cwd` (four-way after WX-EXIT), `proc_priv_intro`, `proc_dormant_seal`, `proc_dormant_unused`, `proc_priv_to_dormant_zombie`); iris/SpecAllocproc.v (`allocproc_post`: `gen_own … (DfracOwn 1)` beside `proc_priv_nocwd`); iris/ProofAllocproc.v (`wp_ap_pidsec` :~648 -- the inlined allocpid under pid_lock, the `p->pid = pid` store :~1036 and the payload re-seal :~1071; the mint `gen_alloc` :~2810 OUTSIDE the section); iris/SpecFreeproc.v + iris/ProofFreeproc.v (`p->pid = 0` under pid_lock :~523-540); iris/ProofKforkMain.v (~735-760: `gen_set`/`gen_split` and the child's block assembly); iris/ProofUserinit.v (init's block assembly, the `gen_split` at `fun _ => True`); iris/WaitInv.v (`ch_frag`, `children_inv` -- STATED, not carried; the canonical-name class on `Xv6Cameras.wchG`); iris/Xv6Cameras.v (`wchG`, the boot mint in `WaitInv.children_res_alloc`/`BootShared`).

=== OPERATING RULES (hard) ===
- BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 under `run_in_background` (summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); log names `wxgen1`, `wxgen2`, …; ONE build at a time (never a chained waiter plus a manual run); never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal.  `ProcDefs`/`ProcInv`/`SchedCtx`/`PidLock` are near the bottom of the tree (~316-554 dependents): batch every statement before the first build.
- Gate: zero `Error`, `EXIT=0`, COMPILED>0; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` clean (zero Admitted; no new Axiom/Hypothesis/Parameter; sealed Parameters retyped in place reported by group).
- GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`. DO NOT COMMIT OR STAGE. No other checkouts, no worktrees. Temp files in your scratchpad. Do not edit claude-notes/.
- DESIGN FRICTION = STOP AND REPORT. Never weaken a landed form. Comments state the current shape as fact (no dates, no lane markers, no "now"/"used to").

=== THE DESIGN ===
G1. `slot_gen k dq γ` -- "slot k's current generation is γ".  New file `iris/SlotGen.v` (or inside WaitInv if the import order forces it; PREFER its own file below ProcDefs): ONE canonical name (a third name on `Xv6Cameras.wchG`, minted in the same boot fupd as the map; or its own tiny name-carrying class -- pick what keeps binders unchanged) at `gmapUR nat (dfrac_agreeR (leibnizO gname))`, `slot_gen k dq γ := own wsg_name {[k := to_dfrac_agree dq γ]}`.  Lemmas: `slot_gen_agree` (any two fractions agree), `slot_gen_halves` (`DfracOwn 1 ⊣⊢ 1/2 ∗ 1/2`), `slot_gen_update` (whole → whole at any γ'), `slot_gen_alloc` (NPROC wholes at boot, at an arbitrary/boot generation -- decide: mint at boot for all `seq 0 NPROC` and carry them to the dormant blocks on `ch_frag`'s exact route: `children_boot` → `procs_inv_alloc` pass 3 → `proc_dormant_seal`).  THE DORMANT BLOCK: `proc_dormant`/`_noctx` gain `slot_gen (slot of pa) (DfracOwn 1) (pv_gen V)` when UNUSED and `slot_gen … (DfracOwn (1/2)) (pv_gen V)` when ZOMBIE (the other half is in the invariant's entry -- WX-INV; this lane the ZOMBIE half is simply the block's), on the existing `if bool_decide (st = ZOMBIE)` guard; the slot index: `proc_dormant` is stated at `pa`, so either add a `k` parameter or key `slot_gen` by ADDRESS (`gmapUR (mword 64) …` keyed at `pa`) -- PREFER keying by address, which needs no index and matches `ch_frag`'s `pa`.  `proc_dormant_unused` hands the whole out; `allocproc_post` carries `slot_gen (proc_addr j) (DfracOwn 1) (pv_gen (us_V U))` beside `gen_own … 1` (updated to the new generation inside the pid section, G3).  `proc_priv_core` gains `slot_gen pa (DfracOwn (1/2)) (pv_gen (us_V U))` beside the `∃ Q, gen_kq ∗ my_pay` pair (LAST); the four-way `proc_priv_split_cwd`/`proc_priv_intro` route it; `proc_priv_to_dormant_zombie` puts the block's half into the ZOMBIE block.  freeproc takes the WHOLE (`slot_gen pa (DfracOwn 1) (pv_gen V)`) and rebuilds the UNUSED block with it; its callers pay: allocproc's failure tails hold the whole (they never split), kwait's reap holds the ZOMBIE block's half AND -- this lane -- the OTHER half must come from somewhere: since the invariant is not carried yet, kfork's spare half has no home this lane.  RULING for this lane: kfork DEPOSITS its spare halves (`slot_gen` and `pid_reg`, plus `gen_slot γ (proc_addr j)` and `gen_pid γ pid_c`) into `wait_res` at +0xd4 in a NEW, EXISTENTIALLY-CLOSED conjunct `WaitInv.gen_halves ps := [∗ list] k ↦ v ∈ ps, if v = 0 then emp else ∃ γ pid, slot_gen (proc_addr k) (1/2) γ ∗ pid_reg pid (1/2) γ ∗ gen_slot γ (proc_addr k) ∗ gen_pid γ pid` -- i.e. the RESOURCE half of `children_inv` already, indexed by the parent cells, WITHOUT the pure ties (WX-INV adds those).  `wait_res_at ξ := ∃ ps, parents_own_at ξ ps ∗ gen_halves ps ∗ children_res ∗ orphans_res` -- `ps` is now bound OUTSIDE `parents_res_at` (which dies or becomes the pair); every `wait_res` destruct adapts (kwait, kexit, kfork B5, reparent's callers, ProofMain's `wait_res_alloc`, `SpecKexit`).  kwait's reap takes entry k out (`ps !! k = Some pj`, the scan's fact) and hands both halves to freeproc; kexit's `rp_map` keeps every entry (the clause is `v = 0`-keyed, and reparent sends nonzero cells to nonzero `ip`) -- state `gen_halves_rp_map`; kfork's +0xd4 inserts entry j (kfork holds the WHOLE `slot_gen (proc_addr j)`, so `ps !! j = Some 0` follows by fraction: prove `slot_gen_whole_excl : slot_gen pa 1 γ -∗ slot_gen pa dq γ' -∗ False`).  userinit: init's entry has no home (its parent cell is 0) -- init's block takes its halves and userinit DROPS the spares (comment: init has no parent and is never reaped).
G2. `pid_reg pid dq γ := pid ↪[wpr_name]{dq} γ` -- a `ghost_map` at `mword 32 → gname` with a canonical name (fourth on `wchG`, or with `slot_gen`'s class).  AUTHORITY IN `pid_lock`'S PAYLOAD: `PidLock.nextpid_res_at ξ` binds the 64 quarter cells' values as `pids : list (mword 32)` (`length pids = NPROC`; `pid_lock_share_at` becomes value-explicit) and carries `pid_reg_auth R ∗ ⌜∀ p, p ∈ dom R ↔ (p ≠ 0 ∧ p ∈ pids)⌝` (or `⊆`, if `↔` is not provable at boot/freeproc -- decide; `↔` is what makes "fresh key" follow from the scan; boot: `R = ∅`, all cells 0).  allocproc's pid section (`wp_ap_pidsec`): after the scan (which established `pid ∉ pids`) and at the `p->pid = pid` store, `ghost_map_insert pid γ` -- so `gen_alloc` MOVES INTO the pid section (the mint needs the pid; the registration needs the name): `wp_ap_pidsec`'s post carries `gen_own γ 1 (proc_addr k) pid (fun _ => True) ∗ pid_reg pid 1 γ ∗ slot_gen (proc_addr k) 1 γ` (it takes the dormant block's whole `slot_gen` in and updates it there too), and `allocproc_post` relays `pid_reg (pid) (DfracOwn 1) (pv_gen (us_V U))`.  freeproc's pid section deletes it (`ghost_map_delete` with the whole fragment: `wp_freeproc_sconf` takes `pid_reg pid 1 (pv_gen V)`; the payload's domain fact is re-established because the cell goes to 0).  `proc_priv_core` gains `pid_reg pid (1/2) (pv_gen (us_V U))` (LAST, after `slot_gen`'s half); kfork splits the whole at the block assembly (`ProofKforkMain` ~735-760, beside `gen_split`) and keeps the spare half for +0xd4 (G1's deposit); userinit splits and drops.  Pid uniqueness is now the agreement lemma `pid_reg_agree : pid_reg p dq γ -∗ pid_reg p dq' γ' -∗ ⌜γ = γ'⌝` -- rewrite PidLock's header ("Uniqueness … is a further step nothing consumes") to state this.
G3. THE ROUTE OF THE WHOLES: boot mints NPROC `slot_gen` wholes (with the rows) and `pid_reg`'s empty authority (into `nextpid_res_at` at main's `newlock` for pid_lock -- find where `BootCarveMain`/`SpecMain` hand main the pid cells; the authority is minted in the boot fupd like the map's); dormant block → allocproc (pid section: update + insert) → `allocproc_post` → kfork/userinit (split) → block halves + kfork's deposit; ZOMBIE block keeps the block's halves; kwait's reap: entry k out of `gen_halves` + ZOMBIE block's halves → freeproc (wholes) → UNUSED block (`slot_gen` whole; no `pid_reg`).
G4. SANITY: `grep -n "slot_gen\|pid_reg" iris/ProcDefs.v iris/ProcInv.v iris/SpecAllocproc.v iris/SpecFreeproc.v iris/PidLock.v iris/WaitInv.v iris/SpecKfork.v` shows the block's halves, the wholes in the dormant block and allocproc_post, freeproc's wholes, the authority, `gen_halves`; `children_inv` still STATED not carried (WX-INV); nothing about `uvis`/the residue/the U tier changes in this lane (the halves live inside `proc_priv_core`, which the residue carries opaque) -- if a U-tier file turns red, STOP AND REPORT.

=== PHASE 1 -- then STOP AND REPORT ===
G1/G2/G3 as STATEMENTS (the two ghosts and their lemmas; the dormant block's conjuncts; `proc_priv_core`'s halves; `allocproc_post`, `wp_ap_pidsec`'s post, `wp_freeproc_sconf`'s premises; `nextpid_res_at`'s list + authority; `gen_halves` and the new `wait_res_at`); NO Admitted at report time.  Build with -k.  Report: (1) the two ghosts' definitions and agreement/exclusion lemmas, quoted; (2) the dormant block's and `proc_priv_core`'s new conjuncts, quoted; (3) `nextpid_res_at` and `wait_res_at`, quoted, and where the boot mints land (file:line); (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (allocproc's pid section incl. the moved mint, freeproc, kfork's split + deposit at +0xd4, userinit's split, kexit's `rp_map` pass-through, kwait's reap taking entry k out, ProofMain's boot route, every red file), G4.  Gate.  Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense).  Do not commit or stage.  If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
