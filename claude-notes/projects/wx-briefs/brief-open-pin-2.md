You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is OPEN-PIN PHASE 2 (a fresh agent; phase 1 and the kernel tightening are landed): the general pinned-observation lemma admits a pin that resolves to SOME inum and a walk that legitimately MISSES; BOTH of init's console opens are pinned; init's mknod is the application step that moves the console claim and shoots the flag; init's head is the three-arm disjunction CONSOLE ∨ CLOSED ∨ TAINT with the named ledger and the tracked dups; and all of it is PROVED. Design of record: claude-notes/projects/app-echo.md "OPEN-PIN PHASE 1 LANDED + FINDINGS" (items (a)-(e) are RULINGS), "SPEC-TIGHTEN LANDED" (take anchors from it: `open_trunc_piece`, `pinned_open_bundle_notrunc`, `aunarm_of_arm`), "OPEN-PIN FINDINGS -- RULED WITH THE OWNER" (FACTS 1-3), "PINNING -- THE OWNER'S RULING", "PATH-ARGS LANDED", "E1 ECHO-PRED LANDED". The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1 (the statements of P1-P3 below), then the proofs on go-ahead.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; design/user-wp-slot.md; design/user-fd.md; iris/PinnedObs.v (whole: `pin_resolves_at`, `pobs_P`/`pobs_Pmiss := T`/`pobs_recv`/`pobs_Fo`, `pobs_node`, `pobs_walk`, `pinned_obs`; the comment "the general walk is ex_start at ONE path"); iris/PinnedExec.v (its instance -- must keep compiling unchanged in statement); iris/PinnedOpen.v (`pinned_open_bundle_at`, `pinned_open_bundle`, `pinned_open_bundle_notrunc`, `pinned_open_dev`); iris/FsConsPin.v (`cons_absent`, `cons_present_at i`, `cons_dev`, `era0_cons_absent`, `cons_state_mknod`, the arm/create/unarm/trunc pin lemmas and the two corollaries SPEC-TIGHTEN added); iris/AppEcho.v (`cons_tok`/`cons_shot`/`cons_made`, `cons_state`, `echo_pred` (LINEAR), `echo_cons_law`, `cons_shoot`, `echo_xfer`); iris/UInitCons.v (`cons_pin_resolves_at`, `init_cons_recv`, `init_cons_head` (four arms today -- becomes three), `init_cons_open_bundle_rdwr`); iris/UkRunSys.v (`wp_uk_ecall_open_recv_body` -- the receipt-keeping leaf, a body at the TRAPPING key; `wp_uk_ecall_open` :~718; `wp_uk_ecall_dup` :~851 (tracked); `wp_uk_ecall_dup_untracked` :~1000); iris/UkInit.v (`wp_kinit_open` :135, `wp_kinit_mknod` :213, `wp_kinit_dup` :285); iris/UkInitMain.v (0x16 open, `blt` at 0x1a, dups at 0x20/0x26, the fork at 0x38, the repair arm @0x64 `wp_kinit_main_repair` :~1511 with the mknod at :~1615 and the second open at 0x7e whose result is DROPPED at 0x82 (`c.j 0x1e`); `wp_kinit_main_from_1e`; the head `wp_kinit_main_loop`); iris/SpecSysMknod.v (`mknod_au_at`, `mknod_post_ok` at the argument's path; the create leg's pieces `Farm Fun Fok Fex` and the two-phase `acre_commit_at_gen` -- where phase 2's fupd is and how `Fok` hands the flag out); iris/FsAbsCreateFire.v (`acre_commit_at_gen`'s phases; `aunarm_of_arm`); iris/AppInv.v (`app_top_update`, `app_step`, `app_step_acc`); iris/UserFd.v (`ustd`, `ualloc`, `fd_lowest_closed`, `fdt0`); iris/UConsLine.v (`ush_std_cons` -- what sh's entry will consume).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `openpin30`, `openpin31`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. KERNEL FILES ARE OFF LIMITS (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*Fire`, `UexecExecInst`'s rows, the `uexecSG` class); a needed kernel change is design friction: STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact.

=== THE DESIGN ===
P1 PINNEDOBS ADMITS A MISS AND AN EXISTENTIAL INUM.  Generalize `PinnedObs`: (a) the pin resolves to SOME inum -- `pin_resolves_pred Pin cw pl hops_of (fun a => a = cons_dev)`-shaped, or keep `pin_resolves_at` and let `pinned_obs` be stated for a pin `Pin v := ∃ i, Pin_at i v` with `hops := [ROOTINO; i]` under the same `∃` (DECIDE: PREFER a `pinned_obs_ex` beside `pinned_obs` that takes `Pin_at : Z -> aview -> Prop` and yields `pobs_node`'s conclusion as `∃ i, ⌜i' = i ∧ b = a⌝ ∨ T` -- the cursor family at hop 1 is `∃ i, d = i ∧ Pin_at i v`-shaped: think about whether `pobs_P` can stay pure (`⌜∃ i, d = i⌝` is trivial -- the tie to the pin must ride in `pobs_recv`, the observation piece, which already carries `⌜Pin v⌝`; so the existential inum needs NO change to the cursor: `pobs_P T hops k d` at `k = 1` is `⌜d = hops !!! 1⌝ ∨ T` with `hops` UNKNOWN -- state the cursor at hop 1 as `⌜Pin_at d v⌝`-free `True`? then the walk's own pure receipt (`ex_start`'s terminal cursor carries `d` = the inum the walk resolved to) plus `pobs_recv`'s `⌜Pin v⌝ = ∃ i, present_at i v` gives `d = i` by `apath_at`'s determinism (`arun`/`apath_at` are functional): so the general lemma at an existential inum is the SAME lemma with `hops !!! 1` replaced by "whatever the walk found" and the tie made in `pobs_node` -- do it that way); (b) a FREE MISS: `pobs_Pmiss T k d := T` becomes a parameter `Pmiss`, and for a pin that says "either the entry is absent or it resolves to the node", the miss arm is `⌜Pin_miss v⌝ ∨ T` (the observation at a miss: `namei_walk_dead`'s receipt names the view where the walk died -- check what `ex_start`'s miss piece receives and whether it carries a view; if it does, `pobs_Pmiss` can be `⌜Pin v⌝ ∨ T` read at that view, and for the console pin `Pin v := cons_absent v ∨ ∃ i, cons_present_at i v` REFUTES the miss on the present arm and the hit on the absent arm). `PinnedExec` stays at the old shape (a wrapper: `Pmiss := fun _ _ => T`, the fixed inum); its statement does not move.
P2 BOTH OPENS PINNED; THE MKNOD STEP.  `UInitCons`: (a) the FIRST open at the pin `cons_absent ∨ ∃ i, cons_present_at i` (no flag in hand; `echo_pred`'s claim law for that disjunction: from the claim's three arms, `cons_absent` or `∃ i, present_at i` -- no `cons_made` needed): receipt = MISS (`r = -1`, absent) ∨ DEVICE CONSOLE at some `i` (present) ∨ TAINT; (b) on the miss, the repair arm's `wp_kinit_mknod` is THE STEP: mknod's bundle at the argument's path "console" with `ma = CONSOLE, mi = 0` at the root, `Fok`'s receipt hands out `cons_made r i` after phase 2's fupd shoots the flag (`AppEcho.cons_shoot`; the step `absent ∗ tok → present_at i ∗ tok` is `FsConsPin.cons_state_mknod` at phase 1; phase 2 re-closes at the third arm) -- state `init_cons_mknod_bundle` (the application-side bundle at `mknod_au_at`) and `init_cons_mknod_recv` (the receipt reading: `cons_made r i` OR the taint; a mknod FAILURE (the walk found an existing entry? `filealloc`? no -- mknod fails at `create` if the name exists or `ialloc` fails: on failure init's C proceeds to the second open anyway; state the failure arm as "no step, no flag": init's second open is then unpinned-by-flag -- handle it as the CLOSED-like arm: the second open at the ABSENT∨PRESENT pin still works WITHOUT the flag (P2(a)'s form) -- so use the flag-free pin for the second open too and drop `cons_made` from the open path entirely? DECIDE: the flag is what E5/SH-LINE may want for "the console node stays" (monotonicity); for the OPEN it is not needed: PREFER the flag-free pin `Pin v := cons_absent v ∨ ∃ i, cons_present_at i v` for BOTH opens, mint `cons_made` at the mknod step anyway (it costs nothing and records the monotone fact), and note where it is (not) used); (c) the SECOND open through `wp_uk_ecall_open_recv_body` at the same pin: DEVICE CONSOLE ∨ MISS (the mknod failed and the node is still absent: then fd 0 stays closed -- fold into CLOSED) ∨ TAINT; (d) the ledger: `wp_kinit_open` restated at the NAMED ledger `ustd γfd l0` (`l0 = take NSTD fdt0`, all closed) returning `ustd γfd (<[0 := FdOpen true true (FdDevice CONSOLE)]> l0)` on the CONSOLE arm (`fd_lowest_closed` of an all-closed ledger is 0; `open_fd_rcpt`'s `∃ fd, sts !! fd = Some FdClosed` is pinned to 0 by `ualloc`'s lowest-free discipline -- read `UserFd.ualloc`), `ustd γfd l0` unchanged on the MISS/CLOSED arm; `wp_kinit_dup` twice through the TRACKED `wp_uk_ecall_dup`: CONSOLE → `[console; console; console]`; CLOSED → dup fails (`r = -1`, the ledger unchanged) -- check `wp_uk_ecall_dup`'s failure arm.
P3 INIT'S HEAD.  `UkInitMain`'s head (`wp_kinit_main_loop` / `wp_kinit_main_from_1e`) carries `init_cons_head`'s THREE arms: CONSOLE (`ustd γfd [c;c;c]` at `FdDevice CONSOLE`, readable+writable), CLOSED (`ustd γfd l0`), TAINT (`echo_taint`; init's continuation generic from here -- how init's later fork/exec go generic on the taint arm: they already run on the pinned exec of sh whose taint arm is the generic slot; state that the head's taint arm hands the fork the generic supply); the fork's child arm carries the ledger at a fresh name with the same list; `UConsLine.ush_std_cons` is payable on the CONSOLE arm; SH-LINE consumes CONSOLE and CLOSED (sh's entry at either).
P4 SANITY: `grep -n "pinned_obs\|pinned_exec_bundle\|pinned_open_bundle" iris/PinnedObs.v iris/PinnedExec.v iris/PinnedOpen.v` shows the generalization and both instances; `grep -n "wp_uk_ecall_dup_untracked" iris/UkInit.v` EMPTY; `grep -n "init_cons_head" iris/UkInitMain.v` shows the three arms at the head; `git diff --stat -- iris/Spec*.v iris/Proof*.v iris/ConsoleInv.v iris/FsAbs*Fire.v` EMPTY; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
P1's generalized lemma, P2's bundles/receipts/ledger lemmas and P3's head as STATEMENTS; NO Admitted at report time. Build with -k. Report: (1) the generalized `pinned_obs` (miss arm, existential inum), quoted, and `PinnedExec` unchanged; (2) both opens' pins and receipts and the mknod step, quoted; (3) the three-arm head and the ledger at each arm, quoted; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (the general lemma, both opens, the mknod step with the shoot, the dups, init's head and its callers), P4. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
