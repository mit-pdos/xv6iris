You are an ENGINE-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-tlw (a sibling clone; the main checkout /shared/xv6iris-2 belongs to other lanes -- never touch it, never build it). Your lane is TEXT-LW: the user-mode engine's text-segment reader is byte-wide all the way down, and sh's jump tables need a FOUR-byte load from the text/rodata half; today that is a Hypothesis assumed at three sites. Build the leaf and discharge the hypothesis. Design of record: claude-notes/projects/review-echo-plan-2026-09-12.md finding 11; the site comments quoted below.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; design/icache.md (why the text half is STAMPED and persistent: `UserHeap.utext γt a b := a ↪[γt]□ b`); iris/UkShDiag.v:7556-7600 (`ushd_clw_text_ty` -- THE statement to discharge: a `C_LW` whose four source bytes are `utext`, returning the word in `rd` sign-extended as the ISA says, the run re-closed at pc+2); iris/UkShRun.v (header: where the hypothesis is used -- runcmd's jump table and nulterminate's); iris/UkShParseCmd.v:~1332 and iris/UkShFork.v:208/466/822 (the other sites; `Hclw` threaded as a section variable); iris/UkRunMem.v (`wp_uk_lbu_text` :651 -- the byte-wide text load at the run level, YOUR MOLD; `wp_uk_lw` :503 the width-4 DATA load; `wp_uk_ld` :394); iris/UkLoad.v (`wp_uk_lbu` :1516, `wp_uk_lw` :1551 -- the key-level leaves under the run; find the text variant of `wp_uk_lbu` and see what it consumes); iris/WpUmodeTextLoad.v (the machine-tier text reader: width-1 -- read its header for WHY (the icache/stamp argument) and how `uinstr`'s fetch reads 2 and 4 bytes of text (a fetch IS a multi-byte text read: `UmodeText.v`, `WpUmodeFetch*.v`), which is the proof you will generalise or reuse); iris/UserHeap.v (`utext` :214, `utext_str` :337, `uheap`'s text half `Mt`: `ux_addr`, NOT `uw_addr`); iris/UserPtTree.v / iris/UmodeText.v (`umem_x`, the stamped image); iris/RiscvLang.v and the Sail model for C.LW semantics (`uoff_c4`, `creg2reg_idx`, sign extension of the 32-bit load result).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-tlw <logname>` FROM /shared/xv6iris-2-tlw (names `tlw10`, `tlw11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'` run from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Your remote tree was seeded from the main tree and your first build may recompile a large cone -- that is expected, run it detached and wait. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-tlw` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter; `grep -rn "ushd_clw_text_ty" iris/*.v` shows the Definition and its DISCHARGE only (no Hypothesis/section variable left). Kernel files (`Spec*.v`, `Proof*.v`) are off limits; the machine-tier user-mode files (`WpUmode*.v`, `UmodeText.v`) ARE yours. GIT: work on a branch `lane/text-lw` in your checkout (`git checkout -b lane/text-lw`); commit there when green (message: what the leaf is and its gate); never push; never stash/reset/add -A/commit -a/amend; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact.

=== THE DESIGN ===
T1 THE MACHINE-TIER TEXT WORD LOAD.  Whatever `WpUmodeTextLoad.v` proves for one byte, prove for a 4-byte aligned load whose four bytes are stamped text: the argument is the fetch's (a 4-byte instruction fetch already reads four stamped bytes -- reuse its lemma or its shape). Alignment: `a mod 4 = 0` keeps the access inside one page (`uaccess_arith`).
T2 THE KEY-LEVEL AND RUN-LEVEL LEAVES.  `UkLoad.wp_uk_lw_text` on `wp_uk_lbu`'s text variant's mold, then `UkRunMem.wp_uk_clw_text` (compressed form, the exact shape of `ushd_clw_text_ty`) and, if cheap, `wp_uk_lw_text` (the uncompressed LW) beside it. Result in `rd`: the 32-bit word `wv` sign-extended to 64 (check the Sail semantics of C.LW and state what `ushd_clw_text_ty` expects -- if the hypothesis's statement disagrees with the ISA, STOP AND REPORT the goal).
T3 THE DISCHARGE.  `UkShDiag.ushd_clw_text_holds : ushd_clw_text_ty` proved from T2; the three sites take it instead of the Hypothesis (the section variables go; the theorems that carried `Hclw` become unconditional -- report each).

=== PHASE 1 -- then STOP AND REPORT ===
T1-T2 STATED (compiling, no Admitted; prove what is short), T3 stated. Report: (1) the statements verbatim, file:line; (2) which fetch lemma the 4-byte text read reuses and what it costs; (3) red files; (4) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
Proofs, discharge, gate, commit on `lane/text-lw`. Report log name, the commit hash, `git diff --stat main..lane/text-lw | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 15 lines, present tense).
