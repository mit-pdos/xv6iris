You are a KERNEL-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-sup (a sibling clone at current main; never touch or build /shared/xv6iris-2, /shared/xv6iris-2-tlw, /shared/xv6iris-2-disc). Your lane is PRINTK-LEDGER (the K side of E5): every kernel `printk` appends its RENDERED message to a ledger held under `pr.lock`, so the kernel-tagged (`TxK`) bytes on the UART are exactly the concatenation of the ledger's messages, in call order; the boot's ten messages are pinned byte-for-byte; and every other printk site's message is pinned to its template. Design of record: claude-notes/projects/app-echo.md "TX-TAG LANDED" (the printk inventory: the sixteen sites, which are reachable, `boot_k_shape` stated without a producer; `panic()` prints nothing at the pin), "E5 -- THE OUTPUT SIDE" O1/O2/O4 (the LEDGER and GOOD_OUT paragraphs), "DISC-RATE LANDED" (`boot_msgs` = the ten, `boot_stream`, `good_out`), and `review-echo-plan-2026-09-12.md` finding 2. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the bundle-intro note, the pinned-C note: read C via `git -C /shared/xv6iris-2/xv6-riscv show 06ea57f:kernel/<file>` -- printk.c, console.c, uart.c, main.c, and every file with a printk site); remote-build-gcp.md; iris/SpecPrintk.v (`pr_res` :216 -- currently `emp`; `printk_env` :228; the full contract `wp_printk_sconf_body` (:~150-170: "SOME byte list cs was appended", `uart_sent_sub_at γd TxK …`); the GEN corollary `wp_printk_gen_sconf_body` :254 and its ~15 callers; the header :176-230 on why the corollary exists); iris/ProofPrintk.v, iris/SpecPrintint.v + iris/ProofPrintint.v (the `%d`/`%x`/`%lx`/`%p` renderer: what `printint` promises about the bytes it emits -- today: some bytes; find whether the digit rendering is pinned anywhere), iris/PrintkFmt.v (the format-string walker: `pk_kinds`, the per-directive kinds; what it proves about the walk), iris/PrintkArgs.v (the vararg model), iris/LinkPrintk.v (:54 the one instantiation of the full contract at `bs := []`), iris/SpecConsputc.v (the post now PINS its bytes: `cs = [cp_byte a00]` or the BACKSPACE triple), iris/SpecMain.v (:7-20 the boot's printks; :251 `boot_k_shape`; :213-250 the header on the K side) + iris/ProofMain.v (:713/:774/:835 the three banner calls) + iris/SpecMainSecondary.v/ProofMainSecondary.v (:588 "hart %d starting\n" with `cpuid()`), iris/UartTxInv.v (`uart_sent_sub_at`, the tagged claims), iris/UartSentLoc.v (`tag_proj`, `uart_sent_exact_at` -- the exactness vocabulary, stated by TX-TAG without a producer for `TxW`; for `TxK` YOU are the producer), iris/WpUart.v (`uart_tagsE`, the lockstep), iris/EchoDisc.v (`boot_msgs`, `hart_line`, `boot_stream`, `good_out`, `k_done` -- the application-side consumer: `boot_stream` will be GENERALISED by the application lane, not by you; state what it needs from you), the lock machinery `pr.lock` is proved as (find `SpecPrintk`'s lock: `is_lock γpr a_pr_lock … <{ pr_res γd }>`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-sup <logname>` (names `pkl10`, `pkl11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Your remote tree was last built at an older base: your FIRST build recompiles most of the tree -- run it detached BEFORE editing and wait. `SpecPrintk` sits under every kernel function that prints (~15 callers) plus the boot: batch statements. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-sup` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref main` justified; nothing Admitted; no new Axiom/Parameter. Files: `SpecPrintk`/`ProofPrintk`/`SpecPrintint`/`ProofPrintint`/`PrintkFmt`/`PrintkArgs`/`LinkPrintk`, `SpecMain*`/`ProofMain*`, `UartTxInv`/`UartSentLoc`/`WpUart` (only if the K-exactness tie needs the invariant), and every GEN caller's file ONLY to pass the ledger through (each a mechanical premise change; report them). `App.v`, `SystemAdequacy.v`, `EchoDisc.v`, `AppEcho.v`, `ConsoleInv`, `FsAbs*`, the U tier: off limits. GIT: branch `lane/printk-ledger`; commit there when green; never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NO placeholder arms.

=== THE DESIGN ===
P1 THE LEDGER.  `pr_res γd` (the `pr.lock` resource, today `emp`) becomes the K-ledger's authority: `∃ ms : list (list (bv 8)), k_ledger_auth γk ms ∗ ⌜tag_proj TxK tg = concat ms⌝`-style tie to the tagged accepted trace (the exact form: every `TxK` push in the kernel happens inside a printk call under `pr.lock` -- `consputc` from `consoleintr` is `TxE`, `uartwrite` is `TxW` -- so the K-projection of the accepted list is the concatenation of completed messages plus the in-flight one; state the tie at the lock's release: `k_proj tg = concat ms`; between acquire and release the lock holder owns the discrepancy). `k_ledger_lb γk ms` persistent lower bound. Where does `γk` live: beside `γd` in `printk_env` (allocated at boot with `pr.lock`'s init: find `printkinit`'s spec `SpecPrintkinit.v`).
P2 THE FULL CONTRACT PINS ITS MESSAGE.  `wp_printk_sconf_body`'s post says `ms' = ms ++ [render fmt args]` where `render` is a pure function of the format string and the vararg values -- `PrintkFmt`'s walker gives the directive kinds; `printint`'s rendering of a `%d`/`%x`/`%lx` value must be PINNED (the decimal/hex digits of the value: state `printint_bytes (v : Z) (base : Z) (sign : bool) : list (bv 8)` and prove `SpecPrintint`'s post at it -- if `ProofPrintint`'s digit loop cannot currently be pinned (say, the buffer reversal), report the goal). `%s` renders the C string's bytes (the argument is a kernel pointer to a NUL-terminated string the caller owns: state what the caller supplies), `%p` the hex of the pointer, `%%` a percent, an unknown directive prints `%` and the character (printk.c's default arm). Every site in the inventory uses only `%d`, `%s`, `%lx`, `%x`? -- enumerate.
P3 THE GEN CALLERS.  The GEN corollary keeps its shape for callers but now appends `∃ m, ms' = ms ++ [m] ∗ ⌜m matches the site's template⌝` -- i.e. the corollary is parameterised by the FORMAT STRING (a literal at every site) and its post pins the message to `render fmt args` with the args EXISTENTIAL where the caller does not care (`%d` of an unknown value renders to SOME digit string: `template_match fmt m`). So the ledger's every entry is a template match, and the boot's ten are exact (their args are known: `cpuid()` = the hart's id, 1..7 -- read how `ProofMainSecondary` knows the hart id).
P4 THE K FACT FOR THE APPLICATION.  `k_bytes_of_ledger : k_ledger_lb γk ms -∗ uart_sent_tagged γ tg -∗ (the lock's tie at a quiescent point) -∗ ⌜tag_proj TxK tg = concat ms⌝` in whatever form the application's `Htx` reasoning can use at a pop (the app holds the tagged bound it read at the pop and the ledger's lower bound; the in-flight message is the one discrepancy: state the "prefix of concat (ms ++ [in-flight])" form). And `boot_k_ledger : the ledger's entries in this run are the ten boot messages (each once, hart 0's three first) followed by entries each matching one of the error templates` -- state it as a predicate on `ms` (`kernel_msgs ms`) that the application lane will put into `EchoDisc.boot_stream`'s generalisation; prove that every printk site in the tree appends an entry satisfying it (the inventory's sixteen: ten boot + six templates: "balloc: out of blocks\n", "ialloc: no inodes\n", "ireclaim: orphaned inode %d\n", "%d %s: unknown sys call %d\n", "usertrap(): unexpected scause 0x%lx pid=%d\n" + "            sepc=0x%lx stval=0x%lx\n", "recovering tail %d dst %d\n"; procdump's three are dead code -- confirm no proof reaches them, or include their templates). This is what lets the claim admit kernel error messages exactly as O5 admits sh's failure prints.
P5 SANITY: `grep -n "pr_res" iris/SpecPrintk.v` shows the ledger; `grep -rn "wp_printk_gen_sconf" iris/*.v` shows every caller at its format literal; `grep -n "boot_k_shape" iris/SpecMain.v` shows it REPLACED by (or derived from) `kernel_msgs`; the audit unchanged; forbidden-set diff EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
P1 STATED (the ledger, the tie, the lock resource) with the boot allocation; P2 INVESTIGATED and STATED (`render`, `printint_bytes`, what `ProofPrintint` can pin); P3 STATED (the parameterised corollary) with the callers' premise changes compiling; P4 STATED. NO Admitted. Report: (1) the statements verbatim, file:line; (2) whether `printint`'s digits can be pinned (the goal if not); (3) the sixteen sites' format strings and how each caller supplies its args (or leaves them existential); (4) the red files; (5) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs, P5, the gate, commit on `lane/printk-ledger`. Report log name, commit hash, `git diff --stat main..lane/printk-ledger | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), the exact `kernel_msgs` predicate the application lane must adopt.
