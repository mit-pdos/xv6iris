You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-WAIT: `wait()` returns the reaped child's ESCROW with the two facts a parent needs to redeem it, all the way from kwait through sys_wait, the dispatcher, the trap round and the U-tier leaf to init's `wp_kinit_wait`. Design of record: claude-notes/projects/app-echo.md, section "WX-GEN / WX-INV / WX-WAIT — DESIGN" (whole; "THE POST (WX-WAIT)" is your statement), then the LANDED notes newest first ("WX-INV LANDED" -- take every file:line anchor from it; "WX-GEN LANDED", "WX-EXIT LANDED", "WX-RES + WX-ROW LANDED", "WX-FORK LANDED"), then "WAIT-EXIT — DESIGN OF RECORD". The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; iris/ChildTok.v (`child_tok`, `exit_tok`, `gen_pay`, `gen_pay_timeless`, `gen_pid`, `child_tok_pid`); iris/WaitInv.v (the CARRIED invariant after WX-INV and its consequence lemmas `children_inv_reap`/`children_inv_empty` -- they already yield (W2) `⌜γ' ∈ cs ∨ γ' ∈ orph_row O pj⌝` (the orphans are an address-keyed table, `orph_map`; no `ip` anywhere), (W3) `children_inv_pid` (per γ: `g ∈ cs → children_inv -∗ gen_pid g pid -∗ pid_reg pid dq g' -∗ ⌜g = g'⌝`; the □-summary over `cs` is extracted by set induction under the lock, borrowing one entry at a time -- THIS lane's first job), (W5) `cs = ∅`); iris/SpecKwait.v + iris/ProofKwait.v (`kw_reap`: the ZOMBIE block out of `fp_of_dormant_zombie` with the escrow `exit_tok (pv_gen Vc) pidz (xstate_val xsv)` and the cell half; the copyout of `pp->xstate`; the row move WX-INV added; the -1 arm `kw_exit_neg`); iris/SpecSysWait.v + iris/ProofSysWait.v; iris/SpecSyscall.v + iris/ProofSyscall.v (the wait arm; the FORK arm's `∃`-split and `sysc_fork_out`/`ufork_ans` -- THE MOLD); iris/UexecRet.v (`ufork_ans`, `uwait_ans` (WX-INV), the wait rows); iris/UsysMemOk.v (the wait row ~222: the copyout of the four-byte status at `addr`); iris/UkRunSys.v (`wp_uk_ecall_wait_null`); iris/UkInit.v (`wp_kinit_wait` ~585 and init's loop); iris/UkFork.v (`wp_uk_ecall_fork`: the parent arm's `child_tok γ pidv Q` -- what init holds).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `wxwait1`, `wxwait2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal. Gate: zero `Error`, `EXIT=0`, COMPILED>0; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` with every line justified (sealed Parameters retyped in place listed by group; nothing Admitted; no new Axiom/Parameter). GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact (no dates, no lane markers).

=== THE DESIGN ===
W1. KWAIT'S POST, ONE SPEC, TWO ARMS (beside the copyout row it already has; the row in/out from WX-INV):
  reap arm: `⌜rv = pid'⌝ ∗ exit_tok γ' pid' xs ∗ ⌜xs = xstate_val (the four-byte word copied out)⌝ ∗ (⌜γ' ∈ cs⌝ ∨ ⌜γ' ∈ orph_row O pj⌝ -- `O` is not the caller's; see W2's decision) ∗ □ (∀ γ, ⌜γ ∈ cs⌝ -∗ gen_pid γ pid' -∗ ⌜γ = γ'⌝) ∗ ch_frag (pv_chg (us_V U)) pj (cs ∖ {[γ']})`;
  -1 arm: `⌜rv = -1⌝ ∗ ⌜cs = ∅⌝ ∗ ch_frag … cs`.
  The escrow's `xs` is tied to the copied-out status by the two halves of `p_xstate` the reaper holds (`proc_pub`'s and the ZOMBIE block's -- `ctx_word4_pointsto_agree`; WX-EXIT recorded the site); `O` is not the caller's to know, so state the reap arm's membership as `⌜γ' ∈ cs⌝ ∨ ⌜γ' ∉ cs⌝`-free: PREFER dropping membership from the POST altogether and keeping only the uniqueness fact over `cs` (what init spends: a token for a γ ∈ cs plus `r = pid` forces `γ = γ'`); if a later consumer needs "γ' was my child or my orphan", the kernel-side lemma `children_inv_reap` already yields it. Decide and report.
W2. THE RELAY: `SpecSysWait` (its post relays the arms; the copyout row unchanged), the dispatcher's wait arm `∃`-splits sys_wait's answer to bind `cs'` and hands `sysc_wait_out f U r cs cs'` = `uwait_ans r cs cs'` extended with the escrow and the facts: `uwait_ans r cs cs' := ⌜r = -1 ∧ cs' = cs ∧ cs = ∅⌝ ∨ ∃ γ' pid' xs, ⌜r = sign_extend' 64 pid'⌝ ∗ ⌜cs' = cs ∖ {[γ']}⌝ ∗ exit_tok γ' pid' xs ∗ □ (∀ γ, ⌜γ ∈ cs⌝ -∗ gen_pid γ pid' -∗ ⌜γ = γ'⌝)` (the fork mold `ufork_ans Q r cs cs'`); `UexecRet`'s wait row and `UexecApply.uexec_ret_round_slot`'s wait case read `uvis_ch W' = cs'` off the answer; `usys_ch_ok` stays pure-quiet for wait (the set's move rides the answer, as fork's does). The answer carries no membership disjunct (see W1): the leaf's parent arm is `exit_tok γ' pid' xs ∗ □ (uniqueness over cs)`.
W3. THE U TIER: `UkRunSys.wp_uk_ecall_wait_null` (and the non-null variant if it exists) returns the two-armed answer: `r = -1 ∗ uch (ukn_ch N) cs ∗ ⌜cs = ∅⌝` or `r = pid' ∗ exit_tok γ' pid' xs ∗ □ (∀ γ, ⌜γ ∈ cs⌝ -∗ gen_pid γ pid' -∗ ⌜γ = γ'⌝) ∗ uch (ukn_ch N) (cs ∖ {[γ']})` (the leaf takes `uch (ukn_ch N) cs`, the fork leaf's mold; `wp_uk_ecall_wait_any` index-free at the trivial payload drops the escrow, keeps `r ≠ 0`); `UkInit.wp_kinit_wait` REDEEMS: init holds `child_tok γsh pidsh Q` (from its fork leaf's parent arm) and `uch … cs` with `γsh ∈ cs`; on `r = pidsh`: `gen_pid γ' pidsh` from the escrow (`exit_tok` carries `gen_kq` → `gen_pid`), the uniqueness fact at `γsh` gives `γsh = γ'`, `gen_pay_timeless` (the payload is `fun _ => True` today -- Timeless) yields `Q xs`; on `r ≠ pidsh` (an orphan, or -1 after a kill of nothing) init drops the escrow and loops -- as its code does. State init's loop invariant so that it re-forks with `Q xs` in hand (today `True`; L7 puts the console-input resource there and `Q (-1)` is what a killed sh returns).
W4. SANITY: `grep -n "exit_tok" iris/SpecKwait.v iris/SpecSysWait.v iris/SpecSyscall.v iris/UexecRet.v iris/UkRunSys.v iris/UkInit.v` shows the escrow travelling; `grep -n "gen_pay" iris/UkInit.v` shows the redemption; nothing in the tree says "nothing in the tree ties a pid to the private exit-status word" any more (SpecKwait's copyout note).

=== PHASE 1 -- then STOP AND REPORT ===
W1's post, W2's `uwait_ans`/`sysc_wait_out` and the dispatcher's arm shape, W3's leaf statements as STATEMENTS; NO Admitted at report time. Build with -k. Report: (1) kwait's post, quoted; (2) `uwait_ans` and the leaf's answer, quoted, and the membership decision; (3) init's redemption sketch (the lemma statement for `wp_kinit_wait`); (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (kwait's reap arm from the invariant's consequence lemmas, the -1 arm, sys_wait, the dispatcher's wait arm, the round, the leaf, init), every red file, W4. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.

=== SCOPE AFTER WX-INV'S RULING 2 (read app-echo.md "WX-INV RULING 2" and "WX-INV LANDED") ===
WX-INV already landed the ROW half of the route: kwait/sys_wait take and return the row with `ch_reaped cs cs'`; `sysc_wait_out`; the round's wait ARM `uexec_wait_F` carrying `uwait_ans r cs cs'`; `usys_ch_ok`/`ut_ch_kept` exempting wait; the leaf `wp_uk_ecall_wait` taking `uch (ukn_ch N) cs` and returning it moved, and `wp_uk_ecall_wait_any` at `uch_any`.  THIS lane WIDENS: kwait's reap arm gains the escrow and the uniqueness fact (W1); `uwait_ans`'s second disjunct gains `exit_tok γ' pid' xs ∗ □ (∀ γ, ⌜γ ∈ cs⌝ -∗ gen_pid γ pid' -∗ ⌜γ = γ'⌝)` and `⌜r = sign_extend' 64 pid'⌝` (W2); the leaf's reap arm hands them to the program (W3); init redeems (W3).  The uniqueness summary is extracted under the lock from `children_inv_pid` by set induction over `cs` (one entry borrowed at a time) BEFORE the reap's `children_inv_reap` -- do it as a WaitInv lemma `children_inv_pid_all : … -∗ children_inv … ∗ □ ([∗ set] γ ∈ cs, ∀ pid, gen_pid γ pid -∗ pid_reg pid dq g' -∗ ⌜γ = g'⌝)`-shaped, or directly the `□ (∀ γ, ⌜γ ∈ cs⌝ -∗ gen_pid γ pid' -∗ ⌜γ = γ'⌝)` at the reaped `γ'`/`pid'` using the ZOMBIE block's `pid_reg` quarter; decide and report.  Take every file:line from "WX-INV LANDED".

=== RE-ANCHORED AT THE WX-INV LANDING (commit `5b228fcad`, on top of the XV6_REV bump `92e0b0415`) ===
Anchors (verified): `WaitInv.wait_res_at` :1248, `children_inv` :702, `inv_pure` :251, `orph_row` :199, `children_inv_reap` :1035, `children_inv_pid` :1172, `children_inv_empty` :1200; `UserChildren.ch_reaped` :123; `UexecRet.uwait_ans` :856, `uexec_wait_F` :1068 (the arm; `uexec_pay_arm f -∗ uexec_wait_F …` at :1089/:1133 -- the arm also returns `Q (-1)`); `SpecSyscall.sysc_wait_out` :501; `SpecUsertrap.ut_wait_out` :550; `SpecKwait` row in :205 / `ch_reaped` :230; `SpecSysWait` :124/:141/:147; `ProofKwait.kw_reap`: `fp_of_dormant_zombie` :1449 (the ZOMBIE block with `exit_tok (pv_gen Vc) pidz (xstate_val xsv)` and the cell half), `children_inv_reap` :1465 -- the escrow is ALREADY in the reaper's hands there and currently dropped; W1 hands it out; `UkRunSys.wp_uk_ecall_wait_null` :1757 (takes `uch (ukn_ch N) cs`; the reap arm's answer is where the escrow and the fact go), `wp_uk_ecall_wait_any` :1876; `UkInit.wp_kinit_wait` :617 (carries `uch_any`; the redemption needs the INDEXED leaf and init's `child_tok γsh pidsh Q` from its fork leaf's parent arm -- `UkInitMain`/`UkInit`'s fork call uses `wp_uk_ecall_fork_any` today (token dropped): switch init's fork to the indexed leaf at `Q := fun _ => True` and thread `uch (ukn_ch N) cs` instead of `uch_any` through init's loop, or keep `_any` and add an indexed twin `wp_kinit_wait_tok` -- decide and report in phase 1).  The kernel bump: `panic_env` is `emp` now and ~90 specs still name it -- not this lane's business; do not touch it.  Build log names `wxwait1`, `wxwait2`, ….
