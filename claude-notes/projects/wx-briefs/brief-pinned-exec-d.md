You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is PINNED-EXEC D: init execs "sh" on ITS OWN pinned exec bundle (`PinnedExec.pinned_exec_bundle`, landed), so init's kernel-side slot constructor stops taking the generic exec supplier `uxsup`. Design of record: claude-notes/projects/app-echo.md -- "THE PINNED EXEC BUNDLE — design", "PINNED-EXEC C LANDED", "D IS BLOCKED ON THREE EXEC-CHANNEL FACTS -- rulings" (all three are now landed: EXEC-CHANNEL, EXEC-ARGS -- read their LANDED notes), and this brief. The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1. The kernel contracts (SpecUserinit, forkret, the theorem, `app_sup`) are NOT this lane's -- ARM-c (1) is next.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Proofmode & bitvector gotchas"); remote-build-gcp.md; design/user-wp-slot.md; the headers of iris/PinnedExec.v, iris/UInitKernel.v, iris/UShKernel.v, iris/UkInitMain.v, iris/UkFork.v (Forkable).

=== OPERATING RULES (hard) ===
- BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 under `run_in_background` with the log's `COMPILED=`/`EXIT=` lines as the sentinel; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`; ONE build at a time; never a remote top-level `make`; never local `coqc`. Never `simpl`/`cbn` a goal mentioning a dumped byte literal (UCodeInit/InitData literals included; use `vm_compute` on closed terms or `lookup`-lemmas).
- Gate: zero `Error`, `EXIT=0`, COMPILED>0; `make audit-only` on the VM (tree root `/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline"; `tools/lemma_diff.py` clean (zero Admitted; no new Axiom/Hypothesis/Parameter).
- GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`. DO NOT COMMIT OR STAGE. No other checkouts, no worktrees. Temp files in this scratchpad. Do not edit claude-notes/.
- DESIGN FRICTION = STOP AND REPORT. Never weaken a landed form. Comments state the current shape as fact (no dates, no "used to"/"now"). `T`, the claim law and sh's payload stay PARAMETERS of init's constructor (AppEcho instantiates them in ARM-c (1)).

=== THE LANDED PIECES (file:line verified 2026-09-09) ===
- `PinnedExec.pinned_exec_bundle γfs X Pin T cw pl hops ino f nl Pay M pv av sts` (PinnedExec.v:~438): premises `pin_resolves Pin cw pl hops ino f nl`, `kexec_loadable f`, `exec_path_of M pv pl`, the duplicating claim law `□ (∀ v, app_pred app_run v -∗ app_pred app_run v ∗ (⌜Pin v⌝ ∨ T))`, `app_inv γfs`, the constructor wand `□ (∀ na alen afun W', ⌜kexec_image_ok f na alen afun sts W'⌝ -∗ ⌜exec_args_of M av na alen afun⌝ -∗ Pay -∗ X W')` (EXEC-ARGS moved it from `exec_args_shape`; confirm), `□ (T -∗ ∀ W', X W')`, `Pay`; yields `∃ P Pmiss Fo R, sys_exec_au_pre (MkPfam X R) (fs_gamma_L γfs) γfs cw P Pmiss Fo M pv av sts`. `UexecExecInst.sbundle_exec_intro X W P Pmiss Fo Rs` (UexecExecInst.v:785) turns that, at `cw := uvis_cwd W`, `M := uvis_M W`, `pv := tf_w … (tf_arg_idx 0)`, `av := tf_w … (tf_arg_idx 1)`, `sts := uvis_fd W`, into `sbundle X USYS_exec W`. Here `X := uslot` and `γfs := fsc_fs`; `app_inv fsc_fs` is `FirstTok.fsabs_env` (FirstTok.v:344).
- `FsShPin.era0_sh_pins av` (FsShPin.v:286): `apath_at av ROOTINO sh_path = Some SH_INO ∧ av !! SH_INO = Some (MkAnode (AFile sh_bytes) 1) ∧ arun av ROOTINO sh_path [ROOTINO; SH_INO]`; `sh_bytes = ElfUser.sh_elf`; `ElfLoadable.sh_elf_loadable`. So `pin_resolves era0_sh_pins ROOTINO sh_path [ROOTINO; SH_INO] SH_INO sh_elf 1` is a pure lemma (state it in FsShPin or PinnedExec's consumer side; `um_start_of ROOTINO sh_path = ROOTINO` because "sh" is relative).
- `UkRun.udepw_at N m pc n c` (UkRun.v:~333; LENDS `uheap`/`ufd_auth` at every `M pm sz fdv`, cwd fixed at `c`); `udepw_at_of_uxsup`, `udepw_at_mint`. `UkRunSys.wp_uk_ecall_exec_at_cwd` (UkRunSys.v:~1408) takes `ucwd (ukn_cwd N) c -∗ udepw_at N m pc USYS_exec c -∗ (∀ h', ucwd (ukn_cwd N) c -∗ urun N h' (<[a0 := -1]> m) (pc+4) avail -∗ WP) -∗ WP`. It has NO caller yet; `UkInit.wp_kinit_exec` (UkInit.v:~439-476) calls the plain `wp_uk_ecall_exec` with `udepw N m pc USYS_exec`.
- `UShKernel.sh_slot_of_kexec R na alen afun sts W' n0` (UShKernel.v:370): premises `kexec_image_ok sh_elf na alen afun sts W'`, ROOM `kexec_sz sh_elf - PGSIZE + 8 * (2 + (8 + (16 + (ush_Dbody + n0)))) <= kxc_sp_final (kexec_sz sh_elf) alen na`, `length sts = NOFILE`, `fd_lowest_closed sts = None`, the `□` payload wand (usz + the data below the frame ↦ `∃ f, R γt γd γs ∗ ubytes γd sh_buf sh_nbuf f`), `udep`, `∀ N, ush_rest N (R …)`; yields `uslot W'`.
- `UInitKernel.init_uexec_slot W n0` (UInitKernel.v:~155): premises pc = start, `init_img_sub (uvis_M W)`, page 0 is x∧¬w, sp alignment/room/stack presence, `length (uvis_fd W) = NOFILE`, map-stop, `psok`; `udep -∗ uxsup -∗ uslot W`; proof = `uslot_of_urun` then `wp_kinit_start` with `ucwd_any_of` at ~199. `UInitKernel.init_slot_of_kexec` (~205) bridges from `kexec_image_ok init_elf …` and takes `udep -∗ uxsup -∗ uslot W'`.
- UkInitMain: SIX lemmas take `uxsup`: `wp_kinit_main_child N'` (:444, no cwd at all -- the fork child arm drops `ucwd_any` at ~997), `wp_kinit_main_loop` (:726), `wp_kinit_main_from_1e` (:1134), `wp_kinit_main_repair` (:1280), `wp_kinit_main` (:1496), `wp_kinit_start` (:1750, premises `init_code γt -∗ uxsup -∗ init_rodata γt -∗ usz γs szv -∗ ustd_any γfd -∗ ucwd_any γcwd -∗ urun …`); each pays the deposit by `udepw_of_uxsup` (:552 the mold). `wp_kinit_fork` (:590) takes `ucwd_any γcwd`, returns `ucwd_any` to the parent arm and `ucwd_any (ukn_cwd N')` to the child arm (`ucwd_any_of` at ~706); `UkFork.wp_uk_ecall_fork` (UkFork.v:~777) itself carries `ucwd (ukn_cwd N) c` at a VALUE for the parent -- check what it gives the child (the child's half is "minted at the parent's value").
- After EXEC-ARGS: `exec_args_of M av na alen afun` = `exec_args_shape … ∧ ∃ avf, (∀ i <= na, uimg_word_at M (uint (add_vec_int av (8 i))) (avf i)) ∧ (∀ i < na, avf i ≠ 0) ∧ avf na = 0 ∧ (∀ i < na, copyinstr_got M (avf i) (afun i) (alen i))`; `uimg_word_at M a w` (plain-Z bytes `a..a+7`, little-endian) lives in SpecCopyin.v beside `copyin_got`; `SpecFetchaddr.fetchaddr_got`. `init_args_det` reads the two words at 0x1000/0x1008 through `uimg_word_at` (a closed `bv_to_little_endian` computation) and the string through `copyinstr_got` at 0x9a8 against `init_ro`.
- init's data: `user-rocq/InitData.v` -- segments `[(0x0, 0xe6c, 0xe6c, R-X); (0x1000, 0x10, 0x30, RW-)]`, `.data 0x1000..0x1010` = the argv array `{ "sh", 0 }`: bytes `0x1000: a8 09 00 00 00 00 00 00` (the pointer 0x9a8) and `0x1008..0x100f: 0` (NULL); `.bss 0x1010..0x1030`; the path bytes `0x9a8 = 's', 0x9a9 = 'h', 0x9aa = 0` in `UCodeInit.init_ro` (`filter (kv.1 < 4096)`, UCodeInit.v:5539); `init_img_sub` (UCodeInit.v:106) -- CHECK whether the image map it names includes 0x1000..0x100f (the ELF's .data initial bytes are in `elf_image init_elf`, so `kexec_image_ok`'s `uimg_sub` gives them at exec time either way). At init's exec ecall a0 is pinned to 0x9a8 (UkInitMain.v:~530) and a1 to 0x1000 (find it).
- `UkFork.Forkable P` (UkFork.v:393): reveal text map + PERSISTED data map (`ubyteq γd DfracDiscarded`, duplicated at the reveal) + exclusive map; rebuild at any names. Instances `forkable_ubyteq_disc` (:542), `forkable_ubytesq_disc` (:608). `UserHeap.ubyte_persist`/`uarea_persist` (UserHeap.v:~384-392); `uheap_ubytes_at` (UserHeap.v:693) reads `ubytesq γd dq a n f` into `M` facts through the heap; `uheap_text` (:631) reads `utext` fragments (init's rodata) into `M` facts. `UkRun.uslot_of_urun_all` (UkRun.v:783) hands the data OUTSIDE the frame over exclusively (below the frame's base and at/above sp) -- sh's constructor uses it (UShKernel.v:~348).

=== THE DESIGN ===
D-a. INIT OWNS ITS ARGV ARRAY, PERSISTED. init never writes `.data`, so its sixteen argv bytes are read-only in fact: define `UCodeInit`/`UInitKernel.init_argv_map : gmap Z (bv 8)` (the sixteen `(0x1000+k, byte)` pairs, computed from `InitData`'s image, not retyped) and `init_argv γd := [∗ map] a ↦ b ∈ init_argv_map, ubyteq γd DfracDiscarded a b` (Persistent). `init_uexec_slot` moves its carve from `uslot_of_urun` to `uslot_of_urun_all`, takes the sixteen `ubyte`s out of the exclusive data map handed over (the rest of page 1 -- .bss and slack -- is dropped or kept, your call; say which) and persists them (`uarea_persist`), yielding `init_argv (ukn_d N)`. It rides through every UkInitMain lemma as a `#`-premise beside `init_rodata`, and across the fork as part of the Forkable payload (`forkable_ubyteq_disc` per byte / a map instance -- `forkable_utext_map` :568 is the mold for a map) so the child gets `init_argv (ukn_d N')` for free. New premises `init_uexec_slot` needs for this: the sixteen bytes present in `uvis_M W` at 0x1000.. with page 1 mapped (from `init_img_sub` if it covers them, else a new premise the bridge discharges from `uimg_sub (elf_image init_elf) (uvis_M W')` and `kxb_perm_ok`); state them as pure premises and discharge them in `init_slot_of_kexec`.
D-b. INIT'S EXEC LEAF IS CWD-INDEXED. `UkInit.wp_kinit_exec` takes `ucwd (ukn_cwd N) c -∗ udepw_at N m pc USYS_exec c -∗ …` (the -1 continuation returns the fragment) and calls `wp_uk_ecall_exec_at_cwd`. Every UkInitMain lemma replaces `ucwd_any γcwd` by `ucwd γcwd FsImg.ROOTINO` (the fragment at the VALUE; init's cwd never changes -- `usys_cwd_ok_quiet` and the fork keeps it); `wp_kinit_main_child N'` gains `ucwd (ukn_cwd N') ROOTINO`; `wp_kinit_fork` hands the parent arm its fragment back and the child arm `ucwd (ukn_cwd N') ROOTINO` (from `wp_uk_ecall_fork`'s child half at the parent's value).
D-c. THE DEPOSIT IS INIT'S OWN BUNDLE. Replace `uxsup` in the six UkInitMain lemmas (and in `init_uexec_slot`/`init_slot_of_kexec`) by the ingredients, ALL persistent (init execs sh inside the fork child, inside an `iLöb` the parent re-enters, so nothing linear can be spent there): `app_inv fsc_fs`; `□ (∀ v, app_pred app_run v -∗ app_pred app_run v ∗ (⌜era0_sh_pins v⌝ ∨ T))` for a parameter `T` with `Persistent T`, `Timeless T`; `□ (T -∗ ∀ W, uslot W)`; sh's constructor wand `□ (∀ na alen afun sts W', ⌜kexec_image_ok sh_elf na alen afun sts W'⌝ -∗ ⌜exec_args_of M av na alen afun⌝ -∗ … -∗ uslot W')` -- DECIDE its exact shape against `sh_slot_of_kexec`: the ROOM premise must be discharged from `exec_args_of` at INIT's argv (below), `length sts = NOFILE`/`fd_lowest_closed sts = None` from init's `ufd_auth` (the loan; find the fd-authority lemmas that give them -- `UserFd`), `udep` is in hand, sh's payload wand and `∀ N, ush_rest N (R …)` are the parameters `Pay` (persistent -- `ush_rest` has a Persistent instance; the payload wand is `□`). Bundle those as ONE persistent premise `init_sh_slot T Pay` if that keeps the six statements readable. At the exec site (in `wp_kinit_main_child`'s exec, or wherever `udepw_of_uxsup` is applied today), build `udepw_at N m pc USYS_exec ROOTINO`: for every lent `uheap … M pm sz` and `ufd_auth … fdv`, (i) `exec_path_of M (0x9a8) sh_path` by `uheap_text` on `init_rodata`'s three bytes, (ii) the pure fact that INIT'S ARGV IS DETERMINED BY ITS IMAGE: `init_args_det : (∀ k < 16, M !! (0x1000 + k) = init_argv_map !! (0x1000 + k)) -> exec_args_of M 0x1000 na alen afun -> na = 1 ∧ alen 0 = 2 ∧ afun 0 0 = 's' ∧ afun 0 1 = 'h' ∧ …` (the pointer words at 0x1000/0x1008 pin `avf 0 = 0x9a8`, `avf 1 = 0`, hence `na = 1`; `copyinstr_got` at 0x9a8 with `init_ro` pins the string) -- from `uheap_ubytes_at` on `init_argv`, (iii) `pinned_exec_bundle fsc_fs uslot era0_sh_pins T ROOTINO sh_path [ROOTINO; SH_INO] SH_INO sh_elf 1 Pay M 0x9a8 0x1000 fdv` whose constructor wand is sh's, with ROOM discharged by `init_args_det` (na = 1, alen 0 = 2 → `kxc_sp_final` is `kexec_sz sh_elf` minus a few words; compute), then `sbundle_exec_intro`. The trapframe words: `uvis_of_run m pc M pm sz fdv c` puts `m`'s a0/a1 at `tf_arg_idx 0/1`, and init's a0/a1 are pinned by the leaves before the ecall.
D-d. THE BRIDGE. `init_slot_of_kexec` takes the D-c ingredients and `uvis_cwd W' = FsImg.ROOTINO` (userinit's `namei("/")` -- ARM-c (1) discharges it) and drops `uxsup`; it discharges D-a's byte premises from the image fact.
D-e. SANITY. `grep -n uxsup iris/UInitKernel.v iris/UkInitMain.v iris/UkInit.v` names nothing; `grep -rn "uxsup" iris/*.v` names only UkRun's definition/adapters, UShKernel (sh's own exec of echo -- L6-SH, not yours) and UexecCond/ProofForkret (ARM-c (1)'s); `grep -rn "exec_au_pre_triv" iris/*.v` names SpecKexec's definition/header and ProofForkret's boot arm only.

=== PHASE 1 -- then STOP AND REPORT ===
D-a's `init_argv_map`/`init_argv` and the carve change as STATEMENTS with the persist step proved; D-b's leaf change; D-c's premise bundle `init_sh_slot T Pay` decided against `sh_slot_of_kexec` and stated; `init_args_det` STATED (proved if it closes); D-d's statement. NO Admitted in the tree at report time. Build with -k. Report: (1) `init_argv`, `init_sh_slot` and `init_slot_of_kexec`'s new statement, quoted; (2) how the child gets `init_argv (ukn_d N')` (the Forkable route, file:line) and where the bundle is built (the exec site, file:line); (3) `init_args_det`'s statement and whether ROOM closes from it (the arithmetic); (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs, the six UkInitMain lemmas, the bundle at the exec site, D-e. Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build and report exactly what remains, file:line.
