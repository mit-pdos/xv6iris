You are an APPLICATION/U-tier lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-sup (a sibling clone; never touch or build /shared/xv6iris-2). START FROM BRANCH `lane/supply-split` (= `d2708e5b7`, green, not yet on main) and work on a new branch `lane/sh-open` on top of it. Your lane is SH-OPEN: sh's console preamble -- `while ((fd = open("console", O_RDWR)) >= 0) { if (fd >= 3) { close(fd); break; } }` (xv6-riscv/user/sh.c:152-157, read-only at /shared/xv6iris-2/xv6-riscv/user/sh.c) -- is proved through a PINNED open of the path "console", so the deposit `sh_deps`' 15 conjunct (`UkRun.udepw_law 15`) is discharged and sh's entry arm records what the preamble ESTABLISHES: with the console node present, descriptors 0,1,2 are the console device afterwards whatever init left (init's open having failed only makes sh's loop run three more times); with the node absent, every open returns -1, the ledger stays as it was, and sh proceeds to `getcmd` whose `read(0)` fails and sh exits. Design of record: claude-notes/projects/app-echo.md "OPEN-PIN FINDINGS -- RULED WITH THE OWNER" (FACT 1-3), "OPEN-PIN COMPLETE" (init's pinned open: the mould), "SUPPLY-SPLIT PHASE 2 GREEN" (`sh_deps`), "PINNING -- THE OWNER'S RULING", "GENERIC-PAY LANDED"/"SH-LINE PHASE 2a LANDED" (sh's entry: `ush_fd0`, `sh_uexec_slot`), and `review-echo-plan-2026-09-12.md` finding 9. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the new "`iIntros "#H"` on a bundle" note); remote-build-gcp.md; design/user-fd.md; iris/UkSh.v (`sh_deps` :211; `wp_ksh_ostub` :532 -- the open stub relaying `ualloc`'s disjunction; `wp_ksh_open` :673; `wp_ksh_close` :714; `ush_fd0` :4227 (console / slot-0-closed / taint); `wp_ksh_start` :5750 and the preamble's walk from there: find the loop's lemmas and what they assume today about the four possible opens and the close); iris/UShKernel.v (`sh_uexec_slot`/`sh_slot_of_kexec`: what sh's entry takes; `ush_read_leaf_of_win`); iris/UInitFd.v (`ufd_head T st γfd` -- init's three-arm head that sh's entry receives; `ufd_head_open`, `ufd_head_row`); iris/UkInit.v (`uki_open_console_leaf`, `uki_open_absent_leaf`, `init_cons_leaves` -- init's open leaves over `T`/`K`; `init_deps`); iris/UInitConsK.v (THE MOULD: how init's open of "console" is paid at echo's era -- the path read off the loaned heap, the pinned `xfam`s, the three `udepwf_at` suppliers fixed at the root, the dead-walk families carrying the credential, `init_cons_leaves_echo`); iris/UInitCons.v (`init_cons_laws`, the nine laws; the claim's two states `cons_absent` / `cons_present_at i` with `cons_made r i`; `cons_key`); iris/FsConsPin.v (`cons_absent`, `cons_present_at`, `cons_dev`, `era0_cons_absent`); iris/PinnedOpen.v + iris/PinnedObs.v (the pinned open family: hit arm / miss arm; `pin_misses_at`, `pobs_P_dead`); iris/ArgPath.v (`arg_path_of M pv pl`: the path argument read off the image); iris/UkRunSys.v (`wp_uk_ecall_open_recv` :~2889 and `_img` :~3155 -- the receipt-keeping open leaves at the TRAPPING key: the ledger row and the cwd; `wp_uk_ecall_dup_closed`; `wp_uk_ecall_close*`); iris/UkRun.v (`udepwf_at` :~633 -- the deposit fixed at a path and cwd; `udepw_law`, `udepw_of_law`); iris/UserFd.v (`ualloc`, `ustd`, `fd_lowest_closed`); iris/AppEcho.v (`echo_pred`'s console arm; `echo_taint`; the seven claim laws); the program's binary: sh's preamble code addresses (find them in UkSh's instruction facts `uis_sh_*` and the disassembly the file cites) and its rodata "console" string (`arg_path_of` needs the bytes at the argument's address, as `UInitConsK` reads init's at 0x970).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-sup <logname>` FROM /shared/xv6iris-2-sup (names `shopen10`, `shopen11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-sup` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter. Kernel files (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`, `UexecExecInst`) are off limits; a needed kernel change is design friction: STOP AND REPORT with the goal quoted. The U tier and the application files (`UkSh*`, `UShKernel`, `UInit*`, `UConsLine`, `FsConsPin`, `PinnedOpen`, new `USh*.v` files) are yours. GIT: branch `lane/sh-open` off `lane/supply-split`; commit there when green; never push; never touch `main` or `lane/supply-split`; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NO placeholder arms; every arm of the preamble is sh's verified code.

=== THE DESIGN ===
H1 THE PREAMBLE'S ARMS, from the C and the ledger.  sh's entry ledger is init's head (`ufd_head`'s three arms handed to `ush_fd0`): (i) 0,1,2 all console (init succeeded); (ii) 0,1,2 all closed (init's open failed); (iii) the taint. The console node is PRESENT after init's mknod arm or ABSENT (init's mknod failed) -- `FsConsPin.cons_present_at i` with `cons_made r i`, or `cons_absent`. The preamble under each: PRESENT ∧ (i): the first open returns 3 (lowest free), sh closes 3, breaks -- ledger unchanged. PRESENT ∧ (ii): opens return 0, 1, 2 (each `ualloc` at the lowest closed standard stream), then 3 → close → break -- ledger: 0,1,2 console, (i)'s shape. ABSENT: the first open returns -1 (the path does not resolve: a PINNED MISS, `pin_misses_at`/`pobs_P_dead` as `UInitConsK`'s dead walk) -- ledger unchanged; sh falls into `getcmd`. TAINT: the generic continuation (as SH-LINE 2a's `ush_gen_run` at the taint). State the preamble's post as ONE head: `ush_fd0`'s console arm ∨ (⌜absent⌝ ∗ the closed ledger) ∨ taint -- i.e. the CLOSED arm is now "node absent" rather than "init's open failed", and under the console arm sh's fd 0 IS the console device whether or not init's open succeeded.
H2 THE PINNED OPEN.  sh's open of "console" is paid like init's second open (`UInitConsK`'s `uki_open_console_leaf` discharge): the path read off sh's own rodata via `ArgPath.arg_path_of`, a pinned `xfam` at the PRESENT arm (`cons_present_at i` + `cons_dev`), a `udepwf_at` supplier fixed at sh's cwd (root: sh does not chdir before the preamble; the cwd row comes from the head), the receipt-keeping leaf `wp_uk_ecall_open_recv_img` (or `_recv`) handing back the ledger row, so `ualloc`'s disjunction is replaced by the EXACT row (the lowest closed standard stream or 3). At the ABSENT arm the miss family (the dead walk with the credential -- reuse `UInitConsK`'s `cons_*_dead` family or factor it into `UInitCons`/a shared `UConsOpen.v` so init and sh are two instantiations: the PINNING ruling asks for factoring before a second instance). The deposit `udepw_law 15` disappears from `sh_deps` (sh no longer routes open through the generic law); `sh_deps` becomes `udepw_law 5 ∗ udepw_law 16` (2b / E5).
H3 WHAT SH'S ENTRY TAKES.  The preamble needs the claim's state: `UInitCons.init_cons_laws`-like laws at sh's altitude (the persistent claim laws are the same nine; sh does NOT hold the key `cons_key` -- init keeps it -- so sh's open at the PRESENT arm reads `cons_made r i` (persistent) and never needs the key; at the ABSENT arm sh needs a credential to refute the PRESENT arm of the claim: what is it? `cons_absent` is the claim's own state, read through `app_inv`; if refuting PRESENT needs the key (`cons_key` is what refutes the PRESENT arms in init's first open), then sh's absent arm cannot be proved to MISS -- report exactly what the absent arm needs; a persistent "the era's console was never made" fact minted when init's mknod FAILED may be the right resource, and that is E2/init's to mint (say so)). These laws travel to sh through `PinnedExec`'s linear `Pay` / the persistent `init_sh_slot` conjuncts (SH-LINE 2a): state the additions.
H4 SANITY: `grep -n "udepw_law 15" iris/UkSh.v` EMPTY; `grep -n "wp_ksh_ostub" iris/UkSh.v` shows no live caller (delete it or keep as the taint arm's tool -- say which); the preamble's four opens and the close are walked in `wp_ksh_start`'s cone with the exact ledger; the audit unchanged; kernel diff EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
H1's head STATED; H2's pinned open STATED (the supplier, the xfam, the miss family -- factored if you factor); H3's laws STATED; the preamble's walk compiling against them (old proofs where the statement did not change; NO Admitted -- leave a hard statement out and report it). Report: (1) the statements verbatim, file:line; (2) the preamble's addresses and the rodata "console" bytes, and how `arg_path_of` reads them in sh's image; (3) what the ABSENT arm needs to refute PRESENT and whether sh can have it; (4) the red files; (5) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs, H4, the gate, commit on `lane/sh-open`. Report log name, commit hash, `git diff --stat lane/supply-split..lane/sh-open | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), anything owed with file:line.
