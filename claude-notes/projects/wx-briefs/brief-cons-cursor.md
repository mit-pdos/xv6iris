You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is CONS-CURSOR, the kernel half of E3: the console ring learns the ORDER of the bytes it stores and a CONSUMPTION CURSOR whose program-side half is the console-reader token; consoleread's receipt becomes "the d-byte window at the cursor", and it reaches a user-tier read leaf that HANDS IT TO THE PROGRAM instead of dropping it. Design of record: claude-notes/projects/app-echo.md "E3 — THE INPUT LINE: DESIGN PROPOSAL" (R1-R5, with R4's owner ruling) and "THE REMAINING ARC …" (E3); read also "L5 — findings for the design" and "L5-b LEDGER (LANDED)", and claude-notes/completed/uart-trace.md + console.md. The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Contracts and resources" -- especially "An invariant that has to be re-based must state that its body OWNS its own map" and "An invariant taking an EXCLUSIVE ghost fragment across a sleep must RECORD its value"); remote-build-gcp.md; iris/ConsoleInv.v (whole: `cons_ok` :174, `cons_slot`/`cons_row` :181-191, `cons_tagged` :199-204, `cons_data` :601, `cons_tags` :613-615 + `cons_tags_upd`/`_get`/`_none`, `cons_res` :665-674, `is_conslock` :723, `console_inv` :754); iris/SpecConsoleintr.v (:106-109 `console_caps`, :158-161 the tag premise) + ProofConsoleintr.v (the three store sites ~1403/2155/2643 and the ring-full DROP branch); iris/SpecConsoleread.v (:79-85 the "not promised" note, :157-216 the contract) + ProofConsoleread.v; iris/WpUart.v (`uart_rx_tok`/`uart_rx_popped`/`uart_rx_pushed_lb` :616-624, `uart_col_ok`/`uart_col`/`uart_colE` :681-696, `uart_colE_push` :727-732, `uart_col_poll` :762, `uart_col_pop` :789-795, `uart_colE_flush` :820, `plic_payload` :922, `uart_rx_tok_deposit` :1210, the boot mint :1258, `uart_obs_permit_ledger` :1380 and its rx wand :1392-1396, the push site in `wp_uart_loop` :1510-1517 where the order is TRUE and then discarded); iris/UartNames.v :36-52; iris/ObsTrace.v (`obs_ends_in` :225, `ins`/`open_seg`/`trace_shape`); iris/SystemAdequacy.v (`obs_ledger_at`, `power_interp` -- where the trace history `h` lives as a ghost, how the UART loop reaches it); iris/SpecFileread.v (`console_receipt` :835-848, `console_receipt_of_run` :863-878, `fileread_extra` :880-889, the console arm :355-366/:784-790); iris/SpecSysRead.v (:285-303); iris/UexecExecInst.v (`xv6_spost` :524-530, `spost_at_read_intro`); iris/UkRun.v (`udepw` :292-300, `udepw_of_psok` :303-309); iris/UkRunSys.v (`wp_uk_ecall_read` :1383-1399, `wp_uk_ecall_window` :1937-1966 -- the `_` at :2022 is the dropped post, `wp_uk_ecall_read_win` :2399-2417); iris/BootCarveMain.v (:535-551 the ring's boot shape); iris/BootShared.v/BootChain.v/SpecMain.v/ProofMain.v (how `is_conslock` goes up: `newlock` for cons.lock).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 (log names `conscur1`, `conscur2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal; `WpUart` has ~306 dependents and `ConsoleInv` many -- batch the statements before the first build. Gate: zero `Error`, `EXIT=0`, COMPILED>0; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` with every line justified (sealed Parameters retyped in place listed by group; nothing Admitted; no new Axiom/Parameter); `make vtest-check-ci` unaffected (you do not touch device models). GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== THE DESIGN ===
C1 THE ORDER OF QUEUED HISTORIES (UART).  `uart_col_ok u hs np nk` gains the CHAIN: `∀ j h h', hs !! j = Some h → hs !! (S j) = Some h' → h ⊏ h'` (strict prefix -- `h' = h ++ …` with `obs_ends_in h' _`), AND a last-popped anchor so the chain survives an empty queue: the popper's token becomes `uart_rx_tok γ k hl` ("k bytes popped; the last popped history is hl", or `None` at boot) with `∀ h, hs !! 0 = Some h → hl ⊏ h`. The PUSH must know that the last queued (or last popped) history is a prefix of the ledger's CURRENT `h` (the loop pushes `h ++ [ObsUartIn b]`). That is "histories only grow" AS A RESOURCE: find where the trace history `h` lives (the obs ledger, `ghost_var γobs (1/2) h` in `power_interp`/`obs_ledger_at`) and give it a persistent lower bound -- a `mono_list` (`mono_list_lb_own` at `h0` says `h0 ⊑ current h`) stepped beside the ghost_var at every event, so the column can hold `obs_hist_lb h_j` for each queued history and the push derives `h_last ⊑ h`. PREFER the mono_list lb (general, persistent, no lockstep pairing); the alternative -- a ghost_var pair tying the column's `np` to `length (ins h)` -- is acceptable if the lb cannot be threaded; report which and why. The pop then yields `∃ h, ⌜obs_ends_in h bt⌝ ∗ ⌜hl ⊏ h⌝ ∗ riscv_rx_tag h ∗ obs_hist_lb h` and the token at `(S k, Some h)`. `uart_colE_flush` (the FCR flush) pops everything: the anchor becomes the last flushed history. `plic_payload` parks the token at `∃ k hl`.
C2 THE RING'S STORED SEQUENCE AND THE CURSOR (console).  `cons_res` gains (a) an append-only ghost list `stored : list (list mobs * bv 8)` (`mono_list_auth_own` at a canonical name on the console's class or a new one in `Xv6Cameras` -- the `wch_name` precedent; the lower bound `mono_list_lb_own` is what a receipt hands out), (b) a CONSUMED cursor `ghost_var γrd (1/2) n` (the other half IS the reader token `cons_reader n`), (c) the pure ties: `cons_row r e bs ts` is restated so slot `cons_slot r k` holds `stored !! (n + k)` for `k < e - r` (the k-th unconsumed byte is the (n+k)-th stored one; its tag column entry is its history), `⌜length stored = n + (e - r)⌝` -- decide whether `e` or `w` is the right end (the ring's `w`..`e` are the not-yet-newline-terminated bytes consoleread cannot see: consoleread reads up to `w`; consoleintr stores at `e` and bumps `w` at a newline -- state `stored` over what consoleintr STORED (up to `e`), and let the receipt read up to `w`; check `cons_ok`), and (d) the chain of histories along `stored` (strict prefix), inherited from C1: consoleintr's store premise carries `⌜hl ⊏ h⌝`-shaped evidence that this byte's history extends every stored one -- keep in `cons_res` the ghost half `cons_hi (1/2) hh` (the last stored history) with the other half beside the rx token in the PLIC payload and the pure `⌜hh ⊑ hl⌝` there (the last stored is a pop, so it is at or before the last pop) -- or derive it from `obs_hist_lb` of the last stored history against the popped `h` if the lb suffices (PREFER the lb route: no second ghost). Backspace and Ctrl-U remove bytes from `stored`'s TAIL beyond `w` (they move `e` back): model `stored` as the bytes at or below `e` and let those two edits POP its tail (a `mono_list` cannot shrink -- so either `stored` covers only the committed prefix up to `w`, with the `w..e` editable window kept as today, or use an ordinary `ghost_var` list; DECIDE: PREFER `stored` = the committed bytes up to `w`, appended by the newline/`w := e` step and by the ring-full-flush/Ctrl-D paths -- read the C and ProofConsoleintr's cases and report exactly which transitions extend `stored`). Boot: `stored = []`, `n = 0`, `cons_reader 0` minted in the boot fupd beside the ring (BootShared/BootCarveMain; the token travels to main and, THIS LANE, is returned to the top of the boot chain and DROPPED in SystemAdequacy with a note -- E2/R5 route it into `init_boot_bundle`).
C3 THE RECEIPT.  `SpecConsoleread`: the caller passes `cons_reader n`; the post returns `cons_reader (n + d)` and `⌜cons_window stored_lb n d bs hs⌝`-shaped facts: the d bytes are `stored !! (n + j)` (via a `mono_list_lb_own` of `stored` handed out), their histories `hs !! j`, `cons_tagged` as today plus the chain. `SpecFileread.console_receipt` and `fileread_extra`'s console arm take the token in and out (the console arm is no longer `emp` in `fileread_in`: it takes `cons_reader n`; a caller that has no token cannot read the console -- state that this is the one place the ownership is spent); `SpecSysRead`, `xv6_spost … 5`, `spost_at_read_intro`, and the dispatcher's read arm relay it. THE NEW LEAF: `UkRunSys.wp_uk_ecall_read_recv` (beside `wp_uk_ecall_read_win`) takes `cons_reader n` (as a program resource -- decide its user-tier spelling; it is the same ghost half) and returns, in its continuation, `cons_reader (n + d)`, the window fact and the tags `[∗ list] h ∈ hs, riscv_rx_tag h` -- routed through `udepw`'s EXPLICIT disjunct (`UkRun.v:299`) with a deposit at the process's key that pays the read's `spost_at` shape (find how init's mknod leaf or the exec deposit builds an explicit `sbundle`; the read's family is `rf_F f` at `xv6_spost 5`). sh's use of it is the NEXT lane (SH-LINE); this lane proves the leaf and leaves every existing caller on the old leaves.
C4 SANITY: `grep -n "cons_reader\|stored" iris/ConsoleInv.v iris/SpecConsoleread.v iris/SpecFileread.v iris/UkRunSys.v`; `grep -n "obs_hist_lb\|⊏" iris/WpUart.v`; the three existing read leaves unchanged in statement; the audit unchanged; `make vtest-check-ci` unaffected.

=== PHASE 1 -- then STOP AND REPORT ===
C1-C3 as STATEMENTS (the chain and anchor in the column; the "histories grow" resource and where it lives; `cons_res`'s new conjuncts and the exact transitions that extend `stored`; consoleread's/fileread's/sys_read's/`xv6_spost`'s receipt; the new leaf's statement); NO Admitted at report time. Build with -k. Report: (1) `uart_col_ok`'s chain, the token's new shape and the "histories grow" resource, quoted, with where it lives and how the push uses it; (2) `cons_res`, quoted, and the list of consoleintr transitions with what each does to `stored`/`n`/`hi`; (3) consoleread's new post and `console_receipt`, quoted, and the new leaf's statement; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line) -- in particular the `w`/`e` question and the backspace/Ctrl-U handling.

=== PHASE 2 -- after go-ahead ===
The proofs (the UART loop's push/pop/flush, consoleintr's stores and edits, consoleread, fileread, sys_read, the dispatcher's read arm, the new leaf, boot, every red file), C4. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 20 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
