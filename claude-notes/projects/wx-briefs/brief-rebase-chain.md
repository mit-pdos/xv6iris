You are a MAINTENANCE-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2 (the MAIN checkout, clean at the current main). Your lane is REBASE-CHAIN: land two parked green branches onto the current `main` (which has just landed LAZY-FLAG: 78 files, the stored flag, the two rows on `SpecKexec.exec_slot_pre`'s wands `⌜uvis_cwd W' = cw⌝ -∗ ⌜uvis_lazy W' = false⌝ -∗ …`, `UkRun`/`UexecSG`/`UexecExecInst`/`UkRunSys` reworked). Read first: claude-notes/projects/app-echo.md "LAZY-FLAG LANDED", "SUPPLY-SPLIT PHASE 2 GREEN ON ITS BRANCH", "SH-OPEN PHASE 2 ON ITS BRANCH", "SH-OPEN PHASE 1 + TWO RULINGS" (ruling (B): SH-OPEN stated its consumers with the cwd row as the inner wand premise in exactly LAZY-FLAG's position); durable-notes.md ("Build", "Shared-checkout discipline", "The dev loop", the bundle-intro note, the never-collapse-tokens note); remote-build-gcp.md ("Sharing the VM": builds queue).

THE BRANCHES (fetch them read-only from the sibling clones): (1) `lane/supply-split-r1` = 69543d942 in /shared/xv6iris-2-disc (`git fetch /shared/xv6iris-2-disc lane/supply-split-r1:lane/supply-split-r1`), 31 files, based on the pre-LAZY-FLAG main; expected conflicts in UkRun.v, UexecSG.v, UexecExecInst.v, UInitSh.v (four files LAZY-FLAG also changed) -- both edits must survive (SUPPLY-SPLIT: `free_num`, `uprogSG_free`, `udep_free`, `Hpsok_free`, `udepw_law n`, `init_deps`/`sh_deps`, the deleted `wp_kshr_qstub`; LAZY-FLAG: the `lz` threading, `uslot_of_urun*`'s `uvis_lazy W = false` premise, row 5's tie, `uvis_of_run`'s bit). (2) `lane/sh-open` = ba3249c96 in /shared/xv6iris-2-sup (`git fetch /shared/xv6iris-2-sup lane/sh-open:lane/sh-open`), 11 files on the OLD supply-split d2708e5b7; its one red seam `UInitSh.v:567` (the cwd row) CLOSES now that the kernel row exists -- after rebasing it onto the rebased supply-split, `UInitSh` must compile with no restatement (the row is threaded through `PinnedExec.pex_slot`'s constructor wand as LAZY-FLAG left it).

RULES: `cd /shared/xv6iris-2`; `git status --porcelain -- iris/` must be EMPTY at start (if not, STOP and report). Work on branches `lane/supply-split-r2` (= r1 rebased onto main) and `lane/sh-open-r2` (= sh-open rebased onto supply-split-r2); resolve conflicts by hand keeping both sides' intent; NEVER a tree-wide textual substitution. BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 rebase20` (then rebase21, …) from the main checkout; ONE build at a time; never local coqc; never a remote top-level make. Gate EACH branch: zero Error, EXIT=0; VM `make audit-only` from /mnt/rocq/trees/_shared_xv6iris-2 = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref main` justified (expected: SUPPLY-SPLIT's known items -- 22 `Hpsok` GONE / 18 `Hpsok_free` NEWAXIOM, `wp_kshr_qstub` deleted -- then SH-OPEN's: 23 lemmas MOVED from UInitConsK into UConsOpen, `wp_ksh_ostub`/`wp_ksh_open` and UConsLine's `ush_gen_slot`/`_run` deleted); nothing Admitted; no new Axiom/Parameter; `grep -rn "Hclw\|ushd_clw_text_ty\|Hpsok\b" iris/*.v` only ProofMain's unrelated `Hclw`; `grep -n "udepw_law 15" iris/UkSh.v` a comment only. Never push; never touch `main` itself (the coordinator fast-forwards it); never stash/reset/add -A/commit -a/amend outside the rebase's own mechanics; no other checkouts (read-only fetches from the siblings are fine); do not edit claude-notes/. Leave the checkout ON `lane/sh-open-r2` with a clean tree when done.

REPORT: per branch -- the conflict resolutions (file, what each side wanted, what you kept), the final commit hash, `git diff --stat main..<branch> | tail -1`, the log name, the audit verbatim, lemma_diff justified; and confirm `UInitSh.v` compiles with the cwd row supplied (quote the two lines that supply it).
