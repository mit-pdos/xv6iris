You are a KERNEL-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel) in /shared/xv6iris-2 (the MAIN checkout, on branch main, clean). Your lane is OUT-FUPD, the owner's redesign of the UART output side. THE DESIGN, in the owner's words: "there will be some pure theorem about the raw bytes, tagless. That theorem will apply to the bytes output so far, maintained by the invariant. Every output to the UART will open that invariant and extend the sequence. All UART output needs a fupd to justify outputting. Ditch the uart_bytes_sub or whatever other resource we currently had for the append-only prefixes of the UART output sequence -- that's not a useful way to think about UART output in the application setting." Design of record: claude-notes/projects/app-echo.md "THE OWNER'S REDESIGN OF THE UART OUTPUT SIDE" (read first), "OWNER'S RULING ON PRINTK'S CONTRACT" + its CLARIFICATION, "TX-TAG LANDED" (what is now to be retired), "E5 -- THE OUTPUT SIDE" O1-O3 (the claim `good_out` this predicate will carry; O4's tag mechanism is WITHDRAWN), "DISC-RATE LANDED" (`EchoDisc.good_out`, `disc`, the per-cycle input); `review-echo-plan-2026-09-12.md` findings 3-5. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1 -- phase 1 includes a TRUSTED-STATEMENT DIFF the owner reviews.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the bundle-intro note, the never-collapse-tokens note, the dirty-bottom-file note, the pinned-C note); remote-build-gcp.md ("Sharing the VM"); iris/WpUart.v (the UART invariant: `uart_col_ok` (the receive column: per received byte its history `hs`, the LOOP-off clause, the new `u_wire u = u_out u`), `uart_colE`, `uart_ghosts` (incl. TX-TAG's `uart_tagsE` -- to be retired), `dev_inv`, `uart_sent`/`uart_sent_auth`, `uart_tx_own`, the device threads `wp_uart_loop`, the permit `uart_obs_permit_ledger`, `uart_pop_tag` (to be retired); how `riscv_rx_tag` (a class field) enters the rx wand -- the mould for fixing an application predicate); iris/RiscvPtsto.v (`riscv_rx_tag`, `riscvGS`'s fixed fields -- how the application's tag family is FIXED at boot: `boot_fixedGS … (app_tag A c)` in SystemAdequacy); iris/App.v (`xv6_app`: `app_R`, `app_tag`, `app_phi`; `Htx`/`Hrx`: the per-byte hooks the trace theorem fires at a pop/push with `uart_ghosts γ u'` in hand); iris/SystemAdequacy.v (`xv6_power_adequacy_gen`: where the fixed instance is built, where `Hperm` hands the permit; `xv6_boot_era`); iris/RiscvAdequacy.v (`obs_ledger_at`, the ledger glue); iris/DevModel.v (`uart_acc u = u_out u ++ u_tx u`; `uart_write_thr_acc`: the ONE transition that grows the accepted sequence); iris/WpSconfUartAccess.v (`wp_uart_thr_write_s_sconf` :~327 -- the ONE lemma modelling a store to THR; it currently takes TX-TAG's `src` and `tg0`); its THREE callers: iris/ProofUartPutc.v (:~362, uartputc_sync -- printk's and consputc's path), iris/ProofUartwrite.v (:~1130), iris/ProofUartwriteLoc.v (:~1106); iris/SpecUartPutc.v, iris/SpecConsputc.v, iris/SpecPrintint.v, iris/SpecPrintk.v (`printk_env`, `pr_res`, the full contract and the GEN corollary), iris/SpecConsoleintr.v (the echo through consputc, `console_caps`), iris/SpecUartwrite.v + iris/SpecUartwriteLoc.v, iris/SpecConsolewrite.v (`cons_sent_cnt*`), iris/SpecFilewrite.v (the console arm consuming the receipt), iris/UartTxInv.v (`tx_res`, `is_txlock`, `uart_sent_sub*`), iris/UartSentLoc.v (`uart_sent_from*`, `tag_proj`, `uart_sent_exact_at` -- all to be retired), iris/SystemUartAccepted.v, iris/UartAccepted.v (the pure step invariant of the language about acceptance); iris/EchoDisc.v (`good_out`, `sess`, `disc`, `k_done`, `cycles_of`: the application-side shape the predicate must be able to carry); iris/ObsTrace.v (`obs_wire`, `open_seg`, `cycles_of`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (names `outf10`, `outf11`, …; run_in_background; one at a time; detached; the VM is shared -- builds queue; if a dirty bottom-of-tree file yields bogus "Cannot find library" failures, re-run once). `WpUart`/`DevModel` are at the bottom of the tree: batch statements, expect ~1000-file rebuilds. Gate (phase 2): zero Error, EXIT=0; VM `make audit-only` from /mnt/rocq/trees/_shared_xv6iris-2 = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. Files: the UART/console/printk layer named above, `RiscvPtsto`/`WpUart`/`App.v`/`SystemAdequacy.v`/`RiscvAdequacy.v` ONLY for fixing the predicate at boot and the hooks (the trusted statement: every change listed with a justification, phase 1 reports the diff and the owner approves before phase 2 proofs); `ConsoleInv`, `FsAbs*`, the U tier, `AppEcho.v`/`EchoDisc.v` off limits (report what the application will instantiate). GIT: DO NOT COMMIT OR STAGE (the coordinator commits by path); never stash/reset/add -A/commit -a/amend/checkout -- <file>; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NO placeholder arms; NO premises nobody can discharge. NEVER a tree-wide textual substitution.

=== THE DESIGN ===
F1 THE PREDICATE.  An application-FIXED pure predicate `out_ok : list (list mobs) -> list (bv 8) -> Prop` (the receive column's histories, i.e. the input so far with its provenance, and the accepted output bytes) -- fixed the way `riscv_rx_tag` is (a class field of the fixed instance, instantiated by the theorem at the application's choice; `app_triv` at `fun _ _ => True`). The UART invariant carries `⌜out_ok hs (uart_acc u)⌝` beside `uart_col_ok`. Report exactly how `riscv_rx_tag` is fixed and mirror it (`riscv_out_ok`), and what the trace theorem needs so that `out_ok` on the ACCEPTED sequence yields `app_phi` on the WIRE (`u_wire u = u_out u`, `uart_acc = u_out ++ u_tx`, so the wire is a prefix of `acc`; a prefix-closure premise on `out_ok`, or `Htx`'s hook reading `out_ok` off `uart_ghosts` at each pop -- propose the minimal statement change; the application's `good_out` is prefix-closed in the output).
F2 THE STORE TAKES A VIEW SHIFT.  `WpSconfUartAccess.wp_uart_thr_write_s_sconf` loses `src`/`tg0` and takes
   `Ψ : ∀ (hs : list (list mobs)) (acc : list (bv 8)), ⌜out_ok hs acc⌝ ={⊤ ∖ ↑uartN, ?}=∗ ⌜out_ok hs (acc ++ [sb])⌝ ∗ Φ`
   (choose the masks from where the leaf already opens the UART invariant; the caller's own invariants must be openable inside Ψ -- say which mask discipline you land and why); the leaf invokes Ψ exactly once, at the store, with the invariant's current `hs`/`acc`, and returns `Φ`. Persistent facts a writer may also want at that moment (the current `acc`'s length or content, for its own private witness) come back INSIDE Ψ's post -- that is the caller's business. The three callers thread the caller's Ψ (per byte -- a multi-byte write threads a per-byte family `Ψ i`, or a single Ψ over the run: propose the shape that keeps uartwrite's loop simple).
F3 THE WRITERS' CONTRACTS.  `uartputc_sync`/`consputc`/`printint`/`printk`: take the caller's Ψ (printk's full contract: one Ψ per byte of the rendered message, or a run-shaped Ψ over the whole message -- the owner's ruling is that printk appends its MESSAGE through a fupd; with rendering pinned (lane PRINTK-RENDER, in flight elsewhere) a message-shaped Ψ `∀ hs acc, ⌜out_ok hs acc⌝ ={…}=∗ ⌜out_ok hs (acc ++ cs)⌝ ∗ Φ` is what a caller wants -- but the STORE is per byte, so the leaf-level Ψ is per byte and printk's contract derives the message shape by iterating; state both and the bridge); the GEN corollary and the kernel's own sites get their Ψ from `printk_env` (an era appender `□ Ψ_era`, abstract, fixed at boot: the kernel never invents a justification); `consoleintr`'s echo: its Ψ comes from the console's environment (`console_caps`) the same way, fixed at boot by the application; `uartwrite`/`consolewrite`/`filewrite`/`sys_write`: thread the process's Ψ from the dispatcher (row 16's family field `wf_Q`/`wf_tr0` become the caller's Ψ/Φ pair -- READ how row 16 is stated and propose the replacement; the U tier's write leaf will pass the program's Ψ).
F4 RETIRE.  `uart_sent_sub`, `uart_sent_from*`, `cons_sent_cnt*`, `uart_sent_exact_at`, `tag_proj`, TX-TAG's `txsrc`/`un_tag`/`uart_tagsE`/`uart_sent_tagged`/`uart_tag_at`/`uart_pop_tag`/`_at` variants, the `TxK/TxE/TxW` threading: remove them where nothing else needs them (the device model's own `uart_sent`/`uart_tx_own` stay if `uart_col`/`SystemUartAccepted` need them -- say what stays and why). `SpecConsoleintr`'s echo post keeps its pinned `cs = [echo_of cb]` fact but expressed through its Ψ's post. The boot founds `out_ok hs [] ` at `[]`/`[]` -- the application must make `out_ok [] []` true (report the obligation the trusted statement will carry, like `HR0`).
F5 SANITY (phase 2): `grep -rn "uart_sent_sub\|uart_sent_from\|cons_sent_cnt\|TxK\|TxE\|TxW\|uart_tag_at" iris/*.v` EMPTY; `grep -n "out_ok" iris/WpUart.v iris/WpSconfUartAccess.v iris/SpecPrintk.v iris/SpecConsoleintr.v iris/SpecConsolewrite.v` shows the predicate, the store's Ψ and the three writers; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT (no proofs beyond the free ones; NO Admitted; leave a hard statement out and report it) ===
F1's fixing of the predicate (the trusted-statement diff in `RiscvPtsto`/`App.v`/`SystemAdequacy.v`, verbatim, each change justified as `APP-IFACE` did), F2's leaf statement, F3's contracts as statements with the callers compiling (old proofs where nothing moved; the per-byte/run Ψ bridge stated), F4's retirement list with what stays. Report: (1) the statements verbatim, file:line; (2) the trusted-statement diff with justifications; (3) the mask discipline; (4) the per-byte vs message shape and the bridge; (5) the red files; (6) anything this brief got wrong, with file:line.
=== PHASE 2 -- after the owner's approval of the diff ===
The proofs (the leaf, the three callers, printk's cone at the pinned rendering, consoleintr's echo, uartwrite/consolewrite, the boot founding), F4's removals, F5, the gate. Report as usual, with an as-landed note (≤ 20 lines) and what the U-tier write leaf and the application (E5) will instantiate.
