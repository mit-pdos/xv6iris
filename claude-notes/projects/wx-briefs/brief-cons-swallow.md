You are a KERNEL-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel) in /shared/xv6iris-2. Your lane is CONS-SWALLOW: consoleread's window contract names the byte it pops and does not deliver, and copyout's contract says WHEN it fails. Design of record: claude-notes/projects/app-echo.md "SH-LINE PHASE 2 -- THE SWALLOWED BYTE" (read it first, all of it), "CONS-CURSOR LANDED"/"CONS-CURSOR RULINGS" (the cursor, the window, the credential arm), "CONS-ROUTE LANDED" (the receipt through fileread/the dispatcher/row 5). The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; xv6-riscv/kernel/console.c (consoleread) and vm.c (copyout, vmfault, walkaddr); iris/SpecConsoleread.v (:180-270 the post; :264 the window arm); iris/ProofConsoleread.v (`cr_racc`/`cr_rout`/`cr_out` :325-392, `cr_pop` and `cr_pop_swallow` :~480-630, `cr_winR_intro` :733, the ^D arm :1825-1900 (`HG : nc >= n` is the nothing-copied test), the copyout-failure break :2495-2540, the delivering round :2130-2350 (`Hts1`/`htag`/`btag`/`Hends` show how a popped byte's history and tag are read off the ring), `cr_ghost` :244 with `cons_stored rr ww cur st bs ts`); iris/ConsoleInv.v (`cons_window` :302, `cons_stored` :262, `cons_tagged` :207, `cons_res` :~1399 (`cons_tags ts`), `cons_stored_lb`/`cons_chain`); iris/SpecCopyout.v (`copyout_wrote` :161; the sconf body :168); iris/ProofCopyout.v (the walkaddr call :1329 and its post `Hwapay`, the vmfault call :1489, `co_loop` :561; `copyout_wrote` sites :495/:548/:600/:2373); iris/SpecWalkaddr.v (:60-110, the PRECISE post: 0 iff `2^38 <= va ∨ um !! vpn = None ∨ ¬pte_vu w`); iris/SpecVmfault.v (:100-140: 0 leaves the table unchanged); iris/SpecEitherCopyout.v (`either_copyout_ran` :109, `either_copyout_post` :129); iris/ProofEitherCopyout.v; iris/UserPtTree.v (`uva_mapped` :210, `umem_lazy` :1245); iris/ProcPtOwn.v (`uptd_ext` :536, `uptd_ext_sz` :2515); iris/SpecFileread.v (`console_receipt` :967, `console_receipt_m1`/`_of_run` :1028/`_of_dirty` :1066, `fileread_extra_core` :1122); iris/ProofFileread.v (:1990-2035 where the receipt is built from consoleread's post); iris/SpecSyscall.v and iris/ProofSyscall.v (:3330 the read arm's `fileread_extra_core` instantiation); iris/UexecExecInst.v (row 5 :495/:588 and :1082); iris/UkRunSys.v (`wp_uk_ecall_read_recv` :2672 relays the receipt); iris/UConsLine.v (`ush_read_recv_leaf` :244, the U-tier consumer -- its window disjunct is what phase 2 of this lane must be able to feed; do NOT edit it beyond keeping it compiling).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (names `swallow10`, `swallow11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. SpecCopyout/SpecConsoleread/SpecFileread sit under most of the tree -- batch statements before each build. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. This IS a kernel lane: the kernel files named below are yours; everything else you touch only to keep it compiling (report each such file). Comments state the current shape as fact. NO PLACEHOLDER ARMS (`∨ True`, `emp` standing for a fact): every disjunct says something.

=== THE DESIGN ===
W1 COPYOUT SAYS WHEN IT FAILS.  `SpecCopyout.copyout_wrote M dstva len src res M'` is unconditional on the -1 arm. Strengthen the -1 arm (a NEW clause beside `d < len ∧ M' = umem_wr M dstva d src`) with `¬ uva_mapped P (uint dstva + Z.of_nat d)` -- stated against the ENTRY table `P` (the sconf body has `P`; `copyout_wrote` will need `P` as a parameter, or a sibling predicate `copyout_wrote_at P …` if that is cleaner for the 4 sites). It is true: the loop fails only where `walkaddr` returned 0 on the round's table `P_i ⊇ P` (`SpecWalkaddr`'s precise post) and `vmfault` returned 0 leaving `P_i` unchanged; the failing va0 = PGROUNDDOWN(dstva + d) shares the page with `dstva + d`; unmapped in `P_i` ⇒ unmapped in `P` (`uptd_ext`). Mind the `2^38 <= va` walkaddr arm (a canonicity failure: `uva_mapped` is false there too since `proc_pt_wf`'s `upt_map_wf` keeps every vpn below the trapframe -- find the lemma or state the bound). Relay through `SpecEitherCopyout.either_copyout_ran`/`either_copyout_post` (user arm; it has `U`: the table is `pv_upt (us_V U)`).
W2 CONSOLEREAD NAMES THE SWALLOWED BYTE.  `SpecConsoleread.v:264`'s `⌜(d <= dc <= d + 1)%nat⌝` becomes
   `(⌜dc = d⌝ ∨ (⌜dc = d + 1⌝ ∗ ∃ (h : list mobs) (b : bv 8), ⌜obs_ends_in h b⌝ ∗ cons_stored_lb cn (sl ++ [(h, b)]) ∗ ⌜cons_chain (sl ++ [(h, b)])⌝ ∗ riscv_rx_tag h ∗ (⌜d = 0%nat ∧ cons_xlate b = (mword_of_int 4 : bv 8)⌝ ∨ ⌜¬ uva_mapped (pv_upt (us_V U)) (uint dst + Z.of_nat d)⌝)))`
   inside the existing window (left) arm -- the credential (right) arm is untouched. (`sl ++ [(h,b)]`: the window's bound has `length sl = cur + d`, so the swallowed byte is the next element; if the proof finds it cleaner to hand back ONE bound `sl'` with `length sl' = cur + d + 1` and `cons_window sl' cur d bs hs` restated over the prefix, say so and do that -- but `cons_window`'s exact-length clause is load-bearing for `UConsLine`'s consecutive-reads story; prefer the `++`.) Proof: `cr_rout`/`cr_out` grow the arm; `cr_pop_swallow` reads the popped entry off `st` at index `cur` (`cons_stored` gives `st !! (cur') = Some (h, b)` for the popped slot, `ts !! cons_slot rr 0 = Some (Some h)` gives the tag as the delivering round does), mints `cons_stored_lb` for the longer prefix from `cons_stored_auth`, and carries the two-case reason: at the ^D site (`:1825`, arm `HG`) `nc >= n` gives `d = 0` and the byte is `cbv = 4`; at the copyout-failure site (`:2495`) the reason is `either_copyout_post`'s new clause (W1) at the round's cursor `dst + d`. Adjust `cr_winR_intro` accordingly. NOTE the swallowed byte's `b` vs the ring's stored `cons_xlate b`: the C compares the RING byte (`cons_xlate b`) to 4.
W3 THE RECEIPT RELAYS IT.  `SpecFileread.console_receipt Rd r M' addr` gains the table: `console_receipt P Rd r M' addr` (fileread's spec has `U`; instantiate `pv_upt (us_V U)`); its window arm's `⌜(d <= dc <= d + 1)%nat⌝` becomes the W2 disjunction, restated at the receipt's own vocabulary (`sl`, `cur`, the tag, `⌜¬ uva_mapped P (uint addr + Z.of_nat d)⌝`). `console_receipt_of_run`/`_of_dirty`/`_m1` follow; `ProofFileread` :1990-2035 relays. `fileread_extra_core`/`fileread_extra` gain `P` the same way (or take it from `U` where they already have it -- read the section's binders).
W4 ROW 5.  `UexecExecInst`'s read row (:495/:588/:1082) states the receipt over the KEY `W`, which has no table: state it as `∃ P : uptd, ⌜perm_of (ud_um P) (uvis_sz W) = uvis_perm W⌝ ∗ console_receipt P (rf_ret f) r M' (xk_a W 1)` (the dispatcher, `ProofSyscall.v:3330`, has `U` and the projection equation from the key former -- find it in `UexecRet` (:394 `uvis_perm Wk = perm_of …`) or wherever the read arm gets `π' = π`). This is deliberately the honest weak form: a later lane (MAP-KEY) strengthens the key. `UkRunSys.wp_uk_ecall_read_recv` and `UConsLine.ush_read_recv_leaf` keep compiling: the U-tier leaf may weaken the new arm to what it stated before (`d <= dc <= d+1`) for now -- report exactly what it drops, file:line; phase 2 of SH-LINE will consume the full form.
W5 SANITY: `grep -n "d <= dc <= d + 1" iris/SpecConsoleread.v iris/SpecFileread.v` EMPTY; `grep -n "copyout_wrote" iris/*.v` shows every site at the new form; the audit unchanged; nothing Admitted.

=== PHASE 1 -- then STOP AND REPORT ===
W1-W4 as STATEMENTS with the proofs that are one-liners done and the rest compiling with -k as `Lemma … Admitted` ONLY inside your scratch copy -- NO: the tree may not carry Admitted at report time; instead stop with the statements in place and every downstream file compiling against them, using `Proof. ... Qed.` where you can and leaving the hard proofs (the two swallow sites, copyout's fault clause) for phase 2 by NOT yet changing the statement that would break them (report which). Report: (1) the exact new statements, verbatim, file:line; (2) the copyout proof's plan at the walkaddr/vmfault seam with the hypotheses you have in context there (quote `Hwapay`'s shape); (3) the two swallow sites' plan (what `cr_pop_swallow` will additionally return and from which hypothesis); (4) the red files and why; (5) anything this brief got wrong about the landed code, with file:line.

=== PHASE 2 -- after go-ahead ===
The proofs, W5, the gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, an as-landed note (≤ 20 lines, present tense). Do not commit or stage. If context runs short, STOP after a green build or at a clear milestone and report exactly what remains, file:line.
