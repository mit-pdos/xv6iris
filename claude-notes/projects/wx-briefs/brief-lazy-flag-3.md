You are a KERNEL-and-U-tier lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel) in /shared/xv6iris-2 (the MAIN checkout). Your lane is LAZY-FLAG-3: PHASE 2 of the stored lazy flag, continuing a previous agent's work THAT IS SITTING UNCOMMITTED IN THE TREE (60 dirty files under iris/; build `lazy48`: 506 compiled, 10 red, no Admitted). Do NOT re-apply any patch: the tree is the state. Read first: claude-notes/projects/app-echo.md "LAZY-FLAG-2 PHASE 1" (what is landed, the ten reds, K4's route), "LAZY-FLAG CORE LANDED + STORED-FORM FINDINGS" (design facts; its "ONE BLOCKER" paragraph is CORRECTED by the newer note), "THE OWNER'S RULING ON THE FORK"; then wx-briefs/brief-lazy-flag-2.md (K1-K6; superseded where the notes say); durable-notes.md ("Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the bundle-intro note, the never-collapse-tokens note, the ELF-bytes note); remote-build-gcp.md. The coordinator owns the design; you do the proofs. ONE PHASE: finish and gate.

=== OPERATING RULES (hard) ===
Start: `cd /shared/xv6iris-2 && git status --porcelain -- iris/ | wc -l` (expect ~60; if 0, STOP and report -- the state is in wx-briefs/lazy-flag-stored-2.patch and the coordinator must decide). BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (names `lazy50`, `lazy51`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter; `grep -rn "pv_lazy .*true" iris/ProofKexec*.v iris/KexecBridge.v iris/ProcInv.v` shows NO placeholder (only the dormant/fresh blocks' literal `true`). `ConsoleInv`, `FsAbs*`, `ChildTok`, `App.v`, `SystemAdequacy.v` are off limits. GIT: DO NOT COMMIT OR STAGE; never stash/reset/add -A/commit -a/amend/checkout -- <file>; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NEVER apply a tree-wide textual substitution. Comments state the current shape as fact.

=== THE WORK (from the phase-1 report; file:line as of that report) ===
K1 TAILS (mechanical): ProofUsertrapTail.v:987 (fix made, unbuilt), ProofUserretClosed.v:470, ProofKforkB6.v:1173, ProofForkret.v:832, ProofUservec.v:1667 -- one more `lz` argument on a ut_*/urun_eq_*/uround_ok application, or one more ⌜lazy⌝ conjunct when a proc_priv is re-assembled; ProofSyscall.v:4029 -- the dispatcher's new clause (iv) at ~20 wp_syscall_sconf arms (+ sbrk's usys_sbrk_lazy), then ProofUsertrapSys.v:909 (sysc_mem_ok_usys_sbrk's premise).
K2 CLOSES: ProofGrowproc.v:686 (grow: SpecGrowproc.v:138's dom equation → lazy_free of the new table; shrink: uptd_del_run + um_below); ProofSysSbrk.v:979 (with K3); ProofAllocproc.v:2915 -- give ProcDefs.proc_dormant / proc_dormant_noctx the clause ⌜pv_lazy V = true⌝ (the two producers already build the literal: BootCarveMain.v:2166, ProofFreeproc.v:852; kexit's zombie arm exhibits upd_lazy V true for free since pv_lazy is not a cell).
K3 THE LAST STATEMENT: SpecSysSbrk.sys_sbrk_ok names which arm ran AT THE BLOCK LEVEL -- the LAZY arm writes `upd_lazy V true` (V' = upd_lazy (upd_sz V …) true), EAGER/shrink keep the bit; SpecSyscall.sysc_sbrk_ok relays; ProofSysSbrk pays it; the row usys_sbrk_lazy then discharges from it.
K4 EXEC'S ROW (a)-(d): (a) KexecBuilt.kexec_built gains `lazy_free (ud_um (pv_upt (us_V U'))) (uint sz1)`, proved at ProofKexecD.v:2038's split_and! from Hcov via a bridge `lazy_free_of_covered : bv_unsigned szv <= uvm_maxsz -> um_covered szv um -> lazy_free um (uint szv)` (hoist ProofKexecC.v:162's um_covered_pground; home UserPerm.v beside lazy_free or KexecBuilt.v); (b) ProcInv.upd_exec writes pv_lazy := false (proc_priv_newspace's b at ProofKexecD.v:1539/1546 becomes false with the row; kxd_upd_compose/kxd_priv_exec flip the literal; exec's no-op close at :2148 keeps the caller's bit); (c) KexecDefs.kexec_ok / KexecOkQ relay pv_lazy V' = false; KexecBridge.exec_image_ok_of_built turns it into kexec_image_ok's `uvis_lazy W' = false`; (d) ProofKexecA.v:791/817/1056/1156 and ProofKexec.v:656 discharge exec_slot_pre's two rows (cwd from SpecKexec.exec_key_cwd :517 + kexec_ok_exec_cwi :522; lazy from (c)).
K6 SANITY as in brief-lazy-flag-2 (plus the no-placeholder grep above).

=== REPORT ===
Log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, lemma_diff justified by group, an as-landed note (≤ 25 lines, present tense: the stored flag, the invariant's homes, the rows, exec's row, the read-leaf deliverable, the two slot-wand rows), anything still owed with file:line. If context runs short, STOP at a green -k build, bank the diff as a patch in your scratchpad, and report exactly what remains.
