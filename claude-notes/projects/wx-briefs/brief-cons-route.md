You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is CONS-ROUTE, a kernel-side lane between CONS-CURSOR and SH-LINE: (1) the read syscall's console arm takes the process's exit payload through a WAND, so a program whose payload holds the console-reader token can spend it at a read without holding it; (2) `cons_acc`'s inner wand becomes a basic update; (3) the reader token born at boot reaches init's slot instead of being dropped by main. Design of record: claude-notes/projects/app-echo.md "SH-LINE RULING" (read it whole -- it is the design), then "CONS-CURSOR LANDED" and "CONS-CURSOR RULINGS" (5)-(7), "WX-EXIT LANDED" (the payload machinery: `sexit_pay`, `uexec_pay_dep`/`_arm`, `sysc_pay_in`/`_out`, `ut_kexit` spending `Q (-1)` at +0xca), "PID-KEY LANDED" (the residue is indexed by the pid; `uvis_pid`), "ARM-c (1a) LANDED" (`init_boot_bundle`, the park's boot mode, forkret's boot arm). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; iris/ConsoleInv.v (`cons_acc` :~1467, `cons_acc_reader`/`_cred`/`_open`/`_ret`, `cons_out` :~1438, `cons_reader` :~1173); iris/SpecFileread.v (`fileread_in` :~866, `fileread_extra` :~880, `console_receipt` :~941, `console_recv` :~1076, the fileread contract's post); iris/ProofFileread.v (the console arm's `cons_acc_open` use); iris/SpecSysRead.v (`sys_read_in` :277, `sys_read_arms` :286, `wp_sys_read_sconf_body` :348) + ProofSysRead.v; iris/UexecExecInst.v (`xv6_sbundle` at 5 :~473-474, `xv6_spost` at 5, `xfam.rf_ret`); iris/UexecSG.v (`sexit_pay`, `sexit_pay_pt`); iris/FsAbsInvFire.v (`fsabs_fileread_in`); iris/SpecSyscall.v (`sysc_pay_in` :444, `sysc_pay_out` :449, `sysc_pay_in_ret` :457; the returning post) + iris/ProofSyscall.v (`sysc_arm_read` :5487, the `SysRead.wp_sys_read_sconf` call :5549; how the arm holds `Hpay`/`Hdep` and hands `sysc_pay_out`); iris/UkRunSys.v (`wp_uk_ecall_read_recv_body` -- its `udepwf` deposit); iris/SpecMain.v (:433 `cons_reader cn 0`), iris/ProofMain.v (:~2313 `Hrdtok` bound and dropped), iris/SpecUserinit.v (:223 `console_ready_app` premise; the park), iris/ParkCap.v (`park_pkg`'s boot mode: `init_boot_bundle cw sts` at `None`), iris/ProofForkret.v (the boot arm spends the bundle on kexec("/init") and reads the slot out of `exec_post_ok_recv`), iris/InitBoot.v (`init_boot_bundle` :113, `init_boot_bundle_triv` :124), iris/SystemAdequacy.v (`init_boot_of_sup` :763 -- the generic instance's discharge must still go through), iris/UInitKernel.v (`init_uexec_slot` :195, `init_slot_of_kexec` :293 -- where init's slot piece would receive the token; this lane STATES the input on the bundle's slot wand and lets the constructor DROP it, SH-LINE consumes it).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `consroute1`, `consroute2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group (sealed Parameters retyped in place listed); nothing Admitted; no new Axiom/Parameter. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== THE DESIGN ===
R1 THE PAYLOAD WAND THROUGH READ.  `SpecFileread.fileread_in st F Rd (P : iProp Σ) := P -∗ match st with … | FdOpen true _ (FdDevice mj) => if decide (mj = CONSOLE) then cons_acc fsc_cons app_sup (fun cur dc => P ∗ Rd cur dc) else P | inode arm => P ∗ pf_at … | _ => P end` -- i.e. `P` is threaded through every arm and CONSUMED only by the console arm's `cons_acc`, which must hand it back inside `Rd`; `fileread_extra st n F Rd r M' addr P := P ∗ (the landed extra)`; DECIDE the cleanest spelling (PREFER `fileread_in … P := P -∗ fileread_in_core … (fun cur dc => P ∗ Rd cur dc)` and `fileread_extra … P := P ∗ fileread_extra_core …`, so every non-console arm is `P -∗ P ∗ …` by framing and the landed definitions survive as the `_core`s). `wp_fileread_sconf_body` takes `P` (a parameter) and returns it in the post beside the extra; `ProofFileread` feeds it. `SpecSysRead.sys_read_in/sys_read_arms/wp_sys_read_sconf_body` relay `P` in and out. `UexecExecInst.xv6_sbundle` at 5 := `fileread_in (fd_st …) (rf_F f) (rf_ret f) (sexit_pay f (-1))` -- the family element carries the payload, so the bundle is stated at it; `xv6_spost` at 5 UNCHANGED (what the process sees carries no `P`: the dispatcher peels it). `FsAbsInvFire.fsabs_fileread_in` at `sexit_pay sfam_pt (-1) = True` (by `sexit_pay_pt`): `True -∗ …`, the credential disjunct as today. THE DISPATCHER'S READ ARM (`ProofSyscall.sysc_arm_read`): it holds `sexit_pay f (-1)` from `sysc_pay_in` (`sysc_pay_in_ret`) -- feed it as `P` to `wp_sys_read_sconf`, take `P` back off the post, and hand it to `sysc_pay_out` as every arm does; the kill check at +0xca is BEFORE dispatch and spends the same resource on the other path (nothing changes there). Check `sysc_ret_tail`/`sysc_epilogue_tail` take `sysc_pay_out` where the arm returns -- the read arm now produces it from the post instead of from the untouched `Hpay`.
R2 `cons_acc`'S INNER WAND IS A BUPD: `cons_acc cn Wd Rd := (∃ n, cons_reader cn n ∗ (∀ cur dc, cons_out cn Wd (Some n) cur dc ==∗ Rd cur dc)) ∨ (cons_dirty_cred Wd ∗ ∀ cur dc, |==> Rd cur dc)` (or `={E}=∗` at fileread's mask if the ghost update SH-LINE needs (a `ghost_var` pair) wants it -- a bupd suffices for `ghost_var_update`; PREFER `==∗`); `cons_acc_reader`/`_cred`/`_open`/`_ret` and `ProofFileread`'s use adapt (the kernel proof is under a WP, so `iMod` is free). Note at `cons_acc`: the wand is a bupd so a lease holder can move a program-side position ghost with both halves in hand.
R3 THE TOKEN'S BOOT ROUTE.  `InitBoot.init_boot_bundle cw sts` gains the reader token as an INPUT of its slot piece: the exec bundle's slot wand becomes `… -∗ cons_reader fsc_cons 0 -∗ uslot W'`-shaped (find the slot piece: `MkPfam uslot R` in `exec_au_pre`; the token is best threaded as the bundle's linear `Pay`/`R` -- read `SpecKexec.exec_au_pre`/`exec_slot_pre` and `PinnedExec`'s `Pay` to see which slot the boot bundle already has for a linear payload that reaches the constructor; PREFER the existing `R`/`Pay` slot: `init_boot_bundle cw sts tok := ∃ …, exec_au_pre (MkPfam uslot (R ∗ tok)) …` or the `Pay` field at `cons_reader fsc_cons 0`) so `init_boot_bundle_triv` (the generic instance) takes the token and DROPS it (`init_boot_of_sup`/`init_boot_of_triv` unchanged in force: they receive the token as a premise? NO -- `Hinit_boot`'s statement is UNCHANGED: `app_inv -∗ |==> init_boot_bundle ROOTINO fdt0`; so the token cannot be a premise of the bundle's construction; it must be an input of the bundle's SLOT WAND, supplied by the KERNEL at the kexec of /init: `exec_slot_pre`'s wands take `my_pay …` today -- the boot bundle's slot wand additionally takes `cons_reader fsc_cons 0`, supplied by forkret's boot arm, which got it from the park package, which got it from userinit, which got it from main). Thread: `ProofMain` hands `cons_reader cn 0` to userinit (`SpecUserinit` gains the premise beside `console_ready_app`); `SpecUserinit`/`ProofUserinit` put it in the park package's boot mode beside `init_boot_bundle cw sts` (`ParkCap.park_pkg`'s `None` arm); `ProofForkret`'s boot arm passes it to the bundle's slot wand at the kexec; `SpecKexec.exec_slot_pre`/`exec_au_pre` are NOT changed if the token can ride in `R`/`Pay` (the bundle's own linear slot) -- DECIDE and report; if the slot wand's arity must change, that is design friction: STOP AND REPORT with the goal quoted. `UInitKernel.init_slot_of_kexec`/`init_uexec_slot` take the token as a premise and DROP it in this lane (a note: SH-LINE hands it to init's run).
R4 SANITY: `grep -n "sexit_pay f (-1)" iris/UexecExecInst.v` shows the read bundle at the payload; `grep -n "==∗\|={" iris/ConsoleInv.v` shows `cons_acc`'s bupd; `grep -n "cons_reader" iris/ProofMain.v iris/SpecUserinit.v iris/ParkCap.v iris/ProofForkret.v iris/InitBoot.v iris/UInitKernel.v` shows the route with no drop before init's constructor; the three existing read leaves unchanged in statement; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
R1-R3 as STATEMENTS; NO Admitted at report time. Build with -k. Report: (1) `fileread_in`/`fileread_extra` with `P`, `xv6_sbundle` 5, quoted; (2) `cons_acc` with the bupd, quoted; (3) the token's route main → userinit → park → forkret → the bundle's slot wand → init's constructor, quoted at each hop with file:line, and WHICH slot of the bundle carries it; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (fileread's arms, sys_read, the dispatcher's read arm feeding and peeling `P`, `fsabs_fileread_in`, `cons_acc`'s lemmas and ProofFileread, main/userinit/park/forkret's boot arm/init's constructor threading the token, every red file), R4. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
