You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-ROW + WX-RES FINISH: you take over lane WX-RES (read `brief-wx-res.md` beside this file for its design and rules, and claude-notes/projects/app-echo.md "WAIT-EXIT — DESIGN OF RECORD" + the LANDED notes) whose previous agent landed phase 2 except for a structural blocker, now RULED. The working tree is DIRTY ON PURPOSE with that WIP (23 modified iris/*.v; backup `wxres-p2.patch` beside this file; a copy of the base tree's iris/*.v may exist in the old session's scratchpad -- do not rely on it); do not revert, stash, reset or `git checkout --` anything.

=== STATE (verified by build `wxres6`: COMPILED=1010, EXIT=2, FOUR red roots) ===
Landed and green in the WIP: the children row rides the trap residue (`UsertrapRes.ut_own … ∗ ch_frag (un_ch N) (pv_chg (us_V U)) (un_pj N) cs`, the `cs` index on `ut_env/ut_res/ut_res_parked/ut_res_bare/ut_hold` and the ~26 accessors; `USERTRAP_RES`'s three Parameters retyped in place); `WaitInv` re-keyed to option (C): `children_own_at γc (m : gmap gname (mword 64 * gset gname))`, `ch_frag γc γ0 pa S`, `children_own_lookup/upd/install/del`, `wait_res_alloc pa`, `children_inv ps m` STATED not carried (WX-INV), `children_wf` gone; `park_pkg`/`forkret_closer`/`forkret_park_pkg` gained `γch` with the pin `⌜pv_chg (us_V U') = γch⌝`; both parkers capture `ch_frag … (un_pj N) cs` at a named set; `ut_fork_out := ufork_ans (sfork_pay f) r cs cs'`, `ut_ch_kept`; `usertrap_post`/`uservec_post` bind `cs'`; the four tails carry `cs`/`cs2`; `ProofUserretClosed` READS the set (`Rut_at` has the `cs` index); `kfork_post`'s two arms return the caller's row moved (`csP` / `csP ∪ {[γ]}`), `wp_kfork_sconf_body` takes it in, `SpecSysFork` relays, `SpecSyscall.wp_syscall_sconf_body` gained `γw γc`, the `is_lock`, the row in/out, `sysc_ch_ok`, `sysc_fork_out f U r cs cs'`. Green: WaitInv, Xv6Cameras, ProofMain, ProofUservec, ProofForkret, ProofForkretPark, ProofUserretClosed, ProofUsertrapTail, ProofUsertrapArms, ProofSysFork.
RED ROOTS: `ProofKforkB5.v:~320` (the CHILD's row at the park -- the blocker; also owes the parent's `children_own_upd` at +0xd4 where the lock and the caller's row are in hand), `ProofUserinit.v:~809` (init's row at the park), `ProofSyscall.v:~1743` (the dispatcher relay: `sysc_hcont_ty` :~1652, `sysc_ret_tail` :~2632, `sysc_epilogue_tail` :~1882, `sysc_fallback` :~6796 gain the `cs'` index, the row and `sysc_ch_ok`; ~22 `sysc_ret_tail` sites pass `cs cs` and the row; the fork arm :~4438 ∃-splits sys_fork's answer to bind `cs'`; the capstone :~7141 takes `γw γc` and the row -- NOT blocked), `ProofUsertrapSys.v:~291` (`ut_90` gains `cs2`, relays the dispatcher's returned set to `T.ut_a6` -- blocked only on ProofSyscall).
THE BLOCKER: kfork seals the child's residue at its first `release(&np->lock)` (+0xc4; `ProcGeom.needs_ctx USED = true`, so `proc_lock_res` at USED demands `proc_ctx` -- the child's whole future, opaque once built) BEFORE it takes `wait_lock` (+0xd0..+0xe0), so no row can be installed for the child by kfork; and main cannot name init's slot at `wait_res_alloc` (`SpecUserinit` is ∀-general in `j`).

=== THE RULING: (P) -- ROWS ARE PER-SLOT, BORN AT BOOT, RIDING THE DORMANT BLOCK (the `pv_fdg`/`fd_frags` mold, exactly) ===
`wait_res_alloc` installs NPROC rows at the boot assembly (ProofMain ~1110-1180, where `seq 0 NPROC` is in hand), row `i` at `proc_addr i` with `∅`, and row `i` goes into slot `i`'s DORMANT block (`ProcDefs.proc_dormant`/`proc_dormant_noctx` gains `∃ γ0, ch_frag γc γ0 pa ∅`-shaped row -- decide whether the dormant row is at `∅` (a freed slot's children are gone: reparent/wait emptied them -- state the invariant) or at an existential set; PREFER `∅` with the obligation on freeproc's caller (kwait, after the reap, the zombie's set is `∅` because its children were reparented at exit -- WX-EXIT) -- if `∅` cannot be established at freeproc yet, carry `∃ S` this lane and let WX-EXIT/WX-WAIT tighten). allocproc hands the row out with `fd_frags` and writes its name into the new block's `pv_chg` (`upd_chg`; `allocproc_post`'s found arm gains the row at the block's `pv_chg` and `proc_addr j`); freeproc returns it to the dormant block. `children_own_install`/`_del` become DEAD (delete them; the map is TOTAL over the NPROC names by construction, and the address component is right by construction). `wait_res_alloc`'s signature loses `pa` and yields the NPROC rows (a `[∗ list] i ∈ seq 0 NPROC, ∃ γ0, ch_frag γc γ0 (proc_addr i) ∅`) for the boot carve to distribute into the dormant slots (BootCarveMain/BootShared/SpecProcinit's carve is where the dormant blocks are built -- find how `fd_frags`'s per-slot names are born there and mirror; if the dormant blocks are built before `wait_res_alloc` can run (it needs `parents_res`), install the rows into the blocks at main's assembly where the previous agent already found the `seq 0 NPROC`). kfork then parks the child with the row allocproc handed it (`ProofKforkB5.v:~320` closes), userinit likewise (`ProofUserinit.v:~809`), and `SpecUserinit` needs NO row premise (delete the one the previous agent may have added). Cost: `ProcDefs`/`ProcInv`'s dormant block, `SpecProcinit`/the carve, `ProofMain`'s assembly, `Spec/ProofAllocproc`, `Spec/ProofFreeproc`, and `ProcInv`'s cone rebuilds.

=== OPERATING RULES (hard) ===
As brief-wx-res.md: BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (run_in_background, sentinel `COMPILED=`/`EXIT=`); ONE build at a time; never a remote top-level `make`; never local `coqc`; gate = zero Error, EXIT=0, `make audit-only` on the VM (`/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen (print verbatim), `tools/lemma_diff.py` (justified GONEs: `children_wf`, `children_own_install`, `children_own_del`; sealed Parameters retyped in place reported as such; nothing admitted; no new assumption). GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT. Comments state the current shape as fact.

=== PHASE 1 -- then STOP AND REPORT ===
(P) as STATEMENTS: the dormant block's row, `wait_res_alloc`'s new shape and its call at the assembly, `allocproc_post`'s row, freeproc's return, the deletions; the dispatcher relay's STATEMENTS (`sysc_hcont_ty`/`sysc_ret_tail`/`sysc_epilogue_tail`/`sysc_fallback`) landed; NO Admitted at report time. Build with -k. Report: (1) the dormant block's row and `allocproc_post`'s, quoted; (2) where the NPROC rows are born and how they reach the dormant blocks (file:line); (3) whether the dormant row is at `∅` or `∃ S`, and why; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (the assembly, allocproc, freeproc, kfork's park + the parent's `children_own_upd`, userinit's park, the ~22 dispatcher sites + the fork arm + the capstone, `ut_90`), every red file, R3's greps from brief-wx-res.md, the comment sweep (WaitInv's header: rows are born at boot per slot; `kfork_post`'s note; `ParkCap`'s). Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, and an as-landed note (≤ 15 lines, present tense; the previous agent's partial note is in CHECKPOINT.md's neighbourhood -- complete it). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
