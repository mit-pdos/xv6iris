You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is OPEN-PIN PHASE 3 (a fresh agent; the application-side statements, the console key, the create-leg permit and init's mknod bundle are LANDED and PROVED): the remaining PROGRAM-side items -- the receipt-keeping open leaf as a Lemma, a dup-at-closed leaf, init's open and dups at the named ledger, and init's three-arm head threaded through `UkInitMain` to the fork's child arm. Design of record: claude-notes/projects/app-echo.md "OPEN-PIN PHASE 2 MILESTONE LANDED" (the state; read it first), "OPEN-PIN PHASE 1 LANDED + FINDINGS" (rulings (a)-(e)), "SPEC-TIGHTEN LANDED", "PATH-ARGS LANDED", "OPEN-PIN FINDINGS -- RULED WITH THE OWNER" (FACTS 1-3 -- FACT 3: init proves NOTHING about allocation; both arms of its second open are its own verified code; on CLOSED the dups fail, sh later runs with fds 0-2 closed and its first read fails), "PINNING -- THE OWNER'S RULING". The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity"); remote-build-gcp.md; design/user-wp-slot.md; design/user-fd.md; iris/UInitCons.v (WHOLE: `init_cons_head` and its three constructors, `init_cons_l0`/`init_cons_fd`/`init_cons_l3`, the `init_cons_scan0/1/2`/`init_cons_alloc0/1/2` ledger lemmas, `init_cons_open_bundle_absent`/`init_cons_open_recv_absent` (the FIRST open: `-1 ∨ T`), `init_cons_open_bundle_rdwr`/`init_cons_recv`/`init_cons_open_fd` (the SECOND open at the resolving pin under `cons_made`), `init_cons_mknod_bundle` (PROVED)/`init_cons_mknod_recv`/`init_cons_mknod_fail_recv`/`init_cons_open2_of_mknod_fail`, §8's eight application-law premises and the `AppEcho` lemma discharging each); iris/AppEcho.v (`cons_key`, `echo_cons_abs_law`, `echo_init_key`, `cons_made`, the seven claim laws); iris/UkRunSys.v (`wp_uk_ecall_open_recv_body` :~2586 -- a body at the TRAPPING key, to become a Lemma proved by `wp_uk_ecall_open`'s walk (:~718) keeping `spost_at`; `wp_uk_ecall_dup` :~860 takes `st <> FdClosed`; `wp_uk_ecall_dup_untracked` :~1012; `wp_uk_ecall_read_recv_body` :~2514 as the mold for a receipt-keeping leaf; `udepwf`); iris/UsysMemOk.v (`usys_fd_ok`'s dup arms: the failure arm `fdv' = fdv`, `r = -1`); iris/UkInit.v (`wp_kinit_open` :135 -- the generic leaf, handle dropped at :~189; `wp_kinit_mknod` :213; `wp_kinit_dup` :285 -- untracked at :326); iris/UkInitMain.v (`wp_kinit_main` :~1733, the `blt` at 0x1a :~1953-1985 walking both arms, `wp_kinit_main_repair` :~1511 (mknod at :~1615, second open at 0x7e, result dropped at 0x82 `c.j 0x1e`), `wp_kinit_main_from_1e` :~1358, `wp_kinit_main_loop` :~856, `wp_kinit_fork` :~666 (the child arm at :~711 gets the ledger at a fresh name), `init_exec_sup` and the `ustd_any` threading); iris/UserFd.v (`ustd`, `ualloc`, `fd_lowest_closed`); iris/UConsLine.v (`ush_std_cons` -- payable on CONSOLE; SH-LINE consumes CONSOLE and CLOSED).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `openpin60`, `openpin61`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; `UkRunSys` sits under the whole U tier -- batch its two leaves before the first build. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. KERNEL FILES ARE OFF LIMITS (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`, `UexecExecInst`'s rows, the `uexecSG` class); a needed kernel change is design friction: STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact.

=== THE DESIGN ===
Q1 THE TWO LEAVES.  `UkRunSys.wp_uk_ecall_open_recv` : the body `wp_uk_ecall_open_recv_body` proved (the walk is `wp_uk_ecall_open`'s; keep `spost_at` at the TRAPPING key `W` -- row 15's post reads `uvis_M W`, `xk_a W 0/1` and the ENTRY table `uvis_fd W`; bind the entry `sts` and the resume `fdv'` separately; the ledger moves as `wp_uk_ecall_open` moves it (`ualloc`/`ustd`) -- read how, and give the continuation the receipt `open_receipt … sts r fdv'` AND the moved ledger); `wp_uk_ecall_dup_closed` beside `wp_uk_ecall_dup`: `ustd γfd l` with `l !! fd = Some FdClosed` in, `r = -1` and the ledger unchanged out (from `usys_fd_ok`'s dup-failure arm; the deposit is the generic family's, `udepw_of_psok`).
Q2 INIT'S OPEN AND DUPS.  `UkInit.wp_kinit_open` restated with TWO variants (or one lemma with the pin as a parameter): `wp_kinit_open_absent` (the FIRST open, at `init_cons_open_bundle_absent` with `cons_key` in hand: returns `-1` with the ledger `init_cons_l0` unchanged, or the taint) and `wp_kinit_open_console` (the SECOND open, at `init_cons_open_bundle_rdwr` under `cons_made r i`: returns fd 0 = `init_cons_fd` with the ledger `<[0 := init_cons_fd]> init_cons_l0` (`init_cons_alloc0`), or `-1` unchanged (allocation failure -- CLOSED), or the taint; and a third variant for the second open AFTER A FAILED MKNOD at the miss pin -- `init_cons_open2_of_mknod_fail` says how: reuse `wp_kinit_open_absent`); both through `wp_uk_ecall_open_recv` with the deposit `udepwf` at an `xfam` whose open families are the pinned ones (`of_P := pobs_P_dead …`/`pobs_P …`, `of_Fo := pobs_Fo …`, `of_Ft` trivial at `om_trunc = false`, the create families trivial at `om_create = false`); `UkInit.wp_kinit_mknod` restated through the repair arm's deposit at `init_cons_mknod_bundle` (the `xfam` with `nf_* := init_mk_*`), returning `∃ i, cons_made r i` or the key back (failure) or the taint; `UkInit.wp_kinit_dup` twice: on CONSOLE through the TRACKED `wp_uk_ecall_dup` (`init_cons_alloc1`/`_alloc2`; the source row `init_cons_dup_src`), on CLOSED through `wp_uk_ecall_dup_closed`; the untracked leaf leaves `UkInit`.
Q3 INIT'S HEAD.  `UkInitMain`: `wp_kinit_main` opens at the miss pin with `cons_key`; the `blt` at 0x1a: the `-1` arm goes to `wp_kinit_main_repair` (mknod → `cons_made` or the key back; the second open → CONSOLE or CLOSED; the `c.j 0x1e`), the `≥ 0` arm is REFUTED by the receipt (or is the taint arm); `wp_kinit_main_from_1e` and `wp_kinit_main_loop` carry `init_cons_head γcl γfd` (CONSOLE `init_std_cons γfd init_cons_l3` ∨ CLOSED `ustd γfd init_cons_l0` ∨ TAINT) in place of `ustd_any`; the two dups at 0x20/0x26 per Q2; the fork's child arm carries the same ledger at a fresh name (`UkFork`'s child arm hands `ustd (ukn_fd N') l` for the parent's `l` -- check) plus the head's arm; the eight application laws `UInitCons` §8 takes as premises are DISCHARGED at echo's era by the named `AppEcho` lemmas (state `init_cons_laws_echo : ⊢ init_cons_laws …` or thread them as `UInitCons`'s section hypotheses discharged in `UInitKernel`/the era instance -- decide by where `app_pred = echo_pred` is known: the `file_app = MkAppcfg …` equation `Hinit_boot` carries; if that is E2's, STATE the laws as premises of init's program lemma with a note that E2 discharges them and `cons_key` alongside); `cons_key` and the era-0 claim enter init as premises of `wp_kinit_start` (E2's boot arm supplies them from `echo_init_key` -- note it).
Q4 SANITY: `grep -n "wp_uk_ecall_dup_untracked" iris/UkInit.v` EMPTY; `grep -n "init_cons_head" iris/UkInitMain.v` shows the head at `_from_1e`/`_main_loop`/the fork; `grep -n "ustd_any" iris/UkInitMain.v` shows no console-ledger site left at `_any` (other uses may remain: report); `git diff --stat -- iris/Spec*.v iris/Proof*.v iris/ConsoleInv.v iris/FsAbs*.v` EMPTY; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
Q1's two leaf STATEMENTS (the open one as a Lemma with its proof in progress is fine -- NO Admitted), Q2's `wp_kinit_open_*`/`wp_kinit_mknod`/`wp_kinit_dup` statements, Q3's head in `wp_kinit_main_from_1e`/`_main_loop`/the fork's child arm as STATEMENTS. Build with -k. Report: (1) the two leaves, quoted; (2) init's open/mknod/dup statements, quoted; (3) the head's three arms as they appear at `_from_1e`, the loop and the fork, quoted, and how the eight laws and `cons_key` enter (file:line); (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (both leaves; init's first open, repair arm with the mknod step, second open, the dups, `_from_1e`, the loop, the fork's child arm), Q4. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
