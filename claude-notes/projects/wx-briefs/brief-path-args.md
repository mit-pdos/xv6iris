You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is PATH-ARGS, a kernel-side lane: the open(2) and mknod(2) syscalls' bundles and receipts are stated at the PATH THE CALLER PASSED (trapframe argument 0), exactly as exec's already are, so a program can answer their walk pieces at one path and read back WHICH file it opened or created. Today `open_au_pre_plain`/`_create` contain `namei_walk_pre_era` (the walk at EVERY path, `∀ pl`), row 15 of `xv6_sbundle` reads the omode (`xk_a W 1`) but not the path pointer (`xk_a W 0`), `open_receipt_plain` binds `pl` existentially and ties it to nothing, and mknod (row 17) is the same. Design of record: claude-notes/projects/app-echo.md "OPEN-PIN FINDINGS -- RULED WITH THE OWNER" (FACT 2), and, as the MOLD, "EXEC-CHANNEL LANDED" / "EXEC-ARGS LANDED" and `SpecSysExec.v`'s header (how exec carries its path: `exec_path_of M pv pl` from `SpecFetchstr.fetchstr_got`, `sys_exec_au_pre` quantifying over `⌜exec_path_of M pv pl⌝`; also the earlier note "open/chdir/unlink keep the `∀ pl` form" at app-echo.md ~1475 -- this lane retires it for open and mknod). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/fs-syscall-specs.md (the AU forms; how a walk-shaped bundle is stated); iris/SpecSysExec.v (:~75 the path reading; :197-221 `exec_path_of`; `sys_exec_au_pre` :~353 -- THE MOLD) + ProofSysExec.v (how `argstr`'s `fetchstr_got` becomes `exec_path_of`); iris/SpecFetchstr.v (`fetchstr_got`), iris/SpecArgstr.v if it exists; iris/SysOpenDefs.v (`namei_walk_pre_era` :~368-374, `open_au_pre_plain` :~407-413, `open_au_pre_create`); iris/SpecSysOpen.v (`open_in` :~863, `open_arms`, `open_post_fail_plain` :~539, `open_post_ok_plain`, `open_receipt_plain` :~908-947 with its DEVICE/FILE/DIRECTORY arms, `open_receipt_create`, `open_receipt` :~1005, the `_split` lemmas :~1030+) + ProofSysOpen*.v; iris/SpecSysMknod.v (`mknod_au_pre` :251, its receipt) + ProofSysMknod.v; iris/UexecExecInst.v (rows 15 and 17 of `xv6_sbundle` :~486-510 and `xv6_spost` :~569-595; the `xfam` fields `of_P … of_Ft`; `spost_at_open_intro`/`_mknod_intro` if they exist); iris/ProofSyscall.v (the open and mknod arms: where the a0 word is bound and how the walk piece is fed); iris/FsAbsInvFire.v (the generic slot's dischargers of open's and mknod's bundles at every path -- `fsabs_open_in`-style: they must still hold at the argument's path -- the generic family answers `∀ pl`, so a bundle at ONE path is weaker to supply: check the direction); iris/PinnedObs.v (`pinned_obs`'s (i) `ex_start γfs cw (pobs_P T hops) (pobs_Pmiss T) pl` at ONE path -- what a pinned open will hand in once the bundle is at the argument's path); iris/UkRunSys.v (`wp_uk_ecall_open` :~718 -- the generic leaf; unchanged in statement by this lane, but its deposit is built at `udepw_of_psok`/the generic family, so check it still discharges); iris/UkInit.v (`wp_kinit_open` :135, `wp_kinit_mknod` :213 -- init's calls; unchanged in statement).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `pathargs1`, `pathargs2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group (sealed Parameters retyped in place listed); nothing Admitted; no new Axiom/Parameter. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== THE DESIGN ===
A1 OPEN AT ITS PATH.  `SysOpenDefs.open_au_pre_plain Γ γfs cw pl P Pmiss Fo Ft` (and `_create`) take the path `pl : list (bv 8)` and contain the walk AT THAT PATH (`namei_walk_pre_era`'s body at the one `pl`: `ex_start γfs cw P Pmiss pl`-shaped -- whatever the `∀ pl` form instantiates to; keep a lemma `open_au_pre_plain_of_all : (∀ pl, …) -∗ …` so the generic supplier is one line); the syscall-tier `sys_open_au_pre` (or whatever `SpecSysOpen` calls the caller-facing input) quantifies over the path reading exactly as `sys_exec_au_pre` does: `∀ pl, ⌜open_path_of M pv pl⌝ -∗ open_au_pre … pl …` with `open_path_of M pv pl` := the `fetchstr_got`/`argstr` reading of the NUL-terminated string at `pv` in the image `M` (define it beside `exec_path_of`, or generalize `exec_path_of` to `arg_path_of` and use it for both -- PREFER the generalization: one definition, the exec one an alias); `open_receipt_plain`/`_create`/`open_receipt` gain `pl` and state their arms AT it (the existential `pl` becomes the parameter; the DEVICE arm then says: the walk of THIS path ended at `i` with node `ADev ma mi`, and `open_fd_rcpt …`); `open_post_fail_plain`/`open_post_ok_plain`/the `_split` lemmas follow. Row 15 of `xv6_sbundle` reads `xk_a W 0` (the path pointer) beside `xk_a W 1` and is stated as the `∀ pl, ⌜arg_path_of (uvis_M W) (xk_a W 0) pl⌝ -∗ …` form; row 15 of `xv6_spost` likewise (the receipt at the path the key's image held at a0). `ProofSysOpen*` threads the path from `argstr`'s post (find where sys_open reads the string: `SpecArgstr`/`fetchstr` -- exec's proof is the mold) and `ProofSyscall`'s open arm binds it.
A2 MKNOD AT ITS PATH.  The same for `SpecSysMknod.mknod_au_pre Γ γfs cw ma mi …` and its receipt (row 17): the create step's directory `d` and name `nm` are then the path's parent and last element, so the receipt says "a device node `ADev ma mi` was created at THIS path" (the `delta_create d nm i (ADev ma mi)` at the path's parent -- `FsAbsMknodFire.v:~218` is the delta).
A3 THE GENERIC SUPPLIERS.  `FsAbsInvFire`'s open/mknod dischargers were at `∀ pl`; at one path they are the instance: `fsabs_open_in`-style lemmas take the path and instantiate. The generic slot's supply law (`xv6_sbundle_of_supply_ne` at 15/17) must still hold -- it does if the supplier is `∀ pl` and the bundle asks at one `pl` under `⌜arg_path_of …⌝`. Check the U-tier generic leaf `wp_uk_ecall_open` still discharges via `udepw_of_psok` (its deposit is the generic family's).
A4 SANITY: `grep -n "arg_path_of\|open_path_of" iris/SpecSysOpen.v iris/SpecSysMknod.v iris/SpecSysExec.v iris/UexecExecInst.v` shows one definition used three times; `grep -n "∀ (pl\|forall (pl" iris/SysOpenDefs.v` shows the `∀ pl` form only in the `_of_all` bridge; `open_receipt_plain`'s DEVICE arm names `pl`; the audit unchanged; `UkInit.wp_kinit_open`/`wp_kinit_mknod` and `UkRunSys.wp_uk_ecall_open` unchanged in statement.

=== PHASE 1 -- then STOP AND REPORT ===
A1-A2 as STATEMENTS (`arg_path_of`; `open_au_pre_*`/`open_receipt_*` at `pl`; `sys_open_au_pre`'s `∀ pl, ⌜arg_path_of …⌝ -∗`; rows 15/17; mknod's); A3's supplier restated; NO Admitted at report time. Build with -k. Report: (1) `arg_path_of` and `open_au_pre_plain`/`open_receipt_plain` at the path, quoted; (2) rows 15 and 17 of `xv6_sbundle`/`xv6_spost`, quoted; (3) mknod's bundle and receipt at the path, quoted; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (sys_open's argstr → the path, the walk at the path, the receipt arms, sys_mknod likewise, the dispatcher's two arms, the generic suppliers, the U-tier generic leaves, every red file), A4. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 12 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
