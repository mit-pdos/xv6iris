You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is the FINISH of WX-EXIT. Read, in this order: `brief-wx-exit.md` beside this file (the design, its LAST section "RE-ANCHORED…" wins over the sections above it), then claude-notes/projects/app-echo.md sections "WX-EXIT RULINGS" (the escrow keyed at the stored status via the xstate half-cell; the generation pins on the route; the owner's ruling that a killed process pays `Q (-1)` out of resources its RUN keeps and hands to the kernel at every trap) and "WAIT-EXIT — DESIGN OF RECORD" with the LANDED notes, then claude-notes/README.md, durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Shaping a change so the sweep is small"), remote-build-gcp.md. The working tree is DIRTY ON PURPOSE with the previous agent's WX-EXIT work (99 iris files; backup `wxexit-inflight.patch` beside this file); do not revert, stash, reset or `git checkout --` anything.

=== STATE (build `wxexit37`: COMPILED=1012, EXIT=2, SIX red files; lemma_diff CLEAN; nothing Admitted) ===
DONE AND BUILDING: the exit deposit `UexecRet.uexec_exit_dep` → `SpecUsertrap.ut_exit_in` → `SpecSyscall.sysc_exit_in` → `SpecSysExit` → `SpecKexit` (kexit stated at its status argument `SpecKexit.kexit_status m := xstate_of (m !!! a0)` :~157, premises `my_pay (pv_gen (us_V U)) Q -∗ Q (kexit_status m) -∗`); `ProcGeom.xstate_val`/`xstate_of`/`exit_xs`; `ChildTok.exit_tok` SINGLE-ARMED (`∃ pa Q Q', gen_kq ∗ my_pay γ Q' ∗ Q' xs`), `gen_pay` ▷ `Q xs`, `exit_tok_intro`; the xstate HALF-CELL end to end (`SchedCtx.proc_pub` at 1/2; `ProcInv.proc_priv_core`/`proc_priv_nopt` carry `∃ xsv, p_xstate pa ↦₄{1/2} xsv` LAST; `proc_priv_split_cwd` five-way; `ProcDefs.proc_dormant`/`_noctx` carry `∃ xsv, p_xstate ↦₄{1/2} xsv ∗ (if ZOMBIE then exit_tok (pv_gen V) pid (xstate_val xsv) else emp)`; boot carve, allocproc, freeproc, kexit's store, kwait's reap, the kfork chain, userinit, the boot park all threaded); `proc_priv_core`'s pair `∃ Q, gen_kq ∗ my_pay`; kexit moves its children to `WaitInv.orphans_own` (second canonical name on `wchG`), `wait_res_at = parents ∗ children ∗ orphans`, `children_inv ps m O ip` stated; `UkRun.uk_names` Σ-parametric with `ukn_pay`, `urun` carries `my_pay gn (ukn_pay N)`, `Class ukn_triv`, the entry constructors take `my_pay (uvis_gen W) Q` and give `⌜ukn_pay N = Q⌝`; generic family and exec wands indexed by the pay fact; the three GENERATION PINS (`SpecForkret.forkret_closer` + its two inline copies at `ParkCap`:~203/`SpecForkretParkPaid`:~248 carry `⌜pv_gen (us_V U') = gn⌝`; `SpecUsertrap.ut_gen_kept` :~286 in `usertrap_post`, relayed by `uservec_post`; `ProofUserretClosed.Rut_at` carries `(gn : gname)` + `⌜pv_gen (us_V U) = gn⌝`, a premise of `wp_userret_closed_body`/`wp_forkret_gen_body`/`fkr_tail`/`fkr_boot`, discharged `eq_refl` at `ProofForkretPark`).
RED, one-name plumbing (fix first): `ProofForkretPark.v:~341` (`iSpecialize` -- supply the `park_pkg` closer's new `pv_gen` pin), `ProofForkret.v:~1592` (`iFrame` cannot frame `my_pay … (λ _, True)` -- the boot arm's frame list), `ProofKforkMain.v:~977` (`kfork_arm2`'s continuation must carry `∃ xsv, p_xstate npa ↦₄{1/2} xsv`, mirroring `kfk_pro_exit3`), `ProofUservec.v:~1853` (`iSpecialize` -- the `uservec_post` application must account for `ut_gen_kept`).
RED, blocked on the U-tier design below: `ProofUsertrapTail.v:~159` (`ut_kexit`: the three `kexit(-1)` sites have no `Q (-1)`), `ProofUserretClosed.v:~506` (`UV.wp_uservec_pt` call one row short -- the deposit's producer).

=== THE REMAINING DESIGN: `Q (-1)` RIDES THE RUN AND EVERY TRAP (owner's ruling; implement exactly) ===
P1. `UexecRet`: `uexec_pay_dep sc W := ∃ Q, my_pay (uvis_gen W) Q ∗ (if sc is the exit ecall then Q (exit_xs (uvis_tf W)) ∧ Q (-1) else Q (-1))` -- the ∧ is the ADDITIVE conjunction (the kill check at +0xca runs BEFORE `syscall()`, so a process trapped with the exit number may still exit -1; the kernel eliminates whichever conjunct it needs; a program whose payload is one resource for every status pays `R ⊢ R ∧ R`); `uexec_pay_arm W := ∃ Q, my_pay (uvis_gen W) Q ∗ Q (-1)` returned at EVERY resume (every non-exit ecall number and the non-ecall traps). Wire into `uexec_dep_F`/`uexec_arm_F`/`uexec_ret_F` and their ~15 lemmas; `uexec_exit_dep` becomes the exit case of `uexec_pay_dep` (delete or alias -- one definition).
P2. `UkRun.urun` gains the LINEAR conjunct `ukn_pay N (-1)` beside `my_pay gn (ukn_pay N)` (`urun_close`/`_upd`, `udepw`/`udepw_at`, the three entry constructors take it: at entry `Q (-1)` comes from the slot's arm or the constructor's caller -- init/sh/echo/sync/cat are at `ukn_triv` so it is `True`; state where a real payload's `Q (-1)` enters: the FORK child arm receives it from the parent's chosen `Q` -- at `ukn_triv` trivial); the step/ecall lemmas (`wp_uk_step`/`wp_uk_ecall`, UkStepGen's twins) move it out at the trap (into `uexec_pay_dep`) and back at the resume (from `uexec_pay_arm`) INSIDE their proofs -- no per-leaf statement grows. `UkRunSys.wp_uk_ecall_exit` takes `(ukn_pay N (-1) -∗ ukn_pay N (uexitst m) ∧ ukn_pay N (-1))` instead of `ukn_pay N (uexitst m)`; the program exit stubs (UkInit/UkSh/UkEcho/UkSync/UkCat) pay it free at `ukn_triv`.
P3. Kernel route: `SpecUsertrap.ut_exit_in` → `ut_pay_in` and `SpecSyscall.sysc_exit_in` → `sysc_pay_in` (ungated: at every number; two-armed only at exit), relayed by `SpecUservec`/`ProofUservec`/`ProofUsertrap`/`ProofUsertrapSys`/`ProofUsertrapArms`/`ProofUsertrapTail`/`ProofSyscall`/`ProofSysExit`; the non-ecall trap arms carry it too. `ut_kexit`'s three sites (`ProofUsertrapSys` :~297 -- at +0xca on the exit number take the ∧'s RIGHT conjunct; `ProofUsertrapArms` :~1343; `ProofUsertrapTail` :~1220) pay kexit at status -1 (`kexit_status m = -1` there: a0 is `c.li a0,-1`); sys_exit pays the ∧'s LEFT conjunct; userret returns `Q (-1)` through the arm (`ProofUserretClosed`'s producer at :~506 and the arm at the resume). `ProofUserretClosed.Rut_at`'s `pv_gen` pin is what moves the slot's `my_pay (uvis_gen W)` to kexit's `my_pay (pv_gen (us_V U))`.
P4. Notes: `urun`'s note says why the linear conjunct lives in the run (sh keeps using its stdin ownership between traps and hands it to the kernel at every entry, which spends it only on the kill path); `exit_tok`'s note already points here -- make the names match; `UexecRet`'s dep/arm notes state the shape as fact; no lane markers, no "now"/"used to".

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `wxexit38`, `wxexit39`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time (never a chained waiter plus a manual run); never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal; `UexecRet`/`UkRun` sit under ~50 U-tier files -- batch P1-P3's statements before the first build. Gate: zero `Error`, `EXIT=0`, COMPILED>0; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` with every line justified (sealed Parameters retyped in place listed by group; no GONE unless justified; nothing Admitted; no new Axiom/Parameter). GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT (with the goal in front of you quoted). Comments state the current shape as fact.

=== DELIVERABLE ===
Every red file green; X6's greps from brief-wx-exit.md plus `grep -n "uexec_pay_dep\|uexec_pay_arm\|ukn_pay N (-1)\|ut_pay_in\|sysc_pay_in" iris/UexecRet.v iris/UkRun.v iris/UkRunSys.v iris/SpecUsertrap.v iris/SpecSyscall.v`. Gate. Report: log name, `git status --porcelain | grep -v '^ M iris/'` (only the .swp), `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 20 lines, present tense) covering the whole lane: the block's pair and the half-cell, the escrow keyed at the stored status, the deposit/arm shape with `Q (-1)`, the ∧ at exit, kexit's premises and the kill path, the orphans, the generation pins, `ukn_pay`/`urun`, the generic family and exec wands, `Class ukn_triv`, and any class-index hygiene rules you hit. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line, with the names of every new definition.
