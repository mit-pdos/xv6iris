You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is the FINISH of CONS-CURSOR (the kernel half of E3). Read, in this order: `brief-cons-cursor.md` beside this file (design C1-C4, reading list, hard rules), then claude-notes/projects/app-echo.md "CONS-CURSOR RULINGS" -- especially item (5) THE LEASE, which REPLACES two earlier rulings on the tokenless read: the console arm requires the reader token UNCONDITIONALLY and the token crosses the ecall through a fupd -- then "E3 — THE INPUT LINE: DESIGN PROPOSAL" and the L5 sections. The working tree is DIRTY ON PURPOSE with the previous agent's phase-1 work (30 iris files; backup `conscur-inflight.patch` beside this file); do not revert, stash, reset or `git checkout --` anything.

=== STATE (build `conscur8`: COMPILED=1387, 13 red files, no Admitted; the previous agent's phase-1 report is summarized here) ===
LANDED AS STATEMENTS: `RiscvPtsto.obs_hist_lb`/`obs_hist_auth` (a `mono_list` on the machine layer; `obs_auth h := obs_half h ∗ obs_hist_auth h`, `obs_update`, `obs_hist_lb_prefix`; the power loop steps it; `boot_fixedGS` gained a positional `γhist`); `WpUart.uart_col_ok u hs np nk hl ht` with the strict-prefix chain, the anchor `hl` and the explicit top `ht`; `uart_rx_tok γ k hl`, `uart_rx_hi γ q hh`, `uart_rx_writer`, `plic_payload_uart := ∃ k hl, uart_rx_writer γ k hl`; `uart_colE_push_acc` (the push as an accessor taken with `obs_auth h`), `uart_col_pop` yielding `∃ h, obs_ends_in h bt ∗ ohist_ext hl h ∗ riscv_rx_tag h ∗ obs_hist_lb h ∗ uart_rx_tok γ (S k) (Some h)`, the flush at `hl' := ht`; `ConsoleInv.cons_res cn` with `cons_stored r w n st bs ts` (the COMMITTED prefix r..w as a mono_list, extended ONLY by `cons.w = cons.e`), `cons_pend` (the editable window w..e, an ordinary list), `cons_chain`, `cons_below`, `cons_stored_auth cn st ∗ cons_cursor cn n ∗ cons_hi cn hh`; `cons_names` (`cn_uart`, `cn_log`, `cn_rd`) in `UartNames.v`, carried as `FsCfg.fsc_cons`; `SpecConsoleintr` with `hb cb hh` as parameters and `uart_rx_hi` in/out; `SpecConsoleread` taking `cons_reader cn nrd` and returning `cons_stored_lb cn sl ∗ ⌜cons_window sl nrd d bs hs⌝ ∗ ⌜cons_chain sl⌝ ∗ ⌜d ≤ dc ≤ d+1⌝ ∗ cons_reader cn (nrd + dc)` (two exits swallow a byte: the C('D') arm with nothing delivered, and the copyout-failure break past `cons.r++`); `SpecFileread.console_receipt nrd r M' addr` (no longer persistent: it carries the token), `console_recv`; `fileread_in`/`fileread_extra`/`sys_read_arms`/`xv6_spost 5` relaying an `ord : option nat` (`None` = today's `emp` arm, `Some nrd` = the token in and the window out); `xfam.rf_nrd : option nat`; `UkRun.udepwf N m pc n fdep` (the family-naming deposit; `udepwf_udepw` proved) and the new leaf `UkRunSys.wp_uk_ecall_read_recv_body` STATED (its walk is `wp_uk_ecall_window`'s), handing the continuation `spost_at uslot USYS_read fdep (uvis_of_run …) r M' fdv' cw' cs'` at the exposed resume key.
RED (13; all proof work against the new statements): `BootCarveMain` `ProofConsoleinit` `ProofConsoleintr` `ProofConsoleread` `ProofDevintr` `ProofFileread` `ProofMain` `ProofPlicClaim` `ProofPlicComplete` `ProofSysRead` `ProofSyscall` `ProofUartinit` `ProofUartintr`; their cone (App, AppEcho, BootChain, BootShared, the Fs*Pin files, the Link* chain, SystemAdequacy, SystemUartAccepted, UInitSh) was skipped, not broken.

=== THE RULING THAT CHANGES THE PLAN: THE LEASE (app-echo.md CONS-CURSOR RULINGS (5)) ===
- Keep the `ord : option nat` relay AS THE KERNEL'S TWO CALLER SHAPES for now, BUT the `None` arm is NOT "a read without the token": consoleread ALWAYS moves `cons_cursor`, so `None` cannot be proved against consoleread as stated. RESOLVE IT THIS WAY: the console arm of `fileread_in`/`fileread_extra` takes the token through a FUPD-shaped access rather than as a bare resource: `cons_acc E n := |={E}=> cons_reader cn n ∗ (∀ dc, cons_reader cn (n + dc) ={E}=∗ True)` -- no: state it as the ACCESSOR the ecall needs: the caller supplies `cons_tok_acc E := ∃ n, |={E}=> cons_reader cn n ∗ ∀ dc, cons_reader cn (n+dc) ={E}=∗ cons_tok_ret n dc` where `cons_tok_ret n dc` is what the caller gets back (for a lease holder: the fact at its own position; for a tainted process: nothing). DECIDE the exact spelling so that (i) consoleread's contract stays "token in, token out" with NO fupd (the kernel proof holds `cons.lock`, and the accessor is opened by the CALLER'S tier -- fileread's console arm at the syscall's fancy-update scope; find the mask `E` the dispatcher's arm runs at), (ii) `fsabs_fileread_in`'s console case can be discharged from a PREMISE `cons_tok_acc E` that the generic slot's supply will provide (SH-LINE: from the lease's second disjunct) -- so `xv6_ssupply` GAINS that accessor as a conjunct (`app_sup ∗ □ cons_tok_acc …` or the escrow-open form; it is a DEFINITION in UexecExecInst, not the class field; the class is untouched), and the generic instance's discharge of it (`SystemAdequacy` §2b / App's generic corollary) takes it as an explicit HYPOTHESIS of the generic theorem for now, named and commented (the lease lands in SH-LINE; until then the generic corollaries carry one more premise -- the audit still prints the thirteen because the audited theorem's premises are not axioms; CONFIRM), (iii) the read leaf at `Some` (a lease holder) opens the accessor around the ecall and gets the window at ITS position with `dc`. If a single `option`-free shape works (every caller supplies the accessor; a lease holder's accessor returns the position fact, a tainted one's returns `True`), PREFER it and delete `ord` -- report which.
- THE ORDER FACT for a lease holder: the receipt at `Some nrd` says the window is at `nrd` = the holder's own count; the holder's `gets` invariant carries `nrd`; that is SH-LINE's.

=== OPERATING RULES (hard; as brief-cons-cursor.md) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `conscur9`, `conscur10`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; vtest unaffected. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== DELIVERABLE ===
Phase A (STOP AND REPORT when done, before the long proofs): the accessor's exact spelling landed as STATEMENTS in SpecFileread/SpecSysRead/UexecExecInst/UkRunSys (+ the generic instance's new hypothesis), `fsabs_fileread_in` restated, build -k, report the shape and the red list. Phase B (after go-ahead): the 13 reds + the cone, C4, gate, report (log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, an as-landed note ≤ 20 lines present tense covering C1-C3 and the accessor). If context runs short, STOP at a green -k build or a clear milestone and report precisely (new names, done files, red files with the goal quoted).
