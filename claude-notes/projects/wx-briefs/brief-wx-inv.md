You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-INV: the wait-lock invariant that ties the parent cells, the children map, the orphan set and the per-slot generation halves together is CARRIED (it has been STATED since WX-RES), and the reaper's own children row moves at the reap. Design of record: claude-notes/projects/app-echo.md, section "WX-GEN / WX-INV / WX-WAIT — DESIGN" (whole), then "WX-GEN LANDED" (take every file:line anchor from it -- this brief was drafted before that landing; where the two disagree the landed note wins), "WX-EXIT LANDED", "WX-RES + WX-ROW LANDED", "WAIT-EXIT — DESIGN OF RECORD". The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity" -- especially "An invariant that has to be re-based must state that its body OWNS its own map", "Shaping a change so the sweep is small"); remote-build-gcp.md; iris/WaitInv.v (whole: `parents_own_at`, `children_own_at`, `ch_frag`, `children_own_upd`, `children_inv`, `orphans_own`, `gen_halves` (WX-GEN), `wait_res_at`, `children_res_alloc`); iris/SlotGen.v or wherever WX-GEN put `slot_gen`/`pid_reg` and their agreement/exclusion lemmas; iris/ChildTok.v (`gen_slot`, `gen_pid`, `gen_slot_agree`, `gen_pid_agree`); iris/ProofKforkB5.v (+0xd4: the parent-cell store, the row move, WX-GEN's deposit of the halves); iris/ProofKexit.v (`kx_park`: reparent under `wait_lock`, the row → orphans move); iris/SpecReparent.v (`rp_map`); iris/ProofKwait.v (`kw_reap`: the scan's `pp->parent == p` fact, `pp->parent = 0`, the entry coming out, freeproc); iris/SpecKwait.v, iris/SpecSysWait.v, iris/ProofSysWait.v; iris/ProofSyscall.v (the wait arm; how the FORK arm ∃-splits its answer to bind `cs'` and relays the row -- `sysc_fork_out`, `ufork_ans`; WX-RES's "dispatcher relay"); iris/SpecKexit.v (how the dying process's row reaches kexit: the `ch_frag (pv_chg (us_V U)) pj cs` premise relayed by SpecSysExit and the dispatcher's exit arm -- THE MOLD for kwait's row); iris/UexecRet.v (`ufork_ans`; the wait rows); iris/UsysMemOk.v (`usys_ch_ok`, the wait row ~222).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `wxinv1`, `wxinv2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal; `WaitInv` sits under every wait_lock consumer -- batch the statements before the first build. Gate: zero `Error`, `EXIT=0`, COMPILED>0; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` with every line justified (sealed Parameters retyped in place listed by group; nothing Admitted; no new Axiom/Parameter). GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact (no dates, no lane markers).

=== THE DESIGN ===
I1. THE INVARIANT, CARRIED.  `wait_res_at ξ := ∃ ps m O, parents_own_at ξ ps ∗ children_own_at m ∗ orphans_own O ∗ children_inv ps m O` where (WX-GEN's `gen_halves ps` is absorbed into it)
  `children_inv ps m O := ([∗ list] k ↦ v ∈ ps, if v = 0 then emp else ∃ γ pid, slot_gen (proc_addr k) (3/4) γ ∗ pid_reg pid (3/4) γ ∗ gen_slot γ (proc_addr k) ∗ gen_pid γ pid ∗ ⌜γ ∈ rowset m v ∨ (v = initproc_addr ∧ γ ∈ O)⌝) ∗ ⌜inv_pure ps m O⌝`
  with `rowset m v := ⋃ { S | ∃ γ0, m !! γ0 = Some (v, S) }` (or the predicate form `∃ γ0 S, m !! γ0 = Some (v, S) ∧ γ ∈ S`), and `inv_pure ps m O` the pure converse and owner-uniqueness: (a) `∀ γ0 v S, m !! γ0 = Some (v, S) → ∀ γ ∈ S, ∃ k, ps !! k = Some v ∧ entry_gen ps k = Some γ`; (b) `∀ γ ∈ O, ∃ k, ps !! k = Some initproc_addr ∧ entry_gen … = Some γ`; (c) `m !! γ1 = Some (v, S1) → m !! γ2 = Some (v, S2) → γ1 = γ2`.  "entry_gen" is awkward as a pure function over a resource-indexed list -- PREFER binding an explicit `gs : list gname` (per-slot current generation, meaningful only where `ps !! k ≠ 0`) as a fourth existential and stating both directions purely over `(ps, gs, m, O)`, with the resource part `[∗ list] k ↦ v ∈ ps, if v = 0 then emp else ∃ pid, slot_gen (proc_addr k) (3/4) (gs !!! k) ∗ pid_reg pid (3/4) (gs !!! k) ∗ gen_slot (gs !!! k) (proc_addr k) ∗ gen_pid (gs !!! k) pid`.  Decide and report.  `initproc_addr`: the pinned symbol (`KernelSyms.initproc` is a POINTER cell holding init's proc address -- check how `SpecReparent`/kexit name init's address; if it is read from the `initproc` cell at runtime, the invariant binds `ip` from that cell's value -- find who owns the cell (probably the boot carve, read-only after userinit) and state the tie).
  Every party that destructs `wait_res` adapts (kfork B5, kexit `kx_park`, kwait, reparent's callers, ProofMain's `wait_res_alloc`, SpecKexit, the syscall env park).  The consequence lemmas the writers need, proved in WaitInv: `children_inv_fork` (insert entry j: needs `ps !! j = Some 0` -- WX-GEN's `gen_halves_no_entry` (kfork's 3/4 against a would-be entry's 3/4); the row `cs → cs ∪ {γ}` at owner `pme`; `ps' = <[j := pme]> ps`), `children_inv_reparent` (`rp_map pa ip ps`; the dying row `S → ∅`; `O → O ∪ S`), `children_inv_reap` (entry k out; `ps' = <[k := 0]> ps`; `cs → cs ∖ {γ'}` or `O → O ∖ {γ'}`; the two facts (W2)/(W3) come out as pure conclusions here: `⌜γ' ∈ cs ∨ (pj = ip ∧ γ' ∈ O)⌝` and `∀ γ ∈ cs, gen_pid γ pid' -∗ ⌜γ = γ'⌝` -- prove them in THIS lane as lemmas over the invariant even though WX-WAIT is what puts them in kwait's post), `children_inv_empty` ((W5): no `k` with `ps !! k = Some pj` → `rowset m pj = ∅`, hence the holder's row is `∅`).
I2. THE REAPER'S ROW.  kwait's contract takes the caller's row `ch_frag (pv_chg (us_V U)) pj cs` (the kexit mold, `SpecKexit.v`'s premise) and returns it at `cs'` with `⌜cs' = cs ∖ {[γ']}⌝` on the reap arm and `⌜cs' = cs⌝` on the -1 arm (the escrow and the facts are WX-WAIT's; this lane returns ONLY the row and the pure `cs'`); `kw_reap` does `children_own_upd` under the lock it holds (for an orphan of init: `orphans_own` update instead, `cs' = cs`).  `SpecSysWait` relays; the dispatcher's wait arm relays the row and binds `cs'` the way the fork arm does (∃-split of the answer; `sysc_ret_tail`'s `cs'` index is already there since WX-RES); `UexecRet`'s wait row and `usys_ch_ok` stay pure-quiet for now -- the set's move rides the answer like fork's: add `uwait_ans r cs cs' := ⌜r = -1 ∧ cs' = cs⌝ ∨ ∃ γ', ⌜cs' = cs ∖ {[γ']}⌝` to the wait answer (WX-WAIT adds the escrow and the facts beside `γ'`); the U tier's `uch_auth` moves by `uch_move` in the wait leaf (`UkRunSys.wp_uk_ecall_wait_null` and callers: the leaf's statement should not grow -- the move happens inside like fork's child arm).
I3. SANITY: `grep -n "children_inv" iris/WaitInv.v iris/ProofKforkB5.v iris/ProofKexit.v iris/ProofKwait.v` shows it carried and re-established at the three writers; `grep -n "ch_frag" iris/SpecKwait.v iris/SpecSysWait.v` shows the row in and out; nothing "STATED, NOT CARRIED" remains in WaitInv's notes.

=== PHASE 1 -- then STOP AND REPORT ===
I1's `wait_res_at`/`children_inv` (with the `gs` decision) and the four consequence lemmas as STATEMENTS (proofs may be in progress; NO Admitted at report time -- leave a lemma out rather than admit it); I2's kwait/sys_wait/dispatcher/`uwait_ans` statements.  Build with -k.  Report: (1) `wait_res_at` and `children_inv`, quoted; (2) the four consequence lemmas' statements, quoted, and which are proved; (3) how `initproc`'s address is named; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (the three writers, kwait's row move, the relays, every red file), I3.  Gate.  Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 15 lines, present tense).  Do not commit or stage.  If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.

=== RE-ANCHORED AT THE WX-GEN LANDING (commit `d4a70aa12`; the tree is CLEAN there) ===
Read app-echo.md "WX-GEN LANDED" first.  Anchors (verified): `WaitInv.gen_halves` :225 (one entry per NONZERO parent cell: `∃ γ pid, slot_gen (proc_addr k) (3/4) γ ∗ pid_reg pid (3/4) γ ∗ gen_slot γ (proc_addr k) ∗ gen_pid γ pid`), `gen_halves_no_entry` :251, `gen_halves_rp_map` :276, `parents_res_at ξ := ∃ ps, parents_own_at ξ ps ∗ gen_halves ps` :297, `children_own_upd` :374, `children_inv ps m O ip` :418 (STATED), `children_res` :428, `orphans_own` :451, `orphans_add` :458, `orphans_res` :467, `children_boot` :484, `wait_res_at ξ := parents_res_at ξ ∗ children_res ∗ orphans_res` :496.  `SlotGen.v`: `slot_gen` :149, `slot_gen_agree` :158, `slot_gen_quarters` :193, `slot_gen_tq_excl` :202, `pid_reg` :233 (keyed at `bv_unsigned pid`), `pid_reg_agree` :249 (takes `bv_unsigned pid = bv_unsigned pid'`), `pid_reg_quarters` :259, `gen_halves_priv` :315 (the block's quarters), `gen_halves_dorm` :328.  Writers: `ProofKforkB5.v:~537-560` (the row move `children_own_upd`, `gen_halves_no_entry`, the entry insert into `gen_halves (<[j := pme]> ps)`); `ProofKexit.v:~1175-1184` (`kx_park`: row → `∅`, `orphans_add`, the `rp_map` pass-through); `ProofKwait.v:~1294-1334` (`kw_reap`: entry k out, `gen_halves (<[k := 0]> ps)`), the `kw_*` lemmas' premises are `parents_own ps -∗ gen_halves ps -∗ children_res -∗ orphans_res -∗` (:1294, :1678, :2140-2202, :2610, :2672, :3155 -- the shape WX-INV rebinds: after I1 they take the invariant's pieces bound TOGETHER).  `initproc`'s address: kexit and reparent READ the `initproc` cell at an arbitrary fraction (`SpecKexit.v:265-266` `(mword_of_int KernelSyms.initproc) ↦₈{dqi} ip`; `SpecReparent.v:94/:102`) -- the invariant binds `ip` as the cell's value: PREFER making `wait_res_at` carry a fraction of the `initproc` cell itself (`∃ ip, initproc_cell ↦₈{q} ip` with the pure ties at that `ip`) so no caller has to thread `ip`; find who owns the cell today (the boot carve; userinit writes it once) and take a fraction from there -- if the cell is written AFTER `wait_lock` goes up (userinit runs after main's `newlock`), carry `∃ ip` with the ties trivially true while `O = ∅` and no parent cell equals `ip`… decide and report (this is phase-1 item (3)).  The dispatcher's fork mold: `ProofSyscall.v:~1783/:2039/:2811` `sysc_fork_out f U (a0) cs cs'`; `sysc_fork_out_ne` at the non-fork numbers (:3658, :3965, :4073).  I1's design refers to "WX-GEN's `gen_halves ps` absorbed into `children_inv`": keep `gen_halves` as the RESOURCE half and add the pure ties beside it -- `children_inv ps m O ip` becomes `gen_halves_tied ps gs m O ip` or the like; the point is that `ps`, `gs`, `m`, `O` are bound under ONE existential in `wait_res_at`.  Build log names `wxinv1`, `wxinv2`, ….
