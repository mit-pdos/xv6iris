You are a KERNEL-and-U-tier lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel) in /shared/xv6iris-2 (the MAIN checkout). Your lane is LAZY-FLAG-2: land the STORED lazy flag, continuing a previous agent's work. Read first, in this order: claude-notes/projects/app-echo.md "THE OWNER'S RULING ON THE FORK" (with REFINEMENT WITHDRAWN and TWO CORRECTIONS), "LAZY-FLAG CORE LANDED + STORED-FORM FINDINGS" (the design facts, the blocker, the owed items), "CONS-SWALLOW LANDED"; then wx-briefs/brief-lazy-flag.md (the original design L1-L7, superseded where the notes say so: the flag is STORED, `pv_lazy : bool` in ProcDefs.pprivate); then durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", the `iIntros "#H"` bundle note, the "never collapse a literal token sequence tree-wide" note); remote-build-gcp.md. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== YOUR STARTING MATERIAL ===
`claude-notes/projects/wx-briefs/lazy-flag-stored.patch` -- the previous agent's banked stored-form patch (25 files, 4769 lines, made against `3169da98b`; it compiled through UexecRet/UexecSG/UsysMemOk/UsysMemOkSpec/ProcDefs, with four mechanical red files: ProcInv `proc_priv_split_cwd`'s `iPureIntro`, UexecApply's `uslot_key_cong` eleventh equation at two call sites, UkStepGen's primed `ukc'`/`uvb_F'`/`ukb_F'` family + `uk_ecall_ty`, UkRun's `urun_close` at `ukcq`; behind them ProofUsertrapArms/Tail, ProofUservec, ProofUserretClosed (`ut_round_same`/`ut_round_ueq`'s lazy premise; `Rut_at`'s `pv_lazy` row on the `pv_cwi` precedent), UkFork, UkRunSys's 27 `uslot_bump_run` sites). The L1 core (`UserPerm.lazy_free` + readings, `UserHeap.lazy_free_uw_addr`, `UsysMemOk.usys_lazy_keep`/`usys_sbrk_eager`/`usys_sbrk_lazy`) is ALREADY ON MAIN -- the patch's hunks for those four files may be partly redundant: apply with `git apply --3way` (or per file) and reconcile by hand; NEVER apply a tree-wide textual substitution. Main has moved since the patch's base (APP-IFACE landed in App.v/SystemAdequacy.v/AppEcho.v; TEXT-LW; DISC-RATE) -- none of those touch the patch's files except possibly `SystemAdequacy.v` (`uslot_mint` at :773): check.

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (names `lazy30`, `lazy31`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. `ProcDefs`/`UexecSlot` sit under most of the tree: batch, run detached, wait. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter. Files: the kernel files the design names (`ProcDefs`, `ProcInv`, `UexecSlot`, `UexecSG`, `UexecRet`, `UexecApply`, `UexecRound`, `UexecExecInst` row 5, `UsysMemOk*`, `SpecSyscall`/`ProofSyscall`, `SpecUsertrap`/`ProofUsertrap*`/`ProofUservec`/`ProofUserretClosed`, `SpecSysSbrk`/`ProofSysSbrk`, `SpecGrowproc`/`ProofGrowproc`, `ProofVmfault`, `KforkChild`/`SpecKfork`/`ProofKfork*`, `KexecDefs`/`KexecBuilt`/`KexecBridge`/`ProofKexec*`/`SpecKexec`, `ProofAllocproc`/`ProofFreeproc`/`BootCarveMain`/`LinkNameiRootBoot`/`ProofSysUnlinkPure` for the `MkPPriv` literal) and the U tier (`UkRun*`, `UkStepGen`, `UkStep`, `UkLoad`, `UkStore`, `UkFork`, `UkRunSys`). `ConsoleInv`, `FsAbs*`, `ChildTok`, `App.v`, `SystemAdequacy.v` are off limits. GIT: DO NOT COMMIT OR STAGE; never stash/reset/add -A/commit -a/amend/checkout -- <file>; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact. NO placeholder premises; a stored bit set to `true` where the invariant cannot yet be proved is acceptable ONLY at exec until the dom row exists (say so at the site), never elsewhere.

=== THE WORK ===
K1 APPLY AND CLOSE THE PATCH: the four reds and their tails, to a green build with `pv_lazy` in the block, the invariant in `proc_priv_core` (after `tf_page`, outside `proc_priv_bare`), `uvis_lazy` on the key, `skey_eq`'s clause at its sites, the rows (`lz' = lz` everywhere quiet; exec's failure arm; `uround_ok`; `ut_*`), row 5's two new conjuncts (`⌜proc_pt_wf P⌝`, the tie `⌜uvis_lazy W = false -> lazy_free (ud_um P) (uvis_sz W)⌝`) extracted from the block where `sysc_out_read` is applied, the U tier at `false` (`ukcq`; `uvis_of_run`'s explicit `lz` with the kernel's two users passing the key's bit).
K2 THE INVARIANT'S MAINTAINERS: vmfault arms (`lazy_free_mono` under `uptd_ext`), growproc grow (`SpecGrowproc.v:138`'s dom equation) and shrink (`uptd_del_run` + `um_below`), fork's child (`SpecUvmcopy.v:155-166` via `KforkChild.urun_eq_kfork_child` with `pv_lazy Uc = pv_lazy Up`), the fresh/dormant blocks at `true`.
K3 SBRK'S ROW: `SpecSysSbrk.sys_sbrk_ok` says which arm ran AT THE BLOCK LEVEL (the LAZY arm writes `upd_lazy V true`; EAGER and shrink keep the bit), `SpecSyscall.sysc_sbrk_ok` relays, `usys_mem_ok`'s sbrk row gains `usys_sbrk_lazy lz lz' tf szv szv'`, `ProofSysSbrk` pays it.
K4 EXEC'S DOMAIN ROW (the blocker): add to `KexecBuilt.kexec_built` a row stating the fresh table's coverage -- `lazy_free (ud_um (pv_upt (us_V U'))) (uint sz1)` (or the dom equation `dom (ud_um P') = live_pages_set …` from which it follows) -- proved where the cone still has it (`SpecUvmalloc.v:137`'s dom equations per segment + the guard/stack pair; `ProofKexecB3.v:2807`'s `vpn_run` reasoning; note the guard page: `uvmclear` clears U but the page STAYS in `ud_um`, so coverage is by DOMAIN, unaffected), relayed through `KexecBridge` to `ProcInv.upd_exec`, which then sets `pv_lazy := false`, and `SpecKexec`'s slot key (`sysc_exec_out` / `exec_slot_pre`) carries `⌜uvis_lazy W' = false⌝`. If the segment loop's dom facts have been dropped before `kexec_built` is assembled, STOP AND REPORT with the goal and where the facts die.
K5 THE DELIVERABLE (L6): `UkRunSys.wp_uk_ecall_read_recv`'s continuation hands back `⌜uvis_lazy W = false⌝` (definitional at `ukcq`'s `false`), and a lemma `UkRunSys.uk_read_nofault` (or a form folded into the leaf) that, from `ubytes (ukn_d N) (uint dst) k f`, `d < k`, row 5's `∃ P` tie (`perm_of … = pm`, `proc_pt_wf P`, the lazy tie) and `pm`/`sz` from `urun`, refutes `⌜~ uva_wmapped P (uint (add_vec_int dst (Z.of_nat d)))⌝` -- the content is `UserHeap.lazy_free_uw_addr` + `uheap`'s `Md !! a = Some _ -> uw_addr pm a`; state it so SH-LINE 2b can eliminate `cons_swallow`'s fault disjunct inside the leaf's post (do not edit `UConsLine.ush_read_recv_leaf`; it is 2b's).
K6 SANITY: `grep -n "uvis_lazy" iris/UexecSG.v` shows the pin; `grep -n "pv_lazy" iris/ProcInv.v iris/ProcDefs.v` the field and invariant; `grep -rn "usys_sbrk_lazy\|usys_lazy_keep" iris/UsysMemOk.v iris/ProofSyscall.v iris/ProofUsertrapSys.v` the rows and discharges; `grep -n "uvis_lazy .*= false" iris/SpecKexec.v iris/UkRunSys.v` L4/L6; `grep -rn "pv_lazy .*true" iris/ProofKexec*.v iris/KexecBridge.v` EMPTY after K4; the audit unchanged; `git diff --stat -- iris/ConsoleInv.v iris/FsAbs*.v iris/ChildTok.v iris/App.v iris/SystemAdequacy.v` EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
K1 green (the patch closed), K2-K3 as statements with the trivial discharges done, K4 investigated (the dom facts' route from the segment loop to `kexec_built`, file:line) and STATED, K5 stated. NO Admitted. Report: (1) the statements verbatim, file:line; (2) K4's route and whether it is a row or a re-proof; (3) the red files; (4) anything the notes/brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs (K2-K5), K6, the gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), anything still owed with file:line. Do not commit or stage. If context runs short, STOP at a green build and report exactly what remains (and bank the diff as a patch in your scratchpad, naming it in the report).
