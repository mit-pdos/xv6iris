You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is the SECOND FINISH of CONS-CURSOR (the kernel half of E3). Read, in this order: `brief-cons-cursor.md` (the design C1-C4 and reading list), `brief-cons-cursor-finish.md` (the accessor ruling; its "STATE" section is superseded by this file's), then claude-notes/projects/app-echo.md "CONS-CURSOR RULINGS" (1)-(6) and "E3 — THE INPUT LINE: DESIGN PROPOSAL", then the durable notes it names. The working tree is DIRTY ON PURPOSE with two previous agents' work (40 iris files; backup `conscur-inflight.patch` beside this file); do not revert, stash, reset or `git checkout --` anything.

=== STATE (build `conscur21`: COMPILED=1391, EXIT=2, SIX red files; no Admitted; gate not yet run) ===
GREEN AND LANDED: `ObsTrace` (`ohist_ext_le_ext`, `ohist_le_of_ext`); `RiscvPtsto`'s machine-layer history mono_list (`obs_hist_lb`/`obs_hist_auth`, `obs_auth`, `obs_update`, `obs_hist_lb_prefix`; `boot_fixedGS` has a positional `γhist`; the power loop steps it); `WpUart`'s chain/anchor/top (`uart_col_ok u hs np nk hl ht`, `uart_rx_tok γ k hl`, `uart_rx_hi`, `uart_rx_writer`, `plic_payload_uart`, `uart_colE_push_acc`, `uart_col_pop`, the flush); `UartNames.cons_names` with FOUR names (`cn_uart cn_log cn_rd cn_dirty`); `ConsoleInv` (green): `cons_res cn` timeless with the marker disjunct `(⌜cur = nrd⌝ ∨ cons_dirty_lb cn)`, the escrow `cons_clean_tok`/`cons_dirty_lb`/`cons_dirty_cred Wd := □ Wd`/`cons_cred_body`/`cons_cred_inv cn Wd := inv consN …` with `cons_cred_inv_alloc`/`cons_cred_pay`/`cons_cred_read` (the read yields `▷ □ Wd` -- take it around a machine step), `is_conslock cn Wd γ := is_lock … (cons_res_at cn) ∗ cons_cred_inv cn Wd` (+ `_lock`/`_cred`/`_intro`), `cons_pay`/`cons_out` at `ord : option nat` (BELOW the fileread tier only), `cons_acc cn Wd Rd` (the one arm: `(∃ n, cons_reader cn n ∗ (∀ cur dc, cons_out cn Wd (Some n) cur dc -∗ Rd cur dc)) ∨ (cons_dirty_cred Wd ∗ ∀ cur dc, Rd cur dc)`) with `cons_acc_reader`/`cons_acc_cred`/`cons_acc_ret`, the pure ring kit (`cons_stored_commit`/`cons_pend_commit`/`cons_stored_ins`/`cons_pend_push`/`cons_pend_pop`, `cons_chain_snoc`, `cons_below_snoc`, `cons_below_prefix`); `SpecConsoleintr` (params `hb cb hh`, `ohist_ext hh hb`, `uart_rx_hi` in/out; `console_caps` on the RAW lock handle -- the interrupt path carries no `Wd`); `SpecConsoleread` (`cons_pay cn Wd ord` in; the window `cons_stored_lb cn sl ∗ ⌜cons_window sl cur d bs hs⌝ ∗ ⌜cons_chain sl⌝ ∗ ⌜d ≤ dc ≤ d+1⌝ ∗ cons_out cn Wd ord cur dc` out); `SpecFileread` (`Rd : nat -> nat -> iProp` replaces `ord`; `fileread_in`'s console arm `cons_acc fsc_cons app_sup Rd`; `console_receipt Rd r M' addr` uniform; `console_recv`); `SpecSysRead` (the `Rd` relay; `console_inv fsc_cons AppInv.app_sup (frn_cons fn)`); `UexecExecInst` (`xfam.rf_ret`, `xv6_spost` at 5; `xv6_ssupply := app_sup` UNCHANGED); `FsAbsInvFire.fsabs_fileread_in : app_sup -∗ fileread_in st … (fun _ _ => True)` (the credential disjunct); `UkRun.udepwf`; `UkRunSys.wp_uk_ecall_read_recv_body` STATED; `SpecMain` hands main `cons_clean_tok cn` beside `cons_res cn` and `cons_reader cn 0`; the receive path green end to end: `ProofUartinit`, `ProofConsoleinit`, `ProofPlicClaim`, `ProofPlicComplete`, `ProofDevintr`, `ProofUartintr` (they carry `uart_rx_writer γ k hl`), `ProofSysRead`; `ProofConsoleintr`'s transition table is threaded (`CtBodies.ct_gh`, `ct_gh_push`/`_commit`/`_pop`, `ct_hi_out`).
RED (6): `ProofConsoleintr.v:~614` (ONE `%list` scope annotation already applied in the tree, unverified -- `(pd ++ [(h,c)])%list` because `"cons"%string` opens string_scope; the same fix at :577 is verified); `ProofConsoleread.v:~862` (THE MAIN WORK, untouched: the loop invariants `cr_head_prop` :~1035 / `cr_have_prop` :~1073 gain the stored bound `sl`, the window's start position, the accumulating `cons_window`, and the cursor; DESIGN: a token read holds BOTH halves and sets `nrd := cur_final` at the end, so it leaves the ring CLEAN (`⌜cur = nrd⌝` re-established) and the post's `⌜cur = nrd0⌝ ∨ cons_dirty_cred Wd` is the disjunct read AT ENTRY; a tokenless read bumps `cur` only and re-establishes the disjunct by `cons_cred_pay` from the `□ Wd` it was paid); `ProofFileread.v:~505` (`Rd` on `wp_fileread_sconf`, the `cons_acc` destruct at the console arm, `console_receipt_of_run` at `Rd cur dc`; `is_conslock` arity already fixed); `ProofMain.v:~442` and `BootCarveMain.v:~550` (the boot mint: `cn_dirty`, `cons_clean_tok cn` handed out, `cons_cred_inv_alloc` at `Wd := app_sup` beside `newlock`, `boot_cons_res` at `cons_res cn` -- it becomes a `|==>`); `ProofSyscall.v:~5482` (the pinning below).

=== THE ONE DESIGN ITEM, RULED ===
`ConsoleInv.console_ready := ∃ cn Wd γ, console_inv cn Wd γ` hides `cn`/`Wd`, but `SpecSysRead` needs `console_inv fsc_cons AppInv.app_sup (frn_cons fn)` PINNED, and `ConsoleInv` sits below `FsCfg`/`AppInv`. RULING: define, in `SpecFileread` (which names both), `console_ready_app := ∃ γ, ConsoleInv.console_inv fsc_cons app_sup γ`, carry IT in `syscall_env`'s console conjunct in place of `console_ready` (find the conjunct: `FsSyscalls`/`FsReady`/`SyscParkEnv`; the pin threads from the boot chain where main mints `is_conslock` at `fsc_cons`/`app_sup`), and let the dispatcher's read arm open it. Keep `console_ready` if a consumer that never reads needs the anonymous form; otherwise it is a justified GONE. Note at `console_ready_app` why the pin lives at this tier.

=== OPERATING RULES (hard; as brief-cons-cursor.md) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `conscur22`, `conscur23`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; never `simpl`/`cbn` a goal mentioning a dumped byte literal; `Set Printing Depth 40.` in any file proving over `proc_priv`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; `make vtest-check-ci` unaffected. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== DELIVERABLE ===
The six reds in this order (cheap first): ProofConsoleintr's build confirmation; ProofMain/BootCarveMain's mint; the `console_ready_app` pin + ProofSyscall; ProofFileread; ProofConsoleread (the loop). Then the cone (`SystemAdequacy`, `App`, `AppEcho`, `UInitSh`, `SystemUartAccepted`, the `Link*` chain), C4's greps (`grep -n "cons_reader\|stored" iris/ConsoleInv.v iris/SpecConsoleread.v iris/SpecFileread.v iris/UkRunSys.v`; `grep -n "obs_hist_lb" iris/WpUart.v`; the three existing read leaves unchanged), the comment sweep, the gate, and the report: log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 20 lines, present tense) covering C1 (the chain/anchor/top; the machine-layer history mono_list; the receive path's `uart_rx_writer`), C2 (the ring's committed prefix/pending window/cursor/marker/credential escrow; the consoleintr transition table), C3 (consoleread's post; `cons_acc`; the receipt; the leaf; `console_ready_app`). If context runs short, STOP at a green -k build or a clear milestone and report precisely (new names, done files, red files with the goal quoted).
