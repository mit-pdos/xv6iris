You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-RES: the children reading of the user-execution key gets a RESOURCE behind it -- the per-process fragment of the wait-lock's children map rides the trap residue beside the descriptor fragments, and fork moves the map under the wait lock. Design of record: claude-notes/projects/app-echo.md, sections "WAIT-EXIT — DESIGN OF RECORD", "WX-KEY LANDED", "WX-FORK LANDED" -- read all three first. WX-FORK landed `WaitInv.children_own_at γc (m : gmap gname (gset gname)) := ghost_map_auth γc 1 m` and `ch_frag γc γ S := γ ↪[γc] S` (rows installed under `wait_lock`: kfork installs the child's at its `acquire(&wait_lock); np->parent = p`, writing the fresh name into the child's block `pv_chg`; main installs init's at `wait_res_alloc` and hands `ch_frag γc γ0 ∅` to userinit), `children_wf` (stated, not carried), and the KEY's `uvis_ch` as a ∀-parameter the parker pays (`park_cap`'s `∀ sts cs`), with the fork answer's `uvis_ch W' = uvis_ch W ∪ {[γ]}` chosen by the resumer -- not yet backed by the map. This lane backs it. The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Orchestration", "Build", "Shared-checkout discipline", "The dev loop", "Proofmode & bitvector gotchas"); remote-build-gcp.md; design/user-wp-slot.md; iris/FdSlots.v (`fd_frags (pv_fdg V) sts` -- THE MOLD: how the descriptor fragments ride the residue, are captured at the park (`ParkCap.park_token_park` "AT A NAMED TABLE"), re-keyed by the closer's `pv_fdg V' = g` premise, and read by the trap route into `uvis_fd`); iris/UsertrapRes.v (`usertrap_res_bare`/`_parked`, `ut_own`, `ut_own_nopt`, `ut_env_nopt`); iris/ParkCap.v (`park_pkg`'s `URB`, `park_cap`'s `∀ sts cs`, both parkers, the closer's `⌜pv_fdg (us_V U') = g⌝`); iris/SpecUsertrap.v (~1140/1153: `fd_frags` at the residue's seam); iris/SpecKfork.v (`kfork_post`'s token arm and its note); iris/WaitInv.v (`children_own_at` :189, `ch_frag` :205, `children_own_lookup` :214, `_upd` :223, `_install` :238, `_del` :253, `children_wf` :268, `wait_res_alloc` :337); iris/ProofKforkB5.v/B6.v (kfork's `wait_lock` acquire and the child's park). Anchors at HEAD: `ParkCap.park_pkg` :86, `park_cap` :269, `park_token_park` :384, `park_token_park_steady` :505; `UsertrapRes.ut_own` :740, `ut_own_nopt` :1118, `ut_env_nopt` :1130 (find `usertrap_res_bare`/`_parked` by grep -- they may live in another file); `SpecUsertrap.v:1218/1225/1231` (`fd_frags (pv_fdg (us_V U)) sts` at the seam); `SpecKfork.v:259` (`child_tok γ pidv Q` in the pid arm); `ProofUserretClosed.v:~559` (the loop choosing `cs'`). Read the "WX-FORK LANDED" note for the rest.

=== OPERATING RULES (hard) ===
- BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` from /shared/xv6iris-2 under `run_in_background` with the log's `COMPILED=`/`EXIT=` lines as the sentinel; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`; ONE build at a time; never a remote top-level `make`; never local `coqc`. Never `simpl`/`cbn` a goal mentioning a dumped byte literal. This is a WIDE MECHANICAL SWEEP (WX-FORK sized it: `usertrap_res_bare` ~104 sites, `ut_own` ~42, `usertrap_res_parked` ~30, `park_pkg` ~28, `ut_own_nopt` ~25, `URB` ~22, `ut_env_nopt` ~12; 37 files): batch the statements before the first build; use the `sts` index's own sweep as the template (`git log -S'fd_frags' --oneline` finds the commit that added it).
- Gate: zero `Error`, `EXIT=0`, COMPILED>0; `make audit-only` on the VM (tree root `/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen assumptions in durable-notes "The adequacy-print baseline" (print verbatim); `tools/lemma_diff.py` clean (zero Admitted; no new Axiom/Hypothesis/Parameter; a sealed module's changed Parameter reported as such).
- GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`. DO NOT COMMIT OR STAGE. No other checkouts, no worktrees. Temp files in this scratchpad. Do not edit claude-notes/.
- DESIGN FRICTION = STOP AND REPORT. Never weaken a landed form. Comments state the current shape as fact (no dates, no "used to"/"now"); rewrite `kfork_post`'s token-arm note and `WaitInv.ch_frag`'s note (WX-FORK wrote them saying this lane backs the reading).

=== THE DESIGN ===
R1. THE FRAGMENT RIDES THE RESIDUE.  `ch_frag γc (pv_chg (us_V U)) cs` sits beside `fd_frags (pv_fdg (us_V U)) sts` wherever the residue carries the table: `usertrap_res_bare`/`_parked` (and `ut_own`, `ut_own_nopt`, `ut_env_nopt`) gain the `cs` index and the conjunct; `park_pkg`'s `URB` gains `cs`; the parkers capture it "AT A NAMED SET" exactly as they capture `fd_frags` at a named table (`park_token_park` ~350's note is the mold); the closer gains `⌜pv_chg (us_V U') = γ⌝` beside `⌜pv_fdg (us_V U') = g⌝` (nothing between park and resume changes the incarnation's name); `park_cap`'s `∀ cs` is then DETERMINED by the captured fragment -- state it at the fragment's set (the parameter may stay if its instantiation is forced; PREFER removing what the resource determines, as WX-FORK removed `gn`). `SpecUsertrap`'s seam (~1140/1153) reads `uvis_ch` off the fragment; `SpecUservec.uservec_post`/`usertrap_post`'s `cs` binders follow. `γc` (the map's name) is already threaded (`ut_names.un_ch`).
R2. FORK MOVES THE MAP.  kfork, at its `acquire(&wait_lock)` (it already holds it to write `np->parent` and, since WX-FORK, to install the child's row), now also `ghost_map_update`s the PARENT's row: it holds the caller's residue (`ch_frag γc (pv_chg (us_V Up)) cs`) and the auth (in `wait_res_at γc`) → `cs ∪ {[γ]}` for the child's generation γ; `kfork_post`'s token arm gains `ch_frag γc (pv_chg (us_V Up)) (cs ∪ {[γ]})` (the caller's fragment back, moved) beside `child_tok γ pidv Q`; `SpecSysFork`, the dispatcher's fork row and `UexecApply.uexec_ret_round_slot`'s fork answer relay it so the resume key's `uvis_ch W' = uvis_ch W ∪ {[γ]}` is READ off the fragment, not chosen. Freshness `γ ∉ cs`: from `children_wf` (a live generation is in exactly one parent's set and the child's is fresh) -- carry `children_wf` NOW: `wait_res_at γc ξ` gains `⌜children_wf ps m …⌝` over the parents and the map, in the form the readers need (`γ ∈ m !! (pv_chg of slot j) → the slot with generation γ has parent j`; the generation-of-slot reading is `ChildTok.gen_slot`, persistent -- the invariant can name it as a resource conjunct `[∗ set] γ ∈ S, ∃ pa pid Q, gen_slot γ pa ∗ ⌜parent of pa = proc_addr j⌝` rather than a pure fact, since `gen_slot` is persistent; decide and report). If carrying the invariant is out of this lane's budget, STOP AFTER R1 GREEN and report; R2 then becomes its own lane.
R3. SANITY.  `grep -n "ch_frag" iris/UsertrapRes.v iris/ParkCap.v iris/SpecUsertrap.v iris/SpecKfork.v` shows the residue, the park, the seam and the fork post; the round's fork answer no longer says "chosen by the resumer"; `park_cap` has no free `cs`.

=== PHASE 1 -- then STOP AND REPORT ===
R1 as STATEMENTS (the residue's conjunct and index, the park's capture and the closer's premise, the seam, the route's binders); R2's `kfork_post` arm and the invariant's SHAPE decided; NO Admitted in the tree at report time. Build with -k. Report: (1) the residue's new row and the parkers' capture, quoted; (2) what happened to `park_cap`'s `∀ cs`; (3) the invariant's shape and where it is carried; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs, R2, every red file, R3. Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, and an as-landed note (≤ 15 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build and report exactly what remains, file:line.
