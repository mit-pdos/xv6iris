You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is WX-FORK FINISH: you take over a lane whose previous agent landed everything but TWO red files and ran out of context. Read brief-wx-fork.md in this scratchpad for the lane's design and operating rules, then this file. The working tree is DIRTY ON PURPOSE with the lane's WIP (94 modified iris/*.v + iris/_CoqProject, new iris/ChildTok.v; backup `wxfork-p2.patch` + `ChildTok-p2.v` here); do not revert, stash, reset or `git checkout --` anything.

=== STATE (the previous agent's report, verified by build `wxfork18`: COMPILED=1027, EXIT=2, TWO Errors) ===
Green: the whole kernel side -- `ChildTok` (the saved-anything generation: `gen_own γ dq pa pid Q`, `child_tok γ pid Q`, `gen_kq`, `my_pay`, `exit_tok`, `gen_pay`/`gen_pay_timeless` (`◇`), `gen_alloc`/`gen_set`/`gen_split`; class `ctokG` re-exported by `Xv6Cameras`), `ProcDefs.pprivate`'s trailing `pv_gen`/`pv_chg` and the `MkPPriv` sweep, `WaitInv`'s `ghost_map gname (gset gname)` (`children_own_at`, `ch_frag`, `children_wf` stated), `SchedCtx` (WX-KEY's placeholder gone), allocproc's mint (`SpecAllocproc.v:~210`, `ProofAllocproc.v:~2808`), kfork's `gen_set`/`gen_split`/token arm (`kfork_arm3`; the kernel's quarter DROPPED at the split with a comment naming WX-EXIT's escrow), `SpecSysFork`/`ProofSysFork`, the dispatcher's fork arm (`sysc_fork_out` two-armed), `SpecUsertrap`/`ProofUsertrap*`, `SpecUservec`/`ProofUservec`, `UexecSG.sfork_pay`/`sfam_pay`, `UexecExecInst.xfam`'s `kf_pay`, `UexecRet.ufork_ans Q r cs cs' := ⌜r = -1 ∧ cs' = cs⌝ ∨ ∃ γ pidv, ⌜r = sign_extend' 64 pidv⌝ ∗ ⌜cs' = cs ∪ {[γ]}⌝ ∗ child_tok γ pidv Q`, `UexecApply` (the round; `UexecSlot.tf_resume_gpr_a0`), `ProofUserretClosed` (the loop chooses the resume key's `uvis_ch`, `:~555`), `ParkCap`/`ProofForkretPark`/`ProofUserinit`, `UkFork.wp_uk_ecall_fork` (now taking `(Sc : gset gname) (Q : Z -> iProp Σ)` positionally after `c`, the premise `UserChildren.uch (ukn_ch N) Sc`, and giving the parent arm the two-armed `ufork_ans`-shaped resource instead of `⌜r ≠ 0⌝` alone) and its argv twin, every other `Uk*` file.
RED: `iris/UkInitMain.v:~717` (`wp_kinit_fork`'s call to `wp_uk_ecall_fork`) and `iris/UkShRun.v:~1100` (inside `wp_kshr_runcmd`) -- the two callers not yet adapted to the leaf's new statement. Skipped behind them: the rest of the init and sh chains, `UInitKernel`, `UInitSh`, `UShKernel`, `Link*`, `SystemAdequacy`, `App`, `AppEcho`.

=== THE FIX (the previous agent's plan, ruled) ===
`UkRun.uslot_of_urun_all`/`uslot_of_urun`/`_ro` already hand the program `uch (ukn_ch N) (uvis_ch W)` at entry; init and sh currently drop it. (i) Add `UkFork.wp_uk_ecall_fork_any`: the leaf at `UserChildren.uch_any (ukn_ch N)` (index-free, `ustd_any`'s mold) and `Q := fun _ => True%I`, whose parent arm returns `uch_any` and DROPS the token (destruct `ufork_ans`: the -1 arm and the token arm both give `uch_any` back), keeping `⌜r ≠ 0⌝` for the callers that read it -- state it so the two callers change by one line each; the argv twin likewise if either caller uses it. (ii) Thread `UserChildren.uch_any (ukn_ch N)` beside `UserCwd.ucwd γcwd FsImg.ROOTINO` through init's chain (`UkInitMain.v:~662, 671, 815, 822, 1225, 1377, 1598, 1857` and `wp_kinit_start`) and sh's chain (`UkShRun` → `UkShMain`, beside `ush_pstate`'s cwd -- if `UkSh.ush_pstate` is the natural carrier, add `uch_any γch` to it and let the thirteen sites stay unchanged, as CWD-RESOURCE did), sourcing it from the entry constructors (`UInitKernel.init_uexec_slot`, `UShKernel.sh_uexec_slot`: `uch_any_of` on the `uch` they receive; `UEchoKernel` untouched unless it forks). Fork's child arm hands the child `uch (ukn_ch N') ∅` (WX-KEY) -- weaken to `uch_any (ukn_ch N')` where the child chain wants it.

=== OPERATING RULES (hard) ===
As brief-wx-fork.md: BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (run_in_background, sentinel `COMPILED=`/`EXIT=`); ONE build at a time; gate = zero Error, EXIT=0, `make audit-only` on the VM (`/mnt/rocq/trees/_shared_xv6iris-2`) = EXACTLY the thirteen (print verbatim), `tools/lemma_diff.py` (justified GONEs: the WX-KEY placeholder -- `SchedCtx.gen_tok/gen_slot/gen_slot_agree/gen_tok_excl/gen_alloc/proc_gen/proc_gen_fresh/proc_pub_mint/proc_pub_mint_list/proc_pub_bare`, `Xv6Cameras.genR/genG/genΣ`, `Xv6G.xv6_gen`, plus anything WX-FORK renamed -- list them; a sealed module's changed Parameter reported as such; nothing admitted; no new assumption). GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files here; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT. Comments state the current shape as fact.

=== DELIVERABLE ===
The two red files and their chains green; F6's greps from brief-wx-fork.md (`gen_tok`/`gen_slot` name only ChildTok's readings; `pv_gen`/`pv_chg` in ProcDefs/UexecSlot/SpecUsertrap; `child_tok` in ChildTok, SpecKfork, SpecSysFork, SpecSyscall, UexecRet, UexecApply, UkFork; every `Uk*.v` program statement unchanged except `uch_any` threaded and `Q := fun _ => True` at fork sites); the comment sweep (ChildTok's header; `pprivate`'s two field notes; `park_cap`'s dropped `gn`; `usys_ch_ok`'s note; `kfork_post`'s token-arm boundary sentence; `WaitInv.ch_frag`'s). Gate. Report log name, `git status --porcelain`, `git diff --stat -- iris/`, new files, the audit list verbatim, the justified GONE list, and the ≤15-line as-landed note (present tense). If context runs short, STOP after a green -k build and report exactly what remains, file:line.
