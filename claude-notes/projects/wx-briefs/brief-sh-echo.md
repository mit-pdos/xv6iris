You are an APPLICATION-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-tlw (a sibling clone; never touch or build /shared/xv6iris-2, /shared/xv6iris-2-sup, /shared/xv6iris-2-disc; do not touch the branch `lane/tx-receipt` in your checkout). START FROM BRANCH `lane/supply-split-r1` (SUPPLY-SPLIT rebased onto main, green) and work on `lane/sh-echo` on top of it. Your lane is E4 SH-ECHO: sh's forked child, having read the disciplined line "echo hello world\n", parses it into an EXEC command with argv ["echo"; "hello"; "world"] and calls `exec("echo", argv)`; that exec is proved through the PINNED exec bundle (the path "echo" resolves in the root to the echo binary the claim pins), so the post-exec slot is ECHO's verified slot at those arguments, not the generic one; echo's four writes are then what its proof says; and sh's parent waits and prints the next prompt. Design of record: claude-notes/projects/app-echo.md "E4 SH-ECHO" (the original plan), "PINNING -- THE OWNER'S RULING", "PINNED-EXEC C/D LANDED" and "EXEC-ARGS LANDED" (the syscall-shape bundle and how argv is read off the caller's image), "E1 ECHO-PRED LANDED" + "FsEchoPin" (the echo binary's pin in the claim), "GENERIC-PAY LANDED" (runcmd takes both supplies; the child's payload is trivial after `wp_uk_ecall_fork_any`), "SUPPLY-SPLIT PHASE 2 GREEN" (`sh_deps`; echo owes only `udepw_law 16`), "SH-LINE PHASE 2a LANDED" (sh's entry), "E2 INIT-BOOT PHASE 1 + RULINGS" (`PinnedExec.pex_slot_at`, the boot shape -- on E2's branch, not yours: you use the SYSCALL shape `pinned_exec_bundle`), and `review-echo-plan-2026-09-12.md` findings 13 and 10. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Vacuity", "Shaping a change so the sweep is small", the bundle-intro note, the ELF-bytes note, the pinned-C note); remote-build-gcp.md; xv6-riscv/user/sh.c (read-only at /shared/xv6iris-2/xv6-riscv/user/sh.c: `parsecmd`/`parseline`/`parsepipe`/`parseexec`/`nulterminate`, `runcmd`'s EXEC arm `exec(ecmd->argv[0], ecmd->argv)` then `fprintf(2, "exec %s failed\n", …)`; `main`'s loop: `fork1` → child `runcmd(parsecmd(buf))`; parent `wait(0)`), echo.c (four writes: each arg, then " " or "\n"); iris/UkShRun.v (`Inductive ushcmd` :122; `wp_kshr_runcmd` :2715 -- the induction over an ARBITRARY command with `uxsup_at (ukn_pay N)` + `uxsup` (the generic exec supply = the taint) at its EXEC arm; the EXEC arm's body: how it builds the exec bundle from `uxsup` (`udepw_of_uxsup`) and what the arm knows about argv (the `ecmd->argv` array in the child's heap); its fork/wait arms for LIST/BACK); iris/UkShDiag.v (`wp_kshr_runcmd_final` :7568); iris/UkShParse*.v (the parser's proof: does it produce a FUNCTIONAL model of the parsed command from the buffer -- find the lemma that relates the buffer's bytes to the `ushcmd` value and the heap layout of `argv`; if it is only a safety/shape proof, say what a determinacy lemma costs); iris/UkShMain.v (`wp_kshm_child`, `wp_kshm_child_alloc`: the child's walk parsecmd → runcmd; the trivial payload); iris/UkShFork.v (`wp_kshf_fork`, `wp_kshm_body`, `ushf_rest_of_body`: where the child arm is stated; the line facts it has from SH-LINE (`ush_line_is` -- the buffer IS echo_line under `disc`)); iris/PinnedExec.v (`pinned_exec_bundle`: the SYSCALL shape, `sys_exec_au_pre` at `∀ pl, ⌜exec_path_of M pv pl⌝`; `pex_slot`; the pin `pin_resolves Pin cw pl hops ino f nl`; the two wands: the resolving arm hands `kexec_image_ok f na alen afun sts W'` + the linear `Pay`, the taint arm `T`); iris/PinnedObs.v; iris/FsEchoPin.v (`ECHO_INO = 4`, `echo_bytes = ElfUser.echo_elf`, the pin's content sentence, how the claim's pure half `echo_fs_pure` says the root's "echo" is that inode); iris/FsShPin.v (sh's pin, `era0_sh_pins`, the mould for a per-binary pin bundle); iris/UInitSh.v (`init_sh_slot`, `init_exec_sup_of_sh_slot`, `sh_pay` -- INIT execing SH is the landed instance of "a verified program execs a pinned binary whose slot is verified": your lane is the same shape one level down, SH execing ECHO; copy its structure); iris/UShKernel.v (`sh_uexec_slot`/`sh_slot_of_kexec` at `Q`); iris/UEchoKernel.v (`echo_uexec_slot` :~401 -- ECHO's verified entry: its premises (`Hargs` :22, `echo_args M av argcn` :74, `uvis_argc` :61), what it needs from the exec key (`kexec_image_ok echo_elf na alen afun sts W'`, the argv area above sp), the deposit it owes (`udepw_law 16`, E5), its trivial payload); iris/UkEcho.v (`wp_kecho_write` :~1012 and the whole walk: what it PROVES about the four writes today -- the bytes are its argv's; E5 turns the quiet leaf into a receipt); iris/UexecExecInst.v (`exec_args_of`, row 7); iris/UkRun.v (`uxsup_at`, `udepw_at_of_uxsup_at`, `udepwf_at`); iris/UexecExecMint.v (`uslot_mint_all`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-tlw <logname>` (names `shecho10`, `shecho11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Your remote tree was last built at another branch's content: your FIRST build recompiles a large cone -- run it detached BEFORE editing and wait. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-tlw` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref lane/supply-split-r1` justified; nothing Admitted; no new Axiom/Parameter. Kernel files (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`, `UexecExecInst`, `App.v`, `SystemAdequacy.v`) are off limits; the application and U tier (`UkSh*`, `UShKernel`, `UEchoKernel`, `UkEcho*`, `PinnedExec` (additively), `FsEchoPin`, `UInitSh` (only if the exec-supply seam must move), new `UShEcho.v`/`UShEchoK.v`) are yours. GIT: branch `lane/sh-echo` off `lane/supply-split-r1`; commit there when green; never push; never touch other branches; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NO placeholder premises; NO vacuous arms.

=== THE DESIGN ===
S1 THE LINE'S COMMAND.  Under `disc` the child's buffer is exactly `echo_line` = "echo hello world\n" (SH-LINE: `ush_line_is`). State the parse's RESULT for that literal as a value: `echo_cmd : ushcmd := the EXEC node with argv addresses a0,a1,a2 (in the child's heap, malloc'd by parsecmd) holding "echo\0", "hello\0", "world\0"` -- read UkShParse's model to see whether the parse is functional (then `vm_compute`/a lemma `parse_echo_line`) or whether parsecmd's proof only relates the heap to SOME `ushcmd` (then state the determinacy lemma the disciplined line needs: for `echo_line` the result IS an EXEC node with those three strings; prove what is short). Also the redirection/pipe/list arms are NOT taken for this line.
S2 THE PINNED EXEC AT THE EXEC ARM.  `wp_kshr_runcmd`'s EXEC arm, SPECIALISED to `echo_cmd` at the pinned path: `wp_kshr_exec_echo` (a new lemma beside the generic one, reusing its body) whose exec goes through `PinnedExec.pinned_exec_bundle` at `Pin := the claim's echo pin (FsEchoPin's sentence via echo_fs_pure)`, `cw := ROOTINO` (the child's cwd is the root: read where sh's cwd fact lives -- `ush_pstate`'s `ucwd_any` today; you need `ucwd ROOTINO`: state it as the child's premise, inherited from sh's entry (`uvis_cwd W = ROOTINO`, landed by SH-OPEN on its branch -- take it as a premise now)), `pl := "echo"`, `hops := [ROOTINO; ECHO_INO]`, `f := echo_elf`, `T := the taint`, `Q := fun _ => True` (the child's payload IS trivial: `⌜ukn_triv N'⌝` from `wp_uk_ecall_fork_any`), `na := 3`, argv read off the child's image by `exec_args_of M pv na alen afun` from `echo_cmd`'s addresses; the resolving arm's slot is ECHO's: `UEchoKernel.echo_uexec_slot` at `kexec_image_ok echo_elf 3 …` with `echo_args` = ["echo";"hello";"world"]; the taint arm goes generic (`uslot_mint_all`). The GENERIC `wp_kshr_runcmd` stays for the other arms/lines (it is the taint's walk); `wp_kshm_child` dispatches: under `disc` the line is `echo_line` so the parse is `echo_cmd` and the specialised lemma runs; under the taint the generic one.
S3 ECHO'S OUTPUT.  What ECHO's slot proves about its four writes today (`UkEcho.wp_kecho_write` returns an unconstrained `ret` through the quiet leaf at `udepw_law 16`): state, as ECHO's post over its transcript, that the bytes it writes are "hello", " ", "world", "\n" in that order -- stated against the receipt shape TX-RECEIPT is building (`wp_uk_ecall_write_recv`: "my transcript grew by exactly the first r bytes of my buffer") as a NAMED hypothesis (E5's), so E4's part is the argument that the argv bytes ARE those strings (from `echo_args` at `echo_cmd`) and the loop's order. Do not build the write leaf.
S4 THE PARENT.  sh's `wait` after the fork (LIST/BACK-free: for `echo_cmd` the parent just waits, then prints "$ "): the child's exit status is echo's `exit(0)`; the parent's `wait` returns; the loop continues to `getcmd`. State what the parent's loop invariant carries across the round (nothing about the child's payload: trivial).
S5 SANITY: `grep -n "wp_kshr_exec_echo\|echo_cmd" iris/UkSh*.v iris/UShEcho*.v` shows the specialised arm and its use in `wp_kshm_child`; `grep -n "uxsup" iris/UShEcho*.v` EMPTY (the specialised arm never touches the generic supply); the audit unchanged; kernel diff EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
S1 investigated and STATED (the parse's model and `echo_cmd`; determinacy if needed); S2 STATED (`wp_kshr_exec_echo` at the pinned bundle; the child's cwd premise; the dispatch in `wp_kshm_child`); S3 STATED; S4 STATED; compiling, NO Admitted. Report: (1) the statements verbatim, file:line; (2) whether the parser's proof is functional for `echo_line` and what `echo_cmd`'s heap layout is (addresses, how `exec_args_of` reads them); (3) the exact premises the specialised EXEC arm needs beyond the generic one's (the pin law, the cwd, the image inclusion) and where each comes from; (4) the red files; (5) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs, S5, the gate, commit on `lane/sh-echo`. Report log name, commit hash, `git diff --stat lane/supply-split-r1..lane/sh-echo | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), the named hypotheses left (E5's write receipt; the cwd premise's seam).
