You are a proof-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel) in /shared/xv6iris-2. Your lane is GENERIC-PAY: the generic (tainted, arbitrary-code) user-mode slot exists today only at the TRIVIAL exit payload `fun _ => True`; make it exist at any CONSTANT payload `fun _ => R`, then use it to give sh its real payload (the console reader token, or the taint). Design of record: claude-notes/projects/app-echo.md "SH-LINE PHASE 2a LANDED" + "SECOND SEAM FOUND" (read first), "SH-LINE RULING" (Qsh, the token in the payload), "EXEC-PAY LANDED" (exec relays `Q (-1)`; `sexit_pay f (-1) -∗ uslot`), "WX-EXIT LANDED" (`Q xs ∧ Q (-1)` at exit; kill pays at -1), "CONS-CURSOR RULINGS (5) THE LEASE" (why the generic slot must be able to HOLD a token: it is the reclaimed lease). The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; iris/UexecRet.v (`uexec_wp_uslot` :~1948 -- THE generic slot; how it pays exit/kill/exec/fork today at `fun _ => True`; `uexec_pay_dep`/`uexec_pay_arm`; `uslot_run` :1530); iris/UexecCond.v (`cond_entry_slot` :~296); iris/UexecExecMint.v (`uslot_mint` :~87 and every caller: `grep -rn uslot_mint iris/`); iris/UexecExecInst.v (`xv6_sbundle_of_supply` :~775 at `⌜kf_xpay f = (fun _ => True)%I⌝`; the exec row's `Q (-1)` relay; `kf_xpay`); iris/UexecApply.v (`uexec_ret_round_slot`'s fork case: the generic child is minted from the family -- it STAYS trivial); iris/ChildTok.v (`my_pay_agree` :~243, `my_pay`, `gen_pay`); iris/SpecSyscall.v (`sysc_pay_out` unconditional; `sysc_exec_out`); iris/UkRun.v (`uxsup` :~447, `udepw_of_uxsup`, `ukn_triv`, `ukn_const`, `ukn_const_of_eq` :~202, `urun`'s `ukn_pay N (-1)`, `uslot_of_urun_all` :~1213 taking `Q (-1)`, `urun_gen` :~873); iris/PinnedExec.v (`pex_slot` :~163-219; the taint arm :201 at `Q`); iris/UInitSh.v (`init_sh_slot`, `sh_pay`, `init_exec_sup_of_sh_slot` :~434 with its commented undischargeable premise); iris/UkInit.v (`init_exec_sup_pos` :~1094, header :1076-1093 records the wall; `init_exec_sup_lend` :~1105); iris/UShKernel.v (`sh_uexec_slot` :~303, `sh_slot_of_kexec` :~391, the `ukn_triv` ascription :388); iris/UInitKernel.v (`init_uexec_slot` :196, `init_slot_of_kexec` :317); iris/UkInitMain.v (`wp_kinit_fork` :~685 at `Q := fun _ => True`; the restart head; the wait head with `child_tok γsh pidsh (fun _ => True)`; the mint at :~1030); iris/UserConsole.v (`ucons_pay cn γ T xs` :~151 -- CONSTANT in xs (`ucons_pay_const` :160); `uinit_tok`/`uinit_lend`/`uinit_redeem` :213-247, proved; `ucons_reader`); iris/UInitFd.v (`ufd_head` :~181, `ufd_head_open` :~248); iris/UkSh.v (`ush_fd0` :~4173, `ush_pstate` :~4220, `wp_ksh_start` :~5692).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (names `gpay10`, `gpay11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. `UexecRet`/`UexecExecInst`/`UkRun` sit under the whole U tier -- batch statements before each build. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. THIS LANE MAY EDIT `UexecExecInst.v`, `UexecRet.v`, `UexecCond.v`, `UexecExecMint.v`, `UexecApply.v`, `PinnedExec.v` and the U tier; it may NOT edit `Spec*.v`/`Proof*.v`/`ConsoleInv`/`FsAbs*`/`ChildTok.v` -- a needed change there is design friction: STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact. NO vacuous arms, NO placeholder premises.

=== THE DESIGN ===
G1 THE GENERIC SLOT AT A CONSTANT PAYLOAD.  `UexecRet.uexec_wp_uslot` (and `cond_entry_slot`, `uslot_mint`) are generalised from `my_pay (uvis_gen W) (fun _ => True)` to `my_pay (uvis_gen W) (fun _ => R)` for an arbitrary `R : iProp Σ`, taking `R` as a LINEAR premise (the slot holds it): exit pays `(fun _ => R) xs ∧ (fun _ => R) (-1)` from the one `R` (both conjuncts are `R`); kill pays it at -1 the same way through `uexec_pay_dep`/`uexec_pay_arm`; exec relays it through the landed EXEC-PAY row (the dispatcher's `sexit_pay f (-1) -∗ uslot` hands the SAME `R` to the new image's generic slot, minted again by this lemma); every other syscall gets `R` back on resume from `sysc_pay_out`'s unconditional row; fork's generic CHILD is minted at `fun _ => True` as today (`uexec_ret_round_slot`'s fork case is untouched: the parent keeps `R`). State the trivial instance as a corollary so no existing caller changes (`uslot_mint_triv := uslot_mint (R := True)` with `emp`/`True` supplied) -- the sweep must be SMALL: report the callers you had to touch.
G2 EXEC'S SUPPLY AT A CONSTANT PAYLOAD.  `UexecExecInst.xv6_sbundle_of_supply` takes `⌜kf_xpay f = (fun _ => R)%I⌝` in place of the trivial equation (the trivial one is the instance `R := True`); its proof uses G1 to build the post-exec generic slot from the relayed `R`. `UkRun.uxsup`/`udepw_of_uxsup` follow at `ukn_const N` (read `R := ukn_pay N (-1)`; `ukn_const` says the predicate is the same at every status -- if the class's current statement is not literally `∃ R, ukn_pay N = fun _ => R`, state and prove the bridge). `UkShRun.wp_kshr_runcmd`/`UkShDiag.wp_kshr_runcmd_final` then drop `ukn_triv` (the three fork child arms' `⌜ukn_triv N'⌝` stay: the child IS trivial).
G3 THE PINNED TAINT ARM.  `PinnedExec.pex_slot`'s taint arm now discharges at any constant `Q` through G1: `init_sh_slot`/`init_exec_sup_of_sh_slot`/`init_exec_sup_pos` take `Q := ucons_pay fsc_cons γ (echo_taint γcl)` (constant by `ucons_pay_const`).
G4 SH'S PAYLOAD (the acceptance test; the code is already shaped for it -- `UkInit.init_exec_sup_pos`'s header says where): `UShKernel.sh_uexec_slot`/`sh_slot_of_kexec` at `Qsh γ := ucons_pay cn γ T`, building sh's run with `⌜ukn_pay N = Qsh γ⌝` (`ukn_const_of_eq`), `UShKernel.v:388`'s `ukn_triv` ascription GONE; `UkInitMain.wp_kinit_fork` at `Q := Qsh γ` (fresh `γ` per round from `uinit_lend`, `Rc := upos γ n`), `child_tok γsh pidsh (Qsh γ)`, `uinit_redeem` at the reap, `uinit_tok fsc_cons (echo_taint γcl)` on the restart head and absent from the wait head; init KEEPS `ukn_triv` for its own payload. `UInitKernel.init_uexec_slot`/`init_slot_of_kexec` take `UserConsole.ucons_reader cn 0` as a plain linear premise beside `cons_key` (E2 discharges it from the boot bundle's builder through `Pay`; `UInitKernel` binds `ctokG` without `xv6G`, so it is the narrow-class `ucons_reader`, bridged by `ucons_reader_eq`), and init's run starts at `uinit_tok_0`.
G5 THE DESCRIPTOR-ROW PREMISE.  `UInitSh.init_exec_sup_of_sh_slot`'s `(∀ sts, ⊢ UkSh.ush_fd0 T (take NSTD sts))` is undischargeable; replace it by threading `UInitFd.ufd_head T st γfd` into `UkInit.init_exec_sup_pos` in place of the bare `ustd_any` it takes, reading the row off it there (`ufd_head_open` is the shape); the head's three arms ARE `ush_fd0`'s three arms.
G6 SANITY: `grep -n "fun _ => True" iris/UShKernel.v iris/UInitSh.v` EMPTY; `grep -n "ukn_triv" iris/UkSh.v iris/UkShLoop.v iris/UkShCd.v iris/UkShFork.v iris/UShKernel.v iris/UkShRun.v iris/UkShDiag.v` shows ONLY the three fork child arms (comment each: the child's payload is trivial by `wp_uk_ecall_fork_any`); `grep -n "undischargeable\|open obligation" iris/UInitSh.v` EMPTY; `grep -n "ucons_reader\|uinit_tok\|Qsh\|ucons_pay" iris/UInitKernel.v iris/UkInitMain.v iris/UShKernel.v iris/UInitSh.v` shows the route; `git diff --stat -- iris/Spec*.v iris/Proof*.v iris/ConsoleInv.v iris/FsAbs*.v iris/ChildTok.v` EMPTY; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
G1-G2 as STATEMENTS with the trivial corollaries in place so the tree compiles unchanged above them (-k build), and G3-G5 STATED (the lemmas' new signatures; their proofs may remain the OLD proofs where the statement did not change). NO Admitted at report time. Report: (1) `uexec_wp_uslot`'s new statement verbatim and the exact places its proof spends `R` (exit, kill, exec, resume) with file:line; (2) the list of callers of `uslot_mint`/`cond_entry_slot`/`xv6_sbundle_of_supply` and which changed; (3) the red files and why; (4) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (G1-G5), G6, the gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, an as-landed note (≤ 20 lines, present tense). Do not commit or stage. If context runs short, STOP after a green build or at a clear milestone and report exactly what remains, file:line.
