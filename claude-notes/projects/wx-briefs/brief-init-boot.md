You are an APPLICATION-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-disc (a sibling clone; never touch or build /shared/xv6iris-2 or /shared/xv6iris-2-sup). START FROM BRANCH `lane/supply-split-r1` (SUPPLY-SPLIT rebased onto current main, green) and work on `lane/init-boot` rebased onto it (the coordinator tells you the exact base commit at launch). Your lane is E2 INIT-BOOT: discharge the echo application's `Hinit_boot` -- from what the theorem now hands over (lane APP-IFACE) build `InitBoot.init_boot_bundle ROOTINO fdt0`, i.e. the first process's exec bundle, by instantiating init's verified entry (`UInitKernel.init_slot_of_kexec` / `init_uexec_slot`) at echo's era with every premise supplied and NO premise left except what is explicitly owed to E5 (init's banner write deposit `udepw_law 16`). Design of record: claude-notes/projects/app-echo.md "APP-IFACE LANDED" (WHAT E2 RECEIVES), "APP-IFACE PHASE 1" (6)-(8) (the two-armed boot resource; the arm decided by the VIEW, never the era), "OPEN-PIN COMPLETE" + "OPEN-PIN FINDINGS -- RULED WITH THE OWNER" (init's console dance at the KEY arm: miss → mknod → open; FACT 3: init proves nothing about allocation succeeding), "GENERIC-PAY LANDED" (init lends the reader per round; `init_sh_slot`'s taint arm = `uslot_mint_all`; `init_exec_sup_of_sh_slot`'s premises), "SUPPLY-SPLIT PHASE 2 GREEN" (`init_deps T`), "SH-LINE PHASE 2a LANDED", "E2 ARM-c (1b) INIT-BOOT" (the original plan), `review-echo-plan-2026-09-12.md` findings 1, 6, 10. The coordinator owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Vacuity", "Shaping a change so the sweep is small", the `iIntros "#H"` bundle note); remote-build-gcp.md; design/user-wp-slot.md; iris/App.v (`Hinit_boot`'s statement after APP-IFACE: premises `file_app = MkAppcfg …`, `riscv_rx_tag = app_tag A c`, `app_inv fsc_fs`, `app_boot A c r` (LINEAR); conclusion `|==> init_boot_bundle ROOTINO fdt0`); iris/InitBoot.v (`init_boot_bundle` :128 -- a wand from `cons_reader fsc_cons 0` to `∃ P Pmiss Fo R, exec_au_pre (MkPfam uslot R) … init_boot_path 1 … fdt0`; `init_boot_bundle_triv`; what `exec_au_pre`'s families must be and what `PinnedExec`'s bundle provides); iris/PinnedExec.v (`pinned_exec_bundle` -- init's own exec is pinned at /init's path? NO: /init is exec'd BY THE KERNEL at boot (`forkret` → `kexec("/init")`); the bundle here is the kernel's `exec_au_pre` for that exec, whose SLOT family must be init's verified slot: read how `UInitKernel.init_slot_of_kexec` produces `uslot W'` and how the existing E2 plan wires it into `exec_au_pre`'s post-exec slot -- find the landed mould for a pinned exec whose slot is a verified program: `UInitSh.init_exec_sup_of_sh_slot` (init execing sh) and `FsEchoPin`/`FsShPin` (the binary pins)); iris/UInitKernel.v (`init_uexec_slot` :203, `init_slot_of_kexec` :333: EVERY premise -- `T`/`K` with `Persistent T`/`Timeless T`, `stc`, `cn`, the pc/frame/stack/room arithmetic, `forall k, k <> USYS_exec -> psok k` (after SUPPLY-SPLIT: `free_num`-shaped), `udep` (after SUPPLY-SPLIT: `init_deps T`), `init_exec_sup_lend cn T stc`, `□ (∀ N, init_cons_leaves N T K stc)`, `K`, `ucons_reader cn 0`, `my_pay (uvis_gen W') (fun _ => True)`); iris/UInitConsK.v (`init_cons_leaves_echo` :1224 -- discharges the leaves at `T := echo_taint γ`, `K := cons_key r` from the record equation + `app_inv`: THE KEY ARM); iris/UInitCons.v (`init_cons_laws` :955, the nine laws, `init_cons_laws_echo`); iris/UkInit.v (`init_cons_leaves`, `uki_open_absent_leaf` / `uki_open_console_leaf` / `uki_mknod_leaf`; `init_deps`; `init_exec_sup_pos`/`_lend`); iris/UkInitMain.v (the whole walk: the first open's two arms (absent → repair; console), the dups, the fork/wait loop with `uinit_tok`/`uinit_lend`/`uinit_redeem`, `ufd_head`); iris/UInitFd.v; iris/UInitSh.v (`init_sh_slot T Pay` :381 -- `app_inv ∗ □ (claim → era0_sh_pins ∨ T) ∗ □ (∀ R W, T -∗ my_pay _ (fun _ => R) -∗ R -∗ uslot W) ∗ Pay`; `sh_pay Rsh n0` :340; `init_exec_sup_of_sh_slot` :445 and its premises incl. `⌜∃ wr, st = FdOpen true wr (FdDevice CONSOLE)⌝`); iris/FsShPin.v (`era0_sh_pins`, the sh binary's pin and the claim law that reads it); iris/AppEcho.v (`echo_boot`, `echo_cons_law`, `echo_cons_abs_law`, the seven claim laws, `echo_sup_of_taint`, `echo_taint_of_sup`, `echo_tag`, `echo_init_key`, the `Hinit_boot` placeholder/header's owed list, `app_echo`'s dischargers); iris/AppInv.v (`app_inv`, `app_sup`, how a claim law is proved from `app_inv`); iris/UexecExecMint.v (`uslot_mint_all`, `udep_gen`); iris/UserConsole.v (`ucons_reader_eq`); iris/UConsLine.v (`ush_tag_law`); iris/FsConsPin.v (`cons_present_at`, `cons_absent`, `cons_dev`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-disc <logname>` FROM /shared/xv6iris-2-disc (names `e2boot10`, `e2boot11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-disc` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py --ref <base>` justified; nothing Admitted; no new Axiom/Parameter. Kernel files (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`, `UexecExecInst`, `App.v`, `SystemAdequacy.v`) are off limits; a needed change there is design friction: STOP AND REPORT with the goal quoted. The application and U tier (`AppEcho`, `UInit*`, `UkInit*`, `InitBoot` if application-side, `PinnedExec` consumers, new `UInitBoot.v`) are yours. GIT: branch `lane/init-boot` off the base the coordinator names; commit there when green; never push; never touch other branches; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. NO placeholder premises; NO vacuous arms: both arms of the boot resource are init's verified code at every era.

=== THE DESIGN ===
B1 THE TWO ARMS OF THE CONSOLE DANCE.  `app_boot = echo_boot γ r = cons_key r ∨ ∃ i, cons_made r i`. KEY arm (the view has no console node -- era 0's image, or any era after a crash before the mknod committed or a failed mknod): the landed `init_cons_leaves_echo` at `K := cons_key r` (first open MISSES; repair mknod; second open HITS at the minted flag, or the mknod failed and init proceeds with closed descriptors -- FACT 3). FLAG arm (the view has the node at inum `i`, `cons_made r i` persistent): init's FIRST open HITS -- a pinned observation on `echo_cons_law` at `cons_made r i`, the same leaf as OPEN-PIN's SECOND open (`uki_open_console_leaf` discharged in `UInitConsK` at the flag); the `if (open < 0)` repair arm is NOT taken. So `init_cons_leaves N T K stc` at the flag arm: state `init_cons_leaves_echo_flag : file_app = … -> app_inv -∗ cons_made r i -∗ □ (∀ N, init_cons_leaves N (echo_taint γ) K' init_cons_fd)` for a `K'` that the flag arm can pay (the leaves' absent-open leaf is UNREACHABLE at the flag arm only in the sense that the open does not miss -- but the LEAF must still be provided: at the flag arm the first open is proved by the console leaf, and `uki_open_absent_leaf`'s premise… READ how `init_cons_leaves` bundles the three and whether the first open's leaf is stated as "absent" or as a two-armed receipt; if it is stated as absent-only, the flag arm needs `UkInit`'s first-open site to take a two-armed leaf (hit ∨ miss), which is a U-tier change in your remit: do it, keeping the key arm's proofs). `K` is consumed exactly once per arm; `Timeless K` holds for both (`cons_key` is Excl; `cons_made` is a mono_list frag).
B2 THE OTHER PREMISES, all from what `Hinit_boot` receives: `T := echo_taint γ` (persistent, timeless); the `free_num` admission from `uprogSG_free`; `init_deps T` = `udepw_law 16` (OWED TO E5: this is the ONE premise that may remain, stated on `echo_Hinit_boot` as an explicit hypothesis `Hwr16 : ⊢ udepw_law 16`-shaped, marked as E5's) ∗ `□ (T -∗ udepw_law 15)` ∗ `□ (T -∗ udepw_law 17)` (from `□ (T -∗ app_sup)` + `udep_gen`/`udepw_of_law`-style laws -- prove them); `init_exec_sup_lend cn T stc` (persistent, from the claim laws: read `UkInit.init_exec_sup_lend`'s definition and `GENERIC-PAY LANDED`); `init_sh_slot T (sh_pay Rsh n0)` (from `app_inv`, `FsShPin`'s claim law, `uslot_mint_all`, and `sh_pay` -- `sh_pay` is persistent and is about sh's IMAGE at exec: find what discharges it (the sh binary's pin / `FsShPin`); the taint arm from `UexecExecMint.uslot_mint_all`); `init_exec_sup_of_sh_slot`'s `⌜∃ wr, st = FdOpen true wr (FdDevice CONSOLE)⌝` (from the head's console arm); `ush_tag_law`'s premise from `riscv_rx_tag = app_tag A c` (`rewrite`); the reader token: the bundle's argument `cons_reader fsc_cons 0` bridged by `ucons_reader_eq`; `my_pay (uvis_gen W') (fun _ => True)` and the entry-ledger/cwd/image/frame facts read off `exec_au_pre`'s shape for `init_boot_path` (the landed E2 plan says how; `init_boot_bundle_triv` shows the generic instantiation of the same shape).
B3 THE THEOREM SIDE.  `AppEcho.echo_Hinit_boot : forall HR GEN …, file_app = MkAppcfg echo_names (echo_pred γ) r -> riscv_rx_tag = echo_tag γ -> ⊢ app_inv fsc_fs -∗ echo_boot γ r -∗ |==> init_boot_bundle ROOTINO fdt0` (with the one E5 hypothesis), and `app_echo`'s `Hinit_boot` discharger pointing at it, so that `App.xv6_app_adequacy` at `app_echo` has `Hphi` as its ONLY open hypothesis (state `echo_adequacy_modulo_phi` or the like, taking `Hphi` and the E5 write deposit as its hypotheses -- and NOTHING else).
B4 SANITY: `grep -n "Hinit_boot" iris/AppEcho.v` shows the discharge; `grep -rn "Admitted\|Hypothesis" iris/AppEcho.v iris/UInit*.v` shows only the E5 write-deposit hypothesis (named, commented as E5's); the audit unchanged; kernel diff EMPTY.

=== PHASE 1 -- then STOP AND REPORT ===
B1's flag-arm leaves STATED (and the U-tier first-open change if needed), B2's laws STATED with the short ones proved, B3 STATED; compiling; NO Admitted. Report: (1) the statements verbatim, file:line; (2) the complete premise-by-premise table of `init_slot_of_kexec` at echo's era: each premise, where it comes from, proved/stated/owed; (3) how `exec_au_pre`'s families are instantiated for /init (P, Pmiss, Fo, R) and by what; (4) the red files; (5) anything this brief got wrong, with file:line.
=== PHASE 2 -- after go-ahead ===
The proofs, B4, the gate, commit on `lane/init-boot`. Report log name, commit hash, `git diff --stat <base>..lane/init-boot | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense), the exact remaining hypotheses of `echo_adequacy_modulo_phi`.
