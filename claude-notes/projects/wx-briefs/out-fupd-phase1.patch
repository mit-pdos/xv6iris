diff --git a/iris/App.v b/iris/App.v
index 8b47e096b..5c39d36ea 100644
--- a/iris/App.v
+++ b/iris/App.v
@@ -146,20 +146,33 @@ Record xv6_app (Σ : gFunctors) := MkApp {
      one per queued byte and every reader copies it out.  It is the record's
      entry for the machine's ambient [RiscvPtsto.riscv_rx_tag]. *)
   app_tag   : app_fixed -> list mobs -> iProp Σ;
+  (* THE OUTPUT PREDICATE (app-echo.md, "THE OWNER'S REDESIGN OF THE UART
+     OUTPUT SIDE", lane OUT-FUPD): what the application claims of the RAW
+     BYTES a UART instance has accepted, against the receive column's
+     histories.  PURE, so the device invariant carries a fact and never an
+     application resource; INDEXED BY THE INSTANCE, because the console and
+     the kernel's own UART are two devices and the theorem is about the
+     console's -- an application says what it likes of the console's bytes
+     and [True] of the kernel's.  It is the record's entry for the
+     machine's ambient [RiscvPtsto.riscv_out_ok], and [Hout0] below is the
+     one fact the boot needs of it. *)
+  app_out   : app_fixed -> uart_names -> list (list mobs) -> list (bv 8) -> Prop;
   (* the conclusion, over the operational state and the run's trace *)
   app_phi   : gstate -> list mobs -> Prop;
 }.
-Arguments MkApp {Σ} _ _ _ _ _ _ _ _.
+Arguments MkApp {Σ} _ _ _ _ _ _ _ _ _.
 Arguments app_fixed {Σ} _. Arguments app_cl {Σ} _ _.
 Arguments app_names {Σ} _. Arguments app_pred {Σ} _ _ _ _.
 Arguments app_boot {Σ} _ _ _.
 Arguments app_R {Σ} _ _ _. Arguments app_tag {Σ} _ _ _.
+Arguments app_out {Σ} _ _ _ _ _.
 Arguments app_phi {Σ} _ _ _.
 
 (* THE GENERIC APPLICATION: no fixed part, nothing claimed, nothing read *)
 Definition app_triv (Σ : gFunctors) : xv6_app Σ :=
   MkApp unit (fun _ => True%I) unit (fun _ _ _ => True%I) (fun _ _ => emp%I)
-        (fun _ _ => emp%I) (fun _ _ => True%I) (fun _ _ => True).
+        (fun _ _ => emp%I) (fun _ _ => True%I) (fun _ _ _ _ => True)
+        (fun _ _ => True).
 
 (* ---------------------------------------------------------------------- *)
 (* THE THEOREM.  [xv6_power_adequacy_gen] at the application: the birth    *)
@@ -186,6 +199,13 @@ Theorem xv6_app_adequacy Σ
     (* ...and timeless, because the receive column that files it lives in the
        UART invariant, whose body every device leaf strips a later off *)
     (Htagt : forall (c : app_fixed A) (h : list mobs), Timeless (app_tag A c h))
+    (* THE OUTPUT PREDICATE'S FOUNDING FACT (lane OUT-FUPD).  The device
+       mint runs at power-on, where the receive FIFO is empty and nothing
+       has been accepted ([WpUart.uart_ghosts_alloc]), so this is the one
+       thing the boot needs of [app_out] -- and it is the [HR0] of the
+       output side: a fact about the application's own choice, discharged
+       where the choice is made. *)
+    (Hout0 : forall (c : app_fixed A) (gu : uart_names), app_out A c gu [] [])
     (HR0 : forall c : app_fixed A, app_cl A c ⊢ |==> app_R A c [])
     (Hpow : forall (c : app_fixed A) (h : list mobs) (on : bool) (dk : Z -> bv 8),
        trace_shape h on ->
@@ -209,9 +229,27 @@ Theorem xv6_app_adequacy Σ
                   (c : app_fixed A) (r : app_names A) (γ : uart_names),
        @file_app Σ HF = MkAppcfg (app_names A) (app_pred A c) r ->
        FsCfg.fsc_uart = γ ->
-       ⊢ □ (∀ (h : list mobs) (b : bv 8) (u u' : uart_state),
+       ⊢ □ (∀ (h : list mobs) (b : bv 8) (u u' : uart_state)
+                (hs : list (list mobs)),
               ⌜uart_tx_pop u = Some (b, u')⌝ -∗ ⌜uart_loopback u = false⌝ -∗
               ⌜trace_shape h true⌝ -∗ ⌜obs_wire (open_seg h) = u_wire u⌝ -∗
+              (* THE WIRE IS THE DRAINED SEQUENCE.  A clause of the UART
+                 invariant's receive column since TX-TAG's rider
+                 ([WpUart.uart_colE_wire_out]), handed over here because it
+                 is what makes the accepted sequence an EXTENSION of the
+                 wire: [uart_acc u = u_out u ++ u_tx u] and [u_wire u =
+                 u_out u], so the wire is a prefix of [uart_acc u] and a
+                 prefix-closed output claim transfers to it.  This closes
+                 the premise TX-TAG left owed to E5. *)
+              ⌜u_wire u = u_out u⌝ -∗
+              (* ...AND THE OUTPUT CLAIM at the drain (lane OUT-FUPD): the
+                 bytes this UART has ACCEPTED are good for the input the
+                 receive column's histories record.  Every one of them was
+                 justified at its store by the writer's own view shift, so
+                 this is where the kernel's output discipline reaches the
+                 ledger -- and it is the only channel: the invariant carries
+                 no application resource. *)
+              ⌜app_out A c γ hs (uart_acc u)⌝ -∗
               uart_ghosts γ u' -∗ app_R A c h
                 ={⊤ ∖ ↑uartN ∖ ↑obsN}=∗
               uart_ghosts γ u' ∗ app_R A c (h ++ [ObsUartOut b])%list))
@@ -278,7 +316,8 @@ Theorem xv6_app_adequacy Σ
                (xv6_slot (app_names A) (app_pred A) cov (FsImg.sb_logstart sb)
                   γd γsw γreg γstart c)
                γobs T (obs_ledger_at (app_R A c) γobs) γhist
-               (app_tag A c) (Htagp c) (Htagt c) (app_fixed A) c) g' -∗
+               (app_tag A c) (Htagp c) (Htagt c)
+               (app_out A c) (Hout0 c) (app_fixed A) c) g' -∗
          ghost_var γobs (1/2) h -∗ ⌜obs_wf h g'⌝ -∗
          ▷ xv6_slot (app_names A) (app_pred A) cov (FsImg.sb_logstart sb)
              γd γsw γreg γstart c -∗
@@ -303,20 +342,22 @@ Proof.
              (xv6_slot (app_names A) (app_pred A) cov (FsImg.sb_logstart sb)
                 γd γsw γreg γstart c)
              γobs T (obs_ledger_at (app_R A c) γobs) γhist
-             (app_tag A c) (Htagp c) (Htagt c) (app_fixed A) c
+             (app_tag A c) (Htagp c) (Htagt c)
+             (app_out A c) (Hout0 c) (app_fixed A) c
          /\ @file_app Σ HF = MkAppcfg (app_names A) (app_pred A c) r
          /\ FsCfg.fsc_uart = γ) ->
       ⊢ obs_inv -∗ uart_obs_permit γ).
   { intros HRg GEN HFi ri γ
       (Hi & Gg & Gs & Gr & Gt & Gsw & Gob & Ghist & Gcl & GT & Heq & Happ & Huart).
-    refine (uart_obs_permit_ledger (app_R A Gcl) (app_tag A Gcl) γ (HRt Gcl)
-              _ _ (Htx HRg HFi Gcl ri γ Happ Huart)
-                  (Hrx HRg HFi Gcl ri γ Happ Huart));
+    refine (uart_obs_permit_ledger (app_R A Gcl) (app_tag A Gcl)
+              (app_out A Gcl) γ (HRt Gcl)
+              _ _ _ (Htx HRg HFi Gcl ri γ Happ Huart)
+                    (Hrx HRg HFi Gcl ri γ Happ Huart));
       rewrite Heq; reflexivity. }
   exact (xv6_power_adequacy_gen Σ g sb nib cov
            (app_fixed A) (app_cl A) Hbirth
            (app_names A) (app_pred A) (app_boot A) Happ_boot Happ_init
-           (app_tag A) Htagp Htagt Hinit_boot
+           (app_tag A) Htagp Htagt (app_out A) Hout0 Hinit_boot
            (fun γobs c => obs_ledger_at (app_R A c) γobs)
            (fun γobs c =>
               obs_ledger_at_alloc_cl (app_R A c) γobs (app_cl A c) (HR0 c))
@@ -417,10 +458,12 @@ Proof.
            ltac:(intros c h; cbn [app_triv app_R]; apply _)
            ltac:(intros c h; cbn [app_triv app_tag]; apply _)
            ltac:(intros c h; cbn [app_triv app_tag]; apply _)
+           ltac:(intros c gu; cbn [app_triv app_out]; exact Logic.I)
            app_triv_R0
            ltac:(intros c h on dk _; cbn [app_triv app_R]; iIntros "_"; by iModIntro)
            ltac:(intros HR HFi c r γ _ _; cbn [app_triv app_R];
-                 iIntros "!>" (h b u u') "_ _ _ _ Hg _"; iModIntro; by iFrame "Hg")
+                 iIntros "!>" (h b u u' hs) "_ _ _ _ _ _ Hg _"; iModIntro;
+                 by iFrame "Hg")
            ltac:(intros HR HFi c r γ _ _; cbn [app_triv app_R app_tag];
                  iIntros "!>" (h b u u') "_ _ Hg _"; iModIntro;
                  iFrame "Hg"; auto)
diff --git a/iris/RiscvAdequacy.v b/iris/RiscvAdequacy.v
index 97452a298..7203b71d3 100644
--- a/iris/RiscvAdequacy.v
+++ b/iris/RiscvAdequacy.v
@@ -1156,6 +1156,13 @@ Definition boot_fixedGS {Σ : gFunctors} `{!xv6G Σ, !riscvGpreS Σ}
        in a context that has no [riscvFixedGS] yet. *)
     (Tg : list mobs -> iProp Σ) (HTg : forall h, Persistent (Tg h))
     (HTgt : forall h, Timeless (Tg h))
+    (* THE OUTPUT PREDICATE (app-echo.md, lane OUT-FUPD), the transmit
+       side's twin of the tag family and a Coq-level argument for the same
+       reason: what the application claims of the bytes a UART INSTANCE has
+       accepted, against the receive column's histories, with the founding
+       fact at the empty run. *)
+    (Ook : uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall gu : uart_names, Ook gu [] [])
     (* the application's FIXED PART (app-instances.md §6 ruling 1): its
        type and the one value [riscv_power_adequacy]'s birth step produced,
        before the crash slot *)
@@ -1168,7 +1175,7 @@ Definition boot_fixedGS {Σ : gFunctors} `{!xv6G Σ, !riscvGpreS Σ}
      runs of underscores are one longer each; the trace fields at the end
      are main's.  All resolve from [riscvGpreS]. *)
   RiscvFixedGS Σ Hinv _ _ _ _ _ _ _ _ _ _ _ _ _ γgen γstart _ γreg
-    _ _ _ γdisk ndisk Pcp γswap _ γobs T Ptp _ γhist Tg HTg HTgt CT c.
+    _ _ _ γdisk ndisk Pcp γswap _ γobs T Ptp _ γhist Tg HTg HTgt Ook HOok CT c.
 
 (* ---------------------------------------------------------------------- *)
 (* THE TRACE HOOK'S HELPERS -- ONE PER CONJUNCT OF [state_interp].          *)
@@ -1234,11 +1241,14 @@ Lemma disk_proj_trace {Σ : gFunctors} `{!xv6G Σ, !riscvGpreS Σ}
     (Hinv : invGS Σ) (γgen γstart γreg γdisk γswap : gname)
     (γobs : gname) (T : list mobs) (Ptp : iProp Σ) (γhist : gname)
     (Tg : list mobs -> iProp Σ) (HTg : forall h, Persistent (Tg h))
-    (HTgt : forall h, Timeless (Tg h)) (c : CT)
+    (HTgt : forall h, Timeless (Tg h))
+    (Ook : uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall gu : uart_names, Ook gu [] []) (c : CT)
     (g' : gstate) :
   ⊢ @power_interp Σ
        (boot_fixedGS Hinv γgen γstart γreg γdisk ndisk γswap
-          (Pc γdisk γswap γreg γstart c) γobs T Ptp γhist Tg HTg HTgt CT c) g' -∗
+          (Pc γdisk γswap γreg γstart c) γobs T Ptp γhist Tg HTg HTgt
+          Ook HOok CT c) g' -∗
     ▷ Pc γdisk γswap γreg γstart c -∗
     ◇ ⌜Ppure (v_disk (dvirtio (gdev g')))⌝.
 Proof.
@@ -1517,6 +1527,14 @@ Theorem riscv_power_adequacy Σ `{!xv6G Σ, !riscvGpreS Σ}
     (Tg : CT -> list mobs -> iProp Σ)
     (HTg : forall (c : CT) (h : list mobs), Persistent (Tg c h))
     (HTgt : forall (c : CT) (h : list mobs), Timeless (Tg c h))
+    (* THE OUTPUT PREDICATE, at the application's fixed part (app-echo.md
+       lane OUT-FUPD).  A SLOT of the fixed record like the tag family, so
+       it is a parameter here and the record literal below carries it; no
+       hook of this layer reads it -- the UART invariant carries it
+       ([WpUart.uart_col]), the transmit store re-establishes it from the
+       writer's view shift, and the trace ledger reads it at a drain. *)
+    (Ook : CT -> uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall (c : CT) (gu : uart_names), Ook c gu [] [])
     (* ...born holding the empty history AND what the application's birth
        step yielded (app-instances.md §6 ruling 1): the birth ran first,
        and the trace slot is the owner of its yield from the slot's own
@@ -1588,7 +1606,7 @@ Theorem riscv_power_adequacy Σ `{!xv6G Σ, !riscvGpreS Σ}
        ⊢ @power_interp Σ
             (boot_fixedGS Hinv γgen γstart γreg γdisk ndisk γswap
                (Pc γdisk γswap γreg γstart c) γobs T (Pt γobs c) γhist
-               (Tg c) (HTg c) (HTgt c) CT c) g' -∗
+               (Tg c) (HTg c) (HTgt c) (Ook c) (HOok c) CT c) g' -∗
          ghost_var γobs (1/2) h -∗ ⌜obs_wf h g'⌝ -∗
          ▷ Pc γdisk γswap γreg γstart c -∗ ▷ Pt γobs c -∗
          ◇ ⌜phi g' h⌝)
@@ -1633,7 +1651,7 @@ Theorem riscv_power_adequacy Σ `{!xv6G Σ, !riscvGpreS Σ}
               (c : CT) (T : list mobs),
        F = boot_fixedGS Hinv γgen γstart γreg γdisk ndisk γswap
              (Pc γdisk γswap γreg γstart c) γobs T (Pt γobs c) γhist
-             (Tg c) (HTg c) (HTgt c) CT c ->
+             (Tg c) (HTg c) (HTgt c) (Ook c) (HOok c) CT c ->
        ⊢ obs_inv -∗ power_boot_res HE gen D nproc ndisk Mof (Rb c) g' ={⊤}=∗
           ([∗ list] c ∈ enum CPU,
              WP (LoopE gen c : expr riscv_lang) @ ⊤) ∗
@@ -1711,7 +1729,7 @@ Proof.
      lets [state_interp] tie the history so far to the future *)
   set (F := boot_fixedGS Hinv γgen γstart γreg γfdisk ndisk γswap
               (Pc γfdisk γswap γreg γstart c) γobs κs (Pt γobs c) γhist
-              (Tg c) (HTg c) (HTgt c) CT c).
+              (Tg c) (HTg c) (HTgt c) (Ook c) (HOok c) CT c).
   (* the client's trace hook at the gnames just allocated.  [F] is a local
      DEFINITION, so this statement and the one the final observation below
      faces are convertible. *)
diff --git a/iris/RiscvPtsto.v b/iris/RiscvPtsto.v
index edba08031..a6c857f54 100644
--- a/iris/RiscvPtsto.v
+++ b/iris/RiscvPtsto.v
@@ -12,6 +12,9 @@ Require Import Riscv.rv64d_types Riscv.rv64d.
 Require Import RiscvModelBytes.
 Require Import RiscvLang.
 Require Import ObsTrace.
+Require Import UartNames.  (* [uart_names]: the INSTANCE index of [riscv_out_ok]
+                              below -- a record of gnames, depending on
+                              nothing, so this edge costs the dag nothing *)
 Require Import TsoMemPa TsoGhost.  (* the TSO machine ghosts (tso-machine-flip.md) *)
 Require Export DiskImg.  (* [diskImgG]/[disk_img_auth]: the disk image map *)
 (* [disk_write]/[disk_wr]/[wr_apply]: the disk image and the pure write
@@ -564,6 +567,54 @@ Class riscvFixedGS (Σ : gFunctors) := RiscvFixedGS {
   riscv_rx_tag : list mobs -> iProp Σ;
   riscv_rx_tag_persistent : forall h, Persistent (riscv_rx_tag h);
   riscv_rx_tag_timeless : forall h, Timeless (riscv_rx_tag h);
+  (* THE OUTPUT PREDICATE (claude-notes/projects/app-echo.md, "THE OWNER'S
+     REDESIGN OF THE UART OUTPUT SIDE", lane OUT-FUPD).  The transmit side's
+     twin of [riscv_rx_tag], and PURE where that one is an [iProp]: the
+     application's claim about the RAW BYTES the UART has ACCEPTED so far,
+     read against the receive column's histories -- the input so far, each
+     entry a whole trace prefix, with its provenance.
+
+     WHERE IT LIVES.  The UART invariant carries it at every state, beside
+     the receive column's own well-formedness ([WpUart.uart_col]).  The ONE
+     transition that grows the accepted sequence is the store to the
+     transmit register ([DevModel.uart_write_thr_acc]), and its leaf
+     ([WpSconfUartAccess.wp_uart_thr_write_s_sconf]) re-establishes the
+     clause from a VIEW SHIFT ITS CALLER SUPPLIES: every byte that ever
+     reaches the wire was justified, at the store, by whoever stored it.
+
+     WHY PURE, AND WHY THIS IS ALL THE INVARIANT CARRIES.  Attribution --
+     which writer pushed which byte, and what private ledger that writer
+     keeps -- is APPLICATION-PRIVATE knowledge, updated inside the writers'
+     own view shifts; the device invariant carries only the pure
+     consequence.  So nothing in the kernel's output path depends on an
+     application RESOURCE living in a device invariant, and the append-only
+     located-prefix receipts the tree used to hand writers
+     ([UartTxInv.uart_sent_sub], [UartSentLoc.uart_sent_from]) are retired
+     rather than generalized.
+
+     The application sets it at boot, exactly as it sets [riscv_rx_tag]
+     ([App.app_out] at the [boot_fixedGS] literal); the trivial application
+     sets it to [fun _ _ _ => True].
+
+     INDEXED BY THE UART INSTANCE, and that is not decoration.  xv6 is
+     moving to TWO UARTs -- one the kernel's own (printk), one the CONSOLE,
+     entirely under user-process control, with no mixing -- and the theorem
+     is about the console's.  [uart_names] is the device instance's
+     identity (every UART resource in the tree is already stated at one),
+     so an application says what it claims of the CONSOLE's accepted bytes
+     and says [True] of the kernel's, and the kernel instance's writers owe
+     no justification at all ([WpUart.out_shift_triv]).  Today there is one
+     instance and it is the console's; when the second lands, nothing in
+     this field's statement moves. *)
+  riscv_out_ok : uart_names -> list (list mobs) -> list (bv 8) -> Prop;
+  (* ...AND IT IS FOUNDED AT THE EMPTY RUN.  [WpUart.uart_ghosts_alloc] runs
+     at power-on, where nothing has been accepted and the receive FIFO is
+     empty, so the clause it has to establish is exactly this one.  A FIELD
+     rather than a premise threaded through [BootShared.boot_shared_alloc]:
+     it is a property of the application's own choice of predicate and of
+     nothing else, so it belongs beside the choice -- the same reason
+     [riscv_rx_tag_persistent] is a field. *)
+  riscv_out_ok_nil : forall (gu : uart_names), riscv_out_ok gu [] [];
   (* THE APPLICATION'S FIXED PART (claude-notes/projects/app-instances.md
      §6 ruling 1, round D0).  The machine no longer owns a counter: the
      application declares whatever [Type] its fixed part has, and its BIRTH
@@ -834,6 +885,17 @@ Global Instance rx_tag_triv_timeless {Σ : gFunctors} (h : list mobs) :
   Timeless (rx_tag_triv (Σ := Σ) h).
 Proof. rewrite /rx_tag_triv. apply _. Qed.
 
+(* the trivial OUTPUT predicate: an application that claims nothing of any
+   UART's accepted bytes.  Every writer's view shift is discharged by
+   [WpUart.out_shift_triv] at this instance, and the founding fact is
+   [Logic.I] -- this is what the kernel-only UART will carry once the
+   console/kernel split lands. *)
+Definition out_ok_triv : uart_names -> list (list mobs) -> list (bv 8) -> Prop :=
+  fun _ _ _ => True.
+
+Lemma out_ok_triv_nil (gu : uart_names) : out_ok_triv gu [] [].
+Proof. exact Logic.I. Qed.
+
 (* the TRIVIAL trace predicate -- the client's half and nothing about it.
    What a client that states no trace property fills the slot with. *)
 Definition obs_pred_triv `{!riscvFixedGS Σ} : iProp Σ :=
diff --git a/iris/SystemAdequacy.v b/iris/SystemAdequacy.v
index 0268f78e1..3fd4e7cb7 100644
--- a/iris/SystemAdequacy.v
+++ b/iris/SystemAdequacy.v
@@ -377,11 +377,13 @@ Lemma fs_trace_hook (Σ : gFunctors) `{!xv6G Σ, !riscvGpreS Σ}
     (Hinv : invGS Σ) (γgen γstart γreg γd γsw γobs γhist : gname) (c : CT)
     (T : list mobs) (Tg : list mobs -> iProp Σ)
     (HTg : forall h, Persistent (Tg h)) (HTgt : forall h, Timeless (Tg h))
+    (Ook : uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall gu : uart_names, Ook gu [] [])
     (g' : gstate) :
   ⊢ @power_interp Σ
        (boot_fixedGS Hinv γgen γstart γreg γd XV6_DISK_BYTES γsw
           (xv6_slot N app_fs cov ls γd γsw γreg γstart c)
-          γobs T (obs_pred_at γobs) γhist Tg HTg HTgt CT c) g' -∗
+          γobs T (obs_pred_at γobs) γhist Tg HTg HTgt Ook HOok CT c) g' -∗
     ▷ xv6_slot N app_fs cov ls γd γsw γreg γstart c -∗
     ◇ ⌜fs_boot_pure cov ls (v_disk (g'.(gdev).(dvirtio)))⌝.
 Proof.
@@ -390,7 +392,7 @@ Proof.
            (fs_boot_pure cov ls)
            (xv6_slot_project N app_fs cov ls)
            Hinv γgen γstart γreg γd γsw γobs T (obs_pred_at γobs) γhist
-           Tg HTg HTgt c g').
+           Tg HTg HTgt Ook HOok c g').
 Qed.
 
 (* ...AND A [phi] THAT IS NOT ABOUT THE DISK AT ALL, beside it.
@@ -420,11 +422,13 @@ Lemma xv6_trace_hook (Σ : gFunctors) `{!xv6G Σ, !riscvGpreS Σ}
     (Hinv : invGS Σ) (γgen γstart γreg γd γsw γobs γhist : gname) (c : CT)
     (T : list mobs) (Tg : list mobs -> iProp Σ)
     (HTg : forall h, Persistent (Tg h)) (HTgt : forall h, Timeless (Tg h))
+    (Ook : uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall gu : uart_names, Ook gu [] [])
     (g' : gstate) :
   ⊢ @power_interp Σ
        (boot_fixedGS Hinv γgen γstart γreg γd XV6_DISK_BYTES γsw
           (xv6_slot N app_fs cov ls γd γsw γreg γstart c)
-          γobs T (obs_pred_at γobs) γhist Tg HTg HTgt CT c) g' -∗
+          γobs T (obs_pred_at γobs) γhist Tg HTg HTgt Ook HOok CT c) g' -∗
     ▷ xv6_slot N app_fs cov ls γd γsw γreg γstart c -∗
     ◇ ⌜xv6_trace_pure cov ls g'⌝.
 Proof.
@@ -433,7 +437,7 @@ Proof.
      not spent and the disk projection still has it *)
   iDestruct (power_interp_resv_ok with "Hsi") as %Hresv.
   iDestruct (fs_trace_hook Σ cov ls CT N app_fs Hinv γgen γstart γreg γd γsw
-               γobs γhist c T Tg HTg HTgt g' with "Hsi HP") as ">%Hdisk".
+               γobs γhist c T Tg HTg HTgt Ook HOok g' with "Hsi HP") as ">%Hdisk".
   iModIntro. iPureIntro. split; [exact Hdisk | exact Hresv].
 Qed.
 
@@ -989,6 +993,14 @@ Theorem xv6_power_adequacy_gen Σ
     (Tg : CT -> list mobs -> iProp Σ)
     (HTg : forall (c : CT) (h : list mobs), Persistent (Tg c h))
     (HTgt : forall (c : CT) (h : list mobs), Timeless (Tg c h))
+    (* THE OUTPUT PREDICATE, at the fixed part and at a UART INSTANCE
+       (app-echo.md, lane OUT-FUPD).  The transmit side's twin of the tag
+       family: what the application claims of the bytes THAT UART has
+       accepted, carried by the device invariant ([WpUart.uart_col]) and
+       re-established at the one transmit store from the writer's own view
+       shift.  [HOok] is the founding fact the power-on mint discharges. *)
+    (Ook : CT -> uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall (c : CT) (gu : uart_names), Ook c gu [] [])
     (* THE FIRST PROCESS'S EXEC BUNDLE (ARM-c): the ONE thing the
        application owes the kernel about user execution.  The kernel mints
        no user-execution slot; the first process's comes out of the
@@ -1065,7 +1077,8 @@ Theorem xv6_power_adequacy_gen Σ
             boot_fixedGS Hinv γgen γstart γreg γd XV6_DISK_BYTES γsw
               (xv6_slot app_names app_fs cov (FsImg.sb_logstart sb)
                  γd γsw γreg γstart c)
-              γobs T (Pt γobs c) γhist (Tg c) (HTg c) (HTgt c) CT c
+              γobs T (Pt γobs c) γhist (Tg c) (HTg c) (HTgt c)
+              (Ook c) (HOok c) CT c
           /\ @file_app Σ HF = MkAppcfg app_names (app_fs c) r
           /\ FsCfg.fsc_uart = γ) ->
        ⊢ obs_inv -∗ uart_obs_permit γ)
@@ -1080,7 +1093,8 @@ Theorem xv6_power_adequacy_gen Σ
             (boot_fixedGS Hinv γgen γstart γreg γd XV6_DISK_BYTES γsw
                (xv6_slot app_names app_fs cov (FsImg.sb_logstart sb)
                   γd γsw γreg γstart c)
-               γobs T (Pt γobs c) γhist (Tg c) (HTg c) (HTgt c) CT c) g' -∗
+               γobs T (Pt γobs c) γhist (Tg c) (HTg c) (HTgt c)
+              (Ook c) (HOok c) CT c) g' -∗
          ghost_var γobs (1/2) h -∗ ⌜obs_wf h g'⌝ -∗
          ▷ xv6_slot app_names app_fs cov (FsImg.sb_logstart sb)
              γd γsw γreg γstart c -∗
@@ -1277,7 +1291,7 @@ Proof.
            (* THE TRACE SLOT AND THE TRACE HOOK, threaded straight through:
               this layer fixes the crash predicate but says nothing about the
               trace, so both pass down unexamined. *)
-           Pt Tg HTg HTgt HPt Hobs phi Hphi
+           Pt Tg HTg HTgt Ook HOok HPt Hobs phi Hphi
            Hgen0 Hpow _ n κs t2 g2 Hn).
   (* the per-era boot entailment, at the era instance the power thread just
      minted.  [riscv_fixedGS (RiscvGS Σ F HE)] iota-reduces to [F] and
@@ -1352,6 +1366,7 @@ Theorem xv6_power_adequacy Σ
                   γd γsw γreg γstart c)
                γobs T (obs_pred_at γobs) γhist rx_tag_triv
                (@rx_tag_triv_persistent Σ) (@rx_tag_triv_timeless Σ)
+               out_ok_triv out_ok_triv_nil
                unit c) g' -∗
          ▷ xv6_slot unit (fun _ _ _ => True%I) cov (FsImg.sb_logstart sb)
              γd γsw γreg γstart c -∗
@@ -1379,6 +1394,8 @@ Proof.
             (fun _ : unit => rx_tag_triv)
             (fun (_ : unit) (h : list mobs) => @rx_tag_triv_persistent Σ h)
             (fun (_ : unit) (h : list mobs) => @rx_tag_triv_timeless Σ h)
+            (fun _ : unit => out_ok_triv)
+            (fun (_ : unit) (gu : uart_names) => out_ok_triv_nil gu)
             ltac:(intros HRi GENi HBsi HFdi HIri HPavi HWci HFi ci ri Heq Htag;
                   iIntros "_ _"; iModIntro; iApply init_boot_of_triv;
                   rewrite Heq; intros r' av; reflexivity)
@@ -1408,6 +1425,12 @@ Theorem xv6_trace_adequacy Σ
        rx wand below produces it; the machine's ambient slot IS it. *)
     (Tg : list mobs -> iProp Σ) (HTg : forall h, Persistent (Tg h))
     (HTgt : forall h, Timeless (Tg h))
+    (* THE OUTPUT PREDICATE (app-echo.md lane OUT-FUPD): what the client
+       claims of the bytes a UART instance has accepted, read against the
+       receive column's histories.  The transmit wand below is handed it at
+       every drain; the writers re-establish it at the store. *)
+    (Ook : uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall gu : uart_names, Ook gu [] [])
     (HR0 : ⊢ |==> R [])
     (Hpow : forall (h : list mobs) (on : bool) (dk : Z -> bv 8),
        trace_shape h on ->
@@ -1418,9 +1441,19 @@ Theorem xv6_trace_adequacy Σ
        update needs its [invGS]; the wands themselves mention nothing
        era-specific. *)
     (Htx : forall (HR : riscvGS Σ) (γ : uart_names),
-       ⊢ □ (∀ (h : list mobs) (b : bv 8) (u u' : uart_state),
+       ⊢ □ (∀ (h : list mobs) (b : bv 8) (u u' : uart_state)
+                (hs : list (list mobs)),
               ⌜uart_tx_pop u = Some (b, u')⌝ -∗ ⌜uart_loopback u = false⌝ -∗
               ⌜trace_shape h true⌝ -∗ ⌜obs_wire (open_seg h) = u_wire u⌝ -∗
+              (* THE WIRE IS THE DRAINED SEQUENCE (the clause TX-TAG left
+                 owed): with LOOP off throughout, [u_wire] and [u_out] are
+                 the same list, so the wire is a PREFIX of [uart_acc u] and
+                 a prefix-closed output claim transfers to it. *)
+              ⌜u_wire u = u_out u⌝ -∗
+              (* ...AND THE OUTPUT CLAIM ITSELF, read off the receive
+                 column at the drain: the bytes this UART has accepted are
+                 good for the input the column's histories record. *)
+              ⌜Ook γ hs (uart_acc u)⌝ -∗
               uart_ghosts γ u' -∗ R h ={⊤ ∖ ↑uartN ∖ ↑obsN}=∗
               uart_ghosts γ u' ∗ R (h ++ [ObsUartOut b])%list))
     (Hrx : forall (HR : riscvGS Σ) (γ : uart_names),
@@ -1450,6 +1483,7 @@ Proof.
                   iPureIntro; exact Logic.I)
             (fun _ : unit => Tg) (fun (_ : unit) (h : list mobs) => HTg h)
             (fun (_ : unit) (h : list mobs) => HTgt h)
+            (fun _ : unit => Ook) (fun (_ : unit) (gu : uart_names) => HOok gu)
             ltac:(intros HRi GENi HBsi HFdi HIri HPavi HWci HFi ci ri Heq Htag;
                   iIntros "_ _"; iModIntro; iApply init_boot_of_triv;
                   rewrite Heq; intros r' av; reflexivity)
@@ -1465,7 +1499,8 @@ Proof.
   (* the permit at the ledger: the client's two wands *)
   intros HRg GEN HFi ri γ
          (Hi & Gg & Gs & Gr & Gt & Gsw & Gob & Ghist & Gcl & GT & Heq & _ & _).
-  refine (uart_obs_permit_ledger R Tg γ HRt _ _ (Htx HRg γ) (Hrx HRg γ));
+  refine (uart_obs_permit_ledger R Tg Ook γ HRt _ _ _
+            (Htx HRg γ) (Hrx HRg γ));
     rewrite Heq; reflexivity.
 Qed.
 
@@ -1721,6 +1756,7 @@ Corollary xv6_power_adequacy_xv6Σ (g : gstate)
                   (FsImg.sb_logstart fsimg_sb) γd γsw γreg γstart c)
                γobs T (obs_pred_at γobs) γhist rx_tag_triv
                (@rx_tag_triv_persistent xv6Σ) (@rx_tag_triv_timeless xv6Σ)
+               out_ok_triv out_ok_triv_nil
                unit c) g' -∗
          ▷ xv6_slot unit (fun _ _ _ => True%I) fsimg_cov
              (FsImg.sb_logstart fsimg_sb) γd γsw γreg γstart c -∗
@@ -1786,7 +1822,7 @@ Proof.
                 unit unit (fun _ _ _ => True%I)
                 Hinv γgen γstart γreg γd γsw γobs γhist c T
                 rx_tag_triv (@rx_tag_triv_persistent xv6Σ)
-                (@rx_tag_triv_timeless xv6Σ) g')
+                (@rx_tag_triv_timeless xv6Σ) out_ok_triv out_ok_triv_nil g')
            Hgen0 Hpow Hdisk).
 Qed.
 
@@ -1804,14 +1840,26 @@ Corollary xv6_trace_adequacy_xv6Σ (g : gstate)
     (R : list mobs -> iProp xv6Σ) (HRt : forall h, Timeless (R h))
     (Tg : list mobs -> iProp xv6Σ) (HTg : forall h, Persistent (Tg h))
     (HTgt : forall h, Timeless (Tg h))
+    (Ook : uart_names -> list (list mobs) -> list (bv 8) -> Prop)
+    (HOok : forall gu : uart_names, Ook gu [] [])
     (HR0 : ⊢ |==> R [])
     (Hpow : forall (h : list mobs) (on : bool) (dk : Z -> bv 8),
        trace_shape h on ->
        ⊢ R h ==∗ R (h ++ [if on then ObsPowerOff else ObsPowerOn])%list)
     (Htx : forall (HR : riscvGS xv6Σ) (γ : uart_names),
-       ⊢ □ (∀ (h : list mobs) (b : bv 8) (u u' : uart_state),
+       ⊢ □ (∀ (h : list mobs) (b : bv 8) (u u' : uart_state)
+                (hs : list (list mobs)),
               ⌜uart_tx_pop u = Some (b, u')⌝ -∗ ⌜uart_loopback u = false⌝ -∗
               ⌜trace_shape h true⌝ -∗ ⌜obs_wire (open_seg h) = u_wire u⌝ -∗
+              (* THE WIRE IS THE DRAINED SEQUENCE (the clause TX-TAG left
+                 owed): with LOOP off throughout, [u_wire] and [u_out] are
+                 the same list, so the wire is a PREFIX of [uart_acc u] and
+                 a prefix-closed output claim transfers to it. *)
+              ⌜u_wire u = u_out u⌝ -∗
+              (* ...AND THE OUTPUT CLAIM ITSELF, read off the receive
+                 column at the drain: the bytes this UART has accepted are
+                 good for the input the column's histories record. *)
+              ⌜Ook γ hs (uart_acc u)⌝ -∗
               uart_ghosts γ u' -∗ R h ={⊤ ∖ ↑uartN ∖ ↑obsN}=∗
               uart_ghosts γ u' ∗ R (h ++ [ObsUartOut b])%list))
     (Hrx : forall (HR : riscvGS xv6Σ) (γ : uart_names),
@@ -1828,7 +1876,7 @@ Corollary xv6_trace_adequacy_xv6Σ (g : gstate)
     (forall e2, e2 ∈ t2 -> reducible (Λ := riscv_lang) e2 g2) /\ P κs.
 Proof.
   apply (xv6_trace_adequacy xv6Σ g fsimg_sb fsimg_nib fsimg_cov R HRt Tg HTg
-           HTgt HR0 Hpow Htx Hrx P HR Hgen0 Hpow0).
+           HTgt Ook HOok HR0 Hpow Htx Hrx P HR Hgen0 Hpow0).
   rewrite Hdisk. exact fsimg_image_wf.
 Qed.
 
@@ -1853,6 +1901,8 @@ Proof.
             (fun _ : unit => rx_tag_triv)
             (fun (_ : unit) (h : list mobs) => @rx_tag_triv_persistent xv6Σ h)
             (fun (_ : unit) (h : list mobs) => @rx_tag_triv_timeless xv6Σ h)
+            (fun _ : unit => out_ok_triv)
+            (fun (_ : unit) (gu : uart_names) => out_ok_triv_nil gu)
             ltac:(intros HRi GENi HBsi HFdi HIri HPavi HWci HFi ci ri Heq Htag;
                   iIntros "_ _"; iModIntro; iApply init_boot_of_triv;
                   rewrite Heq; intros r' av; reflexivity)
