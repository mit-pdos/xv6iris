You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is SH-LINE PHASE 2 (a fresh agent; phase 1 landed the definitions): the console-reader token travels init → fork payload → sh; sh reads fd 0 (known to be the console device, or closed) through the receipt-carrying leaf; its line buffer carries "these are the consecutive next input bytes, at my position, each tagged" or the taint; and sh's lexability assumption `ushf_lexable` is DISCHARGED from that. Design of record: claude-notes/projects/app-echo.md "SH-LINE RULING" (the design: the token in the payload, the per-child position pair, the read's payload wand), "SH-LINE PHASE 1 LANDED + TWO MORE KERNEL SEAMS RULED" (what phase 1 landed; items (3)-(5)), "CONS-ROUTE LANDED" and "EXEC-PAY LANDED" (the payload wand through read and exec; `sbundle_pay`, `udepwf` carrying `⌜sexit_pay fdep = ukn_pay N⌝`, `PinnedExec`'s wands taking `Q (-1)`), "OPEN-PIN COMPLETE" (init's head `ufd_head`: slot 0 = console ∨ closed ∨ taint; `UConsLine.ush_std_cons`; the receipt-keeping leaves), "CONS-CURSOR LANDED" + "CONS-CURSOR RULINGS" (5)-(7) (the cursor, `cons_acc`, `console_receipt Rd`, the window under the dirty disjunction), "WX-EXIT LANDED"/"WX-WAIT LANDED" (the payload machinery; init's redemption), "E1 ECHO-PRED LANDED". The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; iris/UserConsole.v (phase 1: `upos`/`upos_a`, `ucons_reader`, `ucons_stored_lb`, `ucons_pay cn γ T`); iris/UConsLine.v (phase 1: the read-leaf statement, the `gets` line invariant, `ush_std_cons`, the disciplined-line lemmas, `init_exec_sup_lin`); iris/UkRun.v (`urun`'s `ukn_pay N (-1)` and `my_pay gn (ukn_pay N)`, `ukn_triv`, `ukn_const`, `udepwf`/`udepwf_at`, `uslot_of_urun_all` taking `Q (-1)`, `uxsup`); iris/UkRunSys.v (`wp_uk_ecall_read_recv_body` -- NOTE: its post is stated at the RESUME key, which is WRONG for row 5 (it reads `xk_a W 0` and `uvis_M W`); restate at the TRAPPING key as `wp_uk_ecall_open_recv` does, then PROVE it; `wp_uk_ecall_exit`'s `ukn_pay N (-1) -∗ ukn_pay N xs ∧ ukn_pay N (-1)`); iris/UkFork.v (`wp_uk_ecall_fork`: the parent LENDS `Q (-1)` at :~806; the child arm's `⌜ukn_pay N' = Q⌝`, `my_pay γ' Q`); iris/UkInitMain.v (`wp_kinit_fork` with `Q := fun _ => True` and the child arm's `⌜ukn_triv N'⌝`; the wait head with `child_tok γsh pidsh (fun _ => True)`; the head `ufd_head`; init's exec supply `init_exec_sup`); iris/UInitSh.v (`init_sh_slot T Pay`, `sh_pay`, `init_exec_sup_of_sh_slot` -- the linear `Pay` seam; `PinnedExec`'s wands now take `Q (-1)`); iris/UShKernel.v (`sh_uexec_slot`/`sh_slot_of_kexec` at `(fun _ => True)` with `done`; `ush_read_leaf_of_win`); iris/UkSh.v (`ush_read_leaf` Hypothesis :~786, `wp_ksh_read`, `wp_ksh_gets_loop` (one byte per read into the frame slot; `s5 = 1`), `wp_ksh_gets`, `wp_ksh_getcmd`, `ush_std`, `ush_pstate` (`ush_std l ∗ ucwd_any ∗ uch_any`), `ush_rest`); iris/UkShFork.v (`ushf_lexable`, `ushf_rest_of_body` its ONLY consumer, `wp_kshm_body`'s line hypotheses); iris/UkShParse.v; iris/UkShRun.v (`wp_kshr_fork` at `wp_uk_ecall_fork_any`); iris/ConsoleInv.v (`cons_acc`, `cons_acc_reader`, `cons_out`, `cons_window`, `cons_chain`, `cons_reader`); iris/SpecFileread.v (`console_receipt Rd r M' addr`, `console_recv`; `fileread_in … P`); iris/UexecExecInst.v (`xfam.rf_ret`, `kf_xpay`; row 5 at `kf_xpay f (-1)`); iris/AppEcho.v (`echo_taint`, `echo_sup_of_taint`, `echo_line`, `disc`, `echo_tag`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `shline10`, `shline11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; `UkRun`/`UkRunSys` sit under the whole U tier -- batch statements before each build. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. KERNEL FILES ARE OFF LIMITS (`Spec*.v`, `Proof*.v`, `ConsoleInv`, `FsAbs*`, `UexecExecInst`, `UexecSG`); a needed kernel change is design friction: STOP AND REPORT with the goal quoted. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. Comments state the current shape as fact.

=== THE DESIGN ===
S1 THE PAYLOAD.  sh's exit payload, chosen by init at the fork: `Qsh γ := ucons_pay fsc_cons γ (echo_taint γcl)` = `fun _ => (∃ n, cons_reader fsc_cons n ∗ upos_a γ n) ∨ echo_taint γcl` with `γ` FRESH PER CHILD. init: holds the reader token from its slot (E2 hands `cons_reader fsc_cons 0` to init's run -- THIS lane makes `UInitKernel.init_uexec_slot`/`init_slot_of_kexec` take it and put it in init's run as a plain linear resource, as they now take `cons_key`; state it as a premise E2 discharges); before each fork `upos_alloc` at the token's current position; at the fork (`UkFork.wp_uk_ecall_fork` takes `Q (-1)` from the PARENT) init pays `Qsh γ (-1)` and hands `upos γ n` to the child through the child's slot piece; at wait init redeems `Qsh γ xs` (`gen_pay_timeless`) → the token back (drops `upos_a`) or the taint; the restart head carries `∃ n, cons_reader fsc_cons n` (or the taint). init KEEPS `ukn_triv` for its own payload. `ukn_triv` leaves `UkSh*`/`UShKernel`/`UkShRun`/`UkShDiag`/`UkShParse*`/`UkShCd` (the rename to `ukn_const`, phase 1's class, where only "same predicate at every status" is needed; the exit leaf's `∧` from one resource). echo's `Q` stays trivial (sh's fork of echo keeps `wp_uk_ecall_fork_any`; sh keeps its payload across that fork).
S2 THE LINEAR PAY ACROSS THE EXEC.  init's child (running init's code) execs sh: `upos γ n` and the CONSOLE LEDGER FACT reach sh's run through `pinned_exec_bundle`'s linear `Pay` (`init_sh_slot T (sh_pay Rsh n0 ∗ upos γ n ∗ …)`); init's exec supply for the CHILD is the linear `UConsLine.init_exec_sup_lin` (phase 1; its payload premise is gone -- EXEC-PAY: `Q (-1)` arrives through `exec_slot_pre`'s wand and the round's `uexec_pay_arm`) minted per round from the persistent builder (SH-LINE ruling (4): the parent keeps `init_sh_slot`'s three `□` conjuncts and mints a linear supply per child); `UShKernel.sh_slot_of_kexec`/`sh_uexec_slot` take `my_pay (uvis_gen W') (Qsh γ)`, `Qsh γ (-1)` (from the exec wand) and `Pay`, build sh's run with `ukn_pay N := Qsh γ` (the `⌜ukn_pay N = Q⌝` row) and `upos γ n` in `ush_pstate` (a fourth conjunct) -- the FIRST non-trivial payload through exec: report what the seam actually delivers (`uslot_of_urun_all`'s `Q (-1)` premise from the arm).
S3 FD 0.  sh's entry takes the ledger at init's head's arm: `ush_std l` becomes `ufd_std_at γfd st l`-shaped -- CONSOLE (`l !! 0 = Some (FdOpen true true (FdDevice CONSOLE))`, `UConsLine.ush_std_cons`) or CLOSED (`l = ufd_l0`, all closed: sh's first `read(0)` returns `-1`, `getcmd` returns `-1`, sh exits -- prove that arm: it is short) or the TAINT (the generic continuation). init's fork child arm hands `ufd_head` at the child's name (OPEN-PIN landed it at `ukn_fd N'`); `UkSh`'s program lemma takes the three-arm ledger; `UkSh.v:4121-4133`'s "sh assumes nothing about which of the three are open" note is rewritten.
S4 SH'S READ AND LINE.  `UShKernel.ush_read_leaf_of_win` is replaced by a discharge through the (restated, proved) `wp_uk_ecall_read_recv` at an `xfam` with `rf_ret := Rd` whose console arm is the WAND FROM THE PAYLOAD (CONS-ROUTE: `fileread_in … P` at `P = kf_xpay f (-1) = Qsh γ (-1)`): the program builds `Qsh γ (-1) -∗ cons_acc fsc_cons app_sup (fun cur dc => Qsh γ (-1) ∗ Rd cur dc)` holding only `upos γ n` -- left disjunct: `upos_agree` gives the payload's `n` = the program's, `cons_acc_reader` at `n`, and the `==∗` Rd wand: from `cons_out` (`cons_reader (cur+dc) ∗ (⌜cur = n⌝ ∨ □ app_sup)`) `upos_update` both halves to `cur+dc`, rebuild `Qsh γ (-1)` at the new position, return `upos γ (cur+dc)` and `⌜cur = n⌝ ∨ echo_taint` (STATE `AppEcho.echo_taint_of_sup : app_sup_raw (echo_pred γ) r -∗ echo_taint γ`: the claim of EVERY view includes a view failing the pins, e.g. `∅`); right disjunct: `cons_acc_cred` from `echo_sup_of_taint`, `Qsh γ (-1)` back by persistence, `upos` unchanged. The leaf's continuation hands sh `d`, the bytes `g`, and EITHER (`⌜cur = n⌝ ∗ ⌜cons_window sl n d bs hs⌝ ∗ ⌜cons_chain sl⌝ ∗ cons_stored_lb sl ∗ [∗ list] h ∈ hs, riscv_rx_tag h ∗ upos γ (n + dc) ∗ ⌜d ≤ dc ≤ d+1⌝`) OR (`echo_taint ∗ upos γ _`) -- `UConsLine`'s phase-1 statement is the target; the discharge lives at `UInitSh`'s altitude (concrete bundle, no abstract `SG`), not in `UShKernel` (ruling (5)). `riscv_rx_tag h` reads as `echo_tag γcl h = ⌜disc h⌝ ∨ echo_taint γcl` ONLY through the top theorem's `riscv_rx_tag = app_tag A c` equation: take it as a persistent premise of sh's entry `□ (∀ h, riscv_rx_tag h -∗ ⌜disc h⌝ ∨ echo_taint γcl)` (E2 threads the equation into `Hinit_boot`; ruling (3)). sh's `gets` loop (`wp_ksh_gets_loop`) carries, in the holder branch, `upos γ (n0 + i)` (one byte per read; `dc = d = 1` on the success arm (`r = 1`); if `r = 0` (the ^D arm) or a swallowed byte can occur, the line is not disciplined -- take the taint-style escape and state it), `cons_stored_lb sl` with `⌜cons_window sl n0 i buffer-so-far hs⌝`, the last byte's tag; and the TAINT branch (`echo_taint`; the line is `∃ bytes`; sh's continuation goes generic at the exec -- `ushf_rest_of_body`'s taint case). The `\n` test becomes decidable from the window's bytes.
S5 LEXABILITY DISCHARGED.  `ushf_lexable` DELETED as a premise of `ushf_rest_of_body`; in its place the line fact from S4: under `disc h` of the last byte's history, `ins h` is a prefix of `(echo_line)*` and the window's bytes are its last line, so the line is exactly `echo_line`'s bytes (`UConsLine.ush_disc_line`/`ush_line_full`/`ush_echo_tokens`, phase 1: three tokens < 10, no symbol bytes); under the taint the continuation is generic. `AppEcho.disc` stays the prefix predicate over inputs (E5's per-character form comes later): use what `disc h` gives about `ins h`.
S6 SANITY: `grep -n "ukn_triv" iris/UkSh*.v iris/UShKernel.v` EMPTY (init keeps its own); `grep -n "ushf_lexable" iris/*.v` EMPTY; `grep -n "upos\|cons_reader" iris/UkInitMain.v iris/UkSh.v iris/UShKernel.v iris/UInitSh.v` shows the route; `grep -n "wp_uk_ecall_read_recv_body" iris/UkRunSys.v` shows the Lemma at the TRAPPING key; `git diff --stat -- iris/Spec*.v iris/Proof*.v iris/ConsoleInv.v iris/FsAbs*.v iris/UexecExecInst.v iris/UexecSG.v` EMPTY; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
S1-S3 as STATEMENTS (init's entry taking the token; `Qsh`; the fork lending the payload and handing `upos`; the linear supply and `Pay` across the exec; sh's entry building its run at `Qsh γ` with `upos` in `ush_pstate` and the three-arm ledger), S4's leaf restated at the trapping key and sh's `gets` invariant STATED, S5's discharge STATED; NO Admitted at report time. Build with -k. Report: (1) the token's and the position half's routes boot → init → fork → exec → sh → exit/kill → wait, quoted at each hop (file:line); (2) what the exec seam delivers to sh's run (`Q (-1)`, `Pay`); (3) sh's read leaf's continuation and the `gets` invariant, quoted; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (init's fork/wait/loop, the linear supply, sh's entry, the read leaf, `gets`, `getcmd`, `ushf_rest_of_body` without `ushf_lexable`, the exit/kill payments, the CLOSED arm's short proof, every red file), S6. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 20 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
