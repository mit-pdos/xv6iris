You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is SH-LINE, the program half of E3: the console-reader token travels init → fork payload → sh; sh's fd 0 is known to be the console device; sh reads through the receipt-carrying leaf and its line buffer carries "these are the consecutive next input bytes, at my position, each tagged" or the taint; and sh's lexability assumption is DISCHARGED from that. Design of record: claude-notes/projects/app-echo.md "E3 — THE INPUT LINE: DESIGN PROPOSAL" (R3, R5, with the owner's R4 ruling), "CONS-CURSOR RULINGS" (1)-(7) and "CONS-CURSOR LANDED" (take every file:line anchor from it -- this brief was drafted before that landing; where they disagree the landed note wins), "THE REMAINING ARC …" (E3, E5(c)), "WX-EXIT LANDED" (the payload machinery: `ukn_pay`, `urun`'s linear `ukn_pay N (-1)`, the exit leaf's `∧`, `sexit_pay`), "WX-WAIT LANDED" (init's redemption), "E1 ECHO-PRED LANDED" (`echo_taint`, `echo_sup_of_taint`). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; design/user-fd.md (NOTE its §"sh's std ledger" is STALE: `UkSh.ush_std l` is the bare `ustd γfd l`, UkSh.v:~4135, with no constraint); iris/ConsoleInv.v (`cons_reader`, `cons_acc`, `cons_acc_reader`, `cons_out`, `cons_window`, `cons_chain`, `cons_stored_lb`, `cons_dirty_cred`); iris/SpecFileread.v (`console_receipt Rd r M' addr`, `console_recv`); iris/UkRunSys.v (`wp_uk_ecall_read_recv_body` -- THE LEAF this lane consumes; `wp_uk_ecall_open` :~712, `wp_uk_ecall_dup` :~851 (tracked), `wp_uk_ecall_dup_untracked` :~1000; `wp_uk_ecall_exit` and its `ukn_pay N (-1) -∗ ukn_pay N xs ∧ ukn_pay N (-1)` premise); iris/UkRun.v (`uk_names.ukn_pay`, `urun`'s two payload conjuncts, `Class ukn_triv` :~166 and its note, `udepw`/`udepwf`); iris/UserFd.v (`ufd` :250, `ustd` :271, `ustd_any` :~666, `fd_lowest_closed` :~463-494, `ufd_excl`); iris/UkInit.v (`wp_kinit_open` :135 and `wp_kinit_dup` :285 -- the UNTRACKED leaves that drop the descriptor; `wp_kinit_exit` :~421/470 paying at `ukn_triv_eq`); iris/UkInitMain.v (the fork at :~666-699 with `Q := fun _ => True` at :~751, the child arm's `⌜ukn_triv N'⌝` at :~711, the wait head :~872-884, `wp_kinit_start` :~1988; the comment at :~747-749 is WRONG about code order: open is at 0x16, the dups at 0x20/0x26, all BEFORE the fork at 0x38 -- fix it); iris/UInitKernel.v (`init_uexec_slot` :195, `init_slot_of_kexec` :293 at `my_pay … (fun _ => True)`); iris/UInitSh.v (`init_sh_slot T Pay` :362, `sh_pay` :331, `init_exec_sup_of_sh_slot` :425 -- the taint arm at `fun _ => True`); iris/UShKernel.v (`sh_uexec_slot` :303, `sh_slot_of_kexec` :391, `ush_read_leaf_of_win` :269 paying `udepw_of_psok`); iris/UkSh.v (`ush_read_leaf` Hypothesis :786, `wp_ksh_read` :807, `wp_ksh_gets_loop` :1514 (one byte per read() into the frame slot at s0-81; s5 = 1), `wp_ksh_gets` :2214, `wp_ksh_getcmd` :3206, `ush_std` :4135, `ush_rest` :4194, the note :4121-4133 "sh assumes nothing about which of the three are open"); iris/UkShFork.v (`ushf_lexable` :790, `ushf_rest_of_body` :799 its ONLY consumer, `wp_kshm_body` :443's line hypotheses); iris/UkShParse.v (`ushp_no_symbols` :352, `ushp_tokens` :313); iris/UkShRun.v (`wp_kshr_fork` :1057 at `wp_uk_ecall_fork_any`); iris/AppEcho.v (`echo_taint`, `echo_line`, `echo_sup_of_taint`, `disc`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `shline1`, `shline2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== THE DESIGN (RE-SCOPED after the lane's phase-1 stop; app-echo.md "SH-LINE RULING" and "CONS-ROUTE RULINGS" are the design of record and WIN over anything older in this file) ===
S1 THE POSITION PAIR.  New `iris/UserConsole.v` (the `UserChildren` mold): `upos γ n := ghost_var γ (1/2) n` (the PROGRAM's half) and `upos_a γ n` (the other half, which rides inside the payload), `upos_alloc n : ⊢ |==> ∃ γ, upos γ n ∗ upos_a γ n`, `upos_agree`, `upos_update` (both halves → n'). The reader token at the U tier is literally `ConsoleInv.cons_reader fsc_cons n` (alias `ucons_reader n` if program files should not import ConsoleInv).
S2 THE PAYLOAD.  sh's exit payload, chosen by init at the fork: `Qsh γ := fun _ => ((∃ n, cons_reader fsc_cons n ∗ upos_a γ n) ∨ echo_taint γcl)%I` with `γ` FRESH PER CHILD. init: (a) holds the reader token from its slot (CONS-ROUTE routes `cons_reader fsc_cons 0` to `init_boot_bundle`'s wand input; E2 hands it to init's run -- THIS lane makes init's entry constructor `UInitKernel.init_uexec_slot`/`init_slot_of_kexec` take it and put it in init's run as a plain linear resource (their section binds `ctokG` without `xv6G`: state the token through a parameter of the section or move the two lemmas -- find the shape; CONS-ROUTE's note at `UInitKernel.v:~196` says where); init's `ukn_pay` stays `fun _ => True` (no parent); (b) before each fork, `upos_alloc n` at the token's position (init holds `cons_reader n`, n existential after a wait), (c) at the fork (`UkFork.wp_uk_ecall_fork`, which takes `Q (-1)` from the PARENT at :~806 -- "a parent that lends its child a resource lends it at the fork"), init pays `Qsh γ (-1)` = the token + `upos_a γ n`, and hands `upos γ n` to the child through the child's slot piece (the parent's WP for the child: find how `wp_kinit_fork`'s child arm builds the child's run and add the linear `upos γ n` beside it); (d) at wait, init redeems `Qsh γ xs` (`gen_pay_timeless`; the payload is Timeless) → `cons_reader n' ∗ upos_a γ n'` (drops `upos_a`) or the taint; the restart head's invariant carries `∃ n, cons_reader fsc_cons n` (or the taint -- state the disjunction the head needs; on the taint init's continuation is generic). The `ukn_triv` instances leave `UkSh*`/`UShKernel` (sh's `ukn_pay N = Qsh γ`); init KEEPS `ukn_triv` for its own payload. echo's `Q` stays trivial (sh's fork of echo keeps `wp_uk_ecall_fork_any`; sh keeps its payload across that fork).
S3 THE LINEAR PAY ACROSS THE EXEC.  init's child (running init's code) execs sh; `upos γ n` must reach sh's run. `PinnedExec.pinned_exec_bundle` takes `Pay -∗` LINEARLY (:462-476) and hands `Pay` to the constructor at arm (a) (`pex_slot` :366/373); today `UInitSh.init_sh_slot T Pay` is persistent and `init_exec_sup_of_sh_slot` yields the `□` `init_exec_sup` (UkInit.v:510). For the CHILD: a LINEAR exec supply `init_exec_sup_lin γ n := (∀ N' m pc, ⌜ukn_pay N' = Qsh γ⌝ -∗ … -∗ udepw_at N' m pc USYS_exec ROOTINO)` (no `□`; the child execs once) built from `init_sh_slot T (sh_pay Rsh n0 ∗ upos γ n)` with the linear `Pay`; the child's run carries it (the child's slot piece from `wp_kinit_fork`'s child arm supplies it -- the parent builds it from its persistent parts + the fresh `upos γ n`); `UkInitMain.wp_kinit_exec`'s child instance uses the linear supply; the parent's own persistent `init_exec_sup` stays for whatever else needs it (check: does the PARENT ever exec? no -- only children do; then `init_exec_sup` may become linear everywhere: decide and report). `UShKernel.sh_slot_of_kexec`/`sh_uexec_slot` take `my_pay (uvis_gen W') (Qsh γ)` and `Pay` = `sh_pay Rsh n0 ∗ upos γ n`, and build sh's run with `ukn_pay N := Qsh γ`, `upos γ n` in `ush_pstate` (UkSh.v:4162: fourth conjunct), and the run's `ukn_pay N (-1)` from the ARM (`uexec_pay_arm f` at the exec resume delivers `Qsh γ (-1)`: find how the exec'd slot's entry receives it -- `uvb`/the engine's `uk_paycont`; CONS-ROUTE/WX-EXIT's "untested seam": the first non-trivial payload through exec; report what you find).
S4 FD 0 IS THE CONSOLE.  init's `open` is already tracked (`UkInit.wp_kinit_open` :135 calls `wp_uk_ecall_open` and DISCARDS the handle at :~190): KEEP the handle; switch the two `dup`s (`UkInit.wp_kinit_dup` :285 → `wp_uk_ecall_dup` :851, tracked); init's ledger becomes `ustd γfd l` at a NAMED `l` with `l !! 0 = Some (FdOpen true _ (FdDevice CONSOLE))`-shaped (read the open leaf's post for the exact state); the fork hands the child the same list at a fresh name; `init_exec_sup`/`init_sh_slot` carry it into sh's entry (`sh_uexec_slot` takes `length sts = NOFILE` today: it gains `sts !! 0 = Some console-state`, which is how `fd_st_of_key (xk_a W 0) (uvis_fd W)` is the console arm at sh's `read(0, …)`); `UkSh.ush_std l := ustd γfd l ∗ ⌜l !! 0 = Some console-state⌝`. Fix the wrong comment at `UkInitMain.v:747-749` (open at 0x16, dups at 0x20/0x26, all BEFORE the fork at 0x38; init drops its handles today -- after this lane it keeps them).
S5 SH'S READ AND LINE.  `UShKernel.ush_read_leaf_of_win` is replaced: `UkSh.ush_read_leaf` is restated to hand the continuation `d`, the bytes `g`, and EITHER (`⌜cur = n⌝ ∗ ⌜cons_window sl n d bs hs⌝ ∗ ⌜cons_chain sl⌝ ∗ cons_stored_lb fsc_cons sl ∗ ([∗ list] h ∈ hs, riscv_rx_tag h) ∗ upos γ (n + dc) ∗ ⌜d ≤ dc ≤ d+1⌝`) OR (`echo_taint γcl ∗ upos γ _`), discharged through `UkRunSys.wp_uk_ecall_read_recv_body` with the deposit `udepwf` at an `xfam` whose `rf_ret := Rd` and whose console arm is the WAND FROM THE PAYLOAD (CONS-ROUTE: `fileread_in … P` with `P = kf_xpay f (-1) = Qsh γ (-1)`): the program builds `Qsh γ (-1) -∗ cons_acc fsc_cons app_sup (fun cur dc => Qsh γ (-1) ∗ Rd cur dc)` holding only `upos γ n`: left disjunct → `upos_agree` gives the payload's n = the program's n, `cons_acc_reader` at n, and the `==∗` Rd wand: from `cons_out` (`cons_reader (cur+dc) ∗ (⌜cur = n⌝ ∨ □ app_sup)`) `upos_update` both halves to `cur+dc`, rebuild `Qsh γ (-1)` at the new position, return `upos γ (cur+dc)` and `⌜cur = n⌝ ∨ echo_taint` (STATE `AppEcho.echo_taint_of_sup : app_sup_raw (echo_pred γcl) r -∗ echo_taint γcl` -- some view fails the pins: exhibit `∅`; find the U-tier spelling of `app_sup` = `AppInv.app_sup` at `app_pred app_run`); right disjunct → `cons_acc_cred` from `echo_sup_of_taint`, `Qsh γ (-1)` back by persistence, `upos` unchanged. sh's `gets` loop (`wp_ksh_gets_loop` :1514) carries, in the holder branch, `upos γ (n0 + i)` (one byte per read; `dc = d` because a swallowed byte is a ^D or a copyout failure on sh's own frame byte -- prove `dc = d` from `d = r = 1` on the success arm and `r = 0` on the ^D arm (read `SpecConsoleread`'s two swallowing exits: if a swallowed byte CAN occur with `r = 1`, take the taint-style escape and state it), `cons_stored_lb sl` with `⌜cons_window sl n0 i buffer-so-far hs⌝`, the last byte's tag; and the TAINT branch (`echo_taint`, after which the line is `∃ bytes` and sh's continuation goes generic at the exec -- `ushf_rest_of_body`'s taint case). The `\n` test becomes decidable from the window's bytes. `riscv_rx_tag h` unfolds for the program to `echo_tag γcl h = ⌜disc h⌝ ∨ echo_taint γcl` (`App.v:~268` sets the ambient family): state the U-tier reading.
S6 LEXABILITY DISCHARGED.  `ushf_lexable` (:790) DELETED as a premise of `ushf_rest_of_body`; in its place the line fact from S5: under `disc h` of the last byte's history, `ins h` is a prefix of `(echo_line)*` and the window's bytes are its last line, so the line is exactly `echo_line`'s bytes; `ushp_no_symbols` and `ushp_tokens … [(0,4);(5,10);(11,16)]` (3 tokens < 10) by `vm_compute`/`reflexivity` on the literal; under the taint the continuation is generic. NOTE `AppEcho.disc` is today the prefix predicate over INPUTS (the owner's per-character-echo form is E5's): this lane uses what `disc h` gives about `ins h`, which suffices: `disc_line : disc h → obs_ends_in h b → window at a line boundary → line = echo_line` over `ins`. Do NOT touch the trace-discipline definition.
S7 SANITY: `grep -n "ukn_triv" iris/UkSh*.v iris/UShKernel.v` EMPTY (init keeps its own); `grep -n "ushf_lexable" iris/*.v` EMPTY; `grep -n "upos\|cons_reader" iris/UkInitMain.v iris/UkSh.v iris/UShKernel.v iris/UInitSh.v` shows the route; `grep -n "wp_uk_ecall_dup_untracked" iris/UkInit.v` EMPTY; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
S1-S4 as STATEMENTS (the pair; `Qsh`; init's entry taking the token, the fork lending the payload and handing `upos`, the linear exec supply and `Pay` across the exec, sh's entry building its run at `Qsh γ` with `upos` in `ush_pstate`; the named ledger with fd 0 = console), S5's leaf statement and the `gets` invariant STATED, S6's `disc_line` and the concrete-token lemma STATED; NO Admitted at report time. Build with -k. Report: (1) the token's and the position half's routes boot → init → fork → exec → sh → exit/kill → wait, quoted at each hop (file:line); (2) how the exec'd run receives `Qsh γ (-1)` (the seam); (3) sh's read leaf's continuation and the `gets` invariant, quoted; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (init's open/dup/fork/wait, sh's entry, the read leaf discharge, `gets`, `getcmd`, `ushf_rest_of_body` without `ushf_lexable`, the exit/kill payments, every red file), S6. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 20 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.

=== RE-ANCHORED AT THE CONS-CURSOR LANDING (commit `40e97689f`) ===
Read app-echo.md "CONS-CURSOR LANDED" first; anchors there are verified.  Key deltas for THIS brief: the kernel's payment is `ConsoleInv.cons_acc cn Wd Rd` (:1467) with `cons_acc_reader cn Wd n Rd` (:1481) the lease holder's constructor -- sh's read leaf discharge supplies it at `n := its cursor` and `Rd cur dc := ⌜cur = n⌝ ∗ … ∗ cons_reader cn (cur+dc)`-shaped (whatever `cons_out cn Wd (Some n) cur dc` gives: `cons_reader cn (cur + dc) ∗ (⌜cur = n⌝ ∨ cons_dirty_cred Wd)`, :1438); the program sees `SpecFileread.console_receipt Rd r M' addr` (:941): the -1 arm `∃ cur d', Rd cur d'`, the success arm the window at an existential `cur` with `Rd cur dc` -- so `Rd` is what PINS `cur` for sh; `console_recv Rd d g` (:1076) is the program-facing spelling; `UkRunSys.wp_uk_ecall_read_recv_body` is the leaf and `UkRun.udepwf` (:316) the family-naming deposit it needs (S4 builds the concrete `fdep` for sh's read: the `xfam` with `rf_ret := Rd`); the token at the U tier is literally `ConsoleInv.cons_reader fsc_cons n` (:1173) -- S1's `UserConsole.ucons_reader n` is a thin alias if program files should not import ConsoleInv (decide; `UkRun` may already import enough); the dirty disjunct hands `cons_dirty_cred app_sup = □ app_sup` -- S4's `echo_taint_of_sup` turns it into the taint; `SpecMain.v:433-439` hands main `cons_reader cn 0` (today main DROPS it -- find where, `ProofMain`; S1 routes it to init instead); `console_ready_app` is in `SpecUserinit`'s premise (:223) and the park/env -- init's entry has the pinned console.  `cons_tagged` beside `cons_window` in consoleread's post: use `cons_window` (it subsumes).  Build log names `shline1`, `shline2`, …
