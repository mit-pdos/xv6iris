You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is SH-LINE, the program half of E3: the console-reader token travels init → fork payload → sh; sh's fd 0 is known to be the console device; sh reads through the receipt-carrying leaf and its line buffer carries "these are the consecutive next input bytes, at my position, each tagged" or the taint; and sh's lexability assumption is DISCHARGED from that. Design of record: claude-notes/projects/app-echo.md "E3 — THE INPUT LINE: DESIGN PROPOSAL" (R3, R5, with the owner's R4 ruling), "CONS-CURSOR RULINGS" (1)-(7) and "CONS-CURSOR LANDED" (take every file:line anchor from it -- this brief was drafted before that landing; where they disagree the landed note wins), "THE REMAINING ARC …" (E3, E5(c)), "WX-EXIT LANDED" (the payload machinery: `ukn_pay`, `urun`'s linear `ukn_pay N (-1)`, the exit leaf's `∧`, `sexit_pay`), "WX-WAIT LANDED" (init's redemption), "E1 ECHO-PRED LANDED" (`echo_taint`, `echo_sup_of_taint`). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; design/user-fd.md (NOTE its §"sh's std ledger" is STALE: `UkSh.ush_std l` is the bare `ustd γfd l`, UkSh.v:~4135, with no constraint); iris/ConsoleInv.v (`cons_reader`, `cons_acc`, `cons_acc_reader`, `cons_out`, `cons_window`, `cons_chain`, `cons_stored_lb`, `cons_dirty_cred`); iris/SpecFileread.v (`console_receipt Rd r M' addr`, `console_recv`); iris/UkRunSys.v (`wp_uk_ecall_read_recv_body` -- THE LEAF this lane consumes; `wp_uk_ecall_open` :~712, `wp_uk_ecall_dup` :~851 (tracked), `wp_uk_ecall_dup_untracked` :~1000; `wp_uk_ecall_exit` and its `ukn_pay N (-1) -∗ ukn_pay N xs ∧ ukn_pay N (-1)` premise); iris/UkRun.v (`uk_names.ukn_pay`, `urun`'s two payload conjuncts, `Class ukn_triv` :~166 and its note, `udepw`/`udepwf`); iris/UserFd.v (`ufd` :250, `ustd` :271, `ustd_any` :~666, `fd_lowest_closed` :~463-494, `ufd_excl`); iris/UkInit.v (`wp_kinit_open` :135 and `wp_kinit_dup` :285 -- the UNTRACKED leaves that drop the descriptor; `wp_kinit_exit` :~421/470 paying at `ukn_triv_eq`); iris/UkInitMain.v (the fork at :~666-699 with `Q := fun _ => True` at :~751, the child arm's `⌜ukn_triv N'⌝` at :~711, the wait head :~872-884, `wp_kinit_start` :~1988; the comment at :~747-749 is WRONG about code order: open is at 0x16, the dups at 0x20/0x26, all BEFORE the fork at 0x38 -- fix it); iris/UInitKernel.v (`init_uexec_slot` :195, `init_slot_of_kexec` :293 at `my_pay … (fun _ => True)`); iris/UInitSh.v (`init_sh_slot T Pay` :362, `sh_pay` :331, `init_exec_sup_of_sh_slot` :425 -- the taint arm at `fun _ => True`); iris/UShKernel.v (`sh_uexec_slot` :303, `sh_slot_of_kexec` :391, `ush_read_leaf_of_win` :269 paying `udepw_of_psok`); iris/UkSh.v (`ush_read_leaf` Hypothesis :786, `wp_ksh_read` :807, `wp_ksh_gets_loop` :1514 (one byte per read() into the frame slot at s0-81; s5 = 1), `wp_ksh_gets` :2214, `wp_ksh_getcmd` :3206, `ush_std` :4135, `ush_rest` :4194, the note :4121-4133 "sh assumes nothing about which of the three are open"); iris/UkShFork.v (`ushf_lexable` :790, `ushf_rest_of_body` :799 its ONLY consumer, `wp_kshm_body` :443's line hypotheses); iris/UkShParse.v (`ushp_no_symbols` :352, `ushp_tokens` :313); iris/UkShRun.v (`wp_kshr_fork` :1057 at `wp_uk_ecall_fork_any`); iris/AppEcho.v (`echo_taint`, `echo_line`, `echo_sup_of_taint`, `disc`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `shline1`, `shline2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group; nothing Admitted; no new Axiom/Parameter. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== THE DESIGN ===
S1 THE READER TOKEN AT THE U TIER.  `UserConsole.v` (new, the `UserChildren` mold): `ucons_reader n := cons_reader fsc_cons n` spelled at the U tier (or re-export; PREFER a thin definition so program files import no kernel invariant file), and the program-side accessor `ucons_acc n Rd := cons_acc_reader …` at the holder's disjunct. The token is BORN at boot (`cons_reader cn 0` is already handed to main by `SpecMain`) and reaches init through `InitBoot.init_boot_bundle`: its slot piece gains the token as a premise beside `my_pay` (`init_boot_bundle cw sts` takes `ucons_reader 0 -∗`-shaped access, or the bundle's `Pay`; decide by how `init_boot_bundle_triv`/`init_boot_of_sup` and `SystemAdequacy`'s `Hinit_boot` discharge can thread it -- the generic instance must keep discharging `Hinit_boot` WITHOUT the token (it never reads at a position), so the token enters init's slot as an OPTIONAL resource: PREFER `init_boot_bundle` unchanged and the token delivered to init's `urun` payload: see S2). Report the route you choose with file:line.
S2 THE PAYLOAD.  init and sh leave `ukn_triv`: init's `ukn_pay` stays `fun _ => True` (init has no parent -- `InitBoot`'s note) BUT init's RUN carries the token as a plain resource (it is not a payload of init's exit); init's fork chooses `Q xs := ucons_reader ∃-at-some-n ∨ echo_taint` -- decide the exact shape: the token's POSITION is not known to init after sh has read, so `Q xs := (∃ n, ucons_reader n) ∨ taint` (PREFER; the position is sh's business and init only needs the token back to lend again); at `UkFork.wp_uk_ecall_fork` init pays `Q (-1)` ITSELF at the fork? NO -- re-read UkFork.v:~830: the parent hands `Q (-1)` in? Check whether the fork leaf takes `Q (-1)` from the parent (the child's `urun` must hold `ukn_pay N' (-1)` from somewhere: WX-EXIT put it in the run; find who supplies it at the fork -- the parent LENDS: state that init hands its token in at the fork and gets it back at wait). sh's `ukn_pay N := Q`; sh's run holds `ukn_pay N (-1)` = the token (or the taint), which is EXACTLY what sh spends at every read and must hold at every trap (the engine's `uk_paycont` moves it -- WX-EXIT); sh's exit pays `Q xs ∧ Q (-1)` from it trivially (same resource both sides). The `ukn_triv` instances leave `UkInit*`, `UkSh*`, `UShKernel`, `UInitKernel` (the survey's list; `UkCat`/`UkSync`/`UkEcho` keep theirs); `UInitSh.init_sh_slot`'s taint arm and `init_exec_sup_of_sh_slot` at `Q`; `UShKernel.sh_uexec_slot`/`sh_slot_of_kexec` take `my_pay … Q` and `Q (-1)` in. echo's `Q` stays trivial (echo never reads): sh's fork of echo keeps `wp_uk_ecall_fork_any`; sh keeps the token across that fork.
S3 FD 0 IS THE CONSOLE.  init's `open("console")` and two `dup`s go through the TRACKED leaves (`wp_uk_ecall_open` at a named state, `wp_uk_ecall_dup`), so init's ledger is `ustd γfd [console; console; console]`-shaped (whatever `fdstate` the console open yields: `FdOpen true _ (FdDevice CONSOLE)` -- find the open leaf's post); `UkInit.wp_kinit_open`/`wp_kinit_dup` restated at the named ledger; the fork hands the child the same ledger at a fresh name (`UkFork`'s child arm); `init_exec_sup`/`init_sh_slot` carry it into sh's entry (`UShKernel.sh_uexec_slot` takes `length sts = NOFILE` today: it gains `sts !! 0 = Some (FdOpen true _ (FdDevice CONSOLE))` or the ledger at a named state); `UkSh.ush_std l` becomes `ustd γfd l ∗ ⌜l !! 0 = Some console-state⌝` (fix design/user-fd.md's stale line only in the CODE's comment; the note is mine). This is what lets sh's read leaf hit `fileread_extra`'s CONSOLE arm at fd 0 (`fd_st_of_key (xk_a W 0) (uvis_fd W)` reads the key's descriptor list -- check how the key's `uvis_fd` is tied to `ustd`'s list: `UserFd`'s ledger IS the key's reading, WX-KEY/STD-LEDGER).
S4 SH'S READ AND LINE.  `UShKernel.ush_read_leaf_of_win` is replaced by a discharge of a NEW `UkSh.ush_read_leaf` Hypothesis through `wp_uk_ecall_read_recv_body` (the receipt-carrying leaf) at `Rd cur dc := ⌜cur = n⌝ ∗ ucons_reader (n + dc)` for a holder at `n` -- OR the dirty disjunct's credential. The leaf's continuation therefore hands sh: `d`, the bytes `g` on `[a, a+d)`, and EITHER (`⌜cur = n⌝ ∗ ⌜cons_window sl n d bs hs⌝ ∗ ⌜cons_chain sl⌝ ∗ cons_stored_lb sl ∗ [∗ list] h ∈ hs, riscv_rx_tag h ∗ ucons_reader (n + dc) ∗ ⌜d ≤ dc ≤ d+1⌝`) OR (`echo_taint ∗ ucons_reader _`) -- the second from `cons_dirty_cred app_sup` via `echo_sup_of_taint`'s converse: STATE `AppEcho.echo_taint_of_sup : app_sup_raw (echo_pred γ) r -∗ echo_taint γ` (app_sup at every view ⇒ the taint, since some view fails the pins -- exhibit one: the empty view, or `∅`), and the U-tier spelling of `app_sup` (`UkRun`'s `ssupply`? `AppInv.app_sup`? find what `cons_dirty_cred app_sup` is at the program tier and how `riscv_rx_tag h` unfolds to `echo_tag γ h = ⌜disc h⌝ ∨ echo_taint γ` for the program -- `App.v:~268` sets the ambient family). sh's `gets` loop invariant (`wp_ksh_gets_loop` :1514) gains, for the holder branch: `ucons_reader (n0 + i)` (one byte per read; `dc = d` because a swallowed byte means `^D` or a copyout failure -- the copyout cannot fail on sh's own frame byte (prove or take as the `dc = d+1 → taint`-style escape: PREFER proving `dc = d` from the window's `d = 1` and `r = 1`: the ^D arm returns 0, the copyout-failure arm returns … -- read `SpecConsoleread`'s two exits and decide; if a swallowed byte can occur, the line is "not disciplined" and sh takes the taint branch: state that), `cons_stored_lb sl` with `⌜cons_window sl n0 i buffer-so-far hs⌝`, the last byte's tag; and the TAINT branch: `echo_taint`, after which the loop's obligations are paid generically (sh's continuation after a tainted read is the GENERIC slot: sh switches to `udepw_of_psok`-style deposits and the trivial family; find the cleanest point: PREFER "the line is `∃ bytes` with `echo_taint`" and let `ushf_rest_of_body`'s taint case go generic at the exec). The `\n` test: the walk currently `remember`s the branch; under the window the byte IS known (`bs j`), so the branch is decidable from the buffer -- the loop's post says `buffer[0..i) = the window's bytes` and `i2` is where the newline landed.
S5 LEXABILITY DISCHARGED.  `ushf_lexable` (:790) is DELETED as a premise of `ushf_rest_of_body`; in its place the line fact from S4: under `disc` of the last byte's history (the tag's left arm) the window's bytes are a prefix of `(echo_line)*` at a line boundary, i.e. the line is exactly `echo_line`'s bytes (the `\n`-terminated command) -- prove `ushp_no_symbols` and `ushp_tokens … [(0,4); (5,10); (11,16)]`-shaped (3 tokens < 10) for the CONCRETE bytes of "echo hello world" (a `vm_compute`/`reflexivity` over the literal); under the taint, `ushf_rest_of_body`'s continuation is the generic one (state the disjunction in `ush_rest`'s premise or in `wp_kshm_body`'s). NOTE the trace discipline's exact shape is E5's (`AppEcho.disc` is today the prefix predicate over inputs; the owner's per-character-echo form comes later) -- THIS lane uses whatever `disc h` gives about the INPUT bytes of `h` (`ins h` is a prefix of `(echo_line)*`), which is enough for the line fact; state the lemma `disc_line : disc h → obs_ends_in h b → window bytes … → line = echo_line` over `ins`.
S6 SANITY: `grep -n "ukn_triv" iris/UkInit*.v iris/UkSh*.v iris/UShKernel.v iris/UInitKernel.v` is EMPTY; `grep -n "ushf_lexable" iris/*.v` is EMPTY; `grep -n "ucons_reader" iris/UkInitMain.v iris/UkSh.v iris/UShKernel.v` shows the route; `grep -n "wp_uk_ecall_dup_untracked" iris/UkInit.v` is EMPTY; the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
S1-S3 as STATEMENTS (the token's U-tier spelling and its boot route; the payload `Q`; init's tracked open/dup and the named ledger; sh's entry with fd 0 = console), S4's leaf discharge and the `gets` invariant STATED, S5's `disc_line` and the concrete-token lemma STATED; NO Admitted at report time. Build with -k. Report: (1) the token's route boot → init → fork → sh → exit → wait, quoted at each hop (file:line); (2) `Q`'s shape and where `Q (-1)` enters the child's run; (3) sh's read leaf's continuation and the `gets` invariant, quoted; (4) the red files and why; (5) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (init's open/dup/fork/wait, sh's entry, the read leaf discharge, `gets`, `getcmd`, `ushf_rest_of_body` without `ushf_lexable`, the exit/kill payments, every red file), S6. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 20 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.

=== RE-ANCHORED AT THE CONS-CURSOR LANDING (commit `40e97689f`) ===
Read app-echo.md "CONS-CURSOR LANDED" first; anchors there are verified.  Key deltas for THIS brief: the kernel's payment is `ConsoleInv.cons_acc cn Wd Rd` (:1467) with `cons_acc_reader cn Wd n Rd` (:1481) the lease holder's constructor -- sh's read leaf discharge supplies it at `n := its cursor` and `Rd cur dc := ⌜cur = n⌝ ∗ … ∗ cons_reader cn (cur+dc)`-shaped (whatever `cons_out cn Wd (Some n) cur dc` gives: `cons_reader cn (cur + dc) ∗ (⌜cur = n⌝ ∨ cons_dirty_cred Wd)`, :1438); the program sees `SpecFileread.console_receipt Rd r M' addr` (:941): the -1 arm `∃ cur d', Rd cur d'`, the success arm the window at an existential `cur` with `Rd cur dc` -- so `Rd` is what PINS `cur` for sh; `console_recv Rd d g` (:1076) is the program-facing spelling; `UkRunSys.wp_uk_ecall_read_recv_body` is the leaf and `UkRun.udepwf` (:316) the family-naming deposit it needs (S4 builds the concrete `fdep` for sh's read: the `xfam` with `rf_ret := Rd`); the token at the U tier is literally `ConsoleInv.cons_reader fsc_cons n` (:1173) -- S1's `UserConsole.ucons_reader n` is a thin alias if program files should not import ConsoleInv (decide; `UkRun` may already import enough); the dirty disjunct hands `cons_dirty_cred app_sup = □ app_sup` -- S4's `echo_taint_of_sup` turns it into the taint; `SpecMain.v:433-439` hands main `cons_reader cn 0` (today main DROPS it -- find where, `ProofMain`; S1 routes it to init instead); `console_ready_app` is in `SpecUserinit`'s premise (:223) and the park/env -- init's entry has the pinned console.  `cons_tagged` beside `cons_window` in consoleread's post: use `cons_window` (it subsumes).  Build log names `shline1`, `shline2`, …
