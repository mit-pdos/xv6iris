You are a proof-lane agent on the xv6iris project (Rocq/Iris weakest-precondition proofs of a RISC-V xv6 kernel) in the checkout /shared/xv6iris-2. Your lane is PID-KEY (owner's request, 2026-09-11): the user-execution key `UexecSlot.uvis` gains the process's PID, and getpid's row says it returns that pid. The pid is NOT bubbled up to `urun`/the U-tier programs in this lane -- it stops at the trap round's row. Mold: WX-KEY (app-echo.md "WX-KEY LANDED": how `uvis_gen`/`uvis_ch` were added -- read it whole and copy its sweep shape), minus the ghost cells (the pid is already a PARAMETER of every kernel contract on the route: `proc_priv γf pj pid U`; no new cell, no new ghost). The coordinator (me) owns the design; you do statements and proofs. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Shared-checkout discipline", "The dev loop", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/user-wp-slot.md; iris/UexecSlot.v (`Record uvis` :91-146 -- the field notes are the mold for the new one; `uvis_of U sts g cs` :158); iris/UexecRet.v (`uvis_of_run` :167, `bump` :174, `bump_gen`/`bump_ch` :495/:501, `uvis_of_run_*` :508-538, `uexec_ret_cont_gen` :~1020-1060 -- the pure rows `usys_mem_ok`/`usys_fd_ok`/`usys_pipe_ok`/`usys_cwd_ok`/`usys_gen_ok`, and `uexec_ret_cont_F`'s `usys_ch_ok` row); iris/UsysMemOk.v (`usys_gen_ok` :674-680 -- THE MOLD for the quiet pid row; `USYS_getpid`'s number: check the table at :~95-110); iris/SpecKexec.v (`exec_key U' sts gn cs na` :473 -- exec KEEPS the pid); iris/KforkChild.v (`kfork_child`; the child's key is built from the PARENT's block with the child's generation ∀-bound -- the child's pid is ∀-bound the same way); iris/SpecKfork.v (`kfork_post`'s pid arm `∃ pidv γ, … child_tok γ pidv Q`, the child slot premise `∀ γ, my_pay γ Q -∗ uslot (uvis_of (kfork_child U) stsP γ ∅)`); iris/SpecSysFork.v, iris/SpecSyscall.v (`sysc_sys_in`/`sysc_sys_out` :346/:380 at `uvis_of U sts gn cs`; `sysc_fork_in`), iris/SpecUsertrap.v (`ut_sys_in`/`ut_sys_out` :434/:462; `usertrap_post`), iris/SpecUservec.v, iris/ParkCap.v (`park_pkg`'s closer at `uvis_of U' sts gn cs` :258; `park_cap` keys the parked slot at `pv_gen (us_V U)` :347 -- the pid is `un_pid N` in the residue names, UsertrapRes.v:503), iris/ProofUserretClosed.v (the loop builds the resume key), iris/ProofSyscall.v (`sysc_arm_getpid` :3594: `wp_sys_getpid_sconf`'s post gives `mf a0 = sign_extend' 64 pid`, which today is DROPPED at `destruct Hmf as [Hcs _]` -- this lane keeps it), iris/SpecSysGetpid.v (:66-95); iris/UkRun.v (`uvis_of_run` at the entry constructors :~1146/:1282 -- the pid parameter reaches them as `∀`-bound or from the slot's key; the programs keep saying nothing about it). Sites: `grep -rn "uvis_of\b" iris/*.v` (26 in ProofSyscall, 12 SpecUsertrap, 10 each in ProofUsertrapSys/ProofForkret/ParkCap, …, ~130 total); `MkUvis` in UexecRet/SpecUsertrap/ProofUserretClosed/UexecSlot/KforkChild; `uvis_of_run` in 11 files.

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2 <logname>` (log names `pidkey1`, `pidkey2`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context `./gcp-rocq/run-on-gcp -q --no-sync bash -c 'grep -n -B2 -A25 "^Error" /tmp/<logname>.log'`); ONE build at a time; never a remote top-level `make`; never local `coqc`; `UexecSlot` sits under every U-tier and trap file -- batch the statements before the first build. Gate: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified by group (sealed Parameters retyped in place listed); nothing Admitted; no new Axiom/Parameter. GIT: never stash/reset/add -A/commit -a/amend/`git rm`/`git mv`/`git checkout -- <file>`; DO NOT COMMIT OR STAGE; no other checkouts, no worktrees; temp files in your scratchpad; do not edit claude-notes/. DESIGN FRICTION = STOP AND REPORT with the goal quoted. Comments state the current shape as fact.

=== THE DESIGN ===
K1 THE FIELD.  `uvis` gains `uvis_pid : mword 32` (after `uvis_cwd`, before `uvis_gen` -- or LAST; pick what minimizes the constructor-pattern churn and say so). Its note, in the record's own style: the pid is user-visible because getpid(2) returns it; a VALUE, the kernel's `p->pid` cell (which `proc_priv` carries at 1/2 and every kernel contract on the route names as `pid`); `uvis_of` takes it as a parameter for `sts`'s reason; exec keeps it; fork's child gets allocproc's, ∀-bound in the child slot premise like its generation; no syscall moves it (`usys_pid_ok`).
K2 THE PROJECTIONS.  `uvis_of U sts g pid cs` (insert `pid` beside `g`), `uvis_of_run m pc M π szv fdv cw g pid cs`, `bump` keeps `uvis_pid W` (NO new parameter: the pid never moves on a returning syscall), `bump_pid` lemma, `uvis_of_run_pid`, `exec_key U' sts gn pid cs na`, `skey_eq`/`urun_eq`/`uslot_of_urun_eq` gain the pid clause, `spost_at` arity UNCHANGED (pid is not an output). `ParkCap.park_cap`/`park_pkg` key at `un_pid N` (it is in the residue names already); `SpecUsertrap`/`SpecUservec`/`SpecSyscall` rows at `uvis_of U sts gn pid cs` with `pid` the contract's own parameter; `KforkChild`: the child's key is `uvis_of (kfork_child U) sts γ pidc ∅` with `pidc` ∀-bound beside `γ` in `SpecKfork`'s slot premise (`∀ γ pidc, my_pay γ Q -∗ uslot …`) and in `sysc_fork_in`/`ut_fork_in`; kfork's proof instantiates `pidc := pid_c` (allocproc's, `ProofKforkMain`/`ProofKforkB5` have it as `pid_c`) -- and NOTE `kfork_post`'s parent arm already returns `pidv` with `child_tok γ pidv Q`; state in the child slot premise's note that the child's key pid IS that `pidv` (kfork parks at `pid_c = pidv`), so a parent holding `child_tok γ pidv Q` knows the child's key pid via `gen_pid`. userinit's park keys at init's pid (`ProofUserinit` has `pid`). `UkRun`'s entry constructors take the pid off the key (`uvis_pid W`) -- `urun` does NOT gain a pid conjunct (owner: not this lane); the constructors' `uvis_of_run … (uvis_pid W) …` just threads it.
K3 GETPID'S ROW.  `UsysMemOk.usys_pid_ok (n : Z) (p p' : mword 32) : Prop := p' = p` (quiet at every number; `_quiet`/`_refl` as `usys_gen_ok`'s) -- since `bump` keeps the pid there is no `p'` to relay; SKIP this row unless the round's shape needs a symmetric pure clause (PREFER skipping: nothing moves). The RETURN row: `UsysMemOk.usys_ret_pid (n : Z) (r : mword 64) (pid : mword 32) : Prop := n = USYS_getpid -> r = sign_extend' 64 pid`, added to `uexec_ret_cont_gen`'s pure rows as `⌜usys_ret_pid n r (uvis_pid W)⌝` (LAST among the pure rows -- durable-notes "a new pure conjunct goes second/last"; check what the intro patterns bind), with `usys_ret_pid_ne` for every non-getpid arm (discharged from the arm's own number as `sysc_fork_out_ne` is) and the getpid arm producing it from `wp_sys_getpid_sconf`'s `mf a0 = sign_extend' 64 pid` (ProofSyscall.v:~3614-3620: keep the second conjunct of `Hmf`) through `sysc_sys_out`/`spost_at`'s route -- find where the a0 word `r` of the answer is bound (`sysc_ret_tail`'s `r`) and where the pure rows are established (the dispatcher's returning tail builds `uexec_ret_cont_gen`'s rows: `usys_mem_ok` etc. are produced where? grep `usys_gen_ok` in ProofSyscall/ProofUsertrap*/UexecApply -- the row is produced at the same place, from the arm's post). If the pure rows are produced in `UexecApply.uexec_ret_round_slot` from the KERNEL's post (`sysc_sys_out`'s `spost_at` + pure facts), add the pid fact to whatever pure bundle crosses there (`ut_sys_out`/`sysc_sys_out` gain `⌜usys_ret_pid n r pid⌝` or a `sysc_ret_ok` conjunct). Report the exact route.
K4 SANITY.  `grep -n "uvis_pid" iris/UexecSlot.v iris/UexecRet.v iris/ParkCap.v iris/SpecKexec.v iris/KforkChild.v iris/SpecKfork.v iris/UsysMemOk.v` shows the field, the projections, the park's key, exec keeping it, the child's ∀-bound pid, and the row; `grep -rn "usys_ret_pid" iris/*.v` shows the row produced at the getpid arm and refuted at the others; no U-tier program statement changed except entry constructors' arity (`grep -n "ukn_pid\|urun.*pid" iris/UkRun.v` EMPTY); the audit unchanged.

=== PHASE 1 -- then STOP AND REPORT ===
K1-K3 as STATEMENTS; NO Admitted at report time. Build with -k. Report: (1) the field and its note, `uvis_of`/`uvis_of_run`/`bump`/`exec_key`/the child key, quoted; (2) the getpid row and its exact route from `wp_sys_getpid_sconf`'s post to `uexec_ret_cont_gen`, file:line; (3) the red files and why; (4) anything this brief got wrong about the landed code, with evidence (file:line).

=== PHASE 2 -- after go-ahead ===
The proofs (the sweep, kfork's instantiation, userinit's, the getpid arm, the `_ne`s at every other arm, the entry constructors, every red file), K4. Gate. Report log name, `git status --porcelain | grep -v '^ M iris/'`, `git diff --stat -- iris/ | tail -1`, new files, the audit verbatim, the lemma_diff report justified, and an as-landed note (≤ 12 lines, present tense). Do not commit or stage. If context runs short, STOP after a green -k build or at a clear milestone and report exactly what remains, file:line.
