You are an INTERFACE-lane agent on the xv6iris project (Rocq/Iris WP proofs of a RISC-V xv6 kernel). YOUR CHECKOUT IS /shared/xv6iris-2-disc (a sibling clone at main 5cd2ae7e0; never touch or build /shared/xv6iris-2). Your lane is APP-IFACE: THREE changes to the TRUSTED TOP-LEVEL STATEMENT (`App.xv6_app_adequacy` and what it rests on) that the echo application needs, batched into one lane so the theorem is re-opened once. PHASE 1 IS A DESIGN PHASE: propose the exact statement diff and prove only what is free; the OWNER reviews the diff before phase 2. Design of record: claude-notes/projects/app-echo.md ("E5 -- THE OUTPUT SIDE" O4 LEDGER; "TX-TAG PHASE 1"; "OPEN-PIN COMPLETE"/"GENERIC-PAY LANDED" for what E2 owes init; "E2 ARM-c (1b) INIT-BOOT") and `review-echo-plan-2026-09-12.md` findings 3, 6, 14 (read them verbatim: they are the three changes). The coordinator owns the design; you propose statements. TWO PHASES; STOP AND REPORT at the end of phase 1.

=== READ FIRST ===
claude-notes/README.md; durable-notes.md ("Guiding principle", "Build", "Vacuity", "Shaping a change so the sweep is small"); remote-build-gcp.md; design/app-instances.md and design/uart-trace.md (the "identification gate" / `P_era` chain; the "Rejected" section); iris/App.v (`Record xv6_app` :117 with `app_cl`/`app_pred`/`app_R`/`app_tag`/`app_phi`; `app_triv` :147; `xv6_app_adequacy` :~160-250: EVERY hypothesis -- `Hbirth`, `HR0`, `Hpow`, `Htx` :~180, `Hrx` :~187, `Happ_xfer`, `Happ_init` :~205, `Hinit_boot` :~215, `Hphi` :~228 -- and its proof, which instantiates `SystemAdequacy.xv6_power_adequacy_gen`); iris/SystemAdequacy.v (`xv6_power_adequacy_gen` :~793 and its `Hinit_boot` :412, the call at :604 `iMod (Hinit_boot … with "Happinv")`, the permit premise `forall γ, …`, `xv6_trace_adequacy` :~1210, how `riscv_rx_tag` is FIXED to `app_tag A c` in `boot_fixedGS`/`power_interp`, where `Happ_init` is consumed to allocate `app_inv`, and the corollaries `xv6_power_adequacy`/`xv6_app_adequacy_triv_xv6Σ` that must keep going through); iris/RiscvAdequacy.v (`obs_ledger_at`, the device-step glue that invokes the ledger's two wands: what it has in hand when it does -- `uart_ghosts γ u'`, and from where `γ` comes); iris/WpUart.v (`uart_obs_permit_ledger`, `uart_ghosts` :~712, `uart_ghosts_alloc` :~1622 -- the era's γ is born here; `uart_pop_tag` :~744 from lane TX-TAG is NOT in your tree yet (it is on a branch): read its statement in the note "TX-TAG PHASE 1" -- your (c) must make it usable inside the app's `Htx`); iris/BootShared.v (where `uart_ghosts_alloc` is called at power-on and what persistent facts the boot mints); iris/SystemUartAccepted.v :25-45 (the note that names the gate); iris/AppEcho.v (`echo_init_key` :882 -- produces the era-0 claim WITH `cons_key r`, which `echo_init` :900 DROPS because `Happ_init`'s type has no room for it; `echo_Happ_init` :1080; the `Hinit_boot` placeholder and the header's list of what E2 owes; `echo_R` and how it would record a tagged shadow); iris/AppInv.v (`app_xfer_raw` :141, `app_inv`, `app_sup`); iris/UInitKernel.v (`init_uexec_slot` :~203 / `init_slot_of_kexec`: the premises E2 must supply -- `cons_key r`, `ucons_reader cn 0`, the record equation, `app_inv`, `init_cons_leaves`, the entry-ledger fact; note which of these are LINEAR); iris/InitBoot.v (`init_boot_bundle` :129 -- a wand from `cons_reader fsc_cons 0`); iris/UserConsole.v (`ucons_reader_eq`, the narrow/wide bridge); iris/UConsLine.v (`ush_tag_law` -- the persistent premise `□ (∀ h, riscv_rx_tag h -∗ ⌜disc h⌝ ∨ T)` sh takes; E2 discharges it from the equation `riscv_rx_tag = app_tag A c` and `echo_tag`).

=== OPERATING RULES (hard) ===
BUILD ONLY with `./gcp-rocq/vmbuild.sh xv6iris-2-disc <logname>` FROM /shared/xv6iris-2-disc (names `iface10`, `iface11`, …; run_in_background; summary prints `COMPILED=`/`EXIT=`; error context from your checkout); ONE build at a time; never a remote top-level `make`; never local `coqc`. Gate for phase 2 only: zero `Error`, `EXIT=0`; VM `make audit-only` from `/mnt/rocq/trees/_shared_xv6iris-2-disc` = EXACTLY the thirteen (print verbatim); `tools/lemma_diff.py` justified; nothing Admitted; no new Axiom/Parameter. Files: `App.v`, `SystemAdequacy.v`, `RiscvAdequacy.v` (if (c) needs it), `WpUart.v`/`BootShared.v` (only for the era token of (c)), `AppEcho.v`, `AppInv.v`; everything else only to keep compiling (report each). GIT: branch `lane/app-iface` in your checkout; PHASE 1 COMMITS NOTHING (the diff is the deliverable; leave the working tree dirty and report `git diff` of the STATEMENTS); never push; never stash/reset/add -A/commit -a/amend; no other checkouts; temp files in your scratchpad; do not edit claude-notes/. THE TCB RULE: every change to `App.v`/`SystemAdequacy.v` statements must be listed in the report with a one-sentence justification a skeptical reader accepts (what new thing the theorem now assumes, and why it is not a GAP premise -- see `xv6_power_adequacy_gen`'s own paragraph on `Hinit_boot`).

=== THE THREE CHANGES ===
(a) THE BOOT RESOURCE (review finding 6).  `xv6_app` gains `app_boot : app_fixed -> app_names -> iProp Σ` -- what the application's birth hands to the FIRST PROCESS'S proof beside the era-0 claim.  `Happ_init : forall c, ⊢ |==> ∃ r, app_pred A c r av0 ∗ app_boot A c r`; `Hinit_boot` takes `app_boot A c r` LINEARLY beside `app_inv` (same `r` as the record equation).  `app_triv`: `app_boot := fun _ _ => emp`.  `xv6_power_adequacy_gen` carries the resource from the point `Happ_init` is consumed (the invariant's allocation) to the `Hinit_boot` call at :604 -- find the shortest route (it may just ride the same bupd).  For echo: `app_boot := fun γ r => cons_key r` (from `echo_init_key`; `echo_init` stops dropping it).  Check `Happ_xfer`/`app_xfer_raw` are untouched (the transport is of the claim only; the boot resource is consumed once).
(b) THE RX-TAG EQUATION.  `Hinit_boot` gains the pure premise `riscv_rx_tag = app_tag A c` (the boot proof HAS it: `boot_fixedGS … (app_tag A c)` fixes the tag; find the exact form -- an equation of functions, or pointwise -- that the call site can supply and that `UConsLine.ush_tag_law`'s discharge needs via `echo_tag`). Justify: it is a fact of the fixed instance, not an assumption about the world.
(c) THE ERA IDENTIFICATION (finding 3; SystemUartAccepted's note).  `Htx`/`Hrx` (and `xv6_power_adequacy_gen`'s permit premise `forall γ, …`) quantify over an arbitrary `γ : uart_names`, so the application's ledger `app_R A c h` cannot hold anything about THE ERA's UART ghosts -- yet E5's ledger must record, at each `Htx` firing, the tag `uart_pop_tag` reads off `uart_ghosts γ u'` (a persistent `uart_tag_at γ i src b` bound AT `γ`), and carry it to `Hphi`.  Propose the minimal evidence: a persistent per-era token `uart_era γ` (or `uart_of_cycle γobs? γ`) minted where `uart_ghosts_alloc` runs at power-on, given to the wands as an extra premise (`Htx : … uart_era γ -∗ uart_ghosts γ u' -∗ app_R A c h ={…}=∗ …`), with an AGREEMENT law (`uart_era γ -∗ uart_era γ' -∗ ⌜γ = γ'⌝` within one cycle) so the ledger's stored bounds at the γ it saw first are about the same ghosts at every later firing -- and a rule for the cycle boundary (`Hpow`: at PowerOff the ledger drops its per-era bounds; the next PowerOn's `γ` is fresh: say how the ledger learns the new γ -- the first `Htx`/`Hrx` firing of the cycle, or a `Hpow`-side premise).  Where must the token be produced so the device-step glue in `RiscvAdequacy`/`WpUart.uart_obs_permit_ledger` can hand it over?  If the glue cannot reach it, say exactly why (quote the wand's invocation site).  ALSO state the LOOP-off fact E5 needs (`u_wire u = u_out u`, from `uart_col_ok`'s LOOP clause) as a lemma the wand's caller can supply, if it is not already derivable from what `Htx` receives.
(d) OPTIONAL, ONLY IF FREE: the `Hphi` conclusion's `◇ ⌜app_phi A g' h⌝` and `obs_ledger_at (app_R A c)` -- confirm the ledger resource at the end of the run is enough to read a per-cycle tagged shadow out of (E5's proof needs `app_R A c h` to determine, for the final `h`, the tagged accepted sequences of every cycle -- or at least of the LAST cycle if `app_phi` may be stated per completed cycle only; note what `cycles_of h`/`disc`'s per-cycle form (lane DISC-RATE, landed) requires).

=== PHASE 1 -- then STOP AND REPORT (no commit) ===
The statement diff for (a)-(c) in `App.v`/`SystemAdequacy.v`(/`RiscvAdequacy.v`/`WpUart.v`), compiling with the OLD proofs adapted only where trivial and `Admitted`-free by leaving a hard proof's statement UNCHANGED and reporting the target statement verbatim instead (as CONS-SWALLOW phase 1 did); `app_triv` and `echo`'s instances adapted. Build with -k. Report: (1) `git diff -- iris/App.v iris/SystemAdequacy.v` (statements only, verbatim) with the TCB justification per change; (2) for (c), the exact token, its minting site and the glue's invocation site, and whether the glue can hand it over; (3) what E2 now receives at `Hinit_boot` (the full premise list, linear/persistent marked) against `UInitKernel.init_uexec_slot`'s premise list -- what is STILL missing for E2; (4) the red files; (5) anything this brief or the review got wrong, with file:line.
=== PHASE 2 -- after the OWNER's go-ahead on the diff ===
The proofs, the corollaries, the gate, commit on `lane/app-iface`. Report log name, commit hash, `git diff --stat main..lane/app-iface | tail -1`, the audit verbatim, lemma_diff justified, an as-landed note (≤ 20 lines, present tense).
