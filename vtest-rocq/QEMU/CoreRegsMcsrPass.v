(* QEMU/CoreRegsMcsrPass.v -- GENERATED by tools/vtest.  Do not edit.

   The proof for the run of case [core_regs_mcsr] on platform [qemu].  What is proved
   is a statement about [RiscvLang.prim_step]; [VExecStep] is what carries a
   computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import CoreRegsMcsrTest CoreRegsMcsrRun.

Module CoreRegsMcsrPass <: TEST_PASSES_AGREE CoreRegsMcsr CoreRegsMcsrRun.
  Lemma agrees :
    run_agrees CoreRegsMcsr.hart CoreRegsMcsr.text CoreRegsMcsr.regions
               CoreRegsMcsr.uart_input CoreRegsMcsr.disk_init CoreRegsMcsrRun.observed.
  Proof.
    intros o Ho.
    cbn [CoreRegsMcsrRun.observed CoreRegsMcsrRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000 2000);
              vm_cast_no_check (eq_refl true) |]).
    destruct Ho.
  Qed.
End CoreRegsMcsrPass.
