(* QEMU/DiskErrPass.v -- GENERATED by tools/vtest.  Do not edit.

   The proof for the run of case [disk_err] on platform [qemu].  What is proved
   is a statement about [RiscvLang.prim_step]; [VExecStep] is what carries a
   computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import DiskErrTest DiskErrRun.

Module DiskErrPass <: TEST_PASSES_AGREE DiskErr DiskErrRun.
  Lemma agrees :
    run_agrees DiskErr.hart DiskErr.text DiskErr.regions
               DiskErr.uart_input DiskErr.disk_init DiskErrRun.observed.
  Proof.
    intros o Ho.
    cbn [DiskErrRun.observed DiskErrRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000);
              vm_cast_no_check (eq_refl true) |]).
    destruct Ho.
  Qed.
End DiskErrPass.
