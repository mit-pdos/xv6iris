(* QEMU/CoreRegsScsrPass.v -- GENERATED by tools/vtest.  Do not edit.

   The proof for the run of case [core_regs_scsr] on platform [qemu].  What is proved
   is a statement about [RiscvLang.prim_step]; [VExecStep] is what carries a
   computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import CoreRegsScsrTest CoreRegsScsrRun.

Module CoreRegsScsrPass <: TEST_PASSES_AGREE CoreRegsScsr CoreRegsScsrRun.
  Lemma agrees :
    run_agrees CoreRegsScsr.hart CoreRegsScsr.text CoreRegsScsr.regions
               CoreRegsScsr.uart_input CoreRegsScsr.disk_init CoreRegsScsrRun.observed.
  Proof.
    intros o Ho.
    cbn [CoreRegsScsrRun.observed CoreRegsScsrRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000 2000);
              vm_cast_no_check (eq_refl true) |]).
    destruct Ho.
  Qed.
End CoreRegsScsrPass.
