(* QEMU/UartRxPass.v -- GENERATED by tools/vtest.  Do not edit.
   VERDICT: agree

   The proof for the run of case [uart_rx] on platform [qemu].  The theorem is
   [VRun.run_passes], a statement about [RiscvLang.prim_step]; [VExecStep]
   is what carries a computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import UartRxTest UartRxRun.

Module UartRxPass <: TEST_PASSES UartRx UartRxRun.
  Lemma passes :
    run_passes UartRx.hart UartRx.text UartRx.regions
               UartRx.uart_input UartRx.disk_init UartRxRun.observed.
  Proof.
    left. intros o Ho.
    cbn [UartRxRun.observed UartRxRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 50000);
              vm_compute; repeat split |]).
    destruct Ho.
  Qed.
End UartRxPass.
