(* QEMU/DiskIdentNotifyPass.v -- GENERATED by tools/vtest.  Do not edit.

   The proof for the run of case [disk_ident_notify] on platform [qemu].  What is proved
   is a statement about [RiscvLang.prim_step]; [VExecStep] is what carries a
   computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import DiskIdentNotifyTest DiskIdentNotifyRun.

Module DiskIdentNotifyPass <: TEST_PASSES_AGREE DiskIdentNotify DiskIdentNotifyRun.
  Lemma agrees :
    run_agrees DiskIdentNotify.hart DiskIdentNotify.text DiskIdentNotify.regions
               DiskIdentNotify.uart_input DiskIdentNotify.disk_init DiskIdentNotifyRun.observed.
  Proof.
    intros o Ho.
    cbn [DiskIdentNotifyRun.observed DiskIdentNotifyRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000 2000);
              vm_cast_no_check (eq_refl true) |]).
    destruct Ho.
  Qed.
End DiskIdentNotifyPass.
