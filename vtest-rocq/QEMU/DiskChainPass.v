(* QEMU/DiskChainPass.v -- GENERATED by tools/vtest.  Do not edit.

   The proof for the run of case [disk_chain] on platform [qemu].  What is proved
   is a statement about [RiscvLang.prim_step]; [VExecStep] is what carries a
   computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import DiskChainTest DiskChainRun.

Module DiskChainPass <: TEST_PASSES_AGREE DiskChain DiskChainRun.
  Lemma agrees :
    run_agrees DiskChain.hart DiskChain.text DiskChain.regions
               DiskChain.uart_input DiskChain.disk_init DiskChainRun.observed.
  Proof.
    intros o Ho.
    cbn [DiskChainRun.observed DiskChainRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000 2000);
              vm_cast_no_check (eq_refl true) |]).
    destruct Ho.
  Qed.
End DiskChainPass.
