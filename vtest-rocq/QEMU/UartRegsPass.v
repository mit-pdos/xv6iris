(* QEMU/UartRegsPass.v -- GENERATED by tools/vtest.  Do not edit.

   The proof for the run of case [uart_regs] on platform [qemu].  What is proved
   is a statement about [RiscvLang.prim_step]; [VExecStep] is what carries a
   computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import UartRegsTest UartRegsRun.

Module UartRegsPass <: TEST_PASSES_AGREE UartRegs UartRegsRun.
  Lemma agrees :
    run_agrees UartRegs.hart UartRegs.text UartRegs.regions
               UartRegs.uart_input UartRegs.disk_init UartRegsRun.observed.
  Proof.
    intros o Ho.
    cbn [UartRegsRun.observed UartRegsRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000);
              vm_compute; repeat split |]).
    destruct Ho.
  Qed.
End UartRegsPass.
