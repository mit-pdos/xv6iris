(* QEMU/PlicMctxPass.v -- GENERATED by tools/vtest.  Do not edit.
   VERDICT: agree

   The proof for the run of case [plic_mctx] on platform [qemu].  The theorem is
   [VRun.run_passes], a statement about [RiscvLang.prim_step]; [VExecStep]
   is what carries a computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.QEMU Require Import PlicMctxTest PlicMctxRun.

Module PlicMctxPass <: TEST_PASSES PlicMctx PlicMctxRun.
  Lemma passes :
    run_passes PlicMctx.hart PlicMctx.text PlicMctx.regions
               PlicMctx.uart_input PlicMctx.disk_init PlicMctxRun.observed.
  Proof.
    left. intros o Ho.
    cbn [PlicMctxRun.observed PlicMctxRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000);
              vm_compute; repeat split |]).
    destruct Ho.
  Qed.
End PlicMctxPass.
