(* JH7110/CoreRegsFprPass.v -- GENERATED by tools/vtest.  Do not edit.
   VERDICT: agree

   The proof for the run of case [core_regs_fpr] on platform [jh7110].  The theorem is
   [VRun.run_passes], a statement about [RiscvLang.prim_step]; [VExecStep]
   is what carries a computation of the interpreter to it.

   THIS RUN PASSES BECAUSE the model EXHIBITS every observation the platform
   produced, from this test's own configuration. *)
From Stdlib Require Import List ZArith.
From stdpp Require Import base list.
Import ListNotations.
From VTest Require Import VTest VRun VExecStep.
From VTest.JH7110 Require Import CoreRegsFprTest CoreRegsFprRun.

Module CoreRegsFprPass <: TEST_PASSES CoreRegsFpr CoreRegsFprRun.
  Lemma passes :
    run_passes CoreRegsFpr.hart CoreRegsFpr.text CoreRegsFpr.regions
               CoreRegsFpr.uart_input CoreRegsFpr.disk_init CoreRegsFprRun.observed.
  Proof.
    left. intros o Ho.
    cbn [CoreRegsFprRun.observed CoreRegsFprRun.results fmap list_fmap] in Ho.
    repeat (destruct Ho as [<-|Ho];
            [ apply (run_shows false lowest_head 2000);
              vm_compute; repeat split |]).
    destruct Ho.
  Qed.
End CoreRegsFprPass.
