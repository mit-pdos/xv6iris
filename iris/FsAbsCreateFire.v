(* FsAbsCreateFire.v -- TOMBSTONE.  Fused into FsAbsMknodFire.v on 2026-08-30 (the FsAbs* leaf
   fuse); its FIRE 2 is that file's section 7.

   THIS STUB EXISTS ONLY SO NO CONSUMER HAD TO MOVE.  It re-exports the
   file the content went to, so [Require Import FsAbsCreateFire] still puts
   exactly the same names in scope.  Removing the stubs (and the
   [Require] lines that name them) is a separate, later pass. *)

Require Export FsAbsMknodFire.
