(* FsAbsPins.v -- TOMBSTONE.  Fused into FsAbs.v on 2026-08-30 (the FsAbs* leaf
   fuse); its [Section FsAbsPins] is [FsAbs.v]'s section 4a''.

   THIS STUB EXISTS ONLY SO NO CONSUMER HAD TO MOVE.  It re-exports the
   file the content went to, so [Require Import FsAbsPins] still puts
   exactly the same names in scope.  Removing the stubs (and the
   [Require] lines that name them) is a separate, later pass. *)

Require Export FsAbs.
