(* FsAbsSeam.v -- TOMBSTONE.  Fused into FsAbsEra.v on 2026-08-30 (the FsAbs* leaf
   fuse); its two pure lemmas and the payload exclusions are that file's section 0.

   THIS STUB EXISTS ONLY SO NO CONSUMER HAD TO MOVE.  It re-exports the
   file the content went to, so [Require Import FsAbsSeam] still puts
   exactly the same names in scope.  Removing the stubs (and the
   [Require] lines that name them) is a separate, later pass. *)

Require Export FsAbsEra.
