# Kernel memory-ordering sites

AUTO-GENERATED by `tools/gen_sites.py` -- do not edit by hand.

| | |
|---|---|
| image revision (`XV6_REV`) | `9dd28f5e3526197b65a58306ce90248647c77777` |
| text range | `0x80000000 .. 0x80006124` |
| flat walk ends at | `0x80006124` (exact) |
| flat-walk positions | 9215 |
| real instructions (gen_code walk) | 8457 |
| flat walk covers every real instruction | yes |
| regenerate | `python3 tools/gen_sites.py --emit-coq` |

## Counts

| class | count |
|---|---|
| fence | 9 |
| fencei | 1 |
| amo | 1 |
| racy_load | 2 |
| release_store | 3 |
| violations | 0 |

## FENCE

| pc | site | word | pred | succ | class | prev insn | next insn |
|---|---|---|---|---|---|---|---|
| `0x80000ca6` | `release+0x16` | `0310000f` | `rw` | `w` | rel | `sd zero,16(s1)` | `sw zero,0(s1)` |
| `0x80000e96` | `main+0x18` | `0230000f` | `r` | `rw` | acq | `lw a5,0(a4)` | `sext.w a5,a5` |
| `0x80000f2a` | `main+0xac` | `0310000f` | `rw` | `w` | rel | `li a4,1` | `sw a4,0(a5)` |
| `0x80001956` | `forkret+0x1e` | `0230000f` | `r` | `rw` | acq | `lw a5,0(a5)` | `sext.w a5,a5` |
| `0x8000196c` | `forkret+0x34` | `0310000f` | `rw` | `w` | rel | `addi a5,a5,-1908 # 8000a1f0 <first.1>` | `sw zero,0(a5)` |
| `0x800058d2` | `virtio_disk_rw+0x172` | `0ff0000f` | `iorw` | `iorw` | full | `sh a0,4(a3)` | `ld a4,8(a5)` |
| `0x800058e2` | `virtio_disk_rw+0x182` | `0ff0000f` | `iorw` | `iorw` | full | `sh a5,2(a4)` | `lui a5,0x10001` |
| `0x8000599e` | `virtio_disk_intr+0x2c` | `0ff0000f` | `iorw` | `iorw` | full | `sw a5,100(a4)` | `ld a5,16(s1)` |
| `0x800059b0` | `virtio_disk_intr+0x3e` | `0ff0000f` | `iorw` | `iorw` | full | `beq a4,a5,800059fc <virtio_disk_intr+0x8a>` | `ld a4,16(s1)` |

## FENCE.I

| pc | site | word | next insn |
|---|---|---|---|
| `0x8000609c` | `userret+0x0` | `0000100f` | `sfence.vma` |

## AMO / LR / SC

| pc | site | word | op | aq | rl | insn |
|---|---|---|---|---|---|---|
| `0x80000c24` | `acquire+0x1c` | `0cf4a7af` | amoswap | True | False | `amoswap.w.aq a5,a5,(s1)` |

## Racy-read sites (plain load, stream successor is `fence r,rw`)

| pc | site | word | width | insn | fence pc | delta |
|---|---|---|---|---|---|---|
| `0x80000e94` | `main+0x16` | `431c` | 16 | `lw a5,0(a4)` | `0x80000e96` | +2 |
| `0x80001954` | `forkret+0x1c` | `439c` | 16 | `lw a5,0(a5)` | `0x80001956` | +2 |

## Release-flag sites (plain store, stream predecessor is `fence rw,w`)

| pc | site | word | width | insn | fence pc | delta |
|---|---|---|---|---|---|---|
| `0x80000caa` | `release+0x1a` | `0004a023` | 32 | `sw zero,0(s1)` | `0x80000ca6` | -4 |
| `0x80000f2e` | `main+0xb0` | `c398` | 16 | `sw a4,0(a5)` | `0x80000f2a` | -4 |
| `0x80001970` | `forkret+0x38` | `0007a023` | 32 | `sw zero,0(a5)` | `0x8000196c` | -4 |

## Discipline violations

None -- `iris/KernelSitesDef.image_disciplineb` is `true`.

## Interrupts-off audit

Source-level, best effort: every enumerated site should sit in a
region where `sstatus.SIE` is clear, so that no trap can be
delivered between a discipline fence and its adjacent access.

| class | site | interrupts off? | why |
|---|---|---|---|
| racy_load | `main+0x16` | yes | pre-scheduler boot path.  start() enables sie.SEIE/STIE but never sets sstatus.SIE; the first intr_on() in the whole kernel is inside scheduler(), which main() only reaches after the started publication (start.c, main.c, proc.c). |
| racy_load | `forkret+0x1c` | yes | reached only by swtch() from scheduler(), whose loop does intr_on(); intr_off(); BEFORE acquire(&p->lock) -- so the push_off recorded intena = 0 and forkret's release(&p->lock) pop_off() does NOT re-enable interrupts.  The `first` acquire-load and release-store therefore run with SIE clear. NOTE: this depends on scheduler()'s intr_on();intr_off() pair; an xv6 revision without it would leave these two sites interruptible (proc.c). |
| release_store | `release+0x1a` | yes | release() runs inside the critical section it is ending: pop_off() comes AFTER the flag store, so the fence+store pair is still under push_off (spinlock.c). |
| release_store | `main+0xb0` | yes | pre-scheduler boot path.  start() enables sie.SEIE/STIE but never sets sstatus.SIE; the first intr_on() in the whole kernel is inside scheduler(), which main() only reaches after the started publication (start.c, main.c, proc.c). |
| release_store | `forkret+0x38` | yes | reached only by swtch() from scheduler(), whose loop does intr_on(); intr_off(); BEFORE acquire(&p->lock) -- so the push_off recorded intena = 0 and forkret's release(&p->lock) pop_off() does NOT re-enable interrupts.  The `first` acquire-load and release-store therefore run with SIE clear. NOTE: this depends on scheduler()'s intr_on();intr_off() pair; an xv6 revision without it would leave these two sites interruptible (proc.c). |
| amo | `acquire+0x1c` | yes | push_off() is the first thing acquire() does, so the amoswap runs with sstatus.SIE clear (spinlock.c). |
| fence | `release+0x16` | yes | release() runs inside the critical section it is ending: pop_off() comes AFTER the flag store, so the fence+store pair is still under push_off (spinlock.c). |
| fence | `main+0x18` | yes | pre-scheduler boot path.  start() enables sie.SEIE/STIE but never sets sstatus.SIE; the first intr_on() in the whole kernel is inside scheduler(), which main() only reaches after the started publication (start.c, main.c, proc.c). |
| fence | `main+0xac` | yes | pre-scheduler boot path.  start() enables sie.SEIE/STIE but never sets sstatus.SIE; the first intr_on() in the whole kernel is inside scheduler(), which main() only reaches after the started publication (start.c, main.c, proc.c). |
| fence | `forkret+0x1e` | yes | reached only by swtch() from scheduler(), whose loop does intr_on(); intr_off(); BEFORE acquire(&p->lock) -- so the push_off recorded intena = 0 and forkret's release(&p->lock) pop_off() does NOT re-enable interrupts.  The `first` acquire-load and release-store therefore run with SIE clear. NOTE: this depends on scheduler()'s intr_on();intr_off() pair; an xv6 revision without it would leave these two sites interruptible (proc.c). |
| fence | `forkret+0x34` | yes | reached only by swtch() from scheduler(), whose loop does intr_on(); intr_off(); BEFORE acquire(&p->lock) -- so the push_off recorded intena = 0 and forkret's release(&p->lock) pop_off() does NOT re-enable interrupts.  The `first` acquire-load and release-store therefore run with SIE clear. NOTE: this depends on scheduler()'s intr_on();intr_off() pair; an xv6 revision without it would leave these two sites interruptible (proc.c). |
| fence | `virtio_disk_rw+0x172` | yes | holds disk.vdisk_lock (acquire -> push_off) across the whole body (virtio_disk.c). |
| fence | `virtio_disk_rw+0x182` | yes | holds disk.vdisk_lock (acquire -> push_off) across the whole body (virtio_disk.c). |
| fence | `virtio_disk_intr+0x2c` | yes | holds disk.vdisk_lock (acquire -> push_off) across the whole body (virtio_disk.c). |
| fence | `virtio_disk_intr+0x3e` | yes | holds disk.vdisk_lock (acquire -> push_off) across the whole body (virtio_disk.c). |
| fencei | `userret+0x0` | yes | trampoline; usertrapret() does intr_off() before jumping here (trap.c, trampoline.S). |

## Racy-byte reference audit

Every reference in the image to a byte a discipline site touches.
A reference that feeds no site is a candidate discipline violation.

| byte | referenced at | insn | feeds site |
|---|---|---|---|
| `first.1` | `forkret+0x18` | `addi a5,a5,-1884 # 8000a1f0 <first.1>` | `0x80001954` |
| `first.1` | `forkret+0x30` | `addi a5,a5,-1908 # 8000a1f0 <first.1>` | `0x80001970` |
| `started` | `main+0x10` | `addi a4,a4,934 # 8000a230 <started>` | `0x80000e94` |
| `started` | `main+0xa6` | `addi a5,a5,784 # 8000a230 <started>` | `0x80000f2e` |

## The Coq site lists

`iris/KernelSitesDef.v`'s enumerations must be exactly these.

```coq
Definition racy_load_pcs : list Z :=
  [ KernelSyms.main + 0x16
  ; KernelSyms.forkret + 0x1c
  ].

Definition release_pcs : list Z :=
  [ KernelSyms.release + 0x1a
  ; KernelSyms.main + 0xb0
  ; KernelSyms.forkret + 0x38
  ].

Definition amo_pcs : list Z :=
  [ KernelSyms.acquire + 0x1c
  ].

Definition acq_fence_pcs : list Z :=
  [ KernelSyms.main + 0x18
  ; KernelSyms.forkret + 0x1e
  ].

Definition rel_fence_pcs : list Z :=
  [ KernelSyms.release + 0x16
  ; KernelSyms.main + 0xac
  ; KernelSyms.forkret + 0x34
  ].
```

